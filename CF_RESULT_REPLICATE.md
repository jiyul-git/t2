# ⑨ 결과 — 표본 밖 재현

설계는 `CF_DESIGN_REPLICATE.md` (커밋 `8f57eab`). **예측 E1~E4 를 측정
전에 박았고, 이 문서는 그 예측을 고치지 않는다.**

```
자료   시드 5100-5154   rows55c   262,122행   7.4MB
비교   시드 5000-5054   rows55b   ⑧ 이 본 자료 (CF_RESULT_DIVERGE.md)
문턱   S 2.0 · T 1.5    ⑦ d6fae23 그대로. 새로 만들지 않았다
```

두 자료를 합치지 않았다. **따로 낸 값의 대조다.**

---

## 1. 건전성 (설계 5절 3단계)

```
엔진 오류                        0 건
attached_O ↔ call_seq==0 불일치   0 행
update_plan 호출                37,446 건
alignment status                7축 × 3팔 전부 OK
```

집계표는 `rows55b` 와 다르다 — **다를 것으로 설계에 적어두었다.** 시드가
다르므로 일치를 요구하지 않는다.

---

## 2. E4 — 분기·축 조합 (충족)

P1-b 가 낸 다축 분기가 사전 등록한 넷 그대로다.

```
DEVIATE:포기 계획이나 지속벳(N%)   4축   aggression · discipline · bluff · cbet_flop
밸류 계획 실행(N%)                2축   aggression · thin_value_turn
블러프 계획 실행(N%)               2축   aggression · bluff
사이즈 0 → 체크                   2축   aggression · bluff
```

`potcontrol` · `looseness` 는 D 팔에서 `Δp≠0` 이 0 건이라 이번에도 A 밖이다.
**다섯 번째 다축 분기는 나타나지 않았다.**

---

## 3. E1 · E2 — DEVIATE (둘 다 충족)

```
                    rows55c (새 표본)              rows55b (⑧)
축                Δp≠0   R(참고)     S       T      S         T
aggression        4382   99.1%   0.10345  0.099   0.10685   0.102
discipline        4422  100.0%   0.32438  0.317   0.32004   0.320
bluff             4319   97.7%   0.05929  0.058   0.06111   0.060
cbet_flop         2232   50.5%   0.14094  0.137   0.14582   0.139
N_b               4422                            4212
```

```
E1   S 비 = 0.32438 / 0.05929 = 5.471   문턱 2.0   →  넘었다
E2   T 비 = 0.317   / 0.058   = 5.459   문턱 1.5   →  넘었다
```

**E1·E2 가 모두 충족됐다. 설계 4절이 정한 정의상 ⑧ 의 분화형 판정이
표본 밖에서 재현됐다.**

⑧ 은 5.237 / 5.303, 새 표본은 5.471 / 5.459 다.

---

## 4. E3 — 2축 분기 (충족)

```
분기                  N_b    S 비(새)   S 비(⑧)   T 비(새)   T 비(⑧)
밸류 계획 실행         9283    1.771     1.765     1.827     1.794
블러프 계획 실행        1879    1.833     1.799     1.769     2.060
사이즈 0 → 체크         542    1.709     1.690     정의 안 됨  정의 안 됨
```

**세 `S` 비가 전부 2.0 미만이다.** E3 충족.

`사이즈 0 → 체크` 의 `T` 는 양쪽 축 모두 `0.000` 이라 비가 정의되지 않는다
(도구는 `inf` 로 찍는다). 설계 2절대로 문턱과 비교하지 않았다. ⑧ 7절과
같은 상황이 반복됐다 — 그 분기의 O 팔 intent 가 이미 check 다.

`블러프 계획 실행` 의 `T` 비만 2.060 → 1.769 로 움직였다. **E3 는 `S` 비만
예측했고 `T` 비는 예측하지 않았다.** 기록만 한다.

### 4-1. E3 를 확대 해석하지 않는다

**"2축 분기에서는 분화가 없다" 가 아니다.** 2축은 `max/min` 이 곧 두 값의
비라 "축끼리 비슷한가" 를 가르는 힘이 약하다고 ⑧ 설계 3-1-3 과 이번 설계
4절에 **측정 전에** 적었다. E3 가 맞은 것은 "⑧ 에서 본 크기 구간이 표본
밖에서도 같은 구간인가" 하나뿐이다.

---

## 5. 판정

```
E1  충족     E2  충족     E3  충족     E4  충족
```

> **재현됐다.** `DEVIATE` 분기의 분화형 판정은 ⑧ 이 본 시드 밖에서도
> 같은 문턱으로 다시 성립한다.

⑧ 이 한 표본에 한정된 관측으로 격하될 조건(E1 또는 E2 실패)은 발생하지
않았다. `CF_RESULT_DIVERGE.md` 는 수정하지 않는다.

### 5-1. 이 판정의 범위

```
재현된 것   DEVIATE 한 분기에서 축별 S·T 가 문턱 이상으로 갈린다는 관측
재현 안 함   왜 갈리는가 — ⑩ 의 질문이다
재현 안 함   축 서열의 원인 — ⑥·⑦ 에서 닫혔다
재현 안 함   축이 개념적으로 독립인가 — 이번에도 답하지 않는다
재현 안 함   2축 분기의 분화 여부 — 판별력이 부족하다
```

**"각 축이 독립적인 심리적 개념을 실제로 구현한다" 는 여전히 증명되지
않았다.** ⑧ 5-1 의 제한이 그대로 유지된다.

---

## 6. 사후 관측 — **예측으로 쓰지 않는다**

`S` 의 축 서열이 두 표본에서 같았다.

```
discipline  >  cbet_flop  >  aggression  >  bluff
rows55b     0.32004  0.14582  0.10685  0.06111
rows55c     0.32438  0.14094  0.10345  0.05929
```

`CF_RESULT_DIVERGE.md` 8-1 에 봉인한 산술 위치 순서와도 같다. **그러나
이 서열은 ⑨ 에서 사전 등록하지 않았으므로 재현 판정에 넣지 않았다.**
E1·E2 는 비(max/min)만 예측했다. 서열 일치는 ⑩ 이 다룰 관측이고,
**여기서 설명식을 만들지 않는다** (H3 는 ⑥ 에서 기각됐다).

---

## 7. 재현

```
python3 tools/cf_axis_l2.py \
  --axes potcontrol,aggression,bluff,thin_value_turn,discipline,looseness,cbet_flop \
  --seeds 5100-5154 --jobs 4 --rows rows55c.jsonl.gz
python3 tools/diverge.py rows55c.jsonl.gz
```

`plan.py` 는 수정하지 않았다. 새 계측도 넣지 않았다 — `rows55b` 를 만든
코드 그대로이고 시드만 다르다.
