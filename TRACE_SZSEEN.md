# 2-A 재측정 — `_sz_seen` 이 `need` 를 통째로 덮는 구조

**production code 는 수정하지 않았다.** 측정만 했다.

- baseline `6574360` (1-A 적용, 지문 `44da00bf…`)
- 도구 `tools/cf_szseen.py`
- 스트리트 3 × n=400, `bf=1.0`

---

## 대상 코드 — `plan.py:668`

```python
_sz_true = tocall/max(1.0, float(pot) - tocall)
_rdz     = PS.read_opponent(profile, opp_est) if opp_est else None
_sz_norm = PS.opp_size_norm(_rdz, _sz_true, street)
_sz_seen = PS.size_read(profile, _sz_norm)
need *= PS.call_bias(profile, street, _sz_seen, made_now, draw)
# 오독한 사이즈로 팟오즈를 다시 계산한다 (인식이 곧 판단 근거다)
if abs(_sz_seen - _sz_true) > 1e-9:
    _p0  = float(pot) - tocall
    need = (_sz_seen*_p0)/max(1.0, _p0 + 2*_sz_seen*_p0)     # ← 대입
```

바로 윗줄 `call_bias` 는 `*=` 인데 이 줄만 `=` 다. 이 한 글자 때문에 앞에서
쌓은 `calc_noise(potodds)` · 뒤 사람 위험 · 리딩 `trust` · 상·하한 클램프 ·
`call_bias` 가 전부 사라진다.

## 세 변종

| | 내용 |
|---|---|
| ① 현재 | 덮어쓰기 유지 |
| ② 제거 | 그 `if` 블록만 삭제 |
| ③ 체인보존 | `need *= 인지팟오즈/실제팟오즈` — 목적은 유지, 대입만 비율로 |

③ 의 근거: 덮어쓰기 식 `sz/(1+2sz)` 는 1-A 로 고친 `need_true` 와 **같은 식**이다.
다른 건 `sz` 가 실제냐 인지냐 뿐이므로, 그 비율만 곱하면 "인지한 사이즈로
팟오즈를 다시 본다"는 목적이 그대로 남는다. **새 임계값을 만들지 않는다** —
이미 그 자리에 있는 두 값의 비다.

변종은 `inspect.getsource` → 문자열 교체 → `exec` 로 메모리에만 만들었다.

---

## 0. 위약 / 격리

- 결정성(같은 코드 두 번): 세 스트리트 전부 **불일치 0건**
- 성향 반사실의 위약 축(`tilt_swing`·`tilt_stack`): 세 변종 × 세 스트리트 전부 **0.0%**
- **비발동군에서 세 변종의 행동 변화 0.0%** (flop 78건 / turn 78건 / river 44건).
  가른 것이 덮어쓰기 하나뿐임이 확인된다.

## 1. 발동률과 오독 크기 — **여기가 핵심이다**

| 스트리트 | 발동률 | `\|sz_seen/sz_true − 1\|` 중앙 | 3% 미만 |
|---|---|---|---|
| flop | 80.5% | 1.53% | 73.6% |
| turn | 80.5% | 1.53% | 73.6% |
| river | 89.0% | 2.36% | 60.4% |

**발동 문턱은 `1e-9` 인데 실제 오독은 중앙 1.5~2.4% 다.**
사이즈를 0.2% 잘못 본 사람도 `need` 가 통째로 덮인다.

## 2. 행동 — 발동군만 (비발동군은 전부 0.0%)

| 스트리트 | | 폴드 | 콜 | 레이즈 | ① 대비 바뀐 건 | 전환 |
|---|---|---|---|---|---|---|
| flop | ① | 48.8% | 39.4% | 11.8% | — | |
| | ② | 48.8% | 38.2% | 13.0% | 57/322 = 17.7% | call→fold 25, fold→call 22, 그 외 10 |
| | ③ | 48.4% | 38.5% | 13.0% | 56/322 = 17.4% | call→fold 24, fold→call 22, 그 외 10 |
| turn | ① | 51.6% | 31.1% | 17.4% | — | |
| | ② | 50.9% | 31.1% | 18.0% | 39/322 = 12.1% | fold→call 14, call→fold 13, 그 외 12 |
| | ③ | 50.3% | 31.4% | 18.3% | 39/322 = 12.1% | fold→call 15, call→fold 13, 그 외 11 |
| river | ① | 57.0% | 28.7% | 14.3% | — | |
| | ② | 59.3% | 26.1% | 14.6% | 29/356 = 8.1% | call→fold 18, fold→call 10, call→raise 1 |
| | ③ | 59.0% | 26.4% | 14.6% | 28/356 = 7.9% | call→fold 17, fold→call 10, call→raise 1 |

`need` 중앙: ① 0.292 / ② 0.311 / ③ 0.308 (flop). **①의 need 중앙이 `need_true`
중앙(0.292)과 같다** — 덮어쓰기가 걸린 80%는 사실상 날 팟오즈로 판정한다.

플랍·턴에서는 방향이 양쪽으로 갈리고(call→fold 와 fold→call 이 비슷),
리버에서만 덮어쓰기가 한 방향으로 느슨하게 만든다(폴드 59.3% → 57.0%).

## 3. ② 와 ③ 이 거의 같다 — 이게 답이다

flop 322건 중 ②와 ③의 행동이 갈린 것은 **1건**, turn 0건, river 1건.

즉 "인지한 사이즈로 팟오즈를 다시 본다"는 **목적 자체가 만드는 행동 변화는
거의 0** 이다. 오독이 중앙 1.5% 뿐이니 당연하다.
**덮어쓰기가 만드는 8~18% 의 행동 변화는 전부 체인 삭제분이다.**

## 4. 성향 반사실 — 삭제가 아니라 **채널 교체**다

축을 1↔9 로 뒤집었을 때 행동이 갈리는 비율:

| 축 | flop ① | flop ② | flop ③ | river ① | river ② | river ③ |
|---|---|---|---|---|---|---|
| `sizing_tell` | **14.5%** | 1.0% | 1.8% | 0.5% | 0.0% | 0.2% |
| `potodds` | 3.0% | **8.8%** | **9.0%** | 1.5% | 4.5% | 4.5% |
| `aggression` | 5.0% | **11.5%** | **11.5%** | 6.0% | 11.8% | 11.8% |
| `looseness` | 2.5% | 6.0% | 5.5% | 1.8% | 3.0% | 2.5% |
| `discipline` | 1.2% | 2.0% | 2.2% | 1.2% | 2.8% | 2.8% |
| `range_read` | 13.5% | 12.2% | 12.8% | **34.2%** | 24.5% | 24.8% |
| `reraise` | 11.2% | 11.5% | 11.5% | 12.8% | 13.0% | 13.0% |

(turn 은 flop 과 같은 모양. `sizing_tell` 10.0 → 1.2 / 1.2,
`potodds` 2.5 → 8.5 / 8.5, `aggression` 6.8 → 14.5 / 14.5)

읽는 법:

- **덮어쓰기는 `sizing_tell` 의 거의 유일한 통로다.** 14.5% → 1.0%.
  `trust` 블록 안의 `sizing_tell` 항은 실제로는 1% 짜리다. 지금까지
  "`sizing_tell` 이 응답층에서 제일 센 축"이라고 본 것은 리딩 능력이 아니라
  **need 재대입 레버**를 본 것이었다.
- **덮어쓰기는 `potodds`·`aggression`·`looseness`·`discipline` 을 절반 이하로 누른다.**
  이 넷이 정확히 덮어쓰기가 지우는 항이다 —
  `calc_noise(potodds)` / `trust`(aggr) / `call_bias`(loose·discipline).
- `reraise` 는 `decide_response` 쪽 축이라 셋 다 동일. 격리가 맞다는 방증이다.
- `range_read` 만 ① 이 더 높다(리버 34.2% vs 24.5%). `range_read` 는
  `read_opponent` → `opp_size_norm` → `_sz_seen` 경로로도 들어가서,
  덮어쓰기가 있으면 need 재대입 레버를 같이 타기 때문이다.

**결론: 이 줄은 "개성을 지운다"가 아니라 "개성의 통로를 하나로 갈아끼운다".**
사이즈 인식 축(`sizing_tell`·`range_read`)만 증폭하고 나머지 넷을 죽인다.

## 5. 왜 `=` 로 쓰였나 — 도입 시점을 보면 설명이 된다

블록 전체가 `8fadcb3` 에서 한 번에 들어왔다. 그 시점의 `need_true` 는

```
plan.py:441 (8fadcb3)   need_true = (tocall*bf)/max(1.0, float(pot))
```

즉 **1-A 로 고치기 전의 틀린 식**이었다. 그래서 그때 이 함수 안에서
`sz/(1+2sz)` 는 **유일하게 올바른 팟오즈 표현**이었다. 대입으로 쓴 것이
결과적으로 틀린 `need_true` 를 덮는 역할을 했다.

커밋에는 "왜 대입인가"에 대한 근거가 없다. 바로 윗줄 `call_bias` 는 `*=` 다.

1-A 로 `need_true` 가 교정된 지금, 이 대입이 덮어주던 것은 더 이상 없다.
남은 것은 체인 삭제뿐이다.

---

## 다음 판단을 위한 정리

이번 측정이 답한 것:

1. 목적(인지 사이즈 반영)이 만드는 행동 변화 ≈ 0 (②와 ③ 차이 1건/322)
2. 실제 효과 8~18% 는 전부 체인 삭제분
3. 삭제가 아니라 채널 교체 — `sizing_tell` 14.5%↔1.0%, `potodds`·`aggression` 반토막
4. 대입으로 쓰인 역사적 이유는 1-A 이전의 틀린 `need_true` 였을 가능성이 높다

아직 답하지 않은 것:

- ③ 으로 바꾸면 `sizing_tell` 이 1.8% 로 떨어진다. 사이징 텔이라는 개념을
  **응답층에 남길 것인지**, 남긴다면 `trust` 블록의 계수를 어떻게 할 것인지는
  별도 결정이다. 임계값 재발명이 되므로 이번 라운드에서 제안하지 않는다
- 토너먼트 레벨 영향(지문·필드 페이스)은 재지 않았다. 수정 결정이 난 뒤에 잰다
