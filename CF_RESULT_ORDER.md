# ⑥ 결과 — 축별 서열은 무엇을 의미하는가

설계는 `CF_DESIGN_ORDER.md`. 이 문서는 그 설계의 경쟁 가설 다섯을 각각
측정한 결과와 **각 가설의 폐쇄 상태**를 기록한다.

**결론부터.** 서열은 재현된다. 그러나 **사전에 적어둔 다섯 후보 중 어느
것도 단독으로 그 서열을 설명하지 못한다.** 그리고 사후 결합식은 만들지
않는다.

---

## 0. 무엇이 재현됐는가

```
L1 POST flip   aggression 9.2 > discipline 4.3 > bluff 3.5 > tvt 2.7 > cbet 1.0 > potcontrol 0.0
L2 D           aggression 3.5 > discipline 1.8 > bluff 1.3 > tvt 1.1 > cbet 0.4 > potcontrol 0.0
```

**확인된 것은 서열의 재현이다.** 원인의 동일성도, 효과 크기의 동일성도
아니다 (`CF_DESIGN_ORDER.md` 0절).

⑥-3-1 에서 두 층의 단위·정의·분모를 맞췄고, 그 결과와 잔여 차이 분해는
`CF_RESULT_NORM.md` 에 있다. **이 문서는 서열의 원인 가설만 다룬다.**

---

## 1. 측정 방법과 산출물

```
H1   정적 분석    tools/axis_dataflow.py   (L1F 적용, 3c1f26d)
H2   기존 archive  collected.jsonl 7,064건
H3   소스 전수     plan.py · persona.py 읽기
H4   소스 전수     같음
H5-A 필드 재구성   tools/axis_sd.py         (447627b, 핸드 미실행 0.2초)
H5-B 기존 원자료   tools/l2_recount.py (f)  (e253f93, rows55b)
```

**H1·H3·H4 는 코드만 읽었다. H2·H5-B 는 기존 자료만 썼다. H5-A 는
`fieldsim.Field` 생성까지만 돌고 핸드를 돌리지 않는다.**
⑥ 전체에서 새 시뮬레이션은 한 번도 하지 않았다.

---

## 2. 가설별 폐쇄 상태

### H1 — 도달 범위

`axis_dataflow.py` 의 `L1F` 가 정의만 되고 사용 0회였다. 실제로 쓰게 해서
실행층 표를 냈다. 기존 계획층 출력은 바이트 단위로 유지된다.

**폐쇄 상태: 정적 지도 완료. 런타임 도달 범위가 아니다.** 세 한계가 있다.

```
1  make_plan 이 barrel_size(480)·bluff_mode(497) 를, trap_judgment 가
   opp_bet_prob(224) 을 부른다 → 계획 구축 중에도 읽힌다 (설계 5-2)
2  perceived_rel(계획층)이 만든 rel 을 실행층이 읽는다 (설계 5-1)
3  호출 그래프가 아니라 읽기 지점 표다 — cbet_freq 는 decide_aggression:863
   안에서 불리는데 표에는 그 중첩이 안 보인다
```

타당성 검사는 통과했다 — 지도상 `decide_aggression` 도달이 없는
`potcontrol`·`looseness` 가 실측 0 이고 나머지 다섯이 비 0 이다. 다만
`cbet_flop` 이 한계 3 에 걸려 **서열이나 크기를 이 표로 설명하지 않는다.**

### H2 — 기저 빈도

`collected.jsonl` 중복 제거 후 포스트플랍 intent 7,064건에서 `plan` 라벨과
`trace.why` 로 분기 실행 빈도가 그대로 나온다.

```
giveup 2,540 (36.0%)  ·  pot_control 1,208 (17.1%)  ·  value_3street 1,031 (14.6%)
밸류 계획 실행 1,949  ·  DEVIATE 지속벳 1,055  ·  블러프 계획 실행 428
```

**폐쇄 상태: 산출 가능함을 확인했고 서열과 붙여보지 않았다.**
"기저 빈도가 높으므로 flip 이 크다" 같은 인과 해석을 하지 않는다
(설계 4-1 에 측정 전부터 적어둔 금지).

### H3 — 계수 크기

실행층 소비처의 계수를 전수로 뽑았다 (축 1→9, 교락축 5.0 고정).

```
aggression      decide_aggression 밸류   0.448 → 0.912   ×2.04
discipline      decide_aggression:866    0.915 → 0.235   ×0.26
bluff           decide_aggression 블러프  0.420 → 0.980   ×2.33
thin_value_turn decide_aggression 밸류   0.640 → 1.360   ×2.12
cbet_flop       cbet_freq                0.560 → 1.440   ×2.57
potcontrol      **실행층 소비처 없음**
```

**폐쇄 상태: 배율 단독으로 서열이 설명되지 않는다.** `cbet_flop` 의 배율이
×2.57 로 가장 큰데 L1 POST flip 은 1.0% 로 뒤에서 두 번째다. `bluff` 도
배율이 `aggression` 보다 큰데 flip 은 작다.

### H4 — 게이트 · 클램프 · 포화

위치를 전부 확정했다.

```
max(0.0, bias(...))  정의역 절반 차단. discipline 과 looseness 가 정확히
                     반대쪽 절반에서만 작동한다
이산 게이트          sk('potcontrol')>=1 (PS.sk ≥ 3.33) 등. 계획층
계단                 persona.derive 의 aggression ≤ 4 → value='lead' → p ×= 1.12
포화                 rel >= 0.65 의 p 승격이 앞의 축 계수를 압축한다
```

**1↔9 범위에서 실제로 닿는 출력 클램프는 하나뿐이다** — `bluff` 축 9 에서
`p = 0.980 > 0.95`. 나머지는 전부 미접촉이다.

**폐쇄 상태: 위치 확정. 서열 설명에 쓰지 않는다.**

### H5-A — 입력 공간

L1/L2 와 같은 필드를 재구성해 쟀다 (`entries=24`, 시드 5000-5054,
`field_quality=0.6490`, 1,265명).

```
축                 sd    1↔9=?sd   R²(잠재3)   잔차sd   1↔9=?잔차sd
aggression      2.420      3.31      0.880     0.839       9.53
discipline      2.244      3.56      0.106     2.123       3.77
looseness       2.261      3.54      0.141     2.096       3.82
potcontrol      1.974      4.05      0.530     1.353       5.91
thin_value_turn 1.858      4.31      0.524     1.281       6.24
bluff           1.711      4.67      0.566     1.127       7.10
cbet_flop       1.549      5.17      0.535     1.056       7.57
```

`1↔9` 는 축마다 같은 크기가 아니다 — 전체 sd 기준 3.31~5.17배, 잔차 sd
기준 3.77~9.53배다. 그리고 스왑이 `latent` 를 그대로 두므로 `aggression`
(r:aggro 0.938)에서는 **생성분포에서 매우 희귀한 조합**이 만들어진다.
생성식상 불가능한 값은 아니다.

**폐쇄 상태: 서열을 설명하지도 배제하지도 못한다.**

```
flip 과의 Spearman ρ    전체 sd  −0.600    잔차 sd  +0.257
```

**두 정규화가 부호부터 다르다.** n=6 이라 두 ρ 모두 정보가 거의 없다.
"정규화하면 서열이 바뀔 수 있다" 고 적었는데, **바뀌는 방향이 정규화
방식에 달려 있다.**

### H5-B — 함수 공간

```
6416d98   측정 **전에** decision quantity = p, 1차 지표 = median(|Δp|) 를 고정
e253f93   그 정의 그대로 측정. rows55b 만 사용, 추가 실행 없음
```

설계(4-9)는 측정 후 **수정하지 않았다.**

```
축                팔          N    med(Δp)   med(|Δp|) mean(|Δp|)      Δp≠0         ↑ / ↓
aggression       D      31176     0.0200      0.0200     0.0942     51.9%    16174 / 0
discipline       D      31176     0.0000      0.0000     0.0432     13.5%        0 / 4212
bluff            D      31176     0.0000      0.0000     0.0359     19.5%     6093 / 0
thin_value_turn  D      31176     0.0000      0.0000     0.0271      9.4%     2927 / 0
cbet_flop        D      31176     0.0000      0.0000     0.0103      7.1%     2198 / 0
potcontrol       D      31176     0.0000      0.0000     0.0000      0.0%        0 / 0
looseness        D      31176     0.0000      0.0000     0.0000      0.0%        0 / 0
```

**사전 1차 지표 `median(|Δp|)` 가 퇴화했다.** `aggression` 을 빼면 전부
`0.0000` 이다. 대부분의 결정이 그 축을 읽는 분기에 도달하지 않아 분포가
0 에 몰린다.

**지표를 사후에 바꾸지 않았다.** 4-9 에 함께 적어둔 나머지 통계량을 그대로
보고했다.

**`Δp≠0` 조건부 중앙값 같은 새 지표는 이번 단계에서 계산하지 않았다.**
4-9 에 없던 통계량이다. 필요하면 **다음 단계에서 별도 설계 후 측정한다.**

확인된 것:

```
potcontrol D · looseness D 의 Δp≠0 이 정확히 0.0% — H1 정적 지도와 일치
thin_value_turn M · cbet_flop M 도 0.0% — 계획층 소비처 없음과 일치
방향이 거의 완전 단조 — aggression D 16,174↑/0↓, discipline D 0↑/4,212↓
```

**폐쇄 상태: 단독 설명 가설로 채택하지 않는다.**
`mean(|Δp|)` 의 D 팔 순서는 관측 flip 순서와 같지만 **`Δp≠0` 비율의 순서는
다르다**(`aggression` > `bluff` > `discipline` 로 가운데 둘이 뒤집힌다).
**두 통계량이 서로 다른 순서를 낸다.**

---

## 3. ⑥ 의 결론

| 가설 | 폐쇄 상태 |
|---|---|
| H1 도달 범위 | 정적 지도 완료. 런타임 도달 범위가 아니다 (한계 3) |
| H2 기저 빈도 | 산출 가능 확인. 서열과 붙여보지 않았다 |
| H3 계수 크기 | 배율 단독으로 서열이 설명되지 않는다 |
| H4 게이트 | 위치 확정. 닿는 클램프는 `bluff` 상한 하나 |
| H5-A 입력 sd | 두 정규화가 부호부터 다르다. 설명도 배제도 못 한다 |
| H5-B 함수 공간 | 1차 지표 퇴화. 두 통계량이 서로 다른 순서. 단독 채택 안 함 |

> **다섯 후보 전부 "단독으로는 관측된 축 서열을 설명하지 못한다" 로
> 닫힌다.**

---

## 4. 하지 않은 것

```
사후 결합식을 만들지 않았다
  배율 × 빈도 × gate 를 결과에 맞춰 묶으면 결과를 보고 모형을 맞춘 것이
  된다. 설계 4-4 에서 측정 전에 금지했고 그대로 지켰다

Δp 로 서열을 설명하지 않았다
  설계 4-9 가 측정 전에 금지했다

H5-A 와 H5-B 를 결합하지 않았다
  Δp / 잔차sd 같은 식을 만들지 않았다. 두 결과를 나란히 둘 뿐이다

새 통계량을 사후에 추가하지 않았다
  1차 지표가 퇴화했지만 조건부 중앙값을 끼워 넣지 않았다

Level 1·2 의 숫자를 수정하지 않았다
  그 실험들은 "1↔9 를 줬을 때" 를 정확히 잰 것이다
```

## 5. 미검증으로 남기는 것

```
H2 를 서열과 대조하는 것 — 산출 가능하지만 하지 않았다
Δp≠0 조건부 중앙값 — 별도 설계가 필요하다
H1 의 런타임 도달 범위 — 호출 그래프까지 파려면 범위가 커진다
CF_RESULT_NORM.md 7절의 항목들 (trace 40 상한 등)
```

## 6. 다음 질문

> 그러면 축 서열을 실제로 만들어내는 구조적 원인은 무엇인가?

**이 문서는 그 답을 담지 않는다.** 다섯 후보가 단독으로는 아니라는 것까지가
⑥ 의 결과다. 새 가설은 별도 설계 단계에서 만든다.

## 7. 재현

```
python3 tools/axis_dataflow.py
python3 tools/axis_sd.py --seeds 5000-5054 --entries 24
python3 tools/l2_recount.py rows55b.jsonl.gz
```

`rows55b.jsonl.gz` 는 용량 때문에 저장소에 넣지 않았다. `CF_RESULT_NORM.md`
8절의 명령으로 다시 만든다.
