# ④ 측정조건 calibration 결과

`tools/cond_calib.py`, 시드 3개(5000-5002), `start_stack=30000` 고정.

**목표는 최대 표본이 아니라 교란이 최소인 측정조건 결정이다.**

---

## 1. 두 손잡이의 교란이 직교한다

```
              인당핸드  플랍기회 | 유효bb  플랍%  | field_q  cbet_flop축
24x12          72.8     2.5    |  62.0   32.8  |  0.649   5.64±1.49
24x40         125.2     5.2    | 106.5   33.7  |  0.649   5.64±1.49
24x80         186.8     6.8    | 127.7   34.1  |  0.649   5.64±1.49
48x40         141.7     6.7    |  97.5   39.6  |  0.714   5.86±1.57
100x40        147.7     6.3    | 100.5   43.0  |  0.783   6.09±1.61
```

### `hpl` — 필드 구성은 **비트까지 동일**, 스택 깊이가 2.06배

`hpl` 12/40/80 세 조건에서 `field_q` 0.649, `study` 5.31±1.93,
`cbet_flop` 5.64±1.49 — 소수점까지 같다. `hands_per_level` 은
`field_quality` 를 전혀 건드리지 않는다.

**그런데 관측 시점 유효 스택이 62.0 → 106.5 → 127.7bb 다.**
플랍 도달률은 32.8 → 34.1% (1.3%p) 로 거의 안 움직였다.

> **도달률 하나만 봤으면 "`hpl` 은 깨끗하다"고 결론냈을 것이다.**
> 62bb 게임과 128bb 게임은 SPR 이 다른 별개의 전략 환경이다.

### `entries` — `field_q` 이동, 도달률 9.3%p, 스택은 불변

```
entries  24 → 48 → 100
field_q       0.649 → 0.714 → 0.783      (코드 예측과 일치)
플랍 도달률     33.7% → 39.6% → 43.0%     (hpl 의 7배)
유효 스택      106.5 → 97.5 → 100.5bb    (거의 불변)
축 평균        cbet_flop    5.64 → 5.86 → 6.09
              barrel_river 3.41 → 3.98 → 4.19
              barrel_turn  4.08 → 4.46 → 4.87
```

---

## 2. 정정 — `skill_bounds` 범위 압축 예측은 틀렸다

측정 전에 **"`skill_bounds` 기각표집이 범위를 좁혀 `entries` 가 ρ 자체를
깎을 수 있다"** 고 예측했다. **실측이 지지하지 않는다.**

```
              entries 24 → 48 → 100
cbet_flop       sd 1.49 → 1.57 → 1.61
barrel_river    sd 1.97 → 2.03 → 2.00
aggression      sd 2.21 → 2.38 → 2.45
discipline      sd 1.99 → 2.25 → 2.24
```

sd 가 줄지 않고 오히려 약간 늘었다. `skill_bounds` 폭이 q 0.649 에서 5.68,
q 0.783 에서 5.57 로 거의 같고, 그나마 개별 축이 아니라 `overall_skill`
에만 걸린다. 압축이 무시할 수준이다.

**`entries` 의 교란은 분산 압축이 아니라 평균 이동 + 플레이 성격 변화다.**

---

## 3. `hpl` 40/80 은 정의된 포맷 밖이다

```
formats.py    deep 18  ·  standard 12  ·  turbo 7  ·  hyper 4
```

가장 느린 `deep` 이 18 이다. `hpl` 40 은 그 2.2배, 80 은 4.4배 —
**이 게임의 포맷 표가 정의하지 않는 구조다.** `hpl` 로 표본을 사면
엔진을 정의되지 않은 영역에서 측정하게 된다.

---

## 4. 세 번째 손잡이 — 시드

`entries`/`hpl`/`start_stack` 안에서만 생각하다 놓쳤다.

**같은 조건에서 시드를 늘리면 `field_q` 가 결정론적으로 동일하므로
교란 0 으로 플레이어 수가 선형으로 는다.**

```
ρ (방향성·분리도)   → 시드.  교란 0
split-half r       → hpl 만 살 수 있고 스택 깊이 2배를 대가로 낸다
```

---

## 5. `f` 층이 필수인 것이 확정됐다

**리버 배럴 기회가 다섯 조건 전부에서 인당 중앙 0.0 이다.**
`24x80`(인당 187핸드)에서도 0 이다.

`barrel_river` 는 실현 행동으로 **영구히** 측정 불가다.
`f` 층이 없으면 "측정 불가"와 "축이 죽었다"를 구별할 방법이 없다.

---

## 6. 결정

```
hpl          정의된 포맷 값에 고정 (12 = standard, 또는 18 = deep)
entries      고정. 시드 간에 동일해야 field_q 가 같다
start_stack  30000 고정
플레이어 수    시드로 번다 (교란 0)
인당 표본 부족  f 층으로 메운다
```

`entries` 값은 `hpl=12` 행을 마저 재고 정한다 — 도달률 이득(43%)이
정의된 포맷 안에서도 나오는지 확인한 뒤.
