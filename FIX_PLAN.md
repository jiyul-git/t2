# 수정 계획 — 읽기 전용 조사 결과의 정리

**이 문서는 경계선을 확정하기 위한 것이다. 아직 아무것도 고치지 않았다.**

근거 문서: `TRACE_PLAN.md`, `CF_PROFILE.md`, `TRACE_DEFEND.md`,
`TRACE_RESPONSE.md`, `TRACE_NEEDPATH.md`, `TRACE_MDF.md`, `TRACE_REFRESH.md`,
`REVIEW_2.md`

---

## 들어가기 전에 — 세 수정 모두 baseline 지문을 깬다

CLAUDE.md 작업 원칙 1에 따라 현재 baseline 은 `8dc14d3`, 지문은

```
python3 tools/fingerprint.py --seeds 3000-3005 --hands 30
1dd5d83f7a638d174636d7dc44d39f1c2250ce35a1d674d5789ff90b57935e59
```

**아래 확정 수정 셋은 전부 판단 분기의 입력값을 바꾸므로 이 지문이 깨진다.**
CLAUDE.md 가 요구한 순서를 지킨다 — *"수정에 들어갈 때는 먼저 반사실
evaluator로 영향 범위를 재고, 그 결과를 보고 나서 별도 브랜치에서 한다."*

반사실 측정은 끝났다(`tools/cf_*.py` 6종). 남은 것은 **별도 브랜치**와
**단계별 새 지문 기록**이다.

각 단계마다:
1. 수정 전 지문 기록
2. 수정
3. 해당 반사실 도구로 영향 재측정 (아래 각 항목의 '검증 방법')
4. 새 지문 기록 + CLAUDE.md baseline 이력에 한 줄 추가
5. 커밋

**세 수정을 한 커밋에 묶지 않는다.** 묶으면 지문이 한 번만 바뀌어
어느 수정이 무엇을 바꿨는지 추적이 끊긴다.

---

# 1. 확정 수정

계산 일관성 문제다. "무엇을 의도했는가"를 묻지 않고도 틀렸다고 말할 수 있다.

## 1-A. `need_true` 팟오즈 산수

| | |
|---|---|
| **위치** | `plan.py:609` (`calldown_need` 첫 줄) |
| **현재 동작** | `need_true = (tocall*bf)/max(1.0, float(pot))`. `pot` 은 `pot_live`(상대 벳 포함, **내 콜 미포함**, `session.py:449`) |

**왜 문제인가.** 콜하면 내 칩도 팟에 들어간다. 교과서 팟오즈는

$$\text{필요승률}=\frac{\text{tocall}}{\text{pot\_live}+\text{tocall}}$$

이다. 분모에 `tocall` 이 두 번 들어가는 것이 맞다 — 한 번은 상대 벳으로,
한 번은 내 콜로. 현재 식은

$$\frac{\text{need\_true}}{\text{교과서}}=\frac{1+2\,\text{sz}}{1+\text{sz}}$$

배 과대평가다 (0.5팟 벳 1.33배, 1팟 1.50배, 2팟 1.67배).

호출부 주석(`plan.py:1264`)이 *"tocall 을 또 더하면 분모에 콜 비용이 두 번
들어간다"* 고 적어놨는데 **그 주석이 틀렸다.**

방증 셋:
- 같은 파일 670-671 줄의 덮어쓰기 식 `sz/(1+2sz)` 가 **교과서식과 대수적으로
  동일**하다. 즉 코드 안에 올바른 식이 이미 있다
- CLAUDE.md '아직 안 본 것' 2번(h69: `eq .338 ≥ 필요승률 .311` 인데 폴드)이
  `.311×1.40 = .435 > .338` 로 정확히 설명된다
- 반사실에서 이 경로 교정이 **단방향 `폴드 → 콜`** 만 만든다

| | |
|---|---|
| **실측 영향** | 덮어쓰기가 안 걸린 상황(플랍 15%, 리버 13%)에서 `폴드→콜` 12/0, 6/0. `need` 평균 −0.152 / −0.156. 단방향 |
| **기대 효과** | 과잉 폴드 해소. 특히 `giveup`/`bluff_2street` 계획이 `decide_response:812` 조기 return 으로 순수 팟오즈를 타는 경로가 바로잡힌다 |

**부작용 가능성.**
- **`bf`(버블팩터)의 실효 크기가 같이 바뀐다.** `bf` 가 분자에 곱해져
  있으므로 분모를 키우면 ICM 압박이 상대적으로 약해진다. 별도 측정 필요
- `calldown_need` 의 상·하한(`need_true*1.75+0.05` / `need_true*0.55`)이
  `need_true` 기준이라 클램프 위치도 같이 내려간다
- 전체 폴드율이 내려간다 → 봇이 전반적으로 루즈해 보일 수 있다

**수정안** (아직 적용 안 함)

```python
# plan.py:609
- need_true = (tocall*bf)/max(1.0, float(pot))
+ # pot 은 pot_live 다 — 상대 벳은 들어 있고 **내 콜은 아직 아니다**
+ # (session.py:449). 콜하면 내 칩도 팟에 들어가므로 분모에 tocall 을
+ # 더해야 한다. 670줄의 덮어쓰기 식 sz/(1+2sz) 가 같은 값이다.
+ need_true = (tocall*bf)/max(1.0, float(pot) + float(tocall))
```

호출부 주석(`plan.py:1264-1266`)도 같이 고쳐야 한다 — 지금 내용이 반대다.

**검증 방법**

```bash
# 1) 수정 전후 need 분포와 행동 분포
python3 tools/cf_needpath.py --n 500 --street flop|turn|river
#    현재 문서의 'C 팟오즈 교정 + 성향 보존' 이 아니라, **덮어쓰기는
#    그대로 둔 채 need_true 만 고친 변종(D)** 을 새로 넣어 비교한다.
#    기대: 덮어쓰기가 걸린 84% 에서는 거의 변화 없음(이미 같은 값),
#          안 걸린 16% 에서 단방향 폴드→콜.
# 2) 성향 효과가 약해지지 않았는가
python3 tools/cf_response.py --n 400 --read --reads
#    기대: range_read / sizing_tell / reraise 의 뒤집힘 비율이 유지.
#          내려가면 클램프가 더 자주 걸린 것이므로 조사 필요.
# 3) ICM
#    bf 를 1.0 / 1.5 / 2.2 로 나눠 같은 측정을 반복. bf 가 클수록
#    수정 전후 차이가 커지면 ICM 재보정이 필요하다는 신호.
# 4) 지문
python3 tools/fingerprint.py --seeds 3000-3005 --hands 30   # 격리 폴더
```

### 1-A 시도 결과 — **적용했다.**

측정 도구는 `tools/cf_potodds.py`. 현재 소스에서 `need_true` 분모만 예전 식으로
되돌린 변종을 메모리에 만들어(`inspect.getsource` → 문자열 교체 → `exec`)
같은 상황·같은 seed 로 비교한다. `_sz_seen` 덮어쓰기(2-A)는 양쪽 다 그대로다.

**결정성(위약에 해당)** — 같은 코드 두 번 돌려 불일치, 9개 조합 전부 0건.

**bf × 스트리트, 각 n=400**

| 스트리트 | bf | 폴드 예전→현재 | 뒤집힘 | 전환 |
|---|---|---|---|---|
| flop | 1.0 | 53.2 → 50.8 (−2.5%p) | 10 (2.5%) | fold→call 10 |
| flop | 1.5 | 53.8 → 52.0 (−1.8%p) | 9 (2.2%) | fold→call 7, call→raise 2 |
| flop | 2.2 | 55.5 → 52.5 (−3.0%p) | 13 (3.2%) | fold→call 12, call→raise 1 |
| turn | 1.0 | 54.5 → 52.8 (−1.8%p) | 8 (2.0%) | fold→call 7, call→raise 1 |
| turn | 1.5 | 56.8 → 53.8 (−3.0%p) | 12 (3.0%) | fold→call 12 |
| turn | 2.2 | 57.5 → 56.0 (−1.5%p) | 6 (1.5%) | fold→call 6 |
| river | 1.0 | 59.5 → 58.0 (−1.5%p) | 6 (1.5%) | fold→call 6 |
| river | 1.5 | 59.8 → 59.2 (−0.5%p) | 2 (0.5%) | fold→call 2 |
| river | 2.2 | 60.5 → 59.8 (−0.8%p) | 3 (0.8%) | fold→call 3 |

전체 뒤집힘 69건 = fold→call 65 + call→raise 4. `call→fold` 0, `raise→*` 0.
**한 방향이다.**

**bf 에는 추세가 없다 — 이게 맞다.** 두 식의 비는 예전/현재 = 1+sz 로
`bf` 가 약분돼 사라진다. 계획서에 적은 "bf 가 클수록 차이가 커지면 ICM
재보정 신호"는 애초에 성립할 수 없는 기대였다. bf 는 need 의 절대 수준만
올려 판정이 eq 분포의 다른 자리에 떨어지게 할 뿐이다.

**크기가 작아 보이는 이유는 2-A 다.** `_sz_seen` 덮어쓰기 발동률은
flop/turn 80.5%, river 89.0%. 걸리면 need 가 `sz/(1+2sz)` 로 통째로 덮여
1-A 와 무관해진다. 덮어쓰기가 안 걸린 자리만 분모로 잡으면 flop bf=1.0
기준 78건 중 10건 = **12.8%** 다. 리버에서 효과가 가장 작은 것도
발동률이 89% 로 제일 높기 때문이다.

### 관문 2(성향 보존) — `sizing_tell` 이 플랍에서 깎였다

`cf_response --n 400 --read --reads`, 같은 시드로 수정 전후:

| 축 | flop 전 | flop 후 | river 전 | river 후 |
|---|---|---|---|---|
| `sizing_tell` | 21.2% | **14.8%** | 0.8% | 0.2% |
| `range_read` | 13.8% | 14.5% | 40.8% | 40.5% |
| `reraise` | 10.8% | 10.5% | 13.2% | 13.5% |
| `aggression` | 8.5% | 6.5% | 7.0% | 6.2% |
| `potodds` | 2.8% | 1.8% | 1.5% | 1.5% |

위약 축은 수정 전후 모두 0.0%. `range_read`·`reraise` 는 유지.
`sizing_tell` 만 −6.4%p 내려갔다.

계획서가 "내려가면 클램프가 더 자주 걸린 것"이라고 적어 둔 그대로였다.
원인은 리딩 조정의 **하한이 `need_true` 에 비례**한다는 것이다:

```
need -= trust * (read - 0.35)      ← 절대량. 1-A 와 무관하다
need = max(need, need_true*0.55)   ← 내려갈 여지 = 0.45 × need_true
```

`need_true` 가 1/(1+sz) 배로 작아지면 그 여지도 같은 비율로 줄어든다. 실측:

```
하한 걸림                                7.2% → 12.5%
대역폭(need_true*1.75+0.05 − need_true*0.55) 중앙  0.564 → 0.410
sizing_tell 이 need 에 만드는 진폭 중앙   0.122 → 0.081   (−34%)
range_read  진폭 중앙                    0.0135 → 0.0132  (거의 그대로)
```

`range_read` 는 진폭이 0.013 이라 대역 안쪽에 여유가 많아 안 눌리고,
`sizing_tell` 은 진폭이 커서 하한에 눌린다. 뒤집힘 감소율 −30% 와
진폭 감소율 −34% 가 맞는다.

**해석은 갈린다.** 하한 주석의 의도("리딩은 문턱을 조정하는 것이지 팟오즈를
뒤집는 게 아니다")를 기준으로 보면, 예전에는 부풀려진 `need_true` 에 하한이
걸려 있어 리딩이 의도보다 넓게 움직이고 있었던 것이고 지금이 제자리다.
"성향이 행동에 닿는 정도"만 보면 플랍에서 가장 센 읽기 축이 30% 깎였다.

**하한 계수 `0.55` 는 이번 패치에서 건드리지 않는다.** 임계값 재발명
(작업원칙 2)에 걸린다. 다음 라운드에서 2-A 와 같이 본다.

### 지문

```
수정 전 (982a8f8)  1dd5d83f7a638d174636d7dc44d39f1c2250ce35a1d674d5789ff90b57935e59
수정 후 (1-A)      44da00bfccd58820da549a7ef91125814467ef2edc7b3ddd074929d4959c15d4
```

수정 전 값이 `CLAUDE.md` 의 baseline 과 정확히 일치한다 — 측정 장치가 성하고,
1-C 가 지문을 깨지 않았다는 기록도 재확인됐다. 1-A 는 시드 6개가 전부 달라진다.
판단을 실제로 바꾸는 수정이니 당연한 결과다.

`tools/wirecheck.py` 36/36, 배선 누락 없음.

---

## 1-B. `mdf` 6bb 초과 포화

| | |
|---|---|
| **위치** | `gto.py:150` (`_MDF` 표), `gto.py:153-154` (`mdf`), 호출 `gto.py:171` |
| **현재 동작** | `_interp` 가 표 밖을 **끝값으로 고정**한다. 6bb·25bb·100bb 오픈이 전부 `0.380` (비율 0.679) |

**왜 문제인가.** 오픈이 클수록 디펜스 폭은 계속 좁아져야 한다.
표는 2.0~3.0bb 구간에서 이론 MDF 와 **1.6% 이내**로 맞는데
(`TRACE_MDF.md` 2절), 6bb 에서 보정이 멈춘다. 6bb 를 넘는 모든 오픈이
6bb 취급이다. 표의 다섯 점과 6bb 끝은 **루트 커밋(`8fadcb3`)** 이고
근거 기록이 없다.

| | |
|---|---|
| **실측 영향** | BB 가 직면한 오픈 156회 중 **38회(24%)가 6bb 초과**(최대 111.8bb). 반사실: 전체 행동 변화 4.2%, 그러나 `>12bb` 구간 방어율 **31.0% → 9.4% (+21.6%p)**. 경로별로 올인 대면 +27.0%p, 일반 +15.1%p |
| **기대 효과** | 큰 오픈·오픈 쇼브에 대해 과도하게 넓게 방어하던 것이 좁아진다. `h31`(36.5bb, 스택 86bb)·`h123`·`h140`·`h204`·`h190` 처럼 **올인 대면이 아닌** 큰 오픈에 특히 효과 |

**부작용 가능성.**
- `calloff_cap` 도 `defend_thresholds` 를 거치므로 **올인 대면 콜오프가
  같이 좁아진다.** 거기엔 `CALLOFF_TIGHTEN` 과 `cap /= bf` 가 이미 걸려
  있어 **이중으로 좁아질 수 있다.** 반드시 따로 측정할 것
- `ranges.preflop_range` 도 같은 문턱을 쓰므로 **상대 레인지 모델도
  같이 좁아진다.** 방향은 맞지만 포스트플랍 `eq` 가 전부 이동한다
- 짧은 스택 판이 많은 후반에 영향이 몰린다

**수정안** (아직 적용 안 함)

표를 늘리는 대신 **마지막 점에서 이론 MDF 모양으로 이어간다.**
새 상수를 만들지 않는다 — 앵커는 표의 마지막 점 `(6.0, 0.38)` 이고,
`1.5` 는 블라인드 합계로 `preflop.py` 에 이미 쓰이는 값이다.

```python
# gto.py:153
  def mdf(open_bb):
-     return _interp(_MDF, max(1.5, float(open_bb or 3.0)))
+     x = max(1.5, float(open_bb or 3.0))
+     if x <= _MDF[-1][0]:
+         return _interp(_MDF, x)
+     # 표 밖에서 _interp 는 끝값으로 고정한다 — 6bb 와 100bb 가 같아진다.
+     # 마지막 점에서 이론 MDF(1.5/(1.5+X)) 모양 그대로 이어간다.
+     # 1.5 는 블라인드 합계(SB 0.5 + BB 1.0)다.
+     x0, y0 = _MDF[-1]
+     return y0 * ((1.5 + x0) / (1.5 + x))
```

이 확장이 내는 비율 (기준 `mdf(3.0)`):

| 오픈 | 현재 | **제안** | 순수 이론 |
|---|---|---|---|
| 6bb | 0.679 | 0.679 | 0.600 |
| 12bb | 0.679 | **0.377** | 0.333 |
| 25bb | 0.679 | **0.192** | 0.170 |
| 50bb | 0.679 | **0.099** | 0.087 |

표가 이론보다 완만한 정도(6bb 에서 1.13배)가 그대로 이어진다.
**순수 이론으로 갈아타지 않는다** — 기존 설계의 완만함을 보존한다.

**검증 방법**

```bash
python3 tools/cf_mdf.py --table       # 확장 후 비율표 확인
python3 tools/cf_mdf.py --n 3000      # M0 자리에 수정본이 들어간다
#   기대: ≤3bb 구간 변화 0 (표 안이라 건드리지 않음)
#         >12bb 구간 방어율이 이론 쪽으로 내려간다
#   반드시 확인: ≤3bb 가 0.0%p 로 유지되는가. 움직이면 회귀다.
python3 tools/cf_defend.py --n 3000   # 성향 효과 유지 확인
#   기대: looseness +21.5%p, pf_defend +3.8%p 가 그대로.
#         약해지면 문턱이 좁아지며 혼합층에 눌린 것이므로 조사 필요.
# 올인 경로 따로: TRACE_MDF.md 6~7절의 경로별 분해를 다시 돌린다
```

### 1-B 시도 결과 — **관문 3 에서 멈췄다. 커밋하지 않았다.**

제안한 수정을 실제로 적용해 세 관문을 돌렸다 (2026-09-14,
`claude/fix-calc-nks1k7`). 코드는 판정 전까지 되돌려 두었다.

**관문 1 통과** — `cf_mdf.py --n 3000`

| 구간 | n | 전 | 후 | 이론 | 후−이론 |
|---|---|---|---|---|---|
| **≤3bb** | 2184 | 38.3% | **38.3%** | 38.3% | **+0.0%p** |
| 3~6bb | 122 | 30.3% | 30.3% | 29.5% | +0.8%p |
| 6~12bb | 217 | 25.3% | 20.7% | 19.4% | +1.4%p |
| >12bb | 477 | 31.0% | 10.3% | 9.4% | +0.8%p |

이론과의 괴리가 `>12bb` 에서 +21.6%p → +0.8%p 로 줄었다. 표의 완만함
(6bb 에서 1.13배)만 남았다 — 의도한 모양이다.

**관문 2 통과** — `cf_defend.py --n 1500`

`looseness` +21.5%p, `pf_defend` +3.8%p, `aggression` −2.5%p, 위약 0.0%.
**수정 전과 완전히 동일하다.** 표 안(2~3bb)을 안 건드렸으므로 당연하고,
회귀가 없다는 증거다.

**관문 3 — 이중 적용은 아니다. 그러나 다른 문제가 드러났다.**

코드 확인: `calloff_cap` 에서 `open_bb` 는 `defend_thresholds` 로 **한 번만**
간다. `CALLOFF_TIGHTEN`(레이즈 단계)·`base_feel`(스택 깊이)·`bf`(ICM)는
전부 다른 축이다. **mdf 는 한 번만 들어간다.**

그런데 크기가 크다.

| 경로 | n | 전 방어율 | 후 방어율 | 행동 변화 |
|---|---|---|---|---|
| 일반 (`defend_decision`) | 4381 | 26.5% | **12.1%** | 15.7% |
| **올인 (`calloff_cap`)** | 177 | 37.3% | **9.6%** | **27.7%** |

`cap` 중앙값: 12~25bb `0.271→0.113`(0.42배), 25~50bb `0.281→0.058`(0.21배),
50bb+ `0.271→0.032`(**0.12배**). 하한 0.005 에 붙은 것은 0% 라 클리핑은 아니다.

### 근본 원인 — 두 곡선의 극한이 반대다

| 올인 X | MDF `1.5/(1.5+X)` | 팟오즈 필요승률 |
|---|---|---|
| 6bb | 20.0% | 40.0% |
| 25bb | 5.7% | 47.5% |
| 50bb | 2.9% | 48.8% |
| 100bb | 1.5% | **49.4%** |

MDF 는 `X → ∞` 에서 **0 으로** 가고, 팟오즈 필요승률은 **50% 로 수렴**한다.

올인 대면은 포스트플랍이 없다. *"상대 블러프를 억제하는 최소 빈도"* 가
아니라 *"내 에쿼티가 팟오즈를 넘는가"* 문제다.
**MDF 곡선을 올인 경로에 쓰는 것 자체가 개념적으로 맞지 않는다.**

이건 이 수정이 만든 문제가 아니다. **원래 있던 어긋남을 6bb 포화가
가리고 있었고, 포화를 없애니 드러났다.** 50bb 올인을 상위 3.2% 로만
받는 것은 명백히 좁다 — 필요 승률 48.8% 면 상대 쇼브 레인지 대비
상위 10% 안팎이어야 한다.

### 선택지 (미결정)

| | 내용 | 비용 |
|---|---|---|
| (가) | 그대로 커밋 | 일반 경로 개선(이론과 +1.4%p), 올인 경로는 개념적으로 틀린 채 과도 축소. 올인은 6bb 초과 중 4% |
| (나) | 일반 경로에만 적용 | `mdf` 에 경로 플래그 필요 — **새 설계 결정** |
| (다) | 보류 | 올인 경로 기준(MDF 대신 팟오즈)을 먼저 정하고 함께 수정. 일반 경로 개선분(행동 15.7%)을 당분간 포기 |

**CLAUDE.md 2번("임계값을 새로 발명하지 마라")에 비추면 (다)가 맞다.**
(나)는 "올인은 다른 곡선을 쓴다"는 설계를 지금 만드는 것이고,
(가)는 개념적으로 틀린 것을 알면서 커밋하는 것이다.

---

---

## 1-C. `refresh` 의 `outs` 계산 불일치

| | |
|---|---|
| **위치** | `plan.py:1661` (`refresh`), 대조군 `plan.py:277` (`make_plan`) |
| **현재 동작** | `make_plan` 은 `outs_true * PS.calc_noise(profile,'outs',rng)` 로 **체감값**을 쓰고, `refresh` 는 `draw_strength(hero, board)` **날것**을 쓴다 |

**왜 문제인가.** 같은 사람이 플랍과 턴에서 자기 아웃츠를 다르게 센다.
`refresh` 바로 아랫줄 주석이 `rel` 에 대해 같은 문제를 이미 고쳤다고
적어놨다 — *"make_plan 과 같은 편향을 쓴다. 예전에는 여기만 날것이라
**같은 사람이 플랍과 턴에서 자기 핸드를 다르게 평가했다.**"*
`outs` 만 그 수정에서 빠졌다. CLAUDE.md '기타 확인된 이슈' 에도 기록돼
있다.

| | |
|---|---|
| **실측 영향** | `outs` 축 반사실이 턴 갱신에서 **1.7%** (`TRACE_REFRESH.md` 4절). 영향 자체는 작다. 직접 걸리는 분기는 `old == 'semibluff' and outs < 6` 하나 |
| **기대 효과** | 계측 일관성. `outs` 개념이 낮은 사람이 턴에서 갑자기 정확해지는 것이 사라진다. 리뷰 도구의 `outs` 해석도 스트리트 간 비교가 가능해진다 |

**부작용 가능성.**
- **`refresh` 에 `rng` 가 없다.** `seed` 만 받는다. 새로 만들어야 하는데,
  `refresh` 안의 다른 난수는 `_allowed` 가 자기 `random.Random(crc32(...))`
  를 따로 만들어 쓰므로 **스트림 간섭이 없다**
- `perceived_rel` 에 넘기는 `outs` 는 **날것을 유지해야 한다.**
  `make_plan:312-314` 가 `bot.draw_strength(...)` 를 그대로 넘기기 때문이다.
  여기를 체감값으로 바꾸면 새 불일치를 만든다
- `st['outs_true']`(1690줄)가 지금 체감값을 저장하고 있다. 같이 고쳐야 한다

**수정안** (아직 적용 안 함)

```python
# plan.py:1661
- outs = draw_strength(hero, board)
+ outs_true = draw_strength(hero, board)
+ # make_plan:277 과 같은 체감 보정. 여기만 날것이라 같은 사람이 플랍과
+ # 턴에서 아웃츠를 다르게 셌다 (rel 은 아래에서 이미 고쳐져 있다).
+ # refresh 에는 rng 가 없다 — seed 로 새로 만든다. _allowed 는 자기
+ # random.Random(crc32(...)) 를 쓰므로 스트림이 겹치지 않는다.
+ outs = (int(round(outs_true * PS.calc_noise(profile, 'outs',
+                                             random.Random(seed))))
+         if profile.get('concepts') else outs_true)
  made = bot.made_strength(hero, board)
  rel_true = relative_strength(hero, board, opp_range)
- rel = perceived_rel(profile, rel_true, hero, board, outs, made)
+ # perceived_rel 에는 **날것**을 넘긴다 (make_plan:312-314 와 같게).
+ rel = perceived_rel(profile, rel_true, hero, board, outs_true, made)
```

그리고 1690줄:

```python
-               'outs_true': outs,
+               'outs_true': outs_true,
```

**검증 방법**

```bash
python3 tools/cf_refresh.py --n 350
#   기대: outs 축 뒤집힘이 1.7% 에서 올라간다 (make_plan 3.5% 수준까지).
#         위약이 0.0% 로 유지되는지 반드시 확인 — rng 를 새로 만들므로
#         스트림이 어긋나면 위약이 0 이 아니게 된다.
python3 tools/collect.py 250 out.jsonl && python3 tools/review.py out.jsonl --flags
#   기대: '리버 드로우 잔존'(audit.py 데이터 검사) 건수가 늘지 않는다
#   확인: intent 의 outs 와 outs_true 가 스트리트마다 다른 값이 된다
```

---

# 2. 설계 결정 후 수정

수식 하나를 바꿔서 될 문제가 아니다.
**"어떤 봇 성격 차이를 만들고 싶은가"를 먼저 정해야 한다.**

## 2-A. `_sz_seen` 으로 `need` 전체를 재대입하는 구조

| | |
|---|---|
| **위치** | `plan.py:668-671` |
| **현재 동작** | 사이즈 인식이 1e-9 라도 다르면 `need` 를 **대입**으로 다시 계산. 그 앞의 `potodds` 오차·`to_act_behind`·리딩 `trust`·클램프·`call_bias` 가 전부 버려진다 |
| **실측 영향** | `opp_est` 있으면 **84.1% 발동**(사이즈 오차 중앙 1%). 팟오즈까지 같이 고친 반사실에서 행동의 9.4~16.4% 가 달라진다 |

**결정해야 할 것.** 사이즈 오독을 반영하는 방법이 둘이다.

```
(가) 대입          현재. "인식이 곧 판단 근거다" (주석의 설계)
(나) 입력 교체     기본 팟오즈를 계산할 때부터 _sz_seen 을 쓴다
                  → 체인 전체가 보존된다
```

(나)는 교과서식 `tocall/(_p0 + 2·tocall)` 에서 `tocall` 대신
`_sz_seen·_p0` 를 쓰는 것이므로 **같은 의미를 다른 위치에서 반영**하는
것이다. 다만 **어느 쪽이 더 나은 플레이인지는 측정하지 않았다.**
반사실의 85%(체인 복원분)는 양방향이고, 승률·EV 로 재야 판정된다.

**1-A 를 먼저 하면 이 항목의 성격이 바뀐다.** `need_true` 를 고치면
`_sz_seen == _sz_true` 인 경우 대입이 무해해진다(같은 값). 남는 것은
사이즈 오독이 있을 때뿐이다. **1-A 이후 다시 측정할 것.**

### 1-A 이후 재측정 결과 — **수정 대상으로 확정. 아직 수정하지 않았다.**

`TRACE_SZSEEN.md` (구조) 와 `TRACE_STELL.md` (스위치) 두 문서에 전문이 있다.
`tools/cf_szseen.py` / `tools/cf_stell.py`. baseline `6574360`.

**(1) 목적이 만드는 변화는 거의 0 이다.**

| | ① 대비 바뀐 건 (발동군) |
|---|---|
| ② 덮어쓰기 제거 | flop 17.7% / turn 12.1% / river 8.1% |
| ③ 체인보존(비율로 곱함) | flop 17.4% / turn 12.1% / river 7.9% |

②와 ③이 서로 갈린 것은 flop 1건, turn 0건, river 1건.
"인지한 사이즈로 팟오즈를 다시 본다"는 **목적 자체가 만드는 행동 변화는
거의 0** 이고, **8~18% 는 전부 체인 삭제분**이다.
비발동군에서는 세 변종 모두 0.0% — 가른 것이 이 줄 하나뿐임이 확인된다.

**(2) 그 삭제를 켜는 스위치가 `sizing_tell` 이다.**

| 스트리트 | `sizing_tell=1` 발동률 | `sizing_tell=9` 발동률 |
|---|---|---|
| flop / turn | **0.0%** | 89.0% |
| river | 88.5% | 89.0% |

`see_size = (stl-2)/6` 이라 `sizing_tell ≤ 2` 면 `size_gap`·`size_big` 이 0 이
되고 `opp_size_norm` 이 입력을 그대로 돌려준다 → 덮어쓰기가 안 걸린다.
**개념이 높은 사람일수록 응답 체인을 잃는다. 설계 의도의 반대다.**

**(3) 축 하나가 다른 축을 지운다.**

`potodds` 뒤집힘이 ① 에서 `sizing_tell` 1→9 로 **9.2% → 2.2%**,
③ 에서는 9.2% → 8.2% 유지. 두 축이 경쟁하는 것이 아니라 덮어쓰기가
`potodds` 가 계산해 둔 체인을 없애는 것이다.

**확정된 것:** 이 줄이 `need` 전체를 재대입하는 구조는 **수정 대상**이다.
(가) 대입을 유지할 근거는 남지 않았다 — 1-A 이전에는 이 식이 이 함수 안에서
유일하게 올바른 팟오즈였다는 역사적 이유(`8fadcb3`)가 있었으나, 1-A 로
`need_true` 가 교정되면서 그 이유가 사라졌다.

**아직 미결:** `sizing_tell` 이 체인 안에서 가져야 할 영향력.
③ 에서 1.8% 가 나왔다고 해서 "너무 낮다"며 10% 로 맞추는 것도,
기존 14.5% 를 유지하려고 `trust` 계수를 조정하는 것도 **둘 다 금지**다
(작업원칙 2). 버그 수정이 아니라 밸런스 재설계가 된다.

## 2-B. 성향이 `_allowed` 게이트 하나로 수렴한다

| | |
|---|---|
| **위치** | `plan.py:1593` (`_allowed`), `make_plan` 의 인라인 게이트들, `refresh` 끝 |
| **현재 동작** | 계획 라벨에 성향이 닿는 곳이 사실상 개념 게이트뿐이다. 통과율 `semibluff` 99% / `potcontrol` 86% / `bluff` 78% / `blockbet` 71% |
| **실측 영향** | `make_plan` 에서 `bluff` 18.0% · `potcontrol` 16.5%, `refresh` 에서 14.0% · 12.9%. **전환이 전부 `giveup → bluff_2street` / `showdown → pot_control`** — 게이트의 강등표와 정확히 짝이다 |

**결정해야 할 것.** 게이트는 "할 줄 아느냐"를 묻는다. 그 위에
"같은 상황에서 얼마나 자주 하느냐"를 성향이 정하게 할 것인가.
지금은 게이트를 통과하면 전원이 같은 확률이다.

## 2-C. `aggression`·`looseness` 가 계획 라벨에 거의 안 들어간다

| | |
|---|---|
| **실측 영향** | 1↔9 전 범위를 흔들어도 `aggression` 0.5~0.6%, `looseness` **0.0%**. `make_plan` 안에 `looseness` 가 **존재하지 않는다**. `aggression` 은 `block_p`(1.1% 빈도) 한 줄과 `pc` 안의 `(10-aggr)` 뿐 |
| **대조** | 응답 단계에서는 `aggression` 6.8~13.0%, 프리플랍 BB 방어에서는 `looseness` +21.5%p 로 **잘 작동한다** |

**결정해야 할 것.** 계획 라벨이 상황 판단이고 성향은 실행에만 들어가는
것이 설계라면 현재가 맞다. 사용자가 원하는 것이
*"같은 상황에서 LAG 와 NIT 가 다르게 **생각한다**"* 라면 사다리 자체에
성향이 들어가야 한다. **이건 엔진 철학 결정이다.**

## 2-D. `made` 승격 항이 죽어 있다

| | |
|---|---|
| **위치** | `plan.py:1777-1790` |
| **현재 동작** | `made >= max(2, st.get('made',0)+1)` 이 `st.update` 뒤라 `made >= made+1` → 항상 거짓. 실질 조건은 `rel >= 0.70` 단독 |
| **실측 영향** | 미측정 |

**주석이 이미 알고 있고, 고쳤다가 되돌린 기록이 있다.**
`_prev_made` 로 고치면 `made` 1→2 가 *내가 개선한 경우*와 *보드가
페어링된 경우*를 구분 못 한다. 실측 신규 승격 11건이 대부분 페어 보드였고
`rel 0.00 / eq 0.031` 짜리까지 밸류로 승격됐다. **`known issue E` —
hand-strength transition 을 제대로 정의하는 별도 설계가 필요하다.**

단순 버그가 아니므로 1구역에 넣지 않는다.

---

# 3. 보류

지금 손대면 1구역 수정의 효과와 섞인다.

| 항목 | 위치 | 실측 영향 | 보류 이유 |
|---|---|---|---|
| **BB 혼합층 5.5%p** | `preflop.py:600-624` | 문턱 43.8% vs 실제 방어 38.3%. 모델(`ranges.preflop_range`)과 행동이 콜 −2.3%p·3벳 −2.4%p 어긋남 | 1-B(`mdf`)가 같은 문턱을 건드린다. 1-B 를 먼저 하고 나서 재측정해야 크기가 제대로 나온다 |
| **`station`/`bluff_fear` 이중 적용** | `plan.py:665` + `plan.py:802-812` | 행동 변화 **0.4~1.2%**, `station` 편향 0.3 초과 집단에서 세 스트리트 모두 **0.0%** | 측정 결과 영향이 거의 없다. 코드는 지저분하지만 우선순위가 낮다 |
| **오픈 사이즈 반응이 전원 동일** | `gto.defend_pct` | 미측정 | 1-B 가 이 함수의 곡선을 바꾼다. 수정 후에 판단 |
| **`size_river` 가 `see_size` 게이트를 우회** | `persona.py:955` | flop/turn `st=1` 발동 0.0% vs river `st=1` 발동 **88.5%** | 2-A 와 같은 줄(`_sz_seen`)을 통해 나타난다. 둘을 같이 고치면 어느 변경이 결과를 바꿨는지 다시 분리해야 한다 |
| **`size_read` 가 실전에서 항등** | `persona.py:755` | 2.0팟 이하 항등. 아카이브 648건에서 `sz > 1.0` **0건** | `sizing_tell` 의 적정 영향력 결정과 같이 봐야 한다. 문턱 `2.0` 을 임의로 내리는 것은 임계값 재발명 |

---

# 4. 토너먼트 / ICM 단계

싱글 토너 엔진이 안정된 뒤에 본다.

| 항목 | 현재 상태 |
|---|---|
| 버블·ITM·파이널의 위험회피 차이 | 버블팩터 신호는 크다(초반 1.01 → 버블 2.16 → 파이널 2.23). 행동 변화는 **버블 23핸드·ITM 9핸드**라 확인도 반박도 못 함 (`REVIEW_2.md` 5절) |
| 상금 점프에 따른 성향 변화 | `icm_aware`·`icm_press`·`variance_seek` 배선은 있음. 계획 라벨에서 `icm` 축 반사실 1.0% |
| 포맷별 필드 강도·스타일 | `formats.py` 9종 정의됨. `buyin_level`·`itm_frac`·`payout_flat`·`ante_from`·`blind_mult`·`reentry` 는 연결됨. **`start_bb`·`hpl`·`seats` 는 `fieldsim` 이 안 읽음.** UI 선택기 없음 |
| 필드 분포 | `field.pool_for`/`draw_type`/`FAMILY_BASE` 가 **어디서도 호출되지 않음**. 약체 비율 의도 32% vs 실제 11%. `aggr_bias`/`loose_bias` 를 `fieldsim` 이 안 넘김 |

**1-A 가 이 구역에 걸린다.** `bf` 가 `need_true` 의 분자에 있으므로
분모를 고치면 ICM 압박의 실효 크기가 바뀐다. 1-A 검증에 `bf` 분해를
넣어둔 이유다.

---

# 수정 순서

각 단계는 **별도 커밋·별도 지문·별도 검증**이다.

### 1단계 — `1-C` `refresh` 의 `outs` (가장 작다)

영향 1.7%. 계측 일관성 문제라 논쟁 여지가 없고, 범위가 한 함수 안이다.
**여기서 절차(지문 기록 → 수정 → 반사실 재측정 → 새 지문)를 먼저 굳힌다.**

- 검증: `cf_refresh.py` 위약이 **0.0% 로 유지**되는지가 관문.
  `rng` 를 새로 만들므로 스트림이 어긋나면 여기서 드러난다.

### 2단계 — `1-B` `mdf` 6bb 초과 포화

프리플랍 안에서 닫힌다. 포스트플랍 `need` 경로와 겹치지 않는다
(`TRACE_MDF.md` 8절에서 확인).

- 검증 관문: **`≤3bb` 구간이 0.0%p 로 유지**되어야 한다. 움직이면 회귀다.
- `cf_defend.py` 로 `looseness` +21.5%p 가 유지되는지 확인.
- 올인 경로(`calloff_cap`)를 따로 분해. 이중 축소가 나오면 멈추고 보고.

### 3단계 — `1-A` `need_true` 팟오즈

가장 넓게 퍼진다. 포스트플랍 전 스트리트 + ICM.

- **반사실 변종 D(=`need_true` 만 고치고 덮어쓰기는 유지)를 먼저
  `cf_needpath.py` 에 추가**하고 측정한 다음 수정한다.
- 검증: `cf_response.py --read --reads` 로 `range_read`·`sizing_tell`·
  `reraise` 효과가 유지되는지. 약해지면 클램프가 더 자주 걸린 것이다.
- `bf` 를 1.0/1.5/2.2 로 나눠 ICM 영향을 따로 잰다.

### 3단계 이후 재측정 대상

1-A 가 끝나면 **2-A(`_sz_seen` 재대입)의 성격이 바뀐다.** 다시 측정해서
남은 문제가 무엇인지 확인한 뒤에 2구역 설계 논의로 간다.
→ **완료.** 2-A 절의 "1-A 이후 재측정 결과" 참조.

### 그다음 조사 순서

축 하나를 고치는 것보다 **이름과 실제 코드 역할이 다른 축을 먼저 해부**하는
방식이 효율이 좋았다 (`sizing_tell` 이 그 예다). 그 순서로 간다.

```
1. 응답 / need 구조 정리            ← 2-A 수정안 확정이 여기 속한다
2. 성향 축이 이름값을 하는지 검증    ← size_read 처럼 죽은 경로 찾기
3. 블러프 레인지 / 라인 일관성       ← 마지막
```

3번은 **블러프 빈도를 세는 조사가 아니다.** "이 블러프가 그 사람의 이전
라인에서 실제로 대표 가능한 레인지에 속하는가"를 본다. 지금 1·2번과 섞지 않는다.

---

# 공통 회귀 검사

각 단계마다 아래를 함께 돌린다.

```bash
# 1) 지문 — 격리 폴더에서
python3 tools/fingerprint.py --seeds 3000-3005 --hands 30

# 2) 개념 배선 (36/36 나와야 정상)
python3 tools/wirecheck.py

# 3) 핸드 감사 — 새 경보가 생기지 않았는가
python3 tools/collect.py 250 out.jsonl
python3 tools/review.py out.jsonl --flags
python3 tools/field_audit.py out.jsonl

# 4) 성향 효과가 약해지지 않았는가 (전 단계 공통)
python3 tools/cf_profile.py --n 400          # 계획 라벨
python3 tools/cf_response.py --n 400 --read  # 응답
python3 tools/cf_defend.py --n 3000          # 프리플랍 방어
#   각 도구의 **위약 바닥값이 0.0% 로 유지**되는지가 1차 관문이다.
#   0 이 아니게 되면 난수 스트림이 어긋난 것이므로 수정을 재검토한다.
```

**모든 단계에서 `plan.py` 의 판단 분기 순서와 임계값은 건드리지 않는다.**
1구역 셋은 전부 *입력값의 계산*을 고치는 것이지 *조건식*을 바꾸는 것이
아니다. CLAUDE.md 금지 항목("조건식·임계값·확률 계수 변경, 분기 순서 변경")에
걸리지 않는다.

단 `1-B` 는 `_MDF` 표 자체가 아니라 **표 밖 외삽 방식**을 바꾸는 것이므로
경계에 있다. 표의 다섯 점은 그대로 둔다.
