# 대장 구조 (지율 안)

기존 `LEDGER.md` 는 **신호별**로 정리한 것이다.
이 문서는 **실행 단계별**로 정리한다. 둘은 다른 질문에 답한다.

- `LEDGER.md` — "이 값을 누가 만들고 누가 쓰나"
- 이 문서 — "핸드가 돌 때 무엇이 언제 일어나나"

배선은 개념군 구분이 끝난 뒤 한꺼번에 한다. 그 전까지 새 배선을 추가하지 않는다.

---

## 대장 1 — 봇 생성

성향별 분포를 정하고, 가중치를 계산해 저장하고, 그 결과로 봇을 만드는 구간.

관련: `persona.py` (`LOADING`, `make_player`, `skill_bounds`), `archetypes.py`, `field.py`

정리할 것
- 잠재 요인 3개(study / aggro / exp)에서 개념 28개를 뽑는 방식이 맞는가
- `LOADING` 의 `base` 가 개념 난이도를 제대로 반영하는가
- 필드 등급(`field_q`)이 분포를 어떻게 이동시키는가
- 100명을 뽑았을 때 실제 분포가 의도한 모양인가 (측정 필요)

**미해결**: 분포 검증을 한 적이 없다. 원하는 모양이 무엇인지부터 정해야 한다.

---

## 대장 2 — 토너 진행 (바깥 루프)

필드 전체가 도는 층. 한 테이블 밖의 모든 것.

관련: `tourney.py`, `fieldsim.py`, `field.py`, `live.py`, `live2.py`

포함
- 블라인드 레벨 상승
- 탈락 처리, 잔여 인원
- 테이블 밸런싱 / 브레이크 / 히어로 이동
- 공석 충원
- 상금 구조, ITM, 버블
- 필드 평균 스택

---

## 대장 3 — 내 테이블 한 핸드 (안쪽 루프)

한 판이 시작해서 끝날 때까지의 시스템 진행.

관련: `session.py` (`HandRun._run`), `play.py`, `table.py`

포함
- 버튼 이동, 블라인드/안테 징수
- 카드 배분, 보드 오픈
- 액션 순서 (`needs_action`)
- 베팅 라운드 상태 (`to_call`, `min_raise`, `can_raise`)
- 히어로 차례에서 `yield` 로 빠지는 지점
- 팟 계산, 사이드팟

---

## 대장 4 — 봇 판단

스트리트별로 봇이 무엇을 보고 무엇을 정하는가.

관련: `plan.py`, `preflop.py`, `bot.py`, `ranges.py`, `reads.py`, `texture.py`

층 구조 (기존 규약)
- 인식 — texture, ranges, reads, relative_strength, blocker, nut_advantage
- 판단 — make_plan / refresh / decide_aggression / decide_size / trap_judgment
- 집행 — act_with_plan. intent 를 칩으로 환산만 한다. 판단 금지

스트리트
- 프리플랍 — `preflop_plan` → open / iso / defend → `raise_form`
- 플랍 — `make_plan` (계획 확정)
- 턴 — `refresh` / `revise_plan`
- 리버 — 같음

---

## 대장 5 — 핸드 종료 후

쇼다운부터 다음 핸드 시작까지.

관련: `session.py` (`_finish`, `award_pots`), `reads.py` (`Book.observe_*`), `dynamics.py`

포함
- 팟 분배, 사이드팟
- 쇼다운 공개 범위 (폴드로 끝난 핸드는 머킹)
- 관찰 기록 — 누가 무엇을 봤는가
- 틸트/동역학 갱신
- 스택 정산, 탈락 판정

---

# 추가 후보

지율 안 5개에 안 들어가는 것들.

## 6 — 관찰과 기억 (스트리트를 가로지름)

`reads.py` 의 `Book`. 핸드 **중에** 기록되고 다음 핸드 **판단 시점에** 읽힌다.
대장 5(기록)와 대장 4(사용)에 걸쳐 있어서 어느 쪽에도 온전히 안 들어간다.

- `observe_preflop` / `observe_postflop` / `observe_3bet` / `observe_4bet`
- `observe_size` / `observe_showdown`
- `perceived_profile` — 관찰량과 능력으로 걸러진 추정치
- 누가 그 자리에 있었어야 볼 수 있는가 (관찰자 목록)

**따로 둘 이유**: 여기가 "정보 비대칭"의 전부다. 잘못되면 봇이 볼 수 없는 걸 본다.

## 7 — 재현성 / 상태

`_dseed`, `REPLAY` 캐시, 시드 파생, `state.json`.

핸드 진행과 얽혀 있지만 목적이 다르다. 같은 시드로 같은 결과가 나오는지는
대장 2·3·4 어디에도 안 들어가는 별개 축이다.

## 8 — 히어로 입출력 / 표시

`view.py`. 무엇을 보여주고 무엇을 감추는가.

- 팟 금액과 콜 비용만 표시, 필요 승률·팟오즈 금지
- 폴드로 끝난 핸드 머킹
- 좌석 번호 고정

**따로 둘 이유**: 표시 규칙 위반은 시스템 버그가 아니라 학습 도구로서의 실패다.

## 9 — 검증

`tools/regress.py`, `tools/invariants.py`, `tools/trace_pf.py`, `audit.py`

- `regress` — 지문. 익스플로잇 층은 30핸드라 못 본다 (알려진 구멍)
- `invariants` — 이미 아는 것만 잡는다
- `trace_pf` — 좌석별 결정 경로 추적
- 실제 검출력은 "직접 치면서 관찰"이 가장 높다

## 10 — 리뷰 / 평가

`review.py`. 세션이 끝난 뒤 핸드를 다시 보는 층.

지율의 실제 목적이 여기다. 시뮬레이터는 사후 리뷰 도구지 실시간 코칭이 아니다.
그런데 지금 `review.py` 는 함수 5개짜리 조각이고 호출부가 없다.

---

# 정리

| 대장 | 범위 | 상태 |
|---|---|---|
| 1 봇 생성 | 분포·가중치·저장 | 분포 검증 안 됨 |
| 2 토너 진행 | 필드 바깥 루프 | 있음 |
| 3 한 핸드 | 테이블 안 루프 | 있음 |
| 4 봇 판단 | 프리플랍~리버 | 있음. 계단·미배선 다수 |
| 5 핸드 종료 후 | 정산·기록 | 있음 |
| 6 관찰과 기억 | 스트리트 가로지름 | 있음 |
| 7 재현성/상태 | 시드·저장 | 있음 |
| 8 표시 | 히어로가 보는 것 | 있음 |
| 9 검증 | 도구 | 구멍 있음 |
| 10 리뷰 | 사후 분석 | **거의 없음** |
