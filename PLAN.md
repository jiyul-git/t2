# 대장 구조

`LEDGER.md` 는 **신호별** — "이 값을 누가 만들고 누가 쓰나"
이 문서는 **실행 단계별** — "핸드가 돌 때 무엇이 언제 일어나나"

배선은 개념군 구분이 끝난 뒤 한꺼번에 한다. 그 전까지 새 배선을 추가하지 않는다.

**번호는 분류다. 실행 순서가 아니다.**

실제 순서:
```
토너 생성 → 필드 생성 → 봇 생성 → 좌석 배정
  → [핸드 루프]  블라인드 → 좌석별 판단 → 정산 → 관찰 기록
  → 탈락 / 테이블 이동 / 블라인드 상승 → 다시 핸드 루프
  → 토너 종료 → 리뷰
```
대장 1 은 **한 번**만 돈다. 대장 2 가 바깥에서 감싸고 3~6 이 그 안에서 반복된다.
2-3(검증)은 루프에 없다 — 사람이 부를 때만 별도 프로세스로 돈다.
2-2(표시)는 히어로 차례에만 걸린다. 7(리뷰)은 토너가 끝난 뒤다.

| 대장 | 범위 | 상태 |
|---|---|---|
| 1 | 봇 생성 — 분포·가중치 | 분포 검증 안 됨 |
| 2 | 토너 진행 (바깥 루프) | 있음 |
| 2-1 | └ 재현성 / 상태 저장 | 있음 |
| 2-2 | └ 히어로 표시 | 있음 |
| 2-3 | └ 검증 / 불변식 / 점검 | 구멍 있음 |
| 3 | 한 핸드 (안쪽 루프) | 있음 |
| 4 | 봇 판단 — 프리플랍~리버 | 계단·미배선 다수 |
| 5 | 핸드 종료 후 — 정산 | 있음 |
| 6 | 관찰과 기억 | 있음 |
| 7 | 리뷰 | **거의 없음** |

---

## 대장 1 — 봇 생성

성향별 분포를 정하고, 가중치를 계산해 저장하고, 그 결과로 봇을 만든다.

`persona.py` (`LOADING`, `make_player`, `skill_bounds`), `archetypes.py`, `field.py`

- 잠재 요인 3개(study / aggro / exp) → 개념 28개
- `LOADING` 의 `base` = 개념 난이도. 낮을수록 어렵고 희소
- 개념 값은 **개인별로 저장**된다. 판단 시점엔 `sk(prof, k)` 로 그 값을 읽는다.
  `base` 는 분포의 중심일 뿐 두 번 다시 안 본다
- 필드 등급 `field_q` 가 분포 전체를 이동시킨다
- `skill_bounds` 로 하한/상한 밖이면 다시 뽑는다 (최대 6회)

**한계 (확정)**: 엔트리 2~400, 좌석 6~9. `formats.py` 에 상수로 두고 `Tournament` 이 거부한다.
좌석 상한 9 는 포지션 사다리(`table.orders`)가 9까지만 정의되어 있기 때문이다.

**토너 포맷 9종** — `formats.py`. 축은 구조 / 필드 수준 / 상금 구조 셋.
라이브·온라인은 축으로 두지 않는다 (구조와 필드 수준으로 이미 표현됨).

**엔트리 규모**: 최대 400명. 기본값 100.
`entries` 는 상한이 걸려 있지 않지만 400 을 넘으면 재기 전까지 쓰지 않는다.

실측 (`fieldsim.Field`, 8맥스)

| 엔트리 | 테이블 | 생성 | 핸드당 |
|---|---|---|---|
| 100 | 13 | 0.01s | 0.24s |
| 400 | 50 | 0.06s | 0.37s |

실행 시간은 제약이 아니다. 실제 토너도 10시간 이상 돈다.
속도를 이유로 로직을 단순화하지 않는다.

### 보류 과제 — 개념별 난이도 정리 (지율 검토 필요)

**개념을 다 추가한 뒤, 개념 순으로 난이도(`LOADING` 의 `base`)를 한 번에 정리한다.**
지금은 개념마다 값을 즉흥으로 정하고 있어서 서로 어긋난다.

정리 방식: 개념 전체를 '얼마나 알려졌나 / 얼마나 계산이 귀찮나' 로 줄세우고
그 순서에 맞춰 base 를 배분한다. 지율이 순서를 보고 판단한다.
Claude 가 먼저 초안을 만들어도 된다.

**까먹지 말 것.** 개념 추가가 끝나는 시점에 반드시 수행.

---

**미해결**: 100명을 뽑았을 때 실제 분포가 어떤 모양인지 잰 적이 없다.
원하는 모양(초짜/레귤러/강자 비율)을 먼저 정해야 측정할 수 있다.

---

## 대장 2 — 토너 진행 (바깥 루프)

필드 전체가 도는 층. 한 테이블 밖의 모든 것.

`tourney.py`, `fieldsim.py`, `field.py`, `live.py`, `live2.py`

- 블라인드 레벨 상승
- 탈락 처리, 잔여 인원
- 테이블 밸런싱 / 브레이크 / 히어로 이동
- 공석 충원 (좌석 번호 고정)
- 상금 구조, ITM, 버블, 머니점프
- 필드 평균 스택

**경로 차이 주의**: `tourney` / `fieldsim` / `live` 가 채우는 값이 다르다.
`field_remaining`·`field_itm` 이 `tourney` 에만 빠져 있었고 그래서 ICM 이 안 걸렸다.
새 필드 값을 추가할 때 세 경로 전부 확인할 것.

### 2-1 재현성 / 상태 저장

`_dseed`, `REPLAY` 캐시, 시드 파생, `state.json`

- 시드 하나에서 좌석·스트리트·태그별로 파생시킨다
- 같은 시드 → 같은 결과. 별도 프로세스 3회 해시 일치 확인함
- 공유 RNG 객체, `hash()` 기반 캐시 키, `rng or random` 폴백이 과거 재현성을 깼다
- `state.json` 은 백업 tar 에 안 들어간다 (세션마다 새 게임)

### 2-2 히어로 표시

`view.py`

- 팟 금액과 콜 비용만. 필요 승률·팟오즈 금지
- 폴드로 끝난 핸드는 상대 패 공개 금지 (머킹)
- 좌석 번호 고정. 공석은 빈 상태로 표시
- 요청하지 않은 규칙·표시를 추가하지 않는다

**따로 둘 이유**: 표시 규칙 위반은 시스템 버그가 아니라 학습 도구로서의 실패다.

### 2-3 검증 / 불변식 / 점검

`tools/regress.py`, `tools/invariants.py`, `tools/trace_pf.py`, `audit.py`

- `regress` — 액션 지문 SHA-256. **시드당 30핸드라 익스플로잇 층을 못 본다** (알려진 구멍)
- `invariants` — 칩 총량, 액션 순서 등. 이미 아는 것만 잡는다
- `trace_pf` — 좌석별 결정 경로 추적 (`WANT=form|shove`)
- `audit` — 핸드 단위 점검

**검출력 순서**: 직접 치면서 관찰 > trace > invariants > regress.
지금까지 실제 버그는 대부분 첫 번째에서 나왔다.

---

## 대장 3 — 한 핸드 (안쪽 루프)

`session.py` (`HandRun._run`), `play.py`, `table.py`

- 버튼 이동, 블라인드/안테 징수
- 카드 배분, 보드 오픈
- 액션 순서 (`needs_action`)
- 베팅 라운드 상태 (`to_call`, `min_raise`, `can_raise`)
- 히어로 차례에서 `yield` 로 빠지는 지점
- 팟·사이드팟 계산

---

## 대장 4 — 봇 판단

`plan.py`, `preflop.py`, `bot.py`, `ranges.py`, `texture.py`

층 구조 (규약)
- **인식** — texture, ranges, reads, relative_strength, blocker, nut_advantage
- **판단** — make_plan / refresh / decide_aggression / decide_size / trap_judgment
- **집행** — act_with_plan. intent 를 칩으로 환산만. **판단 금지**

스트리트
- 프리플랍 — `preflop_plan` → open / iso / defend → `raise_form`
- 플랍 — `make_plan` 이 계획 확정
- 턴·리버 — `refresh` / `revise_plan`

**남은 문제**
- 계단: `depth_band`(8/15/25/60), `should_shove`(bb<12, bb<22), `in_hotzone`(12/26)
- 오픈 쇼브 결정이 `should_shove` 와 분산추구 두 곳으로 갈려 있다
- 미배선 축 다수 — `LEDGER.md` 참조

---

### 대장 4 설계 결정 (프리플랍) — 합의됨, 코드 반영은 일부만

**규칙**: yes/no 로 갈리는 게 보이면 그건 대부분 가중치로 가야 하는 신호다.
이분법은 사람을 두 종류로만 만든다.

**규칙**: 크기와 방향을 나눈다.
개념이 "얼마나 벗어나는가"(0.1~1.0)를, 기질이 "어느 방향으로"(-1~+1)를 정한다.
하나로 뭉치면 "루즈한데 잘하는 사람"과 "루즈하고 못하는 사람"이 구분되지 않는다.

#### 1. 기준 레인지 — 반영 완료

- 기준은 **레귤러 평균** 하나. GTO 가 아니다 (GTO 는 분포의 중심이 아니라 소수가 근사하는 전략).
- 색인은 포지션 이름이 아니라 **뒤에 남은 인원**. 6~9맥스가 표 하나로 처리된다.
- 깊이 배수는 **40bb 가 정점**. 깊어도 얕아져도 좁아진다. 얼리/레이트 두 곡선 보간.
- `pf_range`(폭 정확도) / `positional`(포지션 인식) 개념 추가. 둘은 독립이다.
  리크리에이셔널의 특징은 '넓다'가 아니라 '곡선이 평평하다'.
- 정확도 상한 0.90 — 1.0 을 허용하면 실력 있는 LAG 가 기준과 동일해져 사라진다.

#### 2. 오픈 사이즈 — 미반영

- 축 셋: 사이즈 자체 / 일관성 / 반올림
- **반올림은 기질 축을 만들지 않는다.** 대부분이 그렇게 하기 때문.
  호가 단위를 BB 크기에서 뽑고(BB 200→100단위, BB 1000→500단위),
  반올림 강도만 `consistency` 로 흔든다.
- 사이즈는 `open_size` 개념 하나. 일관성은 기존 `consistency`.
- **개념 있는 사람만 사이즈를 갖게 하지 않는다.** 개념 없는 사람의 사이즈가
  천차만별인 것은 재현해야 할 현상이다 — 그게 관찰자에게 정보를 준다
  (`size_info` 축이 이미 그걸 전제한다).

#### 3. 깊이 인식 — 미반영

`depth_band` 의 8/15/25/60 계단을 없애고 `depth_feel(bb, prof, field_avg)` 0~1 연속으로.

기준 곡선 (지율 감각 + 공개 자료 대조. BeyondGTO 100+/40~100/20~40/10~20,
Preflop Wizard 80~100+/40~70/20~35, PokerNews 50 초과는 자유):

```
100bb 1.00   70bb 0.75   50bb 0.55   40bb 0.40
 35bb 0.30   20bb 0.12   15bb 0.05   10bb 0.00
```

- 필드 평균 대비 반영. 배율 0.45, 비율은 0.4~2.5 로 자름.
  40bb·평균25bb → 체감 약 50bb.
- **필드 평균을 얼마나 보는지 자체가 개념.** `spr` 에 얹는다.
- 크기 ← `spr` 개념 / 방향 ← 포스트플랍 자신감(낮으면 짧게 느낌) + `icm`(높으면 깊게 느낌)
- **`gamble` 은 여기 안 쓴다.** gamble 은 "분산을 키우고 싶다"는 충동이고
  깊이 인식은 "스택이 얼마나 남았다고 느끼는가"라는 지각이다. 다른 층이다.

#### 4. variance_seek 전략 경로를 둘로 — 미반영

```
strategic = 열세형 + 축적형
```

- **열세형** (기존) — 실력 열세 자각 × gamble. *약자의 도박.*
  포스트플랍이 스킬 구간인데 내가 거기서 진다 → 프리플랍 올인으로 그 구간을 없앤다.
- **축적형** (신규) — 상금 경사 × 리바인 가능 × gamble. *강자의 도박.*
  칩을 빨리 쌓아 리드하고 그 스택으로 압박한다. 실패하면 리바인.
  - 상금이 가파를수록 강함. `payout_flat` 이 이미 포맷에 있다
  - `reentry` 가능하면 강함. 포맷에 있으나 아직 아무도 안 읽는다 — **이것이 그 통로**
  - 토너 초반일수록 강함
  - 실력은 **U자**로 건다 — 잘하는 사람은 계산해서, 못하는 사람은 그냥 해서

**축적 욕구와 깊이 인식은 곱해질 뿐 서로를 왜곡하지 않는다.**
같은 축적 욕구라도 깊이 인식이 높으면 넓은 오픈·3벳으로, 낮으면 쇼브로 나온다.
깊이를 아는 사람은 60bb 쇼브가 AA/KK 에게만 콜받는다는 것을 안다.

**현재 `gamble` 은 공부량이 깎는다**(`-0.35*(study-4.5)`). 축적형이 붙기 전까지는
잘하는 사람의 의도된 도박이 표현되지 않는다.

#### 5. 다음에 이어서 할 것

- 위 2·3·4 를 코드로
- 디펜스 레인지(`defend_thresholds`)에도 기준표가 없다 — 오픈과 같은 구조로
- 림프 경로 (100핸드에 1번. `t['limp']` 이 아키타입 family 에서 나와 개념 체계와 따로 논다)
- 라벨 층 분리 — 아키타입 30종은 사람을 **만드는** 데 쓰이면 안 된다.
  관찰 결과를 요약할 때 붙는 이름표여야 하고, 관찰자 능력에 따라 다르게 붙어야 한다.

---

## 대장 5 — 핸드 종료 후

`session.py` (`_finish`, `award_pots`), `dynamics.py`

- 팟 분배, 사이드팟, 동점 분배 (칩 단위 내림)
- 쇼다운 공개 범위
- 스택 정산, 탈락 판정
- 틸트/동역학 갱신 (`dynamics.py` — 현재 대부분 죽음)

기록은 대장 6 으로 넘긴다.

---

## 대장 6 — 관찰과 기억

**대장 1(성향)과 대장 4(판단)를 잇는 층.**
성향이 "무엇을 볼 수 있는가"를 정하고, 그 결과가 판단에 들어간다.

`reads.py` (`Book`, `perceived_profile`, `perceived_range`), `persona.read_opponent`

### 기록 — 무엇이 남는가

`Book.observe_*` 가 핸드 중/후에 쌓는다.
- `observe_preflop` — vpip / pfr
- `observe_3bet` / `observe_4bet` — 기회 대비 실행, 대면 대비 폴드
- `observe_postflop` — 스트리트별 씨벳/배럴/폴드
- `observe_size` — 사이즈 평균·분산·극단·리버
- `observe_showdown` — 공개된 핸드 강도

**관찰자 목록이 핵심.** 그 자리에 있었던 사람만 본다.
테이블이 다르면 못 본다. 과거에 테이블 간 데이터가 섞이는 버그가 있었다.

### 능력 — 무엇을 읽을 수 있는가

같은 기록을 봐도 사람마다 다른 것을 얻는다. `persona.read_opponent` 의 축별 게이트.

| 게이트 | 성향 원천 | 읽는 것 |
|---|---|---|
| `see_freq` | `attention` | 빈도. 몇 %로 치는가/접는가 |
| `see_line` | `range_read` | 스트리트 구분, 블러프 성향 |
| `see_size` | `sizing_tell` | 사이즈의 의미 |
| `use` | `adaptability` | 알아도 자기 전략을 바꿀 의지 |

- 게이트는 계단이 아니라 하한+연속. 개념 2 미만 → 0, 8 이상 → 1, 사이는 선형
- 축끼리 **곱하지 않는다**. "빈도는 못 세는데 사이즈에서 이상함을 느끼는" 사람이 있다
- `use` 가 0이면 전부 중립. 볼 줄 알아도 안 바꾸는 사람
- 표본량 `w = use × confidence × min(1, n/12)`

### 규약

```
상황 신호 (0~1)  ×  개념 가중치 (0~1)  =  실효 값
```

신호와 능력을 한 값에 섞지 않는다. 섞으면 "신호는 센데 못 읽는 사람"이 표현되지 않는다.
개념 가중치에 하한을 둘 수 있다 (예: ICM 0.15 — 상금 구조는 개념이 아니라 상식).

**미해결**: `plan.py:625` 가 BF 단위(1.0~4.0)를 그대로 쓴다. 0~1 규약과 어긋남.

---

## 대장 7 — 리뷰

세션이 끝난 뒤 핸드를 다시 보는 층. **시뮬레이터의 실제 목적.**

`review.py` — 함수 5개(`load`, `hand`, `show`, `summary`, `history`)뿐이고 호출부가 없다.

필요한 것 (미구현)
- 핸드별 결정 지점 재구성 — 그 시점에 무엇을 알 수 있었는가
- 결과가 아니라 과정 기준 평가
- 누수 집계 — VPIP, 스트리트별 공격성, 스택 구간별 판단
- 봇 내부 판단 로그 대조 — 상대가 왜 그렇게 쳤는가

**진단된 누수** (참고)
- 15~25bb 오픈·쇼브 혼동
- SPR 계획 없이 진입 후 중도 포기
- 짧은 스택 쇼브에 과도한 폴드
- 얇은 밸류 언더사이징
- VPIP 과다 (~36%, 목표 27%)

**강점**: 턴 공격성 (플로트 → 스탭), 베팅 스토리 읽기
