# READ_SAMPLE_CURVE — 결과 (이 묶음의 마지막 측정)

사전등록: `READ_SAMPLE_CURVE_PREREG.md`
브랜치: `claude/read-sample-curve` · 기준 커밋 `3fd8d01`(동결)
도구: `tools/read_sample_curve.py`

`read_use_fixture.py` · `attack_fixture.py` · `qx_ev_fixture.py` **전부 무수정**.
기존 QX / READ_USE / SYNTH / ATTACK 산출물 무수정. production 무수정.
`stat_read` 없음. 새 축 없음.

---

## 판정: `C1_THRESHOLD_FOUND`

```
공격 FLOOR 통과 n (두 집합 모두):  [40]
공격 FLOOR 미달 n (두 집합 모두):  [8, 10, 12, 16, 24]
```

**공격의 문턱은 `n_obs` 24 와 40 사이**다. 사전등록 5절대로 **격자 구간으로만**
적는다 — 보간해서 점추정을 만들지 않는다.

**수비는 이 범위에 문턱이 없다.** `n = 8` 에서 이미 FLOOR 위다.

---

## 1. 곡선

`ΔR = regret(LOW_READ) − regret(HIGH_READ)`. 양수 = 정확히 읽는 쪽이 이득.
프로필 150, 클러스터 부트스트랩 4000, `FLOOR = 0.002`.

### 공격 — 단조롭고 깨끗하다

| `n_obs` | `w`(HIGH) | SET_A `ΔR` | 95%CI | SET_B `ΔR` | 95%CI |
|---:|---:|---:|---|---:|---|
| 8 | 0.064 | +0.00014 | [+0.00012, +0.00016] | +0.00012 | [+0.00010, +0.00014] |
| 10 | 0.099 | +0.00046 | [+0.00040, +0.00053] | +0.00040 | [+0.00033, +0.00047] |
| 12 | 0.143 | +0.00062 | [+0.00053, +0.00072] | +0.00053 | [+0.00044, +0.00062] |
| 16 | 0.190 | +0.00103 | [+0.00089, +0.00119] | +0.00088 | [+0.00073, +0.00103] |
| 24 | 0.285 | +0.00160 | [+0.00138, +0.00183] | +0.00137 | [+0.00114, +0.00161] |
| **40** | 0.412 | **+0.00292** | [+0.00255, +0.00332] | **+0.00255** | [+0.00216, +0.00295] |

**`Spearman(n, ΔR) = +1.000` — 두 집합 모두 완전 단조.**
여섯 점 전부 양수이고 전부 유의하다. 효과는 `n = 8` 에서도 **존재하지만**
`n = 40` 이 되어야 의사결정 규모(FLOOR)에 닿는다.

### 수비 — 전 구간 FLOOR 위, 그러나 모양은 못 읽는다

| `n_obs` | SET_A `ΔR` | 95%CI | SET_B `ΔR` | 95%CI |
|---:|---:|---|---:|---|
| 8 | +0.00490 | [+0.00373, +0.00620] | +0.00512 | [+0.00391, +0.00647] |
| 10 | +0.00268 | [+0.00167, +0.00377] | +0.00148 | [+0.00068, +0.00231] |
| 12 | +0.00466 | [+0.00355, +0.00585] | +0.00462 | [+0.00365, +0.00564] |
| 16 | +0.00818 | [+0.00594, +0.01065] | +0.00767 | [+0.00579, +0.00969] |
| 24 | +0.00494 | [+0.00359, +0.00635] | +0.00258 | [+0.00154, +0.00367] |
| 40 | +0.00765 | [+0.00516, +0.01045] | +0.00819 | [+0.00572, +0.01080] |

`Spearman +0.657 / +0.429`. **여섯 점 전부 FLOOR 위이고 전부 유의하지만
단조가 아니다.** `n = 10` 과 `n = 24` 에서 두 번 주저앉고, 그 골은 신뢰구간
밖이다(SET_A `n=10` [+0.00167,+0.00377] vs `n=16` [+0.00594,+0.01065] — 겹치지
않는다). 두 집합이 같은 자리에서 같은 방향으로 흔들린다.

### 진동의 원인 — 후보 하나를 시험했고 **기각됐다**

가장 그럴듯한 후보는 합성 장부의 정수 반올림이었다.
`showdowns = max(2, n//4)`, `sd_weak = round(showdowns · b_true)` 이라
블러프 증거의 해상도가 `n` 에 따라 튄다.

```
n      showdowns   6개 b 수준의 weak_rate            서로다른값   폭
8              2   0.00 0.50 0.50 0.50 0.50 1.00        3     1.000
10             2   0.00 0.50 0.50 0.50 0.50 1.00        3     1.000
12             3   0.00 0.33 0.33 0.67 0.67 0.67        3     0.667
16             4   0.25 0.25 0.50 0.50 0.75 0.75        3     0.500
24             6   0.17 0.33 0.33 0.50 0.67 0.67        4     0.500
40            10   0.20 0.30 0.40 0.50 0.60 0.80        6     0.600
```

**`n = 8` 과 `n = 10` 의 양자화가 완전히 같은데 `ΔR` 은 다르다**
(0.00490 vs 0.00268, SET_B 는 0.00512 vs 0.00148). 후보는 기각된다.

**원인을 특정하지 못했다.** 이번 세션에서 기전을 두 번(공격 `n=6` 손해의
위치, 그리고 이 진동) 예측했고 **두 번 다 데이터가 기각했다.** 세 번째
추측은 적지 않는다. 결론에는 영향이 없다 — 수비는 어느 `n` 에서도 FLOOR 위다.

## 2. 게이트

```
G-a 조작 점검   slope·sd 의 HIGH/LOW 비율이 모든 n, 두 집합에서 5.00
                (이론값 A_HI/A_LO = 5.0)                              OK
G-b 구성 균형   수비 최적=call 0.500 · 공격 최적=bet 0.500             OK
G-c 게이트 기전 평균 w 가 n 에 단조 증가
                0.064 0.099 0.143 0.190 0.285 0.412 (SET_A)           OK
```

`G-a` 는 상관이 아니라 기울기·sd 로 잰다 — 상관은 이 개입(양의 아핀 변환)에
불변이라 못 쓴다 (`ATTACK_FIXTURE_FIX` Amendment D2).

## 3. `w` 게이트에 대해 알게 된 것

사전등록은 `min(1, n/12)` 의 꺾임(`n = 12`)에 구조가 있을 것으로 봤다.
실측은 다르다 — `w` 가 **`n = 12` 를 지나서도 거의 선형으로 계속 큰다.**

```
n        8      10      12      16      24      40
w    0.064   0.099   0.143   0.190   0.285   0.412
w/w(12)  0.45   0.69   1.00   1.33    1.99    2.88
n/12     0.67   0.83   1.00   1.33    2.00    3.33
```

`n ≥ 12` 에서 `w/w(12)` 가 `n/12` 를 그대로 따라간다. 즉 12 위에서 `w` 를
키우는 것은 `min(1, n/12)`(이미 포화)가 아니라 **`conf = min(1, n·skill/25)`**
다. `n = 40` 에서만 2.88 < 3.33 으로 처지는데, 거기서 `conf` 가 포화하기
시작한 것이다. **`n = 12` 는 꺾이는 점이 아니다** — 두 게이트가 이어받을 뿐이다.

## 4. 채널 비교

`ATTACK_FIXTURE_FIX` 가 점추정으로 "수비 > 공격 약 2.3배"라고 적었다.
같은 축에서 다시 보면 **비율이 `n` 에 따라 극적으로 변한다.**

```
수비ΔR / 공격ΔR      n=8    n=10   n=12   n=16   n=24   n=40
SET_A               35.0    5.8    7.5    7.9    3.1    2.6
SET_B               42.7    3.7    8.7    8.7    1.9    3.2
```

`n = 8` 의 35~43배는 분모(공격 0.00013)가 거의 0 이라 나온 값이고,
수비 곡선의 진동 때문에 중간 구간도 불안정하다. **믿을 수 있는 것은 방향뿐이다 —
표본이 쌓일수록 두 채널이 가까워지고, `n = 40` 에서 약 2.6~3.2배다.**
"2.3배"라는 단일 숫자를 쓰지 말 것.

## 5. 이 묶음의 결론

```
읽기 정확도는 두 채널 모두에서 EV 를 개선한다.  전 구간 양수, 전부 유의.
수비   n = 8 부터 이미 의사결정 규모.  크기 +0.0015 ~ +0.0082.
공격   완전 단조하게 커지지만 의사결정 규모는 n ≈ 24~40 사이에서야 닿는다.
       크기 +0.00012 ~ +0.0029.
두 채널의 차이는 표본이 쌓이면 줄어든다 (n=40 에서 약 3배).
```

**`stat_read` 를 단일 스칼라로 넣으면 안 되는 이유가 수치로 확정됐다.**
같은 축이 두 채널에 같은 크기로 작용하면, 표본이 적은 국면에서 공격 쪽을
**한 자릿수 배수로 과대 반영**한다(`n = 8` 에서 실측 35~43배 격차).
채널별 분리가 선행되어야 한다.

`stat_read` 는 이번에도 만들지 않았다.

## 6. 한계

1. **수비 곡선의 모양은 해석하지 않는다.** 진동의 원인을 특정하지 못했고
   후보 하나는 기각했다. 수준(전 구간 FLOOR 위)만 쓴다.
2. **문턱은 (24, 40] 구간이다.** 그 안 어디인지는 격자가 없어 모른다.
3. **`u` 는 production 값 하나만 봤다.** `READ_USE_SEPARATION` 이 보인 대로
   `u` 가 크면 읽기의 한계이득이 줄어든다. 이 곡선은 `u` 한 단면이다.
4. **합성 장부다.** 실제 대국에서 쌓이는 장부는 여기보다 지저분하고
   `n` 이 채널마다 다르게 쌓인다.
5. 공격 셀의 가격이 1.10~1.80 팟에 몰려 있다 (`ATTACK_FIXTURE_FIX` 한계 3).

## 7. 근거 지위 정리 (이 묶음을 닫으며)

```
내려놓는다   PIPE_ONLY / CALC_ONLY 합성 arm 기반의 "능력" 해석
             (QX_EV_VALIDATION 1-4, SYNTH_CHANNEL_SPLIT 의 arm 해석)
             이유: 두 arm 은 읽기 파이프라인만이 아니라 APPLY 개념 ~20개가
             통째로 반대다 (ATTACK_FIXTURE_FIX Amendment D1).
             SYNTH 의 채널 상쇄 **산술**은 그대로 유효하다.

유지한다     READ_USE_SEPARATION 계열 — 실제 프로필의 **믿음만** 블렌드하고
             나머지를 비트까지 고정한 R 축. 본 문서와
             ATTACK_FIXTURE_FIX 의 보조 분석이 같은 설계다.
```

기존 결과 문서는 **수정하지 않는다**(CLAUDE.md). 지위 기록은 `CLAUDE.md` 에 남겼다.

## 8. 최종 확인

```
production 무수정   plan.py / persona.py / reads.py / session.py     diff 0
fixture 동결        qx_ev_fixture / read_use_fixture / attack_fixture diff 0
기존 산출물         QX / READ_USE / SYNTH / ATTACK 문서·도구          diff 0
regression 지문     tools/regress.py check -> 전 시드 지문 일치
새 concept          없음. c5 / V7 tier 경계 / baseline 무변경
```

**이 묶음은 여기서 닫는다.** 다음은 A5 blockbet → `revise_plan` context 계약.
