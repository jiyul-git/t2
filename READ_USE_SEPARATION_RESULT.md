# READ_USE_SEPARATION — 결과

사전등록: `READ_USE_SEPARATION_PREREG.md` (+ Amendment B1, 둘 다 측정 전 커밋)
브랜치: `claude/read-use-separation`  ·  기준 커밋 `b0f2d26`
도구: `tools/read_use_fixture.py` · `tools/read_use_check.py` · `tools/read_use_2x2.py`

production 무수정. 새 concept 없음. `c5`·V7 tier 경계·baseline 무변경.
공격 채널은 측정도 수정도 하지 않았다.

---

## 1. 무엇을 분리했나

수비 READ 상태에서 `calldown_need` 가 읽기를 쓰는 자리는 정확히 둘이다.

```
plan.py:678   need -= trust · (read − 0.35)
plan.py:708   need  = PS.blend(need, need·max(0.55, 1−0.35·bluff_gap), _rdz['w'])
```

두 축을 여기에 맞춰 갈랐다.

| 축 | 무엇을 바꾸나 | 어디에 거나 | 고정되는 것 |
|---|---|---|---|
| **R** 읽기 정확도 | 공개 장부에서 만든 믿음을 모집단 사전분포로 블렌드 (`a=0.20` vs `1.00`) | `reads.estimate` 반환값 | `n`·`confidence` → 따라서 `w` 가 **비트까지 동일** |
| **U** 적용 강도 | `read'=0.35+u(read−0.35)`, `w'=clamp(u·w,0,0.85)` (`u=0.40` vs `1.60`) | 적용 두 지점만 | `est` 전체, 그리고 인식 사이즈 `_sz_seen` |

`read` 는 `calldown_need` 안에서 `trust·(read−0.35)` 로만 쓰인다.
그래서 첫 줄은 `trust *= u` 와 **대수적으로 동일**하다 — 지역변수를 patch 할
수 없어 인자로 등가 변환한 것이지 다른 개입이 아니다.

`persona.opp_size_norm` 도 `w` 를 읽어 인식 사이즈를 바꾼다. 사이즈 정규화는
"얼마나 세게 조정하는가"가 아니라 "무엇으로 보이는가"이므로 U 가 닿으면
안 된다. patch 된 `read_opponent` 가 원래 `w` 를 `_w0` 로 보관하고
`opp_size_norm` 이 그것을 복원한다.

## 2. 타당성 검사 — 6/6 통과 (결과를 보기 전에)

```
[4-6] 뒤집힘 셀 20/20            최적=call 비율 0.500  (구성상 정확히 균형)
[4-1] R 이 w 를 바꾼 건수         0 / 14,400
[4-2] U 가 인식 사이즈를 바꾼 건수  0 / 2,880
[4-4] 널 개입에서 need 불일치      R축 0/2,880   U축 0/2,880
[4-5] arm 사이 행동이 갈리는 비율   0.2119  (게이트 0.10)
[4-3] |line_bluff_prior − b_true|  LOW 0.1828 → HIGH 0.1751
```

**`4-3` 은 통과했지만 약하다.** `line_bluff_prior` 는 상태별 배수
(배럴 수·사이즈·보드 위험도)를 곱하므로 `|rv − b_true|` 에 계통 오차가
크게 섞인다. 그래서 개입 강도 진단을 따로 붙였다 — **사전등록에 없는
서술용이고 게이트가 아니다.**

```
r(rv, b_true)        LOW +0.1921 → HIGH +0.5694
slope(b_true → rv)   LOW +0.0918 → HIGH +0.4126     (4.5배)
est['bluff'] 의 sd   LOW  0.224  → HIGH  1.122      (5.0배)
```

신호 추적력으로 보면 개입은 충분히 세다. 동시에 이 표는 production 사실
하나를 드러낸다 — **정확히 읽어도 `line_bluff_prior` 는 진짜 블러프율의
41% 만 추적한다.** 기울기 1.0 이 아니다.

---

## 3. 판정: `NO_CAUSAL_READ_EFFECT` — **그리고 이 라벨은 데이터를 잘못 요약한다**

사전등록 5절의 규칙을 순서대로 적용한 결과다. 규칙은 그대로 적용했다.

```
R0 INCONCLUSIVE             §4 검사 6/6 통과            -> 해당 없음
R1 READ_ABILITY_JUSTIFIED   OK(dR(u_lo)) 이고 OK(dR(u_hi)) 이어야 함
                            u_lo 충족, u_hi 불충족       -> 불성립
R2 READ_ONLY_IN_INTERACTION OK(dR(u_hi)) 이어야 함       -> 불성립
R3 USE_DOMINATES            dR 이 **어느 u 에서도** 불충족이어야 함
                            u_lo 에서 충족했다           -> 불성립
R4 NO_CAUSAL_READ_EFFECT    위 어느 것도 아님            -> **성립**
```

**라벨 이름이 주장하는 것과 데이터가 보여주는 것이 다르다.** 실제로는

```
dR(u=0.40)   SET_A +0.00719 [+0.00642, +0.00799]
             SET_B +0.00721 [+0.00646, +0.00796]
```

**use 를 고정한 채 읽기 정확도만 올리면 regret 이 팟의 0.72% 줄어든다.**
두 집합에서 소수점 셋째 자리까지 같고, 신뢰구간은 0 에서 한참 떨어져 있다.
상대 감소율로는 **−15.4% / −14.7%** 다. "인과적 읽기 효과 없음"이 아니다.

`R4` 가 나온 이유는 규칙 집합에 **빈 칸**이 있었기 때문이다.
사전등록은 읽기 효과가 나타난다면 *use 가 클 때* 나타날 것(=`R2`)이라고
암묵적으로 가정했다. 실제 데이터는 그 **거울상** — use 가 작을 때 나타난다.
`R2` 의 대칭 라벨이 없어서 catch-all 인 `R4` 로 떨어졌다.

**사후에 라벨을 만들지 않는다**(사전등록 7절). 판정은 `R4` 로 기록하고,
규칙 집합의 결함을 여기 남긴다. 다음 사전등록에서는 `dR` 의 두 u 수준을
독립 항목으로 보고해야 한다.

참고로 사용자가 원래 제시한 문구는 "`READ_ABILITY_JUSTIFIED` 는 **use 고정
상태에서** HIGH_READ 의 regret 감소가 holdout 에서도 재현될 때만 허용"
이었다. `u=0.40` 고정 대조는 그 문구를 충족한다. 내가 사전등록에서
"두 u 수준 **모두**"로 조인 것이 `R4` 를 만든 직접 원인이다.
어느 쪽이 맞는지는 판단하지 않는다 — 두 읽기를 다 적는다.

### 3-1. 구현 버그 하나를 고쳤다

`verdict()` 의 첫 구현이 `R3` 의 "`dR` 이 어느 u 에서도 불충족" 조건을
빠뜨려서 `USE_DOMINATES` 를 냈다. **사전등록 문구가 기준이므로 코드를
고쳤다** (`tools/read_use_2x2.py` `R3` 주석). 측정 데이터는 재계산하지 않았다 —
`--from-json` 으로 판정만 다시 냈다. 저장된 JSON 의 `verdict` 는 수정 후 값이다.

---

## 4. 수치

### 4-1. 네 arm (팟 대비 평균 regret, 프로필 400 × 상태 240)

| | U=0.40 | U=1.60 |
|---|---:|---:|
| **LOW_READ** SET_A / SET_B | 0.04664 / 0.04921 | 0.03305 / 0.03333 |
| **HIGH_READ** SET_A / SET_B | 0.03945 / 0.04200 | 0.03198 / 0.03157 |

### 4-2. 주효과 (양수 = 그 축을 올리는 쪽이 regret 이 낮다)

| | SET_A | 95%CI | SET_B | 95%CI | |
|---|---:|---|---:|---|---|
| `dR(u=0.40)` | **+0.00719** | [+0.00642, +0.00799] | **+0.00721** | [+0.00646, +0.00796] | FLOOR 통과 |
| `dR(u=1.60)` | +0.00107 | [+0.00042, +0.00175] | +0.00176 | [+0.00103, +0.00254] | 유의하나 FLOOR 미만 |
| `dU(a=0.20)` | +0.01359 | [+0.01119, +0.01609] | +0.01588 | [+0.01334, +0.01837] | |
| `dU(a=1.00)` | +0.00747 | [+0.00513, +0.00993] | +0.01043 | [+0.00782, +0.01307] | |
| `INT` | **−0.00612** | [−0.00716, −0.00506] | **−0.00545** | [−0.00652, −0.00437] | |

`INT` 가 음수다 — **읽기의 값어치는 조정이 약할 때 더 크다.**
`u=0.40` 에서 0.0072, `u=1.60` 에서 0.0011~0.0018 로 4~7배 차이다.

`dR(u=1.60)` 은 **0 이 아니다.** 두 집합 모두 양수이고 신뢰구간이 0 을
배제한다. 다만 사전에 정한 의사결정 의미 하한 0.002 를 못 넘는다.
"높은 use 에서는 읽기가 무의미"가 아니라 "작지만 실재"다.

### 4-3. 표본 크기 — 사전 방향 예측이 맞았다

`u=0.40` 에서

| | SET_A | SET_B |
|---|---:|---:|
| `n_obs = 6` | +0.00509 | +0.00447 |
| `n_obs = 40` | **+0.00929** | **+0.00994** |

표본이 40 일 때 읽기 정확도의 값어치가 **2.0~2.2배**다. 사전등록 3절의
방향 예측("표본이 6이면 사전분포로 수축하는 것이 실제로 덜 틀릴 수 있다")
대로다. 게이트는 아니었지만 개입이 의도한 것을 재고 있다는 근거가 된다.

### 4-4. U 사다리 (서술용)

```
u            0.0     0.4     0.7     1.0     1.3     1.6     2.0
SET_A LOW    0.0763  0.0466  0.0386  0.0356  0.0338  0.0331  0.0329
SET_A HIGH   0.0732  0.0394  0.0340  0.0323  0.0320  0.0320  0.0321
SET_B LOW    0.0796  0.0492  0.0401  0.0363  0.0343  0.0333  0.0327
SET_B HIGH   0.0764  0.0420  0.0347  0.0325  0.0317  0.0316  0.0314
콜 비율 A     0.423   0.547   0.610   0.652   0.679   0.699   0.717
```

세 가지가 보인다.

1. **과조정으로 손해가 나는 구간이 없다.** `u` 를 2.0 까지 올려도 regret 이
   올라가지 않는다. `u ≈ 1.3` 부터 평탄하다.
2. **두 곡선이 `u` 가 커질수록 붙는다.** `INT` 가 음수인 것의 그림이다.
3. **`u=0` 에서도 읽기가 남는다.** `0.0763 → 0.0732` (SET_A), `0.0796 → 0.0764`
   (SET_B) — 차이 약 **+0.0031**, FLOOR 위다 (서술용이라 CI 는 안 냈다).
   `u=0` 은 `read` 항과 `bluff_gap` 블렌드를 둘 다 끈 상태다. 그래도 차이가
   남는 이유는 **읽기가 `need` 에 닿는 세 번째 경로**가 있기 때문이다 —
   `est` 의 `sz_mean`/`sz_sd`/`sz_big`/`sz_river` → `opp_size_norm` →
   `_sz_seen` → `need_true` 와 `call_bias`. 이 경로는 `u` 로 못 막는다.

### 4-5. `u` 는 순수한 강도 손잡이가 아니다 — 반드시 같이 읽어야 한다

콜 비율이 `u` 에 따라 **0.42 → 0.72** 로 움직인다. 원인은 production 상수다.

```
plan.py:678   need -= trust · (read − 0.35)
```

`0.35` 는 고정 상수인데 실측 `read`(= `line_bluff_prior`) 평균은
`0.513`(LOW) / `0.568`(HIGH) 다. 즉 `(read − 0.35)` 가 **평균적으로 양수**라,
`trust` 를 키우면 `need` 가 내려가 **더 자주 콜한다.** `u` 는 곧 `trust` 이므로
"세게 조정한다 = 많이 콜한다" 가 코드에 박혀 있다.

그래서 **`dU` 를 '세게 쓰는 것이 좋다'로 읽으면 안 된다.**
이 fixture 의 최적 콜 비율은 구성상 정확히 0.500 인데, regret 이 가장 낮은
곳은 콜 비율 0.70 부근이다. 과다콜이 과다폴드보다 싸기 때문이다 —
상태별 EV 격차가 평균 0.5175 팟으로 크고, 틀린 폴드는 팟을 통째로 버리는
반면 틀린 콜은 벳 하나를 잃는다. `dU` 는 "조정 강도"와 "콜 성향"을 섞어 잰다.

**`dR` 은 이 오염이 없다.** 같은 `u` 안에서만 비교하므로 콜 성향 수준이
고정된다. 이번 판의 결론이 `dR` 에만 걸려 있는 이유다.

---

## 5. 질문 넷에 대한 답

**Q1. `u` 를 고정해도 HIGH_READ 가 LOW_READ 보다 regret 이 낮은가?**
**그렇다.** 두 `u` 수준, 두 표본집합, 네 조합 모두 양수이고 CI 가 0 을
배제한다. 크기는 `u=0.40` 에서 팟의 0.72%(−15%), `u=1.60` 에서 0.11~0.18%.

**Q2. `read` 를 고정했을 때 HIGH_USE 가 항상 좋은가, 과조정으로 악화되는가?**
이 fixture 에서는 **악화 구간이 없다.** 다만 그 이득의 상당 부분은
"조정을 잘한다"가 아니라 **"콜을 더 한다"**이다(§4-5). `dU` 를 능력 근거로
쓰지 말 것.

**Q3. 좋은 읽기가 있어도 `use` 가 너무 크면 손해가 나는가?**
**아니다.** HIGH_READ 곡선은 `u=1.3~2.0` 에서 0.0314~0.0321 로 평탄하고
반등하지 않는다. 대신 **읽기의 한계이득이 사라진다** — `u` 가 크면 잘 읽든
못 읽든 비슷한 자리에 도달한다.

**Q4. 합성 `PIPE_ONLY` null 이 이 interaction 으로 설명되는가?**
**아니다 — `CANCEL_NOT_SUPPORTED`.**

```
g[HIGH_READ][HIGH_USE] − g[LOW_READ][LOW_USE]
  SET_A −0.01466  [−0.01743, −0.01200]
  SET_B −0.01764  [−0.02052, −0.01474]
```

상쇄라면 이 차이가 0 근처여야 하는데, 두 축을 같이 올린 arm 이 둘 다 내린
arm 보다 **팟의 1.5~1.8% 좋다.** 두 축은 같은 방향으로 더해진다.
`QX_EV_VALIDATION` 의 `CALC_ONLY` vs `PIPE_ONLY` null(+0.0004)은
R↔U 상쇄로는 설명되지 않는다.

**대신 다른 후보가 코드에 있다.** `tools/qx_ev_validate.py:135` 의
`synth_control` 은

```python
reg = [... for s in states if s.group == 'READ']
```

로 **수비(`D`)와 공격(`A`) 상태를 한 평균에 넣는다.** 그런데 같은 작업이
`QX_E` 의 공격 채널 상관을 fit −0.2477 / holdout −0.2403 으로 **부호가
반대**라고 측정했다. 수비에서 벌고 공격에서 잃으면 합이 0 이 된다.
**채널 간 상쇄가 더 그럴듯한 후보다.**

이번 판에서는 **검증하지 않았다.** 사전등록 1절·7절이 공격 채널을 범위
밖으로 못 박았기 때문이다. 확인하려면 `synth_control` 을 채널별로 갈라
다시 돌리면 된다 — 한 줄짜리 분리이고 새 측정이 아니다.

---

## 6. `stat_read` 에 대해 이 결과가 말하는 것

**아직 만들지 마라. 그러나 과녁은 좁아졌다.**

근거가 된 것:

- **읽기 정확도 자체가 EV 를 움직인다.** `w`·`use`·`adaptability` 를
  비트까지 고정한 채 믿음의 정확도만 올렸는데 regret 이 15% 줄었다.
  두 집합에서 3자리까지 재현된다. `QX_EV_VALIDATION` 이 남긴 "읽어서 좋아진
  것인지 세게 조정해서 좋아진 것인지 모른다"는 모호성은 **해소됐다** —
  읽기 쪽이 실물이다.
- **표본이 클 때 값어치가 두 배다** (`n=40` +0.0099 vs `n=6` +0.0045).
  "관측된 빈도·표본을 얼마나 정확하게 해석하는가"라는 `stat_read` 의
  의도된 정의와 방향이 맞는다.

제동을 거는 것:

- **효과는 `u` 가 작을 때만 의사결정 규모다.** production 의 `u` 는 1.0 이고
  필드 `trust` 는 그 근방에 흩어져 있다. 거기서는 `dR` 이 0.002 부근이다.
  **새 축을 넣어도 지금 구조에서는 대부분의 봇에게 작게 나타난다.**
  `INT` 가 음수인 것이 이 구조의 핵심이다 — 세게 조정하는 사람에게는
  읽기 정확도가 덜 중요해진다.
- **읽기가 `need` 에 닿는 경로가 셋인데 그중 하나(`opp_size_norm`)는
  게이트가 아예 없다** (§4-4 3번). 새 축을 만들면 이 경로에 어떻게 걸지
  먼저 정해야 한다. 안 정하면 축이 사이즈 인식까지 같이 움직인다.
- **공격 채널은 여전히 미해결이다.** Q4 의 대안 가설이 맞다면, 수비만 고친
  축은 전체 EV 에서 공격 손실에 묻힐 수 있다.

그래서 순서 제안은 이렇다.

1. `qx_ev_validate.synth_control` 을 채널별로 갈라 `PIPE_ONLY` null 이
   채널 간 상쇄인지 확인한다. 새 측정이 아니라 기존 도구의 집계 분리다.
2. 맞다면 **공격 채널에서 `decide_aggression` 이 상대 정보를 어떻게 쓰는지**
   부터 본다. 수비 축만 세워 봐야 합계가 0 이면 의미가 없다.
3. 그다음에 `stat_read` 설계. 제약은 그대로 — `attention`·`range_read` 를
   대체하지 않고, `adaptability`/`use` 를 건드리지 않고, 오직 "관측된
   빈도·표본을 얼마나 정확하게 해석하는가"에만 관여한다.
   구현은 **pid 유도 독립 RNG** 여야 한다. `persona.LOADING` 순회에 키를
   추가하면 `rng.gauss` 순서가 밀려 지문·baseline 3세대·V7 `c5` 가 전부
   무효가 된다.

**Tier 5 를 "조정 강도 전문가"로 다시 볼 근거는 이번 데이터에 없다.**
`dU` 가 크게 나오긴 했지만 §4-5 대로 콜 성향 이동과 섞여 있어서
능력 근거로 못 쓴다. `dR` 은 오염이 없고, 그쪽이 양수다.

---

## 7. 한계

1. **히어로는 자기 에쿼티를 정확히 안다** (사전등록 8-1). 상대 모델 채널만
   격리했다. 읽기가 에쿼티 추정을 개선하는 경로는 재지 않았다.
2. **`a` 블렌드는 `estimate` 출력 전체에 걸린다.** 블러프 읽기만이 아니라
   상대 믿음 전부의 정확도다. 어느 채널이 이득을 만들었는지는 분해하지 않았다.
3. **`u` 는 콜 성향과 교락된다** (§4-5). `dU` 를 능력 해석에 쓰지 말 것.
4. **턴 단일 스트리트다.** 멀티스트리트 계획 효과는 없다.
5. **`dR(u=0)` 에 CI 가 없다.** 서술용 사다리에서 평균 차이만 읽었다.
6. **`FLOOR = 0.002` 는 `QX_EV_VALIDATION` 의 사분위 격차 0.0130 에 앵커했다.**
   다른 앵커를 쓰면 `R4` 와 `R1` 의 경계가 움직인다 — `dR(u=1.60)` 이
   0.0011~0.0018 로 바로 그 경계에 있다.

## 8. 최종 확인

```
production 무수정        plan.py / persona.py / reads.py / session.py  diff 0
regression 지문          tools/regress.py check -> 전 시드 지문 일치
                         VPIP 19.1% PFR 11.4% flop 44.4% (기준선과 동일)
새 concept               없음. c5 / V7 tier 경계 / baseline 무변경
공격 채널                측정·수정 없음
```
