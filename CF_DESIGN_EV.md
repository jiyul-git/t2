# EV 설계 — `465` 라벨 플립의 칩 효과

**측정 전에 고정한다.** `plan.py` 무수정.
선행: `CF_RESULT_MADE.md` (라벨 변화) · `CF_RESULT_PCZ.md` (분기 탈출)

이것은 `D1~D6` 의 연장이 아니다. **별도 사전등록(`V`)이다.**

---

## 1. 먼저 — 라벨 플립이 실제로 무엇을 바꾸는가 (코드)

`giveup` 과 `showdown` 을 가르는 곳은 넷뿐이다.

```
plan.py:800    giveup 만  — 포기 계획을 뒤집는 블러프 레이즈 (확률 분기)
plan.py:812    giveup 은 raw need 로 즉시 call/fold 판정 후 return
               showdown 은 여기를 지나 834 의 need_seen(인지 편향) 경로로 간다
plan.py:1795   refresh 의 giveup 구제 — giveup 에만 걸린다
plan.py:1628/1648  _allowed 의 강등 대상 표 (이 실험의 대상 아님)
```

**바뀌지 않는 것:**

```
plan.py:857    decide_aggression 은 giveup 과 showdown 을 **같은 분기**로 다룬다
SIZING         giveup {0,0,0} · showdown {0,0,0} — 동일
BUDGET         둘 다 없다
```

> **라벨 플립은 베팅을 바꾸지 못한다.** 효과는 **벳에 직면했을 때의 응답**과
> `refresh` 의 구제 경로로만 흐른다.

그리고 `812`(raw need) → `834`(need_seen)는 축에 따라 문턱이 **양방향**으로
움직인다 (`station`·`hero_call` 이면 낮아지고 `bluff_fear` 면 높아진다).
**showdown 이 항상 더 많이 콜한다고 가정하지 않는다.**

---

## 2. 페어링 — 같은 핸드를 스냅샷에서 재생한다

`copy.deepcopy(Tournament)` 는 `t.run.gen` 때문에 실패한다. `t.run` 을 떼고
복사한 뒤 `next_hand()` 가 다시 만들게 하면 된다. **검증했다** — 같은
스냅샷에서 두 번 돌리면 `hand hash`·`stacks`·`full_log` 길이가 전부 같고,
원본을 그대로 진행한 것과도 같다.

```
핸드마다
  snap = 스냅샷(t)                     핸드 시작 직전
  기준 정책으로 그 핸드를 끝까지 실행   → 465 도달 여부·좌석·스트리트 기록
  465 에 도달했으면
      각 ARM 마다  t' = 복원(snap) → 같은 핸드를 ARM 으로 끝까지 실행
  **기준 궤적으로만 토너먼트를 계속 진행한다**
```

---

## 3. 난수 — 엔진이 이미 content-derived 다

```
session.py:85-96   _dseed = crc32('hand hash|seat|street|tag|extra')
                   "같은 핸드·같은 좌석·같은 스트리트·같은 시점 = 같은 난수"
카드                핸드 시작에 배분된다 (session.py:307 board = h.board[:nc])
```

같은 스냅샷에서 재생하므로 **카드는 두 경로에서 완전히 동일하다.**

### 3-1. **완전한 event-indexed tape 은 불가능하다 — 명시한다**

`extra` 가 `len(r2.log)` 인 호출이 있다 (`session.py:436`). 행동 이력이
갈리면 그 이후 결정의 시드가 달라진다.

**이것을 CRN 위반으로 보지 않는다.** 행동이 달라졌으면 그것은 같은 사건이
아니다. 결정의 무작위성을 사건 순서가 바뀐 뒤에도 고정하는 것은 정의상
불가능하다. **그래서 "순차 소비 복원"도, "완전한 공통 테이프"도 주장하지
않는다.** 고정되는 것은 카드와 분기점 이전의 모든 것이다.

---

## 4. 모집단 — 기준 정책이 정한다. 사후 확장·제외 없음

```
모집단   기준 실행에서 465 에 **정확히 1회** 도달한 핸드
제외     465 도달이 0회인 핸드 (대상 아님)
제외     465 도달이 2회 이상인 핸드 — 한 번의 ARM 실행이 여러 결정을
         동시에 뒤집어 귀속이 성립하지 않는다. **측정 전에 정한 규칙이고
         제외 건수를 반드시 보고한다**
```

ARM 실행에서 새로 생기는 `465` 도달은 **모집단에 넣지 않는다.** 그것은
개입의 하류 효과이지 표본이 아니다.

---

## 5. 결과변수와 지표

```
대상 좌석   기준 실행에서 465 를 맞은 그 좌석 (collect 의 더미 히어로가 아니다)
ΔC         그 좌석의 **핸드 순칩 변화** = (핸드 종료 후 스택) - (핸드 시작 전 스택)
```

분기점 이전에 넣은 칩은 두 경로에서 **동일**하므로 차분에서 소거된다.
그래서 핸드 단위 순칩 변화의 차를 그대로 쓴다.

```
ΔEV_i = ΔC_arm,i - ΔC_base,i
주 지표 = mean_i(ΔEV_i)          paired
같이 낸다: ΔEV ≠ 0 인 페어 수, 중앙값, 부호 분포
```

---

## 6. 사전등록 — `V`

```
V1  분기 이후 대상 좌석이 벳에 직면하지 않은 페어는 ΔEV = 0 **정확히** 이다
    (1절: 라벨 플립은 베팅을 바꾸지 못한다)
V2  ΔEV ≠ 0 인 페어 수는 각 팔의 플립 건수를 넘지 않는다
V3  응답 전환이 **양방향**으로 나온다 — fold→call 과 call→fold 가 둘 다 있다
    (812 raw need ↔ 834 need_seen 은 축에 따라 반대로 움직인다)
V4  **ΔEV 의 부호를 예측하지 않는다.** 이것은 명시적 비예측이다
```

**결과를 보고 예측을 고치지 않는다.**

---

## 7. 하지 않을 것

```
팔 간 우열을 가르지 않는다
"더 많이 바꾼 팔 = 더 좋은 팔" 로 읽지 않는다 — ARM-S 41 · ARM-R 33 · ARM-D 11 은
  플립 건수일 뿐이고 EV 와 무관하다
한 branch 에 두 ARM 을 같이 적용하지 않는다
pcz 개입과 같이 켜지 않는다
사후 조건으로 decision state 를 넣거나 빼지 않는다
유의성 문턱을 새로 만들지 않는다 — 평균·중앙값·부호 분포·건수를 그대로 낸다
표본이 작으면 작다고 적는다. 검정력을 주장하지 않는다
```

---

## 8. 재현

```
python3 tools/cf_ev.py --seeds 7000-7019 --hands 1000
```
