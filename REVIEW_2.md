# review_2.jsonl 감사 — 234핸드 (엔트리 100, 히어로 우승)

히어로 테이블만 담긴 아카이브다. 필드 전체가 아니다.
재현: `python3 tools/review.py review_2.jsonl --flags`,
`python3 tools/field_audit.py review_2.jsonl`

---

## 1. 계획 → 집행은 깨끗하다

| | |
|---|---|
| 무저항 상황에서 의도대로 집행 | **261 / 261 (100%)** |
| 개념 문턱이 걸리는 계획 중 개념 없이 나온 것 | **0 / 106** |
| 기록된 계획이탈 | 21건, 전부 `포기 계획이나 지속벳` |

집행부는 계획을 그대로 실행한다. `audit.py:171` 이 잡는 "포기계획인데 벳"은
전부 `decide_aggression`(plan.py:836)이 의도적으로 굴리는 이탈이고 `dev` 에
기록돼 있다. **문제는 집행이 아니라 계획 생성이다.**

다만 그 이탈의 빈도가 작지 않다.

```
포기 계획 + 이니셔티브 61건 → 굴림이 터져 실제로 벳 18건 (30%)
확률 p 중앙 0.246, 최대 0.756
```

`giveup` 계획 138건의 실제 행동: 체크 87 (63%) · 폴드 31 (22%) · **벳 13 (9%)** · 콜 7 (5%).
계획 라벨이 giveup 이어도 30% 는 친다.

---

## 2. 성향 → 계획은 헐겁다

15bb 이상일 때만, 프리플랍 25회 이상 본 봇 22명 기준 순위상관.

| 검사 | rho | t | 판정 |
|---|---|---|---|
| looseness → VPIP | +0.48 | +2.46 | 있음 |
| aggression → 포스트 공격 | +0.50 | +2.56 | 있음 |
| bluff → 포스트 공격 | +0.27 | +1.24 | 약함 |
| **aggression → PFR** | **+0.19** | +0.87 | **없음** |
| **discipline → 포스트 폴드** | **−0.05** | −0.22 | **없음** |

성향 값으로 묶으면(표본이 커진다) looseness 는 닿긴 닿는다. 다만 폭이 작다.

```
looseness 0~2  n=196  VPIP 16%
          2~4  n=275       21%
          4~6  n=352       18%
          6~8  n=187       21%
          8~10 n=226       28%
```

**척도 전체(0→10)를 움직여도 VPIP 가 12%p 밖에 안 변한다.**
실제 포커에서 닛과 피쉬는 30%p 이상 벌어진다.

타입별 평균은 타입당 봇이 1~3명뿐이라 사실상 개인 통계다. 그래서
"ROCK 이 FISH 보다 헐겁다" 같은 역전이 나오는데, 이건 타입 정의 문제라기보다
표본 문제로 봐야 한다.

---

## 3. BB 방어가 낮다 — 그리고 개념이 안 닿는다

```
BB 가 단일 레이즈에 직면 156회
  폴드 112 (72%)   콜 37 (24%)   리레이즈 7 (4%)
  → 방어율 28%
```

봇 오픈은 거의 전부 2.5bb 다. 2.5bb 오픈에 BB 는 팟오즈상 절반 이상
방어해야 한다. **28% 는 그 절반이다.**

`pf_defend` 개념별로 갈라도 단조성이 없다.

```
pf_defend 2대  n=15  40%      6대  n=35  37%
          3대  n=35  17%      8대  n= 6  33%
          4대  n= 9  44%      9대  n=12  25%
          5대  n=27  26%     10대  n= 6   0%
```

개념 10 인 봇이 방어율 0%, 개념 2 인 봇이 40%. **`pf_defend` 가 BB 방어에
연결돼 있지 않다.**

---

## 4. 익스플로잇은 돌아가지만 폭이 작고 한쪽 방향뿐이다

상대 읽기 자체는 실물이다.

- `reads` 120건 전부 n>0 (실제 관측 기반), 최대 n=119
- 확신도 0.6 이상 41건
- intent 381건에서 서로 다른 레인지 지문 342개 — 레인지 추정은 상황마다 갈린다

그런데 그 읽기가 판단을 바꾸는 폭이 작다.

```
상대 폴드성향 조정:  −6%p ~ +9%p
그 결과 포기 문턱:   0.29 ~ 0.32   (폭 0.03)
```

그리고 한쪽뿐이다.

```
블러프 사이즈 역산
  "상대가 사이즈를 읽음"     0건
  "상대가 사이즈를 안 읽음"  24건
```

**모든 상대를 사이즈를 못 읽는 사람으로 취급했다.** 반대 방향 조정이
한 번도 안 나왔다.

---

## 5. 버블·ITM — 신호는 크고 행동은 안 변한다 (표본 부족)

아카이브 스택으로 버블팩터를 직접 계산하면 신호는 분명히 있다.

| 단계 | bf 중앙 |
|---|---|
| 초반 (46명+) | 1.01 |
| 중반 (20~45) | 1.50 |
| 버블 (16~19) | **2.16** |
| 인더머니 (10~15) | 2.10 |
| 파이널 (≤9) | 2.23 |

그런데 행동은 단조롭게 안 변한다.

| 단계 | 핸드 | 평균스택 | VPIP | BB방어 | 포스트공격 |
|---|---|---|---|---|---|
| 1 초반 | 96 | 77bb | 22% | 32% (68) | 27% |
| 2 중반 | 51 | 38bb | 19% | 29% (38) | 26% |
| 3 버블 | 23 | 31bb | 24% | 18% (17) | 21% |
| 4 ITM | 9 | 24bb | 13% | 29% (7) | 0% |
| 5 파이널 | 55 | 30bb | 18% | 23% (26) | 17% |

**버블 23핸드, ITM 9핸드다. 이 표로는 판단할 수 없다.**
"ITM 전후가 똑같이 느껴진다"는 체감을 이 자료로 확인도 반박도 못 한다.
여러 대회를 모아야 한다.

배선 자체는 있다 — `play.Hand.bf` → `session.py:191,519` → `preflop.feel_of`,
`plan.calldown_need`. 다만 `icm_aware(prof) = 0.15 + 0.85·(icm/10)` 로 감쇠되고,
bf 가 들어가는 자리가 콜오프·콜다운 위주라 **오픈/디펜드 빈도에는 약하게 닿는다.**

---

## 6. 봇 분포 — 죽은 코드가 둘 있다

### 6-1. `field.pool_for` / `draw_type` / `FAMILY_BASE` 가 호출되지 않는다

```python
FAMILY_BASE = {'reg': 0.30, 'nit': 0.18, 'fish': 0.32, 'maniac': 0.12, ...}
```

"필드의 32% 는 피쉬" 라고 적힌 이 표가 **어디서도 쓰이지 않는다.**
`fieldsim.py:70` 은 `PS.make_player(rng, q, pid)` 만 부른다. 실제 분포는
개념 벡터 표집 + `skill_bounds(q)` 재표집에서 나온다.

결과 (entries=100, buyin 1.0, 표본 4000):

```
TAG_TIGHT 17% · TAG 12% · STUDIED_TAG_TIGHT 9% · ROCK 9% · LOOSE_REG 9%
약체(FISH/STATION/MANIAC) 합계 11%
```

**필드의 89% 가 레귤러류다.** 의도했던 32% 피쉬와 다르다.

모든 개념이 5 근처에 몰려 있다.

```
pf_defend   25% 3.3  중앙 5.2  75% 7.1
bluff       25% 3.6  중앙 4.7  75% 5.9
icm         25% 3.1  중앙 4.9  75% 6.8
sizing_tell 25% 3.6  중앙 5.1  75% 6.5
```

진짜 고수도 진짜 호구도 없다. "전체적으로 난이도가 똑같다"는 체감의 근거다.

### 6-2. `aggr_bias` / `loose_bias` 를 아무도 안 넘긴다

`persona.make_player` 는 이 두 인자를 받는다. docstring 이 명시한다 —
*"공격성 축 하나로는 필드의 참여율 차이를 만들 수 없다. 그러면 어떤 대회를
열어도 8명 평균이 같아진다."* 그런데 `fieldsim` 이 안 넘긴다.

그래서 **bulevel 을 아무리 바꿔도 aggression 중앙값이 5.1 로 고정된다.**

---

## 7. 토너 종류 — 지금 어느 단계인가

`formats.py` (162줄)에 9종이 정의돼 있다.

| 키 | 이름 | buyin_level | q | 약체% |
|---|---|---|---|---|
| lowbuyin | 저바이인 | 0.45 | 0.54 | 19.2% |
| hyper / bounty | 하이퍼·바운티 | 0.85 | 0.72 | 13.6% |
| turbo | 터보 | 0.90 | 0.74 | — |
| deep / standard / main | 딥·표준·메인 | 1.00 | 0.78 | 11.3% |
| satellite | 위성 | 1.10 | 0.83 | 10.4% |
| highroller | 하이롤러 | 2.20 | 1.30 | **3.8%** |

### 연결된 것

`buyin_level` · `itm_frac` · `payout_flat` · `ante_from` · `blind_mult` · `reentry`
— 전부 `fieldsim` 이 읽는다. 하이롤러는 `STUDIED_TAG_TIGHT 28%` 로 실제로
필드가 달라진다. **실력 상·하향 장치는 이미 작동한다.**

`live2.new_game(fmt=...)` 도, `POST /api/new {fmt}` 도 이미 받는다.

### 연결 안 된 것

| 키 | 상태 |
|---|---|
| `start_bb` | **읽지 않음.** `start_stack=30000` 이 따로 온다 |
| `hpl` | **읽지 않음.** `hands_per_level=12` 가 따로 온다 |
| `seats` | **읽지 않음.** `fieldsim.MAXSEAT = 8` 하드코딩 (main/lowbuyin 은 9인용) |
| UI 선택기 | 없음. `app.js` 의 새 게임 폼에 엔트리·시드·스택뿐 |

### 정리

포맷 정의와 필드 생성은 **되어 있고**, 구조(스택·레벨 속도·인원)와
UI 노출이 **안 되어 있다.** 지금 상태로도 `curl -X POST /api/new -d
'{"fmt":"highroller"}'` 면 필드 수준은 바뀐다.

---

## 8. 배팅라인 (앞서 본 것 요약)

- **순수 드로우 19건 중 11건이 `giveup`.** 세미블러프를 받은 6건은 전부
  eq ≤ 0.412. 그 위는 전부 giveup. 가장 센 둘(eq .600 넛플드, eq .555
  넛플드+거트샷)이 둘 다 포기 — h45, h199
- CLAUDE.md 가 적어둔 설명(`eq >= pcz` 0.50 선점)은 **이 표본에 안 맞는다.**
  실제로 잘린 지점은 0.412 다. 코드를 읽어야 안다
- 프리플랍 오픈 158건: 2~3bb 112건 / **3~4bb 2건** / 4bb 초과 44건(43건 올인).
  중간 사이즈가 없다
- 깊은 스택 오픈 올인 6건 중 명백히 이상한 것 2건: h27(69bb, K9o, 91명 남음),
  h31(36bb, 55, 90명 남음)
- 포스트플랍 사이즈는 정상 — 봇 95건, 중앙 67%, 절반이 50~75%

---

## 다음에 볼 것

1. **0.412 가 어디서 나오는지** `plan.py` 세미블러프 분기 진입 조건 추적 (읽기만)
2. **프리플랍 intent 계측** — 지금 프리플랍 판단은 한 건도 기록되지 않는다.
   h27 같은 것을 밝히려면 필요하다. 기록 전용
3. **여러 대회 수집 후 단계별 재측정** — 버블 23핸드로는 아무 말도 못 한다
4. 죽은 코드 둘(`FAMILY_BASE`, `aggr_bias`/`loose_bias`)을 살릴지 지울지 결정
5. 포맷의 `start_bb`/`hpl`/`seats` 배선 + UI 선택기
