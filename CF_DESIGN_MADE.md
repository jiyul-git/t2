# 반사실 설계 — `465` 의 `made >= 1` 술어 교체

**측정 전에 고정한다.** 기준점 `TRACE_MADE.md`.
`plan.py` 는 수정하지 않는다 — 소스 텍스트 패치 + exec (`cf_pcz` 와 동일).

---

## 0. 관측 목적 분리

```
pcz  실험   분기 탈출 효과      eq >= pcz 진입 자체를 막는다   CF_RESULT_PCZ.md
made 실험   분기 내부 게이트    465 의 showdown / giveup 을 가른다   ← 이번
```

**두 개입을 동시에 켜지 않는다.** `pcz` 는 기준 코드 그대로 둔다.

---

## 1. 개입 — 한 줄, 세 팔, 각각 독립

`plan.py:465` 의 술어만 치환한다. **`458` 은 불변.** 새 수식도 새 파생변수도
만들지 않는다. 세 팔은 서로 독립된 단일 개입이고, 한 팔의 결과를 다른 팔의
입력으로 쓰지 않으며, 세 술어를 합치지 않는다.

```
O       plan = 'showdown' if made >= 1 else 'giveup'

ARM-S   made >= 1 or eq >= 0.42 + 0.05*mw       plan.py:503 의 has_sd 식 그대로
ARM-R   made >= 1 or rel >= 0.42                plan.py:1579 의 술어 그대로
ARM-D   made >= 1 or outs >= 8                  plan.py:468 의 8 그대로
```

**`_sd_strength` 를 `made` 자리에 넣는 실험은 하지 않는다.** 보드 기여
차감이 없어 `made` 의 기존 의미를 바꾸는 개입이 된다 (`TRACE_MADE.md` 4절).

**ARM 간 우열을 사전 가정하지 않는다.** 각 팔의 변화량은 기준군(O) 대비로
따로 낸다.

---

## 2. 비교 단위 — `cf_pcz` 에서 틀렸던 지점

```
단위    make_plan 반환 시점의 라벨
        update_plan 통과 후 intent 와 섞지 않는다 (CF_RESULT_PCZ 0절)
스냅샷   반환 dict 를 **사본**으로 담는다. 참조로 담으면 뒷 단계가 변형한다
집합    고정 eq 구간을 쓰지 않는다. why 문자열로 가른다
        진입 '중간강도'  ·  465 도달 '중간강도이나 상대레인지 열세'
```

---

## 3. 코드에서 유도되는 구조

`465` 는 `elif eq >= pcz` 블록의 마지막 문장이고 **난수를 소비하지 않는다.**
그 뒤로 `make_plan` 이 난수를 더 뽑는 지점도 없다. 그래서 이 개입은
`465` 에 도달한 호출의 **라벨 하나만** 바꿀 수 있다.

---

## 4. 사전등록 예측 — 측정 전에 박는다

```
D1  465 에 도달하지 않은 호출은 O 와 ARM 의 라벨이 전부 같다. 불일치 0건
D2  pcz 진입 건수와 465 도달 건수가 세 팔 모두 O 와 같다
D3  giveup 감소량 = showdown 증가량. 그 외 라벨 건수는 전부 불변이다
D4  ARM-S 의 465 giveup 은 **0건**이다
D5  ARM-R 의 465 giveup 은 0건보다 많다
D6  ARM-D 의 465 giveup 은 0건보다 많다
```

`D4` 근거: `465` 도달은 `eq >= pcz` 를 뜻하고
`pcz - (0.42+0.05*mw) = 0.08 + 0.01*mw + w*0.30*sg` 다.
`sg` 가 충분히 음수면 0 이 아닐 수 있다. **그래서 예측이지 확정이 아니다.**

`D5`·`D6` 근거: 실측에 `rel < 0.42` 인 건과 `outs < 8` 인 건이 둘 다 있다
(`TRACE_EQREL_COUNT.md`). 다만 단위가 달라 건수는 예측하지 않는다.

**결과를 보고 예측을 고치지 않는다. 틀리면 틀렸다고 적는다.**

---

## 5. 하지 않을 것

```
EV · 손익을 재지 않는다
ARM 간 우열을 가르지 않는다
pcz 개입과 같이 켜지 않는다
458 을 건드리지 않는다
made 값 자체를 바꾸지 않는다
새 임계값 · 새 plan 라벨을 만들지 않는다
baseline 지문을 재지 않는다 (plan.py 무수정)
```

---

## 6. 재현

```
python3 tools/cf_made.py --seeds 7000-7019 --hands 1000
```
