# ③ 블러프 라인·레인지 일관성 — 1차

**production code 는 수정하지 않았다.** 측정만 했다.

- baseline `613be81` (2-A 적용, 지문 `d8e9271e…`)
- 도구 `tools/bluff_coherence.py`
- 아카이브 11파일 **975핸드**, intent 1,692건 → 블러프 실행 **50건**, 밸류 실행 **271건**(대조군)

묻는 것은 "이 핸드로 블러프가 가능한가"가 아니라
**"이 라인에서 이 핸드가 블러프하기 좋은 핸드인가"** 다.

방법: 관찰자 역할을 **엔진 자신의 레인지 기계**에 맡겼다
(`ranges.preflop_range` → `narrow_by_actions`). 라인을 거슬러 복원한다.

```
스타팅 → 프리플랍 액션 → 플랍 액션 → 턴 액션 → 지금의 후보 레인지
```

**GTO 와 비교하지 않았다.** 먼저 엔진이 자기 모델로 자기 블러프를 설명할 수
있는지만 본다.

---

## 0. 먼저 — 내가 만든 태그 여섯 개 중 넷이 대조군에서 사라졌다

첫 판에 임계값 여섯 개를 지어내고 이렇게 적었다.

```
D 블로커 무의미 (blocker_net <= 0)          36건 (72%)
C 대표 밸류 부족 (value_pct < 15%)          33건 (66%)
B 보드가 상대 유리 (range_adv <= -0.15)     25건 (50%)
A 라인-레인지 불일치                         12건 (24%)
— 지적 없음                                 0건 (0%)
```

**전부 컷을 중앙값 근처에 둔 결과였다.** "지적 없음 0%" 가 그 신호였는데
처음엔 엔진이 나쁜 것으로 읽었다. 밸류 벳을 대조군으로 같은 측정을 하니:

| 지표(중앙) | 블러프 | 밸류(대조) | 차이 |
|---|---|---|---|
| 복원 레인지 크기(콤보) | 320 | 226 | +94 |
| **내 핸드 강도 백분위 %** | **32** | **85** | **−54** |
| 레인지의 밸류 콤보 % | 10 | 11 | −1 |
| **레인지의 에어+드로우 %** | **20** | **11** | **+9** |
| range_adv | −0.22 | −0.13 | −0.09 |
| nut_adv | −0.04 | −0.01 | −0.03 |
| **blocker_net** | **−0.005** | **−0.000** | −0.005 |
| made | 0 | 2 | −2 |
| outs_true | 8 | 0 | +8 |
| eq | 0.317 | 0.765 | −0.448 |
| **복원 레인지 안에 있음** | **76%** | **75%** | **+1%p** |

- `blocker_net` 은 **밸류 벳도 똑같이 0 근처다.** 블러프의 문제가 아니다
- `밸류 콤보 %` 10 vs 11 — 차이 없음
- **`레인지 안에 있음` 76% vs 75%** — 24% 누락은 블러프 특유가 아니라
  **내 복원 절차의 누락률**이다. 도구의 한계이지 엔진의 결함이 아니다
- 진짜로 갈리는 것은 **강도 백분위(32 vs 85)·에어드로우 비율(20 vs 11)·
  made·outs·eq** 이고, 전부 "블러프는 약한 핸드로 한다"는 **정상** 신호다

그리고 강도 백분위 자체도 처음에 잘못 쟀다. `made_strength` 버킷으로 세니
`made == 0` 인 순간 에어끼리 전부 동률이라 백분위가 **0 으로 고정**됐다.
`eval7` 연속값으로 바꾸니 32% 가 나왔다. **"레인지에서 제일 약하다"와
"다른 에어와 같다"가 구분되지 않던 것**이다.

**이번 1차의 결론: 블러프 50건에서 라인-레인지 불일치의 증거는 나오지 않았다.**

---

## 1. 그래도 남는 것 — `river_bluff` 가 975핸드에서 **0건**이다

계획 라벨 분포에 `river_bluff` 가 아예 없다. 리버 intent 351건의 계획은
`giveup` 106 · `showdown` 65 · `pot_control` 68 · `value_2street` 45 ·
`value_3street` 42 · `bluff_2street` 12 · `thin_river` 10 · `trap` 3 이다.

`river_bluff` 를 만드는 곳은 `river_fix` 하나인데, 진입 조건이 이렇다.

```python
plan.py:1540   if st.get('plan') != 'semibluff':
                   return st
```

**리버 시점에 계획이 `semibluff` 여야만 한다.** 그래서 추적했다.

```
플랍/턴에 semibluff 계획을 가진 좌석-핸드      22건
  리버 전 종료                                13건
  리버 도달                                    9건
      그 9건의 리버 계획:  giveup 5 · bluff_2street 3 · trap 1
      river_bluff                                       0
```

**리버에 도달한 9건 전부, 도착했을 때 이미 `semibluff` 가 아니었다.**

원인은 호출 순서다.

```python
plan.py:1505   st = refresh(st, hero, board, opp_range, profile, pot, stack, street, ...)
plan.py:1511   st = river_fix(st, hero, board, profile, opp_range, rng)
```

`refresh` 가 **먼저** 돌면서 리버 보드로 계획을 다시 매긴다. 드로우가 미스한
상태이므로 `has_sd` 블록이 `giveup` 으로 보내거나 다른 라벨을 준다.
그 뒤에 도는 `river_fix` 는 `plan != 'semibluff'` 라 **즉시 반환**한다.

`river_fix` 의 docstring 은 이렇게 적혀 있다.

> **미스한 드로우가 전부 giveup 으로 가면 안 된다.** busted 드로우는
> 리버 블러프의 대표 후보다 … 예전에는 그 라인이 통째로 없어서 리버
> 블러프가 거의 나오지 않았다.

**그 라인을 되살리려고 만든 함수인데, 되살리려던 그 경로(`refresh` 의
giveup)가 자기보다 먼저 돈다.** 975핸드에서 한 번도 발동하지 않았다.

### 이것이 "블러프가 어색하다"와 연결되는 지점

리버 블러프의 대표 후보(미스한 드로우)가 구조적으로 제외되면, 리버에서
실제로 치는 블러프는 **다른 경로로 들어온 것들**만 남는다. 아카이브의
리버 블러프 12건이 전부 `bluff_2street` 라벨인 것이 그 결과다 —
"턴까지 블러프하기로 계획했던 것"이지 "드로우가 미스해서 치는 것"이 아니다.

**아직 확정이 아니다.** 이 12건이 실제로 부자연스러운지는 2차에서
`bluff_2street` 의 리버 실행만 따로 봐야 한다. 지금은 **경로가 하나 막혀
있다**는 것까지가 확인된 사실이다.

---

## 2. 표본이 부족하다

블러프 실행 50건은 "여러 핸드에서 반복되는 공통 원인"을 가리기에 적다.
특히 스트리트별로 쪼개면 `semibluff/flop` 8건 같은 칸이 나온다.

```
semibluff / turn        18
bluff_2street / flop    14
bluff_2street / turn    10
semibluff / flop         8
```

`tools/collect.py` 로 봇 대전을 더 돌려 표본을 늘려야 한다.
**다만 이번 1차에서 "블러프가 구조적으로 이상하다"는 증거는 안 나왔으므로,
표본을 늘리는 것보다 1절의 막힌 경로를 먼저 보는 것이 순서로 보인다.**

---

## 다음

1. **`refresh` → `river_fix` 순서 확인.** 이건 코드 구조 문제이고 측정 근거도
   있다(9/9). 다만 **순서를 바꾸는 것이 옳은 수정인지는 아직 모른다** —
   `refresh` 가 리버에서 계획을 다시 매기는 것 자체는 필요한 동작이다.
   `river_fix` 가 `semibluff` 대신 **"직전 스트리트에 semibluff 였는가"** 를
   보게 하는 쪽이 후보지만, 그건 새 상태를 만드는 일이라 신중해야 한다
2. `bluff_2street` 의 리버 실행 12건 개별 검토
3. 표본 확대(`collect.py`)는 1·2 다음

**코드는 한 줄도 고치지 않았다.**
