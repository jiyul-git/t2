# ③ 블러프 라인·레인지 일관성 — 1차

**production code 는 수정하지 않았다.** 측정만 했다.

- baseline `613be81` (2-A 적용, 지문 `d8e9271e…`)
- 도구 `tools/bluff_coherence.py`
- 아카이브 11파일 **975핸드**, intent 1,692건 → 블러프 실행 **50건**, 밸류 실행 **271건**(대조군)

묻는 것은 "이 핸드로 블러프가 가능한가"가 아니라
**"이 라인에서 이 핸드가 블러프하기 좋은 핸드인가"** 다.

방법: 관찰자 역할을 **엔진 자신의 레인지 기계**에 맡겼다
(`ranges.preflop_range` → `narrow_by_actions`). 라인을 거슬러 복원한다.

```
스타팅 → 프리플랍 액션 → 플랍 액션 → 턴 액션 → 지금의 후보 레인지
```

**GTO 와 비교하지 않았다.** 먼저 엔진이 자기 모델로 자기 블러프를 설명할 수
있는지만 본다.

---

## 0. 먼저 — 내가 만든 태그 여섯 개 중 넷이 대조군에서 사라졌다

첫 판에 임계값 여섯 개를 지어내고 이렇게 적었다.

```
D 블로커 무의미 (blocker_net <= 0)          36건 (72%)
C 대표 밸류 부족 (value_pct < 15%)          33건 (66%)
B 보드가 상대 유리 (range_adv <= -0.15)     25건 (50%)
A 라인-레인지 불일치                         12건 (24%)
— 지적 없음                                 0건 (0%)
```

**전부 컷을 중앙값 근처에 둔 결과였다.** "지적 없음 0%" 가 그 신호였는데
처음엔 엔진이 나쁜 것으로 읽었다. 밸류 벳을 대조군으로 같은 측정을 하니:

| 지표(중앙) | 블러프 | 밸류(대조) | 차이 |
|---|---|---|---|
| 복원 레인지 크기(콤보) | 320 | 226 | +94 |
| **내 핸드 강도 백분위 %** | **32** | **85** | **−54** |
| 레인지의 밸류 콤보 % | 10 | 11 | −1 |
| **레인지의 에어+드로우 %** | **20** | **11** | **+9** |
| range_adv | −0.22 | −0.13 | −0.09 |
| nut_adv | −0.04 | −0.01 | −0.03 |
| **blocker_net** | **−0.005** | **−0.000** | −0.005 |
| made | 0 | 2 | −2 |
| outs_true | 8 | 0 | +8 |
| eq | 0.317 | 0.765 | −0.448 |
| **복원 레인지 안에 있음** | **76%** | **75%** | **+1%p** |

- `blocker_net` 은 **밸류 벳도 똑같이 0 근처다.** 블러프의 문제가 아니다
- `밸류 콤보 %` 10 vs 11 — 차이 없음
- **`레인지 안에 있음` 76% vs 75%** — 24% 누락은 블러프 특유가 아니라
  **내 복원 절차의 누락률**이다. 도구의 한계이지 엔진의 결함이 아니다
- 진짜로 갈리는 것은 **강도 백분위(32 vs 85)·에어드로우 비율(20 vs 11)·
  made·outs·eq** 이고, 전부 "블러프는 약한 핸드로 한다"는 **정상** 신호다

그리고 강도 백분위 자체도 처음에 잘못 쟀다. `made_strength` 버킷으로 세니
`made == 0` 인 순간 에어끼리 전부 동률이라 백분위가 **0 으로 고정**됐다.
`eval7` 연속값으로 바꾸니 32% 가 나왔다. **"레인지에서 제일 약하다"와
"다른 에어와 같다"가 구분되지 않던 것**이다.

**이번 1차의 결론: 블러프 50건에서 라인-레인지 불일치의 증거는 나오지 않았다.**

---

## 1. `river_bluff` 0건 — **버그가 아니다. 기대 빈도가 0.45건이다.**

이 절은 세 번 고쳤다. 앞의 두 판이 전부 틀렸으므로 그 과정을 남긴다.

### 1차 결론(틀림) — "refresh 가 먼저 돌아 막는다"

`plan.py:1772` 에 **이미 `street != 'river'` 가드가 있다.**

```python
elif old == 'semibluff' and outs < 6 and street != 'river':
    ...
# 리버는 river_fix 가 맡는다. 여기서 먼저 giveup 으로 내리면
# **미스한 드로우의 블러프 전환 경로가 통째로 막힌다**
```

누군가 이미 발견해 막아 놓은 것을, 그 가드를 안 읽고 통계(9/9)만 보고
인과를 지어냈다. 작업원칙 4 위반이다.

### 2차 결론(틀림) — "경로가 막혀 있어 표본을 늘려도 안 늘어난다"

경로는 **열려 있다.** 아래 계측이 보여준다.

### 3차 — 파이프라인을 계측했다

아카이브에는 중간 상태가 없다. 그래서 `tools/river_trace.py` 로 파이프라인
함수들을 **메모리에서 감싸고**(plan.py 무수정) 봇 대전을 돌렸다.
`why` 가 아니라 **반환된 state 의 `plan` 값**으로 판정했다.

여기서도 한 번 더 틀렸다. `river_fix` 가 `semibluff` 를 5회 받았다고 찍혔는데,

```python
plan.py:1529   if len(board) < 5: return state
```

`river_fix` 는 `street` 를 인자로 받지 않고 **보드 길이로 스스로 막는다.**
그래서 플랍·턴에서도 호출되고 즉시 반환한다. 그 no-op 4회를 같이 센 것이다.

**harness 건전성 카운터를 넣고 다시 재니** (300핸드):

```
make_plan 호출                200
river_fix 호출(보드 5장)        82      ← 실제 리버 판정
river_fix 호출(보드 <5장)      252      ← no-op
핸드                          300

river_fix 가 리버에서 semibluff 를 받은 횟수   1회
  → value_2street  1   (드로우가 완성돼 밸류 전환)
  → river_bluff    0
  → giveup         0
```

**`p_bluff` 블러프 굴림에 도달한 적이 0회다.** 경로가 막힌 것이 아니라
**거기까지 오는 표본이 없다.**

### 기대 빈도

필드 프로필 70명의 중앙값으로 계산한다.

```
bluff 중앙 5.0 · barrel_river 중앙 4.1
p_bluff = 0.10 + 0.55 × (5.0/10) × (4.1/10) = 0.212      (블로커·쇼다운 보정 전)

리버 판정 중 semibluff 진입 비율        1/82 = 1.2%
아카이브 리버 intent                    351건
  → river_fix 에 semibluff 로 진입 기대   4.3건
  그중 미스라야 굴림 대상 (절반으로 잡으면)  2.1건
  × p_bluff 0.212                      → **0.45건**
```

**기대 0.45건에 관측 0건이다.** Poisson λ=0.45 에서 P(0) = 0.64 —
완전히 정상 범위다.

**`river_bluff` 0건은 버그의 증거가 아니다.** "975핸드에서 0건"이라는
숫자만 보고 구조적 신호로 읽은 것이 처음부터 잘못이었다.

### 그래도 남는 사실 하나

`SIZING` 에서 리버에 벳 사이즈가 있는 블러프 계획은 `river_bluff`(0.72)
하나뿐이다(`semibluff`·`bluff_2street`·`giveup` 의 river 는 전부 0.0).
그래서 리버 `bluff_2street` 계획 12건은 **전부 check/fold** 였다 — 칠 수단이 없다.

이건 설계다(주석이 "이름 그대로 2스트리트"라고 적고 있다). 다만
**리버 블러프의 공급이 희소한 한 경로에만 걸려 있다**는 것은 기록해 둘 만하다.
기대 0.45건이면 실전에서 리버 블러프는 사실상 안 나온다.
그것이 의도인지는 별개 질문이고, **여기서 답하지 않는다.**

## 2. 리버 이탈 공격 5건 — 대조군을 놓으면 이상하다는 증거가 없다

개별로 읽으면 "플랍을 체크해 놓고 리버에만 친다"가 공통으로 보였다.
리버 밸류 벳 49건을 대조군으로 놓으니:

| | n | 플랍 체크 | 턴 체크 | 둘 다 체크 | 레인지 안 | 밸류콤보 | 강도 백분위 | rel | eq |
|---|---|---|---|---|---|---|---|---|---|
| 리버 이탈 공격 | 5 | 100% | 60% | 60% | 60% | 51% | 17% | 0.08 | 0.048 |
| 리버 밸류 벳(대조) | 49 | 67% | 63% | 51% | 49% | 25% | 91% | 0.94 | 0.847 |

**밸류 벳도 67%가 플랍을 체크한다.** 100% vs 67% 는 n=5 에서 p ≈ 0.14 로
유의하지 않다. 턴 체크·둘 다 체크·레인지 안 비율은 차이가 없고, 밸류콤보
비율은 오히려 이탈 공격 쪽이 높다.

갈리는 것은 강도 백분위(17 vs 91)·rel·eq 뿐이고 전부 "블러프는 약한
핸드로 한다"는 정상 신호다.

**n=5 로는 리버 블러프가 부자연스럽다는 증거가 나오지 않는다.**

## 3. 표본 확대의 순서 문제

블러프 실행 50건(플랍·턴), 리버 블러프 5건은 공통 원인을 가리기에 적다.
그런데 **표본을 늘려도 계획된 리버 블러프는 늘지 않는다** — 유일한 경로인
`river_bluff` 가 0건으로 닫혀 있기 때문이다. 늘어나는 것은 이탈 공격뿐이다.

그래서 순서가 뒤집힌다. "표본을 늘려 판정한다"가 아니라
**"경로가 왜 0 인지부터 특정한다"** 가 먼저다.

## 다음 — 지금 할 것

1. **`turn=semibluff → river=giveup` 3건의 라벨 변경 지점을 특정한다.**
   `why` 에 리버 줄이 없다는 것이 단서다. `revise_plan`·`update_plan`·
   `refresh`·`river_fix` 중 어디가 `why` 를 남기지 않고 라벨을 바꾸는지,
   또는 리버 intent 가 만들어지는 시점 자체를 봐야 한다.
   **통계로 인과를 짓지 않는다 — 이번에 그래서 한 번 틀렸다.**
2. 특정된 뒤에야 `river_fix` 입력 조건 변경이 후보인지 판단한다
3. 표본 확대는 그 다음

**`river_fix` 는 수정하지 않는다.** 직전 스트리트의 semibluff 이력만으로
리버 블러프 자격을 자동 부여하면, 보드가 상대에게 유리하고 대표할 밸류가
없는 자리에서도 블러프가 나와 **지금보다 더 어색한 블러프**를 만들 수 있다.
경로를 여는 것이 좋은 블러프를 만드는지는 아직 모른다.

**코드는 한 줄도 고치지 않았다.**

---

# ③-2 — 표본 4,000핸드. **블러프의 절반이 계획 밖에서 나온다**

`tools/collect.py` 로 4,000핸드를 새로 모았다(`intent` 8,270건).
세 집단으로 나눠 같은 측정을 했다.

```
계획 블러프   242건   bluff_2street · semibluff · river_bluff 로 bet/raise
밸류(대조)  1,532건   value_2street · value_3street · trap · thin_river
이탈 블러프   277건   giveup · showdown 계획인데 bet/raise
```

`pot_control` 은 이탈에 넣지 않았다. `SIZING['pot_control']` 이 flop 0.30 /
river 0.30 이라 **계획대로 치는 것**이다. 처음에 넣었다가 168건을 이탈로
잘못 셌다.

**`river_bluff` 가 4,000핸드에서 6건 나왔다.** 경로가 열려 있다는 최종 확인이다.
975핸드 0건은 희소성이었다.

## 분포 (10 / 25 / 50 / 75 / 90 분위)

| | 계획 블러프 | 밸류(대조) | 이탈 블러프 |
|---|---|---|---|
| 내 핸드 강도 백분위 % | 2 · 5 · **14** · 33 · 49 | 52 · 70 · 84 · 93 · 98 | 4 · 13 · **25** · 42 · **55** |
| `outs_true` | 0 · 0 · **4** · 8 · 9 | 0 · 0 · 0 · 0 · 0 | 0 · 0 · **0** · 0 · 4 |
| `eq` | .107 · .175 · **.278** · .375 · **.418** | .567 · .659 · .762 · .870 · .947 | .077 · .167 · **.279** · .412 · **.540** |
| 레인지의 에어+드로우 % | 2 · 8 · 17 · 28 · 38 | 0 · 0 · 12 · 23 · 34 | 0 · 1 · 9 · 20 · 28 |
| 복원 레인지 안에 있음 | 81% | 73% | 90% |

**임계값을 하나도 두지 않았다.** 세 집단의 분포가 직접 말한다.

## 읽는 법

**계획 블러프는 자연스럽다.** 레인지 하위(중앙 백분위 14), 드로우를 갖고 있고
(`outs` 중앙 4), `eq` 90분위가 **0.418** 에서 끊긴다 — `make_plan` 의
`eq < 0.42` 게이트가 그대로 보인다.

**이탈 블러프는 다르다.**

```
outs_true 중앙   계획 4  →  이탈 0        아무것도 없이 친다
eq 90분위        계획 .418 → 이탈 .540    게이트 상한을 넘는다
강도 백분위 중앙  계획 14  →  이탈 25      더 강한 핸드로 친다
```

`eq` 0.42~0.54 구간은 **쇼다운 가치가 있는 구간**이다. 계획 블러프는 게이트가
막아 주는데 이탈 블러프는 그 게이트를 **아예 안 거친다.**

## 기제 — 이니셔티브가 있으면 `giveup` 이 사실상 씨벳 계획이 된다

사유를 분해하면 277건 중 **257건(92%)** 이 한 곳에서 나온다.

```python
plan.py:857   if plan in ('giveup', 'showdown'):
                  if not initiative:
                      return 0.0, '포기 계획 + 이니셔티브 없음 → 체크'
                  cf = cbet_freq(profile, board, n_opp, street, oop, rel, opp_est, ...)
                  if has_c:
                      disc = PS.temper(profile, 'discipline', 5.0)
                      cf *= max(0.05, 1.0 - 0.085*disc)
                  return max(0.0, min(0.9, cf)), 'DEVIATE:포기 계획이나 지속벳(%.0f%%)'
```

`cbet_freq` 는 **구조적 빈도**다 — 보드·인원·포지션·이니셔티브를 보고
핸드 강도·드로우·레인지는 보지 않는다. 규율로 감쇠하지만
`max(0.05, 1 - 0.085*disc)` 는 `discipline=9` 에서도 **0.235** 다.

이니셔티브 비율도 맞는다: 이탈 공격 445건(당시 분류) 중 이니셔티브 True 337건(76%).

## 그래서 확정된 것

**봇이 치는 블러프성 공격의 53%(277 / 519)가 계획이 아니라 규율 실패다.**
그리고 그 절반은 계획 블러프가 받는 심사(`eq < 0.42`, `outs`, 블로커, 넛우위)를
**하나도 거치지 않는다.**

관찰자 입장에서 "왜 저기서 치지"로 읽히는 블러프가 있다면 **이쪽일 가능성이
크다.** 계획 블러프 쪽은 분포가 정상이다.

## 아직 답하지 않은 것

- **이것이 의도인가.** `DEVIATE:` 접두사와 `deviations` 기록이 있는 것을 보면
  설계자는 "계획 이탈"을 의식하고 만들었다. 다만 그 비중이 53%까지 가는 것이
  의도인지는 코드에 근거가 없다
- 이탈 블러프가 실제로 **손해**인지는 재지 않았다. 빈도·프로파일만 봤다
- `cbet_freq` 가 핸드 강도를 안 보는 것 자체는 설계다(구조적 빈도).
  문제는 그것이 **`giveup` 계획에도 그대로 적용**된다는 점이다

**코드는 한 줄도 고치지 않았다.**

---

# ③-3 — 이탈 공격은 **나쁜 공격이 아니다**

도구 `tools/deviate_ev.py`. 표본 4,000핸드(결과 필드 포함해 새로 수집).

## 절대 EV 대신 필요 폴드율 vs 실측 폴드율

"체크했으면 어땠을까"를 알려면 핸드를 다시 돌려야 하고, 그러면 이후 전개가
통째로 달라진다. 그래서 **양쪽 다 측정값인 두 숫자**를 비교했다.

```
EV(체크) = eq·P
EV(벳)   = f·P + (1-f)·[eq·(P+2B) - B]

                  B(1-2eq)
      f* = ─────────────────────────      기록된 pot·bet·eq 로 케이스마다 계산
           P(1-eq) + B(1-2eq)
```

## 결과

| | n | 실측 폴드% | 필요 폴드% f* | 여유 | eq 중앙 |
|---|---|---|---|---|---|
| 계획 블러프 | 222 | 39.6% | 28.0% | **+11.7%p** | 0.281 |
| **이탈 블러프** | 268 | **49.8%** | **22.8%** | **+27.0%p** | 0.264 |
| 밸류(대조) | 1,551 | 45.4% | 109.8% | −64.3%p | 0.758 |

밸류의 `f*` 109.8% 는 "폴드를 받을 필요가 없다"는 뜻이다 — 대조군이 제대로
작동한다는 확인이다.

**이탈 블러프의 여유가 계획 블러프의 2.3배다.** 실측 폴드율은 더 높고
(49.8% vs 39.6%), 필요 폴드율은 더 낮다(22.8% vs 28.0%).

이유는 사이즈다.

```
벳 사이즈(팟 배수)  10/25/50/75/90 분위
  계획 블러프   0.39  0.50  0.63  0.88  1.66
  이탈 블러프   0.38  0.44  0.51  0.59  0.84      ← 작게 친다
  밸류(대조)    0.48  0.58  0.69  0.85  2.00
```

작게 치니 필요 폴드율이 낮고, 그런데도 상대는 **더 자주** 접는다.

스트리트별로도 전부 여유가 양수다.

```
flop   n=167  실측 46.7%  f* 21.1%   +25.6%p
turn   n= 69  실측 51.5%  f* 27.7%   +23.8%p
river  n= 32  실측 63.3%  f* 31.5%   +31.8%p
```

핸드 최종 칩 증감(그 벳 하나의 EV 가 아니다, 참고용):

```
계획 블러프   중앙 +1,500   플러스 비율 56%
이탈 블러프   중앙 +1,900   플러스 비율 64%
밸류(대조)    중앙 +4,600   플러스 비율 82%
```

## 한계 — 계획 블러프 쪽이 과소평가된다

`f*` 는 **한 스트리트** 모형이다. `bluff_2street` 는 정의상 턴에 한 번 더
치는 계획이라, 플랍에서 콜당하는 것이 실패가 아니다. 그 경우 실제 EV 는
턴 배럴까지 합쳐야 하므로 **계획 블러프의 진짜 여유는 +11.7%p 보다 크다.**

그래도 결론은 안 뒤집힌다 — 이탈 블러프의 +27.0%p 는 그 자체로 충분히 크고,
폴드율의 표준오차가 3.1%p 라 노이즈 범위를 한참 넘는다.

가정 셋은 두 집단에 똑같이 적용된다: `eq` 는 올인 에쿼티, 체크 후 상대 벳
무시, 벳 후 레이즈당함 무시.

## 판정

**277건의 이탈 공격이 나쁜 공격이라는 증거가 없다.** 오히려 지표가 계획
블러프보다 낫다.

그러므로 **`cbet_freq` 가 `giveup` 계획에 그대로 적용되는 구조를 수정 후보로
올릴 근거가 없다.** 그 구조는 "이 핸드가 좋은 블러프인가"가 아니라
"이 상황은 원래 지속벳을 할 만한 상황인가"를 보는데, **실측상 그 판단이 맞다.**

빈도가 53%인 것도 그 자체로는 문제가 아니다. 성격 표현(규율 낮은 사람이
계획을 더 자주 뒤집는다)으로 유지할 수 있다.

## ③ 전체 정리

```
블러프 라인·레인지 일관성   대조군 놓으니 구조적 이상 증거 없음
river_bluff 0건            기대 0.45건, 4,000핸드에서 6건 — 정상
이탈 블러프 53%            질을 재니 계획 블러프보다 낫다 — 수정 후보 아님
```

**③에서 "블러프가 어색하다"의 구조적 원인은 나오지 않았다.**
세 갈래를 다 파고 전부 정상으로 닫혔다.

남은 가능성은 셋이다.
1. 어색함이 블러프 자체가 아니라 **밸류 라인**에 있다 (아직 안 봤다)
2. 어색함이 개별 핸드가 아니라 **같은 사람의 라인 일관성**에 있다 —
   한 핸드씩 보면 정상인데 그 사람이 반복해서 같은 패턴을 보이면 읽힌다
3. 체감이 표본 편향이었다 (히어로가 기억하는 핸드가 이상한 것들 쪽으로 쏠린다)

**코드는 한 줄도 고치지 않았다.**
