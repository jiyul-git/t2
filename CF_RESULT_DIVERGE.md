# ⑧ 결과 — 축별 경로 분화

설계는 `CF_DESIGN_DIVERGE.md`.

```
3b1fea7   설계 (관측량 P1·P2·P3, 사전 이분법)
efde4cd   P1 · P3 관측 — 채점 없음
caf7160   **P2 판별 기준을 측정 전에 고정** (3-1 절)
2f0bfbb   P2 측정
```

`rows55b` 만 썼다. **⑧ 전체에서 새 시뮬레이션은 없다.**

> **판정: 분화형.** 같은 분기 안에서도 축끼리 다르다.

---

## 1. 기준을 측정 전에 고정했다

`caf7160` 이 P2 판별 기준을 잠근 커밋이다. 그 시점에 **본 것과 안 본 것**을
설계 3-1-6 에 적어뒀다.

```
봤다     P1 의 분기별 Δp≠0 건수 (efde4cd)
         분기별 전체 쌍 수 N_b — 축마다 같다는 것과 그 값
안 봤다   S(ax, b) · T(ax, b) — P2 의 채점 대상
```

기준을 고정한 뒤 `2f0bfbb` 에서 측정했고 **기준은 수정하지 않았다.**

---

## 2. 채점 대상 — 분기 하나, 통계량 둘

```
주 채점 분기   DEVIATE:포기 계획이나 지속벳(N%)
               세 축 이상이 겹치는 **유일한** 분기다 (4축)
채점 통계량    S · T 둘
문턱          S 비 2.0 · T 비 1.5   — ⑦ d6fae23 의 값을 재사용
```

**분기 선택 규칙은 "겹치는 축 수가 가장 많은 것" 이다.** 건수 문턱을 새로
만들지 않았다 (작업 원칙 2).

---

## 3. `R` 을 채점에서 뺀 논리

`src_O` 는 O 팔의 값이라 **축에 의존하지 않는다.** 실측으로 확인했다 —
분기별 전체 쌍 수 `N_b` 가 다섯 축에서 전부 같고 합계 31,176 으로 ⑦ 의
D 팔 모집단과 일치한다.

```
R(ax, b) = |{Δp≠0, src_O=b}| / N_b
```

분모가 축마다 같으므로 **`R` 비는 P1 의 건수 비와 같다.** P2 에서 새로
얻는 정보가 없다.

**그리고 기준을 정하는 시점에 이미 그 값을 알고 있었다** — `DEVIATE` 에서
`discipline` 4,212/4,212 = 100%, `cbet_flop` 2,198/4,212 = 52.2% 라
`R` 비가 1.916 이다. 문턱 2.0 을 여기 적용하면 **답을 알고 기준을 정하는
것**이 된다.

그래서 `R` 은 표에 참고로만 싣고 채점하지 않았다.

---

## 4. 측정값

```
DEVIATE:포기 계획이나 지속벳(N%)   N_b = 4,212, 겹치는 축 4

축                      Δp≠0    R(참고)          S         T
aggression             4,172    99.1%    0.10685     0.102
discipline             4,212   100.0%    0.32004     0.320
bluff                  4,126    98.0%    0.06111     0.060
cbet_flop              2,198    52.2%    0.14582     0.139

S 비 = 0.32004 / 0.06111 = 5.237        문턱 2.0  →  이상
T 비 = 0.320   / 0.060   = 5.303        문턱 1.5  →  이상
```

---

## 5. 판정

```
S 비 >= 2.0  그리고  T 비 >= 1.5   →   분화형
```

> **분화형.** 같은 분기 안에서도 축끼리 다르다. **축의 차이가 분기 점유
> 만으로 환원되지 않는다.**

`discipline` 과 `bluff` 는 같은 `DEVIATE` 분기에서 거의 같은 빈도로
작동하는데(`R` 100.0% vs 98.0%) `S` 가 5.2배, `T` 가 5.3배 다르다.

### 5-1. 이 판정이 말하지 않는 것

```
축 서열의 원인을 말하지 않는다 — ⑥·⑦ 에서 닫힌 질문이고 다시 열지 않는다
S·T 가 원인이라고 말하지 않는다 — ⑧ 설계 0-1 의 불확실성을 유지한다
분기 점유가 무의미하다고 말하지 않는다 — 점유만으로 환원되지 않는다는 것이다
```

**그리고 "각 축이 독립적인 심리적 개념을 실제로 구현한다" 가 증명된 것이
아니다.** 이번 결과의 범위는 딱 하나다.

```
기각된 것   "분기 선택 빈도 → 관측 행동 차이" 라는 단일 환원 가설
            그것도 사전 지정한 DEVIATE 분기에서
기각되지    축이 개념적으로 독립이라는 것
않은 것     다른 환원 가설들
            다른 분기에서도 같은지
```

환원 가설 하나를 기각한 것이지 축의 의미를 확인한 것이 아니다.

---

## 6. 보조 분기 — 채점·확장·해석 대상이 아니다

```
밸류 계획 실행(N%)     N_b 9,380  2축   S 비 1.765   T 비 1.794
블러프 계획 실행(N%)    N_b 1,817  2축   S 비 1.799   T 비 2.060
사이즈 0 → 체크        N_b   559  2축   S 비 1.690   T 비 inf
```

**세 분기의 `S` 비가 전부 1.7~1.8 로 주 채점 분기(5.237)보다 훨씬 작다.**

**이것을 해석하지 않는다.** 2축 비교는 `max/min` 이 곧 두 값의 비라
"축끼리 비슷한가" 를 가르는 힘이 약하다고 설계 3-1-3 에 측정 전에 적었다.
**사후에 채점 분기를 늘리지 않는다.**

---

## 7. `사이즈 0 → 체크` 의 `T = inf`

```
aggression   T = 0.000        bluff   T = 0.000
T 비 = 0.000 / 0.000  →  코드가 inf 를 낸다
```

**`0/0` 이 아니라 분모만 0 인 경우가 아니다 — 분자·분모가 모두 0 이다.**
정확히는 `max(T) = 0` 이고 `min(T) = 0` 이라 나눗셈에서 `inf` 로 표기됐다.

그 분기에서 O 팔의 intent 가 이미 `사이즈 0 → 체크`(= check)이고 양쪽
tag 도 check 라 `act≠` 가 한 건도 없다. **`T` 가 0 이라는 사실이 정보이고,
`inf` 는 표기 산물이다.** 비로 읽지 않는다.

---

## 8. P1 · P3 (`efde4cd`, 채점 없음)

판정의 근거는 아니고 관측 기록이다.

### P1 — 축별 분기 점유 (D 팔, Δp≠0)

```
aggression       16,174   5종   밸류 44.7 / DEVIATE 25.8 / 팟컨트롤 22.6 /
                                블러프 4.2 / 사이즈0 2.6
bluff             6,093   3종   DEVIATE 67.7 / 블러프 29.8 / 사이즈0 2.5
discipline        4,212   1종   DEVIATE 100.0
thin_value_turn   2,927   1종   밸류 100.0
cbet_flop         2,198   1종   DEVIATE 100.0
potcontrol · looseness   0
```

**세 축이 단일 분기 전용이다.**

### P1-c — 설계 1-1 이 맞았다

D 팔의 `src_lo != src_hi` 는 **전부 `사이즈 0 → 체크` 로의 전이**였다.

```
aggression  309 / 16,174   (팟컨트롤 278 · 블러프 17 · 밸류 14)
bluff       111 /  6,093
discipline · thin_value_turn · cbet_flop   0
```

설계가 코드에서 추론한 그대로다 — `plan` 이 안 바뀌므로
`decide_aggression` 분기는 같고 `size == 0` 인 경우에만 갈린다.
309·111 은 ⑥ 의 숫자와 같다.

### P3 — M 팔 전이 행렬 (관측 전용)

```
aggression    341   plan 대각  0.0%   pot_control → value_2street 240 외
bluff         539   plan 대각  0.0%   giveup → bluff_2street 421 외
discipline  5,696   plan 대각 93.3%   value_3street → value_2street 153 외
potcontrol  1,135   plan 대각  0.0%   showdown → pot_control 1,135 (단일 전이)
looseness     617   plan 대각 96.3%
thin_value_turn · cbet_flop   M 에서 Δp≠0 = 0
```

**두 부류로 갈린다.** `aggression`·`bluff`·`potcontrol` 은 `Δp≠0` 이면
반드시 계획 라벨이 바뀐다. `discipline`·`looseness` 는 93~96% 가 대각 —
계획은 그대로 두고 `rel` 을 통해 `p` 만 움직인다 (`CF_DESIGN_LEVEL2` 5-1
경로).

**M 팔은 flip 서열 채점에 쓰지 않았다** (설계 1-2·5절).

---

## 8-1. 사후 관측 — **예측으로 쓸 수 없다**

측정이 끝난 뒤에 본 것이다. **confirmatory hypothesis 의 예측으로 쓰지
않는다.** 여기 봉인해두고, 쓰려면 새 자료에서 사전 등록해야 한다.

`DEVIATE` 분기 안에서 네 축은 **서로 다른 산술 위치**로 들어간다.

```python
# plan.py:861-868  DEVIATE 분기
cf = cbet_freq(profile, board, n_opp, street, oop, rel, opp_est, range_adv=…)
if has_c:
    disc = PS.temper(profile, 'discipline', 5.0)
    cf *= max(0.05, 1.0 - 0.085*disc)          # discipline — cbet_freq 밖에서 곱
return max(0.0, min(0.9, cf)), 'DEVIATE:…'

# cbet_freq 안 (plan.py:1183-1187)
base *= max(0.35, min(1.85, 0.45 + 0.11*sk(cbet_flop)))   # cbet_flop — base 에 곱
f = base + 0.035*a + 0.020*b                              # aggression·bluff — 더함
```

```
위치                                    S
discipline   cbet_freq 밖에서 전체에 곱    0.32004
cbet_flop    cbet_freq 안에서 base 에 곱   0.14582
aggression   base 에 더함 (계수 0.035)     0.10685
bluff        base 에 더함 (계수 0.020)     0.06111
```

네 위치의 구분이 `S` 서열과 같은 순서다.

**이것을 H3 의 부활로 쓰지 않는다.** 두 질문은 다르다.

```
H3 (기각됨)   계수·배율 크기로 **전체 flip 서열**을 설명하는 설명 체계
⑩ 후보        **한 분기 안에서** 같은 코드 경로를 공유하는 축들이 어느 산술
              연산 위치에서 서로 다른 Δp 를 만드는가 — 구조 탐색이다
```

비슷해 보이지만 논리적으로 다른 질문이고, **경계가 얇다.** ⑩ 설계에서
이 구분을 먼저 박지 않으면 기각된 가설을 되살리는 것이 된다.

---

## 9. 재현

```
python3 tools/diverge.py rows55b.jsonl.gz
```

`rows55b.jsonl.gz` 는 `CF_RESULT_NORM.md` 8절의 명령으로 다시 만든다.
