# 성향 축 검증 ② — 빈도 축, 1단계(기질·계획 라벨)

**production code 는 수정하지 않았다.** 측정만 했다.

- baseline `613be81` (2-A 적용, 지문 `d8e9271e…`)
- 도구 `tools/axis_freq.py` (지표·경로·고정축은 `AXIS_FREQ_PLAN.md` 의 표 그대로)
- n=250, flop, 레벨 1/3/5/7/9 (+게이트 의심 축은 문턱 근처 촘촘히)
- 그림 `axis_freq.png`

위약(`tilt_swing`·`tilt_stack`)은 **모든 축·모든 층에서 최대 변화 0.0000**.

---

## 결과 — 층별로 따로 적는다

| 축 | 층 | 지표 1 → 9 | 판정 |
|---|---|---|---|
| `aggression` | 프리플랍 오픈 | 21.4% → 25.9% | ● |
| | 프리플랍 3벳 구간 `tp` | 0.05 → 0.09 | ● |
| | 프리플랍 참가 구간 `tot` | 0.29 → 0.29 | 안 읽힘 (`looseness` 담당) |
| | 프리플랍 성향표 | 림프 18→0, 콜 18.8→9.2, 3벳 4.3→11.5, 이소 8.6→95.0 | ● |
| | 계획 `pc` | 0.63 → **0.37** | **▲ 이름과 반대 — 설계다** |
| | 계획 `pot_control` | 15.6% → 12.8% | ● (위 ▲의 결과) |
| | 무저항 벳 | 28.4% → 41.6% | ● |
| | 무저항 사이즈 | 0.43 → 0.60 팟 | ● |
| | 저항 레이즈 | 12.8% → 18.4% | ● |
| `looseness` | 프리플랍 오픈 | 16.9% → 30.4% | ● |
| | 프리플랍 참가 구간 `tot` | 0.20 → 0.37 | ● |
| | 프리플랍 성향표 | 림프 0→26, 콜 5.2→22.8 | ● |
| | `bias('station')` **날것** | **−0.36 → +0.36** (완전 선형) | ● |
| | `bias('station')` **clamp 후** | **0 / 0 / 0 / 0.18 / 0.36** | **◐ 하위 절반 차단** |
| | 저항 콜 | 38.0 / 38.0 / 38.0 / 40.8 / 42.4 | **◐ 레벨 1~5 가 동일** |
| `discipline` | 프리플랍 림프 | 12% → 0% | ● |
| | `bias('station')` 날것 | +0.16 → −0.16 | ● |
| | `bias` clamp 후 | **0.16 / 0.08 / 0 / 0 / 0** | **◐ 상위 절반 차단** |
| | `bias('overpair_love')` 날것 | +0.24 → −0.24 | ● |
| | 무저항 벳 | 31.6% → 22.0% | ● |
| | 저항 폴드 | 54.0% → 56.0% | ◐ (7·9 가 동일) |
| `bluff` | 프리플랍 3벳 | 5.5% → 10.3% | ● |
| | 계획 `bluff_2street` | **0 (≤3.3) → 6.8 (3.4) → 25.6 (9)** | **□ 게이트 + 그 위 ●** |
| | 무저항 벳 | 30.4% → 36.0% | ● |
| | 무저항 사이즈 | 0.53 고정 | 안 읽힘 (`decide_size` 에 없다) |
| `semibluff` | 계획 `semibluff` | **0 (≤1.3) → 1.2 (1.4) → 4.0 (9)** | **□ 게이트 + 그 위 ●** |
| | 저항 레이즈 | 11.6% → 12.8% | ◐ 약함 |
| `potcontrol` | 계획 `pot_control` | **0 (≤3.3) → 14.4 (3.4)**, **3.4~9 전부 14.4** | **□ 순수 계단** |
| `trap` | 계획 `trap` | 0.8% → 1.6% | ◐ |
| `checkraise_flop` | 계획 `trap` | 0.8% → **2.4%** | ● (같은 층, 실행 축이 주도) |

---

## 1. 게이트 문턱이 예측과 소수점까지 맞았다

`AXIS_FREQ_PLAN.md` 에서 `make_plan` 의 `sk()` 가 0~3 스케일(`plan.py:398`
`PS.sk/3.33`)이라 문턱이 3.33·1.33 이라고 적었다. 촘촘히 훑은 결과:

```
bluff        3.3 에서 0.00%  →  3.4 에서 6.80%     문턱 3.33
semibluff    1.3 에서 0.00%  →  1.4 에서 1.20%     문턱 1.33
potcontrol   3.3 에서 0.00%  →  3.4 에서 14.40%    문턱 3.33
```

**축을 1/3/5/7/9 로만 훑었으면 셋 다 "1→3 사이 어딘가에서 갑자기 생긴다"로
뭉개졌을 것이다.** 게이트가 의심되면 문턱 근처를 훑어야 한다.

## 2. `potcontrol` 은 빈도 축이 아니라 스위치다

게이트 위에서 **기울기가 0** 이다.

```
PS.sk   1  2.8   3  3.2  3.3 │ 3.4  3.6  4.2   5   7   9
pot_control %   0    0   0    0   0 │14.4 14.4 14.4 14.4 14.4 14.4
```

코드가 그렇다. 확률식 `_pc_p = pc*0.8 + 0.12 + 0.18*mw` 에 `potcontrol` 이
**한 번도 안 들어간다.** 축은 `sk('potcontrol') >= 1` 게이트에만 쓰인다.

그러니 이름이 주장하는 "팟 컨트롤 선택 **빈도**"와 코드가 하는 일이 다르다.
**"팟 컨트롤을 아는가 / 모르는가" 이분법**이다. 3.33 위에서는 개념 3.4인
사람과 9.9인 사람이 완전히 같다.

받는 곳도 확인했다 — `pot_control` 14.4%p 는 `value_2street` 4.8%p 와
`showdown` 9.6%p 에서 온다.

## 3. `looseness`·`discipline` — 축은 완벽한데 절반이 전달되지 않는다

`AXIS_FREQ_PLAN.md` 1절 (다)에서 경고한 `max(0.0, bias(...))` 가 실물로 나왔다.

```
looseness    날것   -0.36  -0.18   0.00  +0.18  +0.36      완전 선형
             clamp   0.00   0.00   0.00   0.18   0.36      하위 절반이 0
             저항 콜 38.0   38.0   38.0   40.8   42.4      1~5 가 동일

discipline   날것   +0.16  +0.08   0.00  -0.08  -0.16
             clamp   0.16   0.08   0.00   0.00   0.00      상위 절반이 0
```

**중간값을 같이 찍지 않았으면 "looseness 1~5 는 무반응"으로 기록됐을 것이다.**
실제로는 축이 정확히 선형으로 움직이고 있고, `call_bias` 의 `max(0, ·)` 가
그 절반을 행동까지 내보내지 않는 것이다. 이 둘은 완전히 다른 문제다.

방향이 반대인 두 축이 **서로 반대쪽 절반**에서 잘린다는 점도 기록해 둔다 —
`looseness` 는 타이트한 쪽이, `discipline` 은 규율 높은 쪽이 잘린다.
`station` 은 "너무 넓게 콜한다"는 편향이므로 음수 쪽(= 정상보다 타이트)에
의미를 안 주는 것이 의도일 수는 있다. **판단하지 않고 기록만 한다.**

## 4. `aggression` 의 ▲ 는 버그가 아니다

계획 층에서만 방향이 반대다.

```python
plan.py:306   pc = clamp((profile['icm'] + (10-profile['gamble'])
                          + (10-profile['aggr'])) / 30.0)
```

`pc` 는 "팟을 키우지 말아야 할 압력"이다. 공격적일수록 그 압력이 낮은 것이
맞다. 측정값 0.63 → 0.37 은 식 그대로다(`(5+5+9)/30 = 0.633` → `(5+5+1)/30`…
부호 확인됨). 이름과 반대인 것이 아니라 **`pc` 라는 중간 변수의 방향이
반대**인 것이다. 최종 `pot_control` 비율은 15.6% → 12.8% 로 "공격적일수록
팟 컨트롤을 덜 한다" — 이름과 맞는다.

## 5. `trap` 은 ◐ 이고, 그게 정상이다

`trap_judgment:218` 이 `tool = 0.07*trap + 0.12*checkraise` 라 실행 축이
1.7배 무겁다. 실측:

```
trap 1→9              trap 라벨 0.8% → 1.6%    (+0.8)
checkraise_flop 1→9   trap 라벨 0.8% → 2.4%    (+1.6)
```

**`trap` 만 흔들어 작게 나온 것을 "죽었다"로 읽으면 안 된다.** 짝 축을 같이
재야 층이 살아 있음이 보인다. 비율(0.8 대 1.6)도 코드 무게(0.07 대 0.12)와
방향이 맞는다.

---

## 측정 중 세 번 틀렸다 — 전부 코드를 읽고 고쳤다

①의 두 건에 이어 이번에도 셋이다. 도구가 만든 가짜 결론이다.

**(1) 무저항 층이 전부 체크(0% 벳)로 나왔다.**
`act_with_plan` 은 무저항에서 **판단하지 않는다** — 의도를 칩으로 환산만
한다(`plan.py:1315`). 의도는 `attach_intent`(→`decide_aggression`→`decide_size`)가
붙이고, 그건 `update_plan` 이 호출한다. `make_plan` 만 부르고 `act_with_plan` 을
호출하니 `intent` 가 `None` 이라 전부 체크였다.
→ 계획 상태를 복사해 의도를 지우고, **그 레벨의 프로필로 `attach_intent` 를
다시 부른다.** `update_plan` 을 통째로 부르지 않은 이유는 그게 `_allowed`·
`river_fix` 까지 돌려 **계획 라벨을 바꿀 수 있어서**다 — 그러면 실행 층만
격리한 것이 아니게 된다.

**(2) 계획 라벨 표에서 행이 통째로 사라졌다.**
표의 키를 **첫 열에서만** 뽑았다. 레벨 1 에서 0건인 라벨은 행 자체가 없어져서,
`bluff` 축의 `bluff_2street` 행과 `semibluff` 축의 `semibluff` 행이 —
**정확히 그 축이 만드는 라벨이** — 사라졌다. 남은 표만 보면 "계획에
아무 영향 없음"으로 보였다.
→ 모든 레벨의 키를 합집합으로 모은다.

**(3) `aggression` 이 프리플랍 방어에서 무반응으로 보였다.**
`defend_thresholds` 의 반환 `(tp, tot)` 중 `tot` 만 기록했다. `aggression` 은
`tp`(3벳 구간)에 걸리고 `tot`(참가 구간)에는 안 걸린다.
→ 둘 다 기록하니 역할 분담이 깨끗하게 드러났다.

```
              tp (3벳 구간)      tot (참가 구간)
aggression    0.05 → 0.09        0.29 → 0.29
looseness     0.06 → 0.07        0.20 → 0.37
```

---

## 다음 단계

2단계(계획 라벨)는 위 표에 이미 상당 부분 들어갔다. 남은 것:

- **3단계 응답 축** — `reraise`·`potodds`·`range_read`·`bluffcatch_*`·
  `hero_call`·`station`·`bluff_fear`. `cf_response.py` 의 구조를 재사용한다
- **4단계 실행 축** — `cbet_flop`/`barrel_turn`/`barrel_river`(스트리트 분리),
  `overbet`, `probe`, `delayed_cbet`, `equity_denial`, `stackoff`, `thin_value_*`
- **별도 harness** — `multiway`(n_opp ≥ 2 표본), `fold_equity`(`opp_est` 존재 +
  `w > 0` 표본, 잘 접는 상대/안 접는 상대 분리)

**이번 단계에서 코드는 고치지 않는다.** `max(0, bias)` 도, `potcontrol` 게이트도
전부 기록만 한다. 고칠지는 ③까지 보고 나서 정한다.

---

# 3단계 — 응답 축

같은 도구, `resist_flip` 층을 추가했다. 계획 라벨은 **레벨 5 프로필로 한 번만**
만들어 고정하고, 응답만 흔든다(`cf_response` 구조 유지).

출력에 셋을 더했다.

```
체감 need 중앙      행동이 안 바뀌어도 문턱은 움직인다. 그 둘을 갈라야
                    '축이 죽었다'와 '계수가 작다'를 구분할 수 있다
체감 need 사분위폭   중앙이 같아도 분산이 다르면 행동이 갈린다 (calc_noise 의 σ)
전환표              fold→call / call→fold / call→raise / raise→fold …
                    기준은 레벨 5. 폴드율 하나로는 질량이 어디서 왔는지 모른다
```

위약은 전 축·전 층 0.0000.

## 먼저 — `station`·`bluff_fear`·`hero_call` 은 축이 아니다

셋 다 `persona.bias` 가 기질·개념에서 **계산하는 파생값**이라 직접 설정할 수
없다. 그리고 둘은 서로 독립도 아니다.

```python
bluff_fear = _z(0.40*(10 - bluffcatch_river) + 0.30*(10 - aggression) + 0.30*(10 - range_read))
hero_call  = _z(0.45*bluffcatch_river + 0.35*aggression + 0.20*tilt_prone)
```

`bluffcatch_river` 와 `aggression` 을 **반대 부호로** 공유한다. 실측:

```
bluffcatch_river   1      3      5      7      9
  bluff_fear     0.554  0.394  0.234  0.074  -0.086
  hero_call     -0.559 -0.379 -0.199 -0.019   0.161      ← 거의 완전한 거울상
```

**하나를 올리면 다른 하나가 반드시 내려간다.** 그래서 이 셋은 축으로 재지 않고
`bias_resp` 층의 **중간값**으로만 기록한다.

## 결과

| 축 | 스트리트 | 폴드 1→9 | 체감 need | 질량이 어디로 (기준 lv5) | 판정 |
|---|---|---|---|---|---|
| `reraise` | flop | 52.0 → 46.8 | — | **레이즈만** 움직인다. 위: call→raise 8, fold→raise 5 / 아래: raise→fold 8, raise→call 8. **fold↔call 직접 전환 0건** | ● |
| `range_read` | flop | 45.6 → 60.8 | — | 위: call→fold 17 / 아래: fold→call 24 | ● |
| `range_read` | river | **55.6 → 78.8** | — | 위: **call→fold 38**, raise→fold 5, raise→call 10 / 아래: fold→call 20 | ● 매우 강함 |
| `potodds` | flop | 53.6 / 56.4 / **57.2** / 54.8 / 53.2 | 중앙 0.33→0.30 (단조↓)<br>사분위폭 0.19→0.12 (단조↓) | **양끝 모두 fold→call** (lv1 10건, lv9 12건) | **▲ ∩ 모양** |
| `bluffcatch_early` | flop | 55.6 → 52.8 | — | lv9 에서 fold→call 9 | ◐ 약함 |
| `bluffcatch_early` | **river** | **60.8 전 레벨 동일** | — | **전환 0건** | ○ 도메인 밖 (정상) |
| `bluffcatch_river` | flop | 59.2 → 55.2 (5·7·9 동일) | — | 아래만: call→fold 10 | ◐ |
| `bluffcatch_river` | **river** | 66.8 → 60.0 | **1→5 −0.05 / 5→9 −0.02** | 아래: call→fold 15 / 위: fold→call 3 | **◐ 아래쪽만 실질 작동** |

## 1. `reraise` 가 교과서적으로 깨끗하다

레이즈 7.6% → 19.2%. 전환표에 **`fold→call` 과 `call→fold` 가 한 건도 없다.**
질량이 오직 레이즈로/에서만 드나든다. 이름이 주장하는 것과 정확히 일치한다.

## 2. `range_read` 의 폴드 증가는 "콜에서 직접" 온다

사용자가 물은 구분 — "상대를 정확히 읽어서 접은 것인가, 다른 경로가 줄어든
결과인가". 리버 5→9 전환표가 답한다.

```
call→fold   38건      ← 주류 (전체 53건의 72%)
raise→fold   5건
raise→call  10건
```

**콜하던 자리를 직접 접는다.** 레이즈에서 빠져나오는 15건도 있지만 부차적이다.
폴드율만 봤으면 구분이 안 됐을 부분이다.

## 3. `potodds` 는 ∩ 모양이다 — 그리고 고정이 구조적으로 불가능하다

체감 need 는 **중앙·분산 모두 단조 감소**하는데 폴드는 레벨 5에서 최대다.
전환표를 보면 **양끝 모두 `fold→call`** 이다(lv1 10건, lv9 12건).
서로 다른 두 경로가 양끝에서 각각 콜을 늘리고 있다.

- 높은 쪽 — `calc_noise` 의 과대평가 편향(`1 + 0.30*(1−s/10)`)이 사라지고
  분산도 줄어든다. need 가 참값으로 수렴하며 내려간다
- 낮은 쪽 — 후보가 둘이다. 분산이 0.19 로 커서 need 가 낮게 나온 절반,
  그리고 `station` 편향(clamp 후 0.24 → 0)

**낮은 쪽의 두 후보는 이 설계에서 분리할 수 없다.** `station` 식이
`potodds` 를 −0.30 가중으로 직접 읽기 때문이다.

```python
station = _z(0.45*looseness + 0.35*gamble + 0.20*(10-discipline) - 0.30*(potodds-5))
```

`looseness`·`gamble`·`discipline` 은 고정했지만 **`potodds` 를 흔들면 `station`
이 필연적으로 움직인다.** 교락 축을 고정해서 해결되는 문제가 아니라 구조다.
분리하려면 `station` 을 죽인 변종이 필요한데 그건 코드 수정이므로 하지 않았다.

## 4. `bluffcatch_*` 의 도메인 분리가 정확하다 — 그리고 내가 또 틀렸다

**이번 4번째 도메인 오류다.** `bluffcatch_river` 를 **플랍에서** 쟀다.
이름 그대로 리버 축인데.

리버에서 다시 재니 `bluffcatch_early` 는 **전 레벨 완전 동일·전환 0건**으로
깨끗하게 꺼지고, `bluffcatch_river` 는 폴드가 66.8 → 60.0 으로 살아난다.
스트리트 분리가 설계대로 작동한다.

그런데 **위아래가 비대칭이다.** 리버 체감 need:

```
레벨      1      3      5      7      9
need   0.35   0.32   0.30   0.28   0.28
아래(1→5) -0.05          위(5→9) -0.02      ← 아래가 배 이상 강하다
```

원인은 `decide_response:827-832` 의 계수와 `max(0, ·)` 방향이다.

```python
need_seen *= 1.0 + 0.30*max(0.0, bias('bluff_fear'))*_bf_w    # 아래쪽 절반이 산다
need_seen *= max(0.60, 1.0 - 0.18*max(0.0, bias('hero_call'))*_bf_w)  # 위쪽 절반
```

`bluff_fear` 는 계수 0.30 에 **하위 절반**이 살아 있고, `hero_call` 은 계수
0.18 에 **상위 절반**이 살아 있다. 그래서 이 축은 **"낮으면 과도하게 접는다"는
아래쪽으로만 실질적으로 작동하고, "높으면 가볍게 받아준다"는 위쪽은 거의
전달되지 않는다.** 7 과 9 의 need 가 0.28 로 같아 위쪽에서 포화한다.

**계수는 건드리지 않았다.** 기록만 한다.

---

## 3단계에서 배운 것

- **전환표가 없으면 `range_read` 를 오독한다.** 폴드율만으로는 콜에서 온 건지
  레이즈에서 온 건지 모른다. 실제로는 72%가 콜에서 직접 왔다
- **중앙값만으로는 `potodds` 를 오독한다.** need 는 단조인데 행동은 ∩ 이다.
  분산을 같이 재야 보인다
- **교락 축 고정으로 해결 안 되는 교락이 있다.** `potodds`↔`station` 은
  후자가 전자를 직접 읽어서 구조적으로 얽혀 있다
- **스트리트 축은 그 스트리트에서 재야 한다.** `bluffcatch_river` 를 플랍에서
  재서 ◐ 로 잘못 볼 뻔했다 (4번째 도메인 오류)

## 남은 단계

- **4단계 실행 축** — `cbet_flop`/`barrel_turn`/`barrel_river`(스트리트 분리 필수),
  `overbet`, `probe`, `delayed_cbet`, `equity_denial`, `stackoff`, `thin_value_*`
- **별도 harness** — `multiway`(n_opp ≥ 2), `fold_equity`(`opp_est` + `w > 0`,
  잘 접는 상대/안 접는 상대 분리)

코드는 계속 고치지 않는다. 수정 후보 선정은 4단계까지 끝난 뒤에.

---

# 4단계 — 실행 축

요청대로 **실행 빈도와 실행 크기를 분리**했다(`noresist_size` 층).
스트리트 축은 각자의 스트리트에서 쟀다. 위약 전부 0.0000.

| 축 | 스트리트 | 빈도 | 크기 | 판정 |
|---|---|---|---|---|
| `cbet_flop` | flop | 벳 34.8% → 38.4% | 0.51 → 0.50 (불변) | ● 빈도만 |
| `barrel_turn` | turn | 26.8% → 28.0% | 0.61 → 0.60 | ◐ 약함 |
| `barrel_river` | river | 26.0% → 28.0% | 0.71 → 0.69 | ◐ 약함 |
| `thin_value_turn` | turn | 벳 22.0% → 26.4% | 불변 | ● 빈도만 |
| `overbet` | **river** | 발동확률 0.00% → **5.53%** | **1.27 → 1.42팟** | ● 빈도·크기 **둘 다** |
| `overbet` | flop | — | — | ○ `return None` (설계) |
| `equity_denial` | flop | 벳 불변 | `decide_size` 0.4677→0.4715 | ◐ 크기만, 희석 |
| `stackoff` | 커밋(SPR<1.2) | 레이즈 8.4% → 12.8% | — | □ 계단 (문턱 3.5) |
| `stackoff` | 계획 | 라벨 뒤집힘 0~2 / 250 | — | ◐ 도달 구간이 좁다 |
| `probe` | turn | 전 레벨 동일 | 동일 | ○ 라인 이력 필요 |
| `delayed_cbet` | turn | 전 레벨 동일 | 동일 | ○ 라인 이력 필요 |

## 1. `overbet` — 실현 빈도로 재면 ✕, 발동 확률로 재면 ●

처음에 플랍에서 재서 **0%** 가 나왔다. 코드를 보니 첫 줄이

```python
plan.py:1120   if street == 'flop': return None      # 플랍 오버벳은 다루지 않는다
```

**플랍은 도메인 밖이다.** 리버로 옮겨도 실현 발동이 102건 중 2건이었다.
게이트가 곱으로 셋이라 그렇다.

```python
p = 0.16*clamp((ob-2.5)/5) × (0.25 + 1.9*nut) × pol × (0.75+0.05*aggr) × 1.35(리버)
```

어느 게이트가 병목인지 쟀다.

```
pol (양극화)   중앙 1.000   pol<=0.02 로 탈락 1%      ← 병목 아님
nut (넛 우위)  중앙 0.005   nut<=0 가 50%             ← **병목**
통과분의 p     중앙 0.054   최대 0.259
```

그래서 상황마다 rng 를 20번 바꿔 **발동 확률 자체**를 추정했다.

```
overbet      1      3      5      7      9
발동 확률 %  0.00   0.44   2.11   4.82   5.53
발동 시 사이즈 —   1.27   1.31   1.36   1.42 팟
```

**빈도와 크기가 둘 다 단조 증가한다.** 실현 빈도만 봤으면 ✕ 무반응이었다.
곱 게이트가 여러 개인 축은 확률로 재야 한다 — `sizing_tell` 과 같은 교훈이다.

## 2. `equity_denial` — 크기만 바꾸고, 두 번 희석된다

빈도는 전혀 안 바꾼다(설계대로 — `decide_size` 에만 있다). 크기는 바꾸지만
중앙값이 0.4677 → 0.4715 로 **+0.8%** 뿐이다. 식의 명목치는 더 크다.

```python
plan.py:1009   base *= 1.0 + 0.55*(sk/10)*danger      # danger 중앙 0.2 → 명목 +7.5%
plan.py:1030   base = 0.45*base + 0.55*tex            # ← 뒤에 와서 45% 로 깎는다
```

보드 위험도로 나누면 방향은 맞다 — 젖은 보드에서 더 크다.

```
             마른(dg<0.2)   중간      젖은(dg>=0.4)
축 1           0.3976     0.4673      0.5071
축 9           0.4012     0.4713      0.5159      (+1.7%)
```

그리고 **두 번째 희석이 실행부에 있다.**

```python
plan.py:1317   amt = min(stack, int(round(pot*it['size']/100))*100)   # 100칩 반올림
```

축 1↔9 를 대조하면:

```
decide_size 출력이 이미 같은 경우      314 / 500 (62.8%)   ← danger ≈ 0 인 보드
100칩 반올림 뒤 금액이 같아지는 경우   369 / 500 (73.8%)
  → 반올림이 추가로 지운 비율                     11.0%p
```

**실제 칩 금액이 달라지는 것은 26.2% 의 상황뿐이다.** 죽은 축은 아니지만
중앙값으로는 안 보인다.

## 3. `stackoff` — 커밋 구간의 계단, 계획 층은 거의 도달 못 함

커밋 구간(SPR<1.2) 저항 대응에서 레이즈 8.4% → 12.8%. 수준별로 보면
**1·3 이 8.4, 5·7·9 가 12.8 로 계단**이다. 코드 그대로다.

```python
plan.py:738   if committed and so < 0.35: p *= 0.5     # so = sk/10 → 문턱 PS.sk 3.5
```

`committed` 가 아니면 이 줄이 아예 안 걸리고, 문턱 위에서는 기울기가 0 이다.
경로 분해도 같이 냈다 — 커밋 구간의 판정은 `포기/블러프계획` 59.2% ·
`팟오즈 산수` 22.4% · `밸류` 16.0% 순이고, 축이 바꾸는 것은 밸류 경로다.

계획 층(`_p2`, 2스트리트↔3스트리트 갈림)은 거의 안 보인다. 밸류 갈림 구간에
도달하는 것이 전체의 약 18% 이고, 그 안에서도 `rng.random() < _p2` 의 롤이
두 문턱(축 1 에서 ~0.30, 축 9 에서 ~0.13) 사이에 떨어져야 뒤집힌다.
별도 표본에서 44건 중 2건(4.5%), 전체 대비 0.8%.

## 4. `probe`·`delayed_cbet` 은 이 harness 로 잴 수 없다

둘 다 **라인 이력**을 요구한다.

```python
plan.py:874   if (street == 'turn' and initiative and has_c
                  and plan_state.get('flop_checked')):        # delayed_cbet
plan.py:913   if has_c and plan_state.get('opp_checked_prev'): # probe
```

단일 스트리트 상황 생성기는 `flop_checked`·`opp_checked_prev` 를 만들 수 없다.
전 레벨 동일하게 나온 것은 **축이 죽어서가 아니라 조건이 한 번도 성립하지
않아서**다. `multiway`·`fold_equity` 와 같은 분류로 넘긴다 — **멀티 스트리트
라인을 재생하는 별도 harness** 가 필요하다.

## 5. 방법론 — 계획 층도 집계 %가 아니라 전환 수를 세야 한다

`stackoff` 를 처음 쟀을 때 `value_2street` 9.60%, `value_3street` 8.80% 가
**전 레벨 완전히 동일**했다. 그런데 별도 표본에서 상황별로 세니 2건이
`value_2street → value_3street` 로 뒤집혀 있었다.

한쪽으로 간 건수와 반대쪽으로 온 건수가 우연히 맞으면 집계표는 안 움직인다.
`resist_flip` 과 같은 이유로 **계획 층에도 뒤집힘 카운터를 넣었다**
(`라벨 뒤집힘 (vs lv5)`).

---

# ② 전체 요약 — 축별 전달함수

측정이 끝난 축의 **전달 형태**다. "정상/비정상"이 아니라 모양으로 적는다.

| 형태 | 축 |
|---|---|
| **연속 ●** | `aggression`(층 5개), `looseness`(프리플랍), `range_read`, `reraise`, `cbet_flop`, `thin_value_turn`, `overbet`(확률·크기) |
| **계단 □** | `potcontrol`(3.33, 위로 기울기 0), `bluff`(3.33), `semibluff`(1.33), `stackoff`(3.5, 커밋 한정) |
| **clamp 로 절반 차단 ◐** | `looseness`(하위 절반), `discipline`(상위 절반) — `max(0, bias)` |
| **비대칭 전달 ◐** | `bluffcatch_river` — `bluff_fear` 0.30(하위) vs `hero_call` 0.18(상위), 위쪽 포화 |
| **구조적 교락 ▲** | `potodds` — `station` 이 `potodds` 를 직접 읽어 고정 불가, 행동이 ∩ |
| **희석 ◐** | `equity_denial` — 텍스처 블렌드 45% + 100칩 반올림 11.0%p |
| **약함 ◐** | `barrel_turn`·`barrel_river`(+1~2%p), `trap`(짝 축이 1.7배 무겁다) |
| **도달 불가 ○** | `sizing_tell`(실전 sz ≤ 1팟), `overbet@flop`(`return None`), `bluffcatch_early@river` |
| **harness 부족 ○** | `probe`·`delayed_cbet`(라인 이력), `multiway`(n_opp≥2), `fold_equity`(opp_est) |
| **축이 아님** | `station`·`bluff_fear`·`hero_call` — 파생값, 서로 거울상 |

**측정 중 총 다섯 번 틀렸고 전부 코드를 읽고 고쳤다.**
①에서 둘(`blocker` 레인지 구성, `pf_range` 교락),
②에서 셋(무저항 의도 미부착, 표 키를 첫 열에서만, `defend_thresholds` 의 `tot` 만),
3단계에서 하나(`bluffcatch_river` 를 플랍에서),
4단계에서 하나(`overbet` 을 플랍에서 + 실현 빈도로).

## 남은 것

**별도 harness 하나로 묶인다** — 전부 "상황의 구조"가 측정 조건인 축이다.

```
multiway        n_opp >= 2 표본
fold_equity     opp_est 존재 + w > 0, 잘 접는 상대 / 안 접는 상대 분리
probe           상대가 이전 스트리트를 체크백한 라인
delayed_cbet    내가 플랍을 체크백한 라인
```

앞의 둘은 상황 인자만 바꾸면 되고, 뒤의 둘은 **멀티 스트리트 라인 재생**이
필요하다. 그것까지 끝나면 수정 후보를 종합한다.
