# ⑩ 설계 — DEVIATE 내부 산술 위치

**초안이다. 아직 아무것도 측정하지 않았다.** 기준점 `fe33e42` (⑨ 종료).

---

## 0. 질문

⑧·⑨ 는 `DEVIATE` 분기를 고정한 뒤에도 축별 `S` 가 갈린다는 것을 보였다.
**왜 갈리는지는 답하지 않았다.**

> 분기를 하나 고정했을 때, 축마다 다른 `Δp` 가 **코드의 어느 산술 연산
> 위치에서 만들어지고, 그 뒤 어떻게 변형되어 최종 `p` 에 도달하는가?**

**원인을 선언하는 단계가 아니다.** 생성 지점과 전달 경로를 관측 가능한
형태로 분해하는 것까지다.

### 0-1. H3 와의 경계

```
H3 (⑥ 에서 기각)   계수·배율의 크기만으로 **전체 flip 서열**을 재현하는
                   설명 체계. 하나의 크기식으로 환원하려는 가설이다
⑩ (이번)           환원 가설이 이미 기각된 뒤, **한 분기 안에서** 축별 Δp 가
                   어느 연산에서 생기고 어떻게 전달되는지의 구조 탐색
```

**⑩ 에서 사후 결합식을 만들지 않는다.** `CF_RESULT_ORDER.md` 4절과
`CF_RESULT_PATH.md` 5절의 금지가 그대로 유효하다.

---

## 1. 코드에서 읽은 것 (측정 아님)

`decide_aggression` 의 `DEVIATE` 경로는 `plan.py:861-868`, 그 안에서
`cbet_freq` 는 `plan.py:1169-1211` 이다. 연산 순서는 이렇다.

```
S1   base = {flop 0.42, turn 0.30, river 0.22}[street]
S2   base *= clamp(0.45 + 0.11*_sc, 0.35, 1.85)          ← cbet_flop
S3   f = base + 0.035*a + 0.020*b                         ← aggression · bluff
S4   f *= (0.80 - 0.30*_mw) ** max(0, n_opp-1)
S5   f *= 1 + (_cm-1)*_bt ;  f *= 1 - 0.18*danger ;  oop → f *= 0.88
S6   f += 0.35*max(0, rel-0.6) ;  range_adv 배율
S7   f = blend(f, f*(1 + 1.10*street_gap), w)
S8   return max(0.03, min(0.95, f))
S9   cf *= max(0.05, 1.0 - 0.085*disc)                    ← discipline
S10  return max(0.0, min(0.9, cf))
```

`S1~S8` 은 `cbet_freq` 안, `S9~S10` 은 그 밖이다.

### 1-1. 네 축의 개입 종류가 다르다

```
축            위치   종류   식                          비고
cbet_flop     S2     배율   clamp(0.45 + 0.11*_sc)      base 에만 곱한다
aggression    S3     가산   +0.035*a
bluff         S3     가산   +0.020*b                    aggression 과 같은 줄
discipline    S9     배율   max(0.05, 1 - 0.085*disc)   cbet_freq 밖, 전체에 곱
```

### 1-2. 입력 폭은 가산 두 축에서 **같다**

`cf_axis.swap` 은 축을 `LO=1.0` / `HI=9.0` 으로 바꾸고 `PS.derive` 를 다시
돌린다. `derive` 는 `aggr = temper['aggression']`, `bluff = concepts['bluff']`
로 **항등**이다 (`persona.py:245-246`). 그래서 `a` 와 `b` 의 실제 입력 변화는
둘 다 정확히 `8.0` 이다.

**가산 두 축 사이에서는 "입력 폭이 달라서" 라는 설명이 코드상 불가능하다.**
이것은 ⑥ 의 H5-A(입력 sd)와 다른 양이다 — H5-A 는 필드 분포의 sd 였고,
여기서는 `lo→hi` 스윕의 실제 변화량이다. **H5-A 를 다시 열지 않는다.**

### 1-3. `cbet_flop` 은 플랍 전용이다 (코드 → 기존 자료로 확인)

`S2` 의 개념 이름은 `PS.street_concept('cbet', street)` 로 정해진다.
`flop → cbet_flop`, `turn → barrel_turn`, `river → barrel_river`
(`persona.py:306-316`). 그래서 `cbet_flop` 축을 흔들어도 턴·리버에서는
`_sc` 가 변하지 않는다.

`rows55c` 에서 확인했다 (새 시뮬레이션 없음, `Δp≠0` 의 스트리트 분포).

```
aggression   flop 2203   turn 1271   river 908
discipline   flop 2242   turn 1272   river 908
bluff        flop 2146   turn 1265   river 908
cbet_flop    flop 2232   turn    0   river   0
```

**`cbet_flop` 의 `R` 50.5% 는 축의 세기가 아니라 스트리트 게이트다.**
⑩ 에서 `cbet_flop` 을 다른 축과 비교할 때는 **플랍으로 조건을 맞춘다.**

---

## 2. 이미 본 값 — 예측으로 쓸 수 없다

```
DEVIATE 축별 S·T 와 그 비            ⑧ 4절 · ⑨ 3절
S 서열 discipline > cbet_flop > aggression > bluff   ⑨ 6절 (사후 관측)
```

그리고 **설계 중에 코드에서 유도한 값 하나를 여기 적어 둔다.**

```
가산 두 축의 생성 시점 Δ 비 = (0.035×8) / (0.020×8) = 1.750
관측된 S 비(aggression / bluff) = 0.10345 / 0.05929 = 1.745   (rows55c)
```

**이 대조는 이미 성립해 버렸다.** 설계 단계에서 코드를 읽다가 계산했고
비교 대상이 이미 공표된 값이다. 따라서 **"가산 축의 S 비가 계수 비와 같다"
를 ⑩ 의 사전등록 예측으로 삼지 않는다.** 배경 사실로만 적는다.

---

## 3. 측정할 양 — 아직 아무도 본 적이 없다

### 3-1. 정의 (측정 전에 잠근다)

```
x            축. lo = 1.0, hi = 9.0 (cf_axis.LO/HI)
생성 지점    축이 코드에서 처음 들어가는 연산
             aggression·bluff → S3 · cbet_flop → S2 · discipline → S9

g_x          생성 지점에서 그 축이 만드는 변화량
M_x          생성 지점부터 최종 p 까지의 **선형 응답 배율곱**
             (클램프를 제외한 경로 전체. aggression·bluff 는 S4~S7 과
              그 사람 자신의 discipline 배율 D 까지 포함한다)
Δp           최종 관측 변화 = p(hi) - p(lo)   ← rows 에 이미 있다

τ_x = Δp / (g_x × M_x)
```

**`τ` 는 초안(`|Δp|/|g|`)에서 바꿨다.** 하류 배율을 빼면 네 축이 전부
`τ < 1` 로 나와 비선형 경계를 분리하지 못한다. 잠근 정의는 위의 것이다.

```
τ = 1   생성 변화가 아핀 경로를 그대로 통과했다
τ < 1   클램프 등 비선형 경계가 일부를 삼켰다
```

### 3-2. 어떻게 꺼내는가 — **plan.py 를 재구현하지 않는다**

`S3` 이후는 `f` 에 대해 아핀이다 (배율·상수 가산·blend). 그래서 축을 작게
흔든 **유한차분**으로 선형 응답을 직접 잰다.

```
L_x = ( p(x + δ) - p(x) ) / δ          δ = 0.01, 축의 실제 값 주변에서
                                        양쪽 다 클램프 밖일 때만 유효
M_x = L_x / (dg_x/dx)

dg/dx   aggression  0.035
        bluff       0.020
        cbet_flop   base × 0.11              base 는 S1 상수 (스트리트로 정해진다)
        discipline  cf_S8 × (-0.085)         cf_S8 = cbet_freq 의 반환값
```

`cbet_freq` 를 **바깥에서 감싸** 호출 인자와 반환값을 기록하고, 같은 인자로
유한차분을 돌린다 (`tools/cf_axis.py` 가 이미 쓰는 방식). 본문을 베껴
계산하지 않는다 — 베끼면 원본과 어긋나는 별도의 오류원이 생긴다.

### 3-3. 클램프 발동 판정

```
S8   반환값이 0.03 또는 0.95 와 같다
S10  최종 p 가 0.0 또는 0.9 와 같다
S2   clamp(0.45 + 0.11*_sc, 0.35, 1.85) 의 경계에 닿았다
발동 = lo 끝점 또는 hi 끝점 중 하나라도 걸린 경우
```

---

## 4. 사전등록 예측 — **측정 전에 박는다**

각 항목은 ①수식 ②입력 ③미관측 예측값 ④판정 순이다.

```
A1  ① L_aggression / L_bluff = 0.035 / 0.020
    ② 같은 호출의 두 축 유한차분 L. 클램프 미발동 호출만
    ③ 1.750 — 호출마다 정확히. 집계 근사가 아니다
    ④ 위반 = |비 - 1.750| > 1e-9 인 호출. 위반 0 이면 충족

A2  ① τ_x = Δp / (g_x × M_x)
    ② g 는 3-2 의 dg/dx × 8, M 은 유한차분, Δp 는 rows
    ③ 네 축 전부 τ = 1. 클램프 미발동 호출에서
    ④ 위반 = |τ - 1| > 1e-9. 위반 0 이면 충족

A3  ① sd(g_x) over 호출
    ② 각 호출의 g
    ③ aggression·bluff 는 sd = 0 (상수 0.280 · 0.160)
       discipline 은 sd > 0 (cf_S8 에 비례하므로)
    ④ 예측대로면 충족. 가산 축에서 sd > 0 이 나오면 **틀린 것으로 적는다**

A4  ① C(sc) = clamp(0.45 + 0.11*sc, 0.35, 1.85),  sc ∈ [1, 9]
    ② 각 호출의 _sc (lo·hi 양 끝점)
    ③ C ∈ [0.56, 1.44] 이므로 S2 클램프 발동 0 건. g_cbet_flop 은 sd > 0
    ④ S2 발동이 1건이라도 있으면 틀린 것으로 적는다

A5  ① 클램프(S8 또는 S10) 발동 건수 / 그 축의 호출 수
    ② 3-3 의 판정
    ③ 네 축에서 서로 다르다
    ④ 네 값이 전부 같으면 틀린 것으로 적는다. 크기 순서는 예측하지 않는다

A6  ① 클램프 미발동 부분집합에서의 S 비 (aggression / bluff)
    ② 그 부분집합의 |Δp|
    ③ 1.750 ± 1e-3.
       **전체 표본의 1.745(⑨)와 계수 비 1.750 의 차이가 클램프 발동
       건에서만 온다는 주장이다**
    ④ 부분집합 비가 1.750 ± 1e-3 밖이면 **차이는 미설명으로 남긴다.**
       다른 원인을 사후에 찾아 붙이지 않는다
```

**새 문턱을 만들지 않았다.** `1e-9` 와 `1e-3` 은 부동소수 등가 판정이고
행동을 가르는 임계값이 아니다. `δ = 0.01` 은 유한차분 폭이다.

### 4-1. A1 이 깨질 수 있는 알려진 경로

`cf_axis.swap` 은 축을 바꾼 뒤 `PS.derive` 를 다시 돌린다. `derive` 의
`value` 가 `t['aggression'] <= 4` 를 보고, `goal` 이 `t['discipline'] <= 3.5`
를 본다 (`persona.py:251-252`). `cbet_freq` 는 이 둘을 읽지 않지만,
`aggression` 을 흔들면 `value` 가 `lead` ↔ `mixed` 로 바뀐다.

**그래서 `aggression` 과 `bluff` 의 하류가 완전히 같다는 보장이 없다.**
A1 이 깨지면 여기를 먼저 본다. 미리 적어 두는 이유는, 깨진 뒤에 원인을
찾아 붙이면 사후 설명이 되기 때문이다.

### 4-2. 조건 고정

```
cbet_flop 이 들어가는 비교는 flop 으로 조건을 맞춘다 (1-3)
분기는 DEVIATE 하나로 고정한다
D 팔, aligned 호출만 쓴다 — ⑧·⑨ 와 같다
```

---

## 5. 하지 않을 것

```
원인을 선언하지 않는다 — 생성 지점과 전달 경로의 관측까지다
사후 결합식을 만들지 않는다 (⑥ 4절 · ⑦ 5절)
H3 · H5-A 를 되살리지 않는다
plan.py 를 수정하지 않는다 — 계수·클램프·분기 순서 전부
새 문턱·새 plan 라벨을 만들지 않는다
⑧ · ⑨ 의 숫자를 고치지 않는다
S 서열을 설명하는 주장으로 확장하지 않는다 — ⑨ 6절이 사후 관측으로 봉인했다
DEVIATE 밖의 분기로 넓히지 않는다
```

---

## 6. 이 단계가 답하지 않는 것

```
축이 개념적으로 독립인가 — 여전히 답하지 않는다
축 서열의 원인 — ⑥·⑦ 에서 닫혔다
어느 계수가 '틀렸는가' — 수정 단계의 질문이고 지금은 원인 규명 단계다
```

---

## 7. 비용

`cbet_freq` 호출 인자를 기록하려면 **새 실행이 필요하다.** 유한차분은
같은 실행 안에서 즉시 수행한다. 규모는 ⑨ 와 맞춘다 (시드 55개).


---

## 8. 측정 범위 축소 — **측정 전에 기록한다**

`457a7c3` 의 사전등록(2~4절)은 **한 글자도 고치지 않는다.** 여기서 줄이는
것은 *무엇을 실행으로 잴 것인가* 뿐이다. 결과를 보고 줄이는 것이 아니므로
사후 선택이 아니다.

### 8-1. A1~A4 를 해석적 확정으로 전환한다

실행으로 재지 않는다. **"코드가 그렇게 되어 있다" 와 "실행 결과가 그렇게
나온다" 를 섞지 않기 위해 analytically established 로 표기한다.**

```
A1  analytically established
    0.035 / 0.020 = 1.750.  derive 가 항등이라 두 축의 입력 변화는 둘 다 8.0
A2  analytically established
    S3 이후가 f 에 대해 아핀이므로 클램프 밖에서 τ = 1 은 항등식이다
    — 아핀 함수가 아핀인지를 실행으로 확인하는 것은 동어반복이다
A3  analytically established
    가산항의 계수는 상수, discipline 만 그 시점 크기에 비례하는 배율이다
A4  analytically established
    sc ∈ [1, 9] → C(sc) = 0.45 + 0.11*sc ∈ [0.56, 1.44] ⊂ [0.35, 1.85]
    → S2 클램프는 정의역 안에서 도달 불가능하다
```

### 8-2. 4-1 은 코드로 닫혔다 (실행 불필요)

`profile['value']` 를 읽는 곳은 셋뿐이다.

```
plan.py:240      trap_judgment — 계획층이다 (M 팔). D 팔 DEVIATE 와 무관
plan.py:942-943  **밸류 계획 분기 안**이다
```

`DEVIATE` 경로는 `plan.py:857-868` 에서 `return` 하므로 `942` 에 **도달하지
않는다.** 따라서 `aggression` 스윕이 `derive` 의 `value` 를 뒤집어도 `DEVIATE`
분기의 하류를 오염시키지 못한다.

**A1 의 알려진 위험 경로는 이 분기에서 성립하지 않는다.** 4-1 을 실행
측정 대상에서 뺀다. 이것도 analytically established 다.

### 8-3. 실행으로 재는 것은 A5 · A6 둘뿐이다

```
A5  네 축의 클램프 발동률          조건: flop (4-2)
A6  클램프 미발동 집합의 S 비      1.750 ± 1e-3
```

**A5 의 사전등록 문구를 유지한다** — "네 축에서 서로 다르다, 네 값이 전부
같으면 틀린 것으로 적는다". 귀무가설 형태로 뒤집지 않는다. 그리고 판정은
발동률 네 값의 동일 여부뿐이고, **유의성 기준을 새로 만들지 않는다.**
크기 순서도 예측하지 않는다 (4절 A5 ④).

### 8-4. A6 의 "클램프 미발동 부분집합" 을 읽는 방식 (측정 전 고정)

한 호출에서 `aggression` 스윕은 클램프에 걸리는데 `bluff` 스윕은 안 걸릴
수 있다. 비가 **호출마다 정확히** 1.750 이려면 **두 축 모두 미발동인
교집합**이어야 한다. 그것으로 채점한다.

축별 부분집합으로 잰 값도 **참고로 같이 적되 채점하지 않는다.**

### 8-5. 표본

```
55시드 → 5시드.  A1~A4 가 빠져 정밀도를 요구하는 항목이 A6 하나뿐이고,
A6 의 예측은 통계량이 아니라 **정확값(1.750)** 이다
```

### 8-6. ⑩ 종료 후

메타 조사를 닫는다. `CLAUDE.md` 의 **현재 조사 / 아직 안 본 것** 으로
복귀하고, `eq`(미래 포함) ↔ `rel`(현재만) 모순부터 본다.
