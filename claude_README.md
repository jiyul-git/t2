# t2 — 포커 토너먼트 시뮬레이터

## 실행 경로 (중요)

핸드를 실제로 진행하는 코드는 **`session.HandRun._run()` 하나뿐**이다.
`play.Hand` 는 핸드의 상태(좌석·포지션·홀카드·장부)만 들고 있는 그릇이다.

    tourney.Tournament / live.py / live2.py   ← 드라이버 (대회 상태 소유)
              ↓ play.Hand(...) 로 그릇 생성
              ↓ session.HandRun(h) 로 진행
        preflop.py → plan.py → bot.py         ← 판단

`legacy/` 는 더 이상 쓰지 않는 코드의 보관소다. import 하지 말 것.

## 단일 출처 규칙 (사본을 만들지 말 것)

과거에 같은 계산이 두 곳에 복사돼 조용히 갈라진 사고가 반복됐다.
아래 항목은 정의가 하나뿐이며, 필요하면 그 함수를 호출한다.

| 대상 | 유일한 정의 |
|---|---|
| 포지션 순서 | `table.SEAT_ORDER` / `PRE_ORDER` / `POST_ORDER` |
| 디펜스 역치 (tp, tot) | `preflop.defend_thresholds()` |
| 성향·오픈폭 조회 | `preflop._tr()` / `preflop._open()` (dict·라벨 모두 수용) |
| 베팅 레인지 | `bot.betting_range()` |
| 대회 실력 수준 | `field.field_quality()` |

특히 `defend_thresholds` 는 **봇의 실제 판단(`defend_decision`)** 과
**상대 레인지 모델(`ranges.preflop_range`)** 이 같이 쓴다.
여기가 갈라지면 봇이 자기 자신을 잘못 모델링한다.

## 상태 수명 규칙

리딩 장부(`reads.Book`)는 **그것을 소유한 대회 안에서만** 산다.

- `tourney.Tournament.book` — 메모리, 대회 1개
- `live.py` / `live2.py` — 대회 상태 파일 안의 `st['book']` 키

전역 `book.json` 은 폐기했다. 그 파일은 (1) 대회끼리 관찰을 섞고
(2) 같은 시드를 디스크 상태에 따라 다르게 재생시켜 핸드 씰링을 무의미하게 만들었다.
`reads.load_book/save_book` 은 경로를 반드시 명시해야 한다.

## 리팩터 전후 안전망

    python3 tools/regress.py save    # 기준선 저장
    python3 tools/regress.py check   # 동작이 안 바뀌었는지 대조

고정 시드로 전 액션을 이어붙여 SHA-256 지문을 낸다.
**동작을 안 바꾸는 정리**라면 전 시드 지문이 같아야 한다.
의도적으로 동작을 바꿨다면 그때만 `save` 로 기준선을 갱신한다.

지문이 실행마다 흔들린다면 그건 리팩터 문제가 아니라
어딘가에 프로세스 밖 상태가 새고 있다는 뜻이다. (전역 book.json 이 그랬다)

## 대회별 필드 성향

`Tournament(entries=, buyin_level=)` 이 이 대회의 성격을 결정한다.

- `field_q` — 규모·바이인이 클수록 study·exp 평균이 높다 (하드 필드)
- `aggr_bias` — `Field.aggression` 에서 나온다. `persona.make_player` 의
  공격 기질 잠재요인을 통째로 이동시키며, `looseness`·`gamble` 이 거기 적재돼 있어
  참여율이 파생된다. **오픈/디펜스 역치 상수를 직접 건드려 수치를 맞추지 말 것.**

`field_descriptor()` 로 현재 대회 성격을 확인할 수 있다.

## 상대 레인지 축소 (액션 경로 반영)

`ranges.narrow_by_actions(base, board, acts, profile)` 가
관측된 포스트플랍 액션마다 남을 수 있는 콤보만 남긴다.
`session.py` 의 상대 레인지 구성이 이걸 통과한다.

- 벳/레이즈 → 양극화. 사이즈가 클수록 밸류가 좁고 블러프 비중이 커진다
- 콜 → 최상위 일부(레이즈했을 것)와 최하위(폴드했을 것)를 잘라낸 가운데 구간
- 체크 → 최상위가 얇아진다. c-bet 성향이 높을수록 더 깎인다

`_MIN_KEEP` 아래로는 줄이지 않는다. 표본이 죽으면 추정 자체가 무의미해진다.

**드로우 주의.** 강도 기준은 `bot._sd_strength` 이며 완성 강도에 드로우 지분을 얹는다.
made 강도만 쓰면 플랍의 강한 드로우가 최하위로 밀려 축소 때 통째로 지워진다.
`bot.draw_strength` 가 그 유일한 아웃 계산이고 `plan.draw_strength` 는 여기에 위임한다.

## 에쿼티 계산

몬테카를로 루프는 `bot.equity_vs_pools()` **하나뿐**이다.
(예전에는 `equity_vs_range` 와 `equity_vs_betting` 이 같은 루프를 따로 갖고 있었다.)

진입점 3개가 전부 여기로 온다:

| 진입점 | 입력 | 쓰는 곳 |
|---|---|---|
| `equity_vs_range` | 레인지 비율 | 추정 레인지가 없을 때의 후퇴 경로 |
| `equity_vs_combos` | 실제 콤보 리스트 | **기본 경로** — 좁혀진 레인지를 그대로 쓴다 |
| `equity_vs_betting` | 비율+블러프축 | 프리플랍 등 보드 정보가 얕을 때 |

`plan._eq_vs()` 가 선택한다. `opp_range` 가 20콤보 이상이면 `equity_vs_combos`,
아니면 비율 근사로 물러난다. **고정 `[0.35]*n_opp` 로 되돌리지 말 것** —
액션으로 좁혀놓은 레인지를 버리고 EV 를 판단하면 리딩 전체가 무의미해진다.

`act_with_plan` 의 콜/폴드 판단은 `opp_est`(관찰 기반 상대 추정치)로 상대 레인지를
모델링한다. 여기에 `profile` 을 쓰면 **내 블러프 성향을 상대에게 투영**하게 된다.
`opp_est` 는 `session.py` 가 `reads.perceived_profile()` 로 만들어 넘긴다.

## 블러프 콤보 선택

`bot.pick_bluffs()` 하나만 쓴다 (`bot.betting_range` 와 `ranges._bet_range` 공용).

'가장 약한 콤보'를 고르면 안 된다. 그러면 같은 무승부라도
A♠5♠ 와 7♦5♣ 가 동등해지는데 실제로는 전혀 다르다. 기준은 셋이다:

1. 쇼다운 가치가 낮을 것 (후보를 아래쪽 절반으로 제한)
2. 다음 스트리트에 배럴을 이어갈 근거 (`draw_strength`)
3. 상대 넛 콤보를 막을 것 (상위 10% 콤보에 등장하는 카드 빈도로 가중)

## 블러프 비중은 사이즈에서 나온다

`bot.bluff_share(size_frac, street, bluff_axis)` — **상수로 두지 말 것.**

균형 잡힌 베터의 블러프 지분은 `s/(1+2s)` 다 (팟의 s 만큼 베팅할 때).
1/3팟 20%, 1/2팟 25%, 팟 33%, 1.5배 오버벳 37.5%.
여기에 개인 성향(`bluff_axis`, 5가 균형점)과 스트리트 계수를 곱한다.
이게 "사이즈가 레인지를 의미한다"의 구현이다.

## 축소 누적 주의

`ranges._DECAY = 0.75` — 연속 액션일수록 축소 강도를 낮춘다.

매번 '남은 것의 상위 X%'를 다시 취하면 3배럴에서 원본의 3%가 남는다.
그러면 상대가 항상 넛인 것처럼 보여 리버 폴드율이 90%를 넘는다.
같은 상대의 두 번째·세 번째 공격은 추가 정보량이 적다.
현재: 1벳 61% / 3배럴 27% / 체크-체크 86%.
`_MIN_FRAC = 0.10` 으로 원본의 10% 아래로는 줄이지 않는다.

## 팟오즈 — pot_live 를 쓸 것

봇 판단에는 `pot_live = pot_now + sum(r2.contrib.values())` 를 넘긴다.
`pot_now` 는 **스트리트 시작 시점** 팟이라 현재 스트리트에 들어온 칩이 빠져 있다.
그걸 넘기면 봇이 팟을 실제보다 작게 보고 팟오즈를 과대 요구한다
(= 전 스트리트 체계적 과잉 폴드). 히어로 화면은 원래부터 올바른 값을 썼다.

검증법: `need` 평균이 `s/(1+2s)` 에 가까워야 한다. s 는 팟 대비 콜 비용.

## 개념 벡터는 판단까지 살아서 가야 한다

`play.Hand.axes()` 는 프로필 **전체**를 넘긴다. 축 몇 개로 추리지 말 것.

예전에는 `aggr/gamble/bluff/value/tilt/icm` 6개 스칼라만 추려서 넘겼다.
그러면 `persona.py` 가 28개 개념으로 사람을 만들어도 포스트플랍은 6개 숫자만 보게 되고,
하위 코드의 `profile.get('concepts')` 분기가 **전부 죽는다**.
`calc_noise`·`sk`·`call_bias` 가 한 번도 실행되지 않는다.

증상: 프리플랍은 개인차가 나는데 포스트플랍만 다들 평균으로 수렴한다.
점검법: `PS.call_bias` 를 감싸서 호출 횟수를 세라. 0이면 벡터가 어디선가 잘린 것이다.

## 행동 편향 — 랜덤 오차와 다르다

`persona.bias(prof, name)` / `persona.call_bias(...)`

`error_rate` 는 '가끔 엉뚱한 짓을 한다'를 만든다. 그것만으로는 사람이 안 된다.
실제 사람의 실수는 무작위가 아니라 **편향**돼 있다.
드로우를 과대평가하는 사람은 플랍에서도 턴에서도 계속 과대평가한다.
블러프를 무서워하는 사람은 항상 같은 지점에서 접는다.

| 편향 | 의미 |
|---|---|
| `station` | 콜을 너무 넓게 (루즈·도박성↑, 규율·팟오즈↓) |
| `bluff_fear` | 큰 벳·후반 스트리트에서 과잉 폴드 |
| `draw_love` | 드로우 과대평가 (아웃 계산↓, 도박성↑) |
| `hero_call` | 상대를 블러프로 몰아 가볍게 콜 |
| `sticky` | 매몰비용 — 들어간 팟을 못 놓는다 |

**중립 검증**: 모든 축이 5인 프로필은 다섯 편향이 전부 정확히 0.0 이어야 한다.
계수 합이 안 맞으면 필드 전체가 한쪽으로 치우친다 (`hero_call` 이 실제로 그랬다).

`call_bias` 가 콜/폴드 문턱에 곱해진다. 이게 없으면 성향이 아무리 달라도
콜/폴드는 `eq >= need` 하나의 문턱으로 수렴한다.

**평균만 보지 말 것.** 평균이 맞아도 개인 편차가 없으면 필드가 죽은 것이다.
점검 지표는 평균이 아니라 개인별 표준편차다.

## 개념이 전부 살아있는지 점검

`persona.py` 는 28개 개념에 사람마다 다른 점수를 매긴다.
그런데 그 개념이 **행동에 연결되지 않으면 생성만 되고 버려지는 축**이다.
개인차를 만들 재료가 있는데 안 쓰는 셈.

점검법 — `PS.sk` 를 감싸 호출된 개념 이름을 세라.
480핸드 정도면 28개가 전부 나와야 한다. 안 나오는 게 있으면 배선이 빠진 것.

과거에 죽어 있던 것과 원인:

| 개념 | 원인 |
|---|---|
| `checkraise_*` | `s in r2.acted` 로 판정 — 풀레이즈가 나오면 `acted` 가 `{레이저}` 로 **초기화**되므로 이 조건은 절대 성립하지 않는다. `r2.log` 의 체크 기록으로 판정해야 한다 |
| `overbet` | 사이징 표에 1.0 을 넘는 값이 아예 없었다 (행동 자체가 부재) |
| `trap` | 트랩 선택이 `sk('checkraise')` 만 보고 `trap` 은 안 봤다 |
| `delayed_cbet` | 어느 스트리트에서 공격했는지 기록이 없었다 (`bet_streets` 추가) |
| `equity_denial`, `sizing_tell` | 참조 지점 자체가 없었다 |

## 오버벳 — 세 조건이 모두 필요

`plan.overbet_frac()`. 아무나 치는 게 아니다.

1. **개념 보유** — `sk('overbet') >= 4`. 낮으면 아예 선택지에 없다
2. **넛 우위** — `R.nut_advantage >= 0.10`. 우위 없이 치면 레이즈당하고 끝
3. **양극화** — 밸류 극단(`rel>=0.85`) 아니면 순수 블러프(`rel<=0.25`). 미들레인지 제외

플랍 트랩으로 상대 레인지에 약한 핸드를 남겨두면 리버 넛 우위가 커진다.
**'플랍 트랩 → 리버 오버벳'이 그래서 성립하는 라인이다.**
이를 위해 `make_plan` 이 `my_range` 를 `plan_state` 에 보존한다.
현재 발생률 약 1.5% (턴 1.6팟, 리버 1.8팟 관측).

## 이분법 금지

어떤 액션도 개념 임계값 통과 여부로 100% 갈려선 안 된다.
트랩 선택이 그랬다 — `sk('checkraise') >= 2` 면 항상 트랩, 아니면 절대 안 함.

지금은 확률로 낸다: 두 개념(`trap`+`checkraise`) 합, 공격성(높으면 그냥 친다),
SPR(딥할수록 함정 실익), 다인원·젖은 보드(체크가 위험) 를 곱해 최대 70%.
같은 사람도 같은 스팟에서 늘 같은 선택을 하지는 않는다.

## 자기 전략 vs 익스플로잇

```
[baseline] 내 패 + 내 개념 벡터        [exploit] 상대 추정치 + 리딩 도구
                        ↓
        w = persona.exploit_weight(prof, confidence, n)
        w = (이 사람이 익스플로잇하는 타입인가) × (데이터가 쌓였는가)
                        ↓
        판단 = PS.blend(baseline, exploit, w)
```

**두 확률을 혼동하지 말 것.**

- *개념을 가질 확률* — 사람을 만들 때 확정된다 (`sk('trap')` 이 7.2 인 사람과 2.1 인 사람).
- *지금 그 도구를 쓸지* — 확률이 아니라 **판단**이다. 상대 성향·스택·사이즈·보드·틸트를 본다.

트랩이 그 예다. `trap_judgment()` 는 `opp_bet_prob()`(상대가 체크에 벳해줄 확률)을
핵심 입력으로 쓴다. 패시브한 상대에게 체크하는 트랩은 무료 카드만 주는 최악의 수다.
남은 확률성은 '가끔 무작위로 트랩한다'가 아니라 실행 편차(집중력·일관성)다.

**초반에 자동으로 자기 전략이 되는 이유**: `confidence=0` 이면 `w=0` 이다.
별도 분기가 필요 없다. 그리고 데이터가 아무리 쌓여도 적응력이 낮은 사람은 계속 `w≈0` —
자기 패만 보고 치는 선수는 그게 정체성이지 버그가 아니다 (모집단의 약 6%).

**같은 구조를 모든 전략에 적용한다.** `cbet_freq` 가 두 번째 사례다:
관측된 상대 폴드율(`ftb`)이 모집단 평균보다 높으면 블러프 빈도를 올린다.

### 리딩 장부가 실제로 쌓이는지 점검

60핸드 돌린 뒤 `t.book.d` 의 카운터를 봐라. 과거에 전부 죽어 있었다:

| 증상 | 원인 |
|---|---|
| `showdowns` 가 항상 0 | `_pid` 가 `_run` 지역 람다라 `_finish` 에서 NameError → `except: pass` 가 삼킴 |
| `cbet_opp` 가 60핸드에 0.3회 | 기회 판정이 '로그의 첫 항목인가'였다. 공격자는 보통 포지션이 있어 마지막에 액션한다 |
| 표본이 전반적으로 부족 | 관찰자를 그 스트리트 **생존자**로만 셌다. 폴드한 사람도 다 보고 있다 |

`except Exception: pass` 로 관찰 실패를 삼키지 말 것. 리딩이 통째로 죽어도 아무도 모른다.

## 재현성 — 결정 시드는 상황에서 유도한다

핸드마다 `h.rng` 하나를 만들어 모든 결정이 나눠 쓰면 안 된다.
조건부로만 난수를 소비하는 함수가 **하나만 있어도** 소비 횟수가 어긋나고,
그 뒤 모든 결정의 난수가 통째로 밀려 같은 시드가 재현되지 않는다.
(`overbet_frac` 처럼 early return 하는 함수, 단축평가되는 `and` 조건 등)

`session.HandRun._dseed(seat, street, tag, extra)` 로 유도한다:

    crc32(핸드해시 | 좌석 | 스트리트 | 용도 | 로그길이)

이러면 '같은 핸드·같은 좌석·같은 시점 = 같은 난수'가 보장되고
다른 결정의 소비량과 완전히 무관해진다. **`h.rng.randrange()` 로
결정 시드를 뽑지 말 것.**

같은 이유로 고친 것들:

| 지점 | 문제 |
|---|---|
| `reads.estimate` | `rng = rng or random` — 전역 RNG 는 OS 엔트로피로 시드된다. 폴백을 없애고 rng 를 필수로 (없으면 예외) |
| `bot._EQ_CACHE` | 키에 seed 가 없어 '어느 호출이 먼저 캐시를 채웠는가'로 값이 달라졌다. 스팟 유도 seed 로 전환 |
| `plan._RS_CACHE` | 키가 `hash(c)` XOR — 프로세스마다 값이 다르고 XOR 이라 순서 무시로 충돌까지. crc32 + 정렬로 교체 |
| `equity_vs_combos` | `rng.choice(pool)` 이 리스트 순서에 의존. 레인지는 집합이지 수열이 아니므로 정렬해 순서 고정 |
| `session` 의 `opp_r`/`my_r` | 같은 이유로 `sorted(set(...))` 으로 순서 확정 |

**점검법**: 같은 시드로 60핸드를 별도 프로세스에서 5회 돌려 로그 해시를 비교하라.
하나라도 다르면 어딘가에 순서 의존이나 프로세스 밖 상태가 남아 있다.

### 전역 RNG 폴백 금지 (재현성 붕괴의 주범)

`rng = rng or random` / `(rng or random).random()` 관용구를 **절대 쓰지 말 것.**
전역 `random` 모듈은 OS 엔트로피로 시드되므로, 폴백이 한 번이라도 타면
그 순간부터 같은 시드가 재현되지 않는다. 그리고 조용히 깨져서 알아채기 어렵다.

실제로 두 곳에 있었고 둘 다 오래 숨어 있었다:

- `reads.estimate` — 상대 추정치에 매번 다른 노이즈가 붙었다
- `plan._allowed` — `semibluff→showdown` 강등이 실행마다 달라졌다.
  호출부 4곳이 **전부** rng 를 안 넘기고 있었다

둘 다 rng 를 필수 인자로 바꾸고 없으면 `ValueError` 를 던지게 했다.
폴백을 남겨두면 다음에 누가 안 넘기고 또 조용히 깨진다.

**점검 명령**:

    grep -rn "rng or random\|or random)\." *.py
    grep -rn "random\.\(random\|choice\|sample\|randrange\|shuffle\|uniform\|gauss\)" *.py \
      | grep -v "rng\.\|self\.rng"

두 번째 검색은 `(rng or random)` 형태를 놓친다 (`rng.` 필터에 걸러진다).
반드시 두 개를 같이 돌릴 것.

## 불변식 검사 (tools/invariants.py)

`regress.py` 와 역할이 다르다. 둘 다 필요하다.

| | 방식 | 잡는 것 | 못 잡는 것 |
|---|---|---|---|
| `regress` | 지문 비교 | 리팩터가 동작을 바꿨나 | **원래부터 틀린 것** |
| `invariants` | 속성 단언 | 처음부터 틀린 것 | 성능·전략 품질 |

**버그를 하나 잡을 때마다 invariants 에 항목을 영구 추가할 것.**
항목은 계속 늘어나는 게 정상이다. 그래야 같은 버그가 다시 안 나온다.

`ChaosHero` 로 히어로 자리에 극단 행동(팟의 20배 벳 등)을 하는 놈을 앉힌다.
버그는 정상 범위 밖 입력에서 나오는데, 봇끼리만 돌리면 그 입력이 영영 안 나온다.

    python3 tools/invariants.py [핸드수] [시드수]

## 실수는 부등호를 무시하지 않는다

`eq < need` 인데 콜하면 **버그다**. 실제로 두 곳에 있었다:

- 마지막 폴백에서 `err` 확률로 eq/need 를 안 보고 55% 코인을 던졌다
- semibluff 에서 `eq >= need - 0.06` 으로 부등호를 임의로 깎았다

실수와 조정은 **`need` 쪽에 넣어야 한다.**
그래야 (1) 모순이 안 생기고 (2) 왜 그렇게 판단했는지 기록에 남고
(3) 같은 사람이 같은 방향으로 일관되게 틀린다.
방향 없는 코인은 개성을 만들지 못한다 — `call_bias` 가 그 역할을 한다.
드로우의 내재 오즈도 부등호가 아니라 `need` 감소로 표현한다.

## 사이즈 오독 — 균형 공식의 정의역

`persona.size_read(prof, size_frac)`

`s/(1+2s)` 는 **양극화된 균형 베터**를 전제한 식이고 대략 0.25~2.0팟에서만 유효하다.
팟의 9배는 정의역 밖이며, 균형 레인지의 극단이 아니라
**넛 아니면 명백한 실수**라는 별개 범주다.

`sizing_tell` 이 높으면 "이건 균형이 아니다"를 인식해 2.0 쪽으로 되돌려 해석하고,
낮으면 사이즈 비례로 계속 외삽해 "크게 쳤으니 블러프도 많겠지"라고 오독한다.
**그 오독은 버그가 아니라 재현해야 할 현상이다.** (라이브에서 흔한 실수다)
9.4팟 → 잘 읽는 사람 2.0, 못 읽는 사람 9.4 그대로.

## 분산 추구 (variance_seek)

실력 열세를 **자각한** 사람이 쓰는 실제 전략.
프리플랍 올인은 포스트플랍이라는 스킬 개입 구간을 통째로 없애므로
상대가 아무리 잘해도 결과가 카드에 수렴한다. 못 이기니까 운으로 가는 것.

두 경로를 구분해 둔다:

- **전략적** — 자각한 실력 격차 × 자각 능력(attention·range_read) × 도박성.
  실력도 없고 자각도 없으면 그냥 평범하게 진다.
- **감정적** — 틸트. `discipline` 이 높으면 억제된다.

스택이 20bb 미만이면 어차피 쇼브 구간이라 '일부러 키울' 여지가 줄어든다(×0.45).
딥스택 오픈 쇼브는 정상 전략에 없는 행동이므로 이 경로에서만 나온다.
