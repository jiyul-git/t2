# ⑤ 반사실 개입 설계 (실험 명세)

`RESULT_AXIS.md` 의 연관을 인과로 승격시키는 실험이다.
**코드보다 명세를 먼저 고정한다.**

---

## 0. 질문

```
④ 관측    축 값이 높은 플레이어일수록 특정 행동을 더/덜 한다
⑤ 반사실  동일 플레이어·동일 시드·동일 상황에서 그 축만 1↔9 로 바꾸면
          행동이 실제로 달라지는가
```

**관측 ρ 가 큰 순서가 인과효과가 큰 순서라고 가정하지 않는다.**
`looseness` ρ +0.830 이 `cbet_flop` ρ +0.171 보다 개입 효과가 크다는
보장이 없다. 개입 결과(플립률·라벨 변경률)를 **별도로 측정**한다.

---

## 1. 개입 지점 — 왜 "결정 단위" 인가

축을 대회 시작 시점에 바꾸면 **게임 전체가 갈라진다.** 다른 핸드,
다른 보드, 다른 팟이 되어 "행동이 달라졌다"가 무의미해진다.

그래서 **결정 단위 반사실**을 쓴다. 기존 `cf_*` 계열과 같은 방식이다.

```
기준 궤적을 정상 실행한다 (개입 없음)
각 결정 지점에서
  ① 원본 프로필로 결정 함수를 부른다 → 기준 팔
  ② 축만 1 로 바꾼 사본으로 다시 부른다 → 개입 팔 A
  ③ 축만 9 로 바꾼 사본으로 다시 부른다 → 개입 팔 B
세 팔이 **같은 상황 입력**(보드·팟·레인지·스택)을 받는다
```

이러면 상황 불변이 **구조적으로 보장된다** — 상황이 결정 함수의
입력이고 세 팔이 같은 값을 받기 때문이다.

대신 **하류 전파는 못 본다.** 그 결정 하나의 효과만 격리한다.
이것이 이번 실험의 범위이고, 문서에 그렇게 적는다.

---

## 2. 공통 난수 (CRN) — 그리고 그것이 깨지는 곳

### 2-1. 원칙

같은 주사위 눈을 세 팔에 쓴다. 그래야 행동 플립이 **확률 변화 때문**이지
주사위가 달라서가 아니다.

```
f_기준 = 0.30,  f_개입 = 0.55,  roll = 0.42
  → 기준 체크 / 개입 벳.  플립은 f 차이가 원인이다
```

`make_plan` 은 `rng = random.Random(seed)` 를 내부에서 만든다
(`plan.py:265`). 같은 `seed` 로 두 번 부르면 난수열이 동일하다.

### 2-2. 깨지는 곳 — 단축 평가

**Python 의 `and` 가 단축 평가를 한다.** 게이트가 거짓이면
`rng.random()` 이 **호출되지 않는다.**

```python
plan.py:447   if sk('blockbet')   >= 1   and rng.random() < block_p:
plan.py:456   if sk('potcontrol') >= 1   and rng.random() < _pc_p:
plan.py:468   elif ... sk('semibluff') >= 0.4 and rng.random() < ...:
```

`potcontrol` 을 1↔9 로 바꾸면 게이트 통과 여부가 바뀌고 **난수 소비
횟수가 달라진다.** 그 시점 이후의 모든 난수가 어긋나므로, 변화가
축 때문인지 난수열이 밀려서인지 구분할 수 없다.

`sk()` 는 0~3 스케일이므로 게이트 `>= 1` 은 `PS.sk >= 3.33` 이다
(CLAUDE.md). 축을 1 로 두면 게이트 아래, 9 로 두면 위다 — **게이트
축은 반드시 단축 평가를 건드린다.**

### 2-3. 대응

```
난수 소비 횟수를 세는 래퍼를 rng 에 씌운다
세 팔의 소비 횟수를 기록한다
  같으면    CRN 유효. 플립을 축 효과로 읽는다
  다르면    CRN 무효. **그 축의 결과에 경고를 달고 별도 집계한다**
```

**소비 횟수가 같을 것이라고 가정하지 않는다 — 센다.**
`calc_noise` 는 프로필과 무관하게 `rng.gauss` 를 항상 한 번 부르므로
(`persona.py:362`) 정렬이 유지되지만, 그런 함수가 전부인지는 실측해야
안다.

게이트 축(`potcontrol`·`semibluff`·`blockbet`)은 CRN 이 깨질 것으로
**예상**하고, 그 축은 `decide_aggression` 반환 확률 `f` 비교만
쓴다(그 함수는 rng 를 소비하지 않는다).

---

## 3. 기록할 네 층

축마다 네 가지를 **분리해서** 기록한다.

```
① 개입          축 1→9 / 9→1. 원본 값과 두 극단을 모두 기록
② 계획 라벨 변화  make_plan / update_plan 반환 plan 의 전환 행렬
③ 최종 행동 변화  intent act (bet/check) 와 실현 action 의 전환 행렬
④ 불변량 위반    바뀌면 안 되는 것이 바뀌었는가
```

### 3-1. ② 와 ③ 은 다르다

계획이 바뀌어도 행동이 같을 수 있고(`value_2street` → `showdown` 둘 다
체크), 계획이 같아도 행동이 바뀔 수 있다(같은 `giveup` 인데 `f` 가
달라져 벳/체크가 갈림). **전환 행렬을 각각 낸다.**

### 3-2. ④ 불변량 — 축마다 다르다

**이것이 실험 오염을 잡는 관문이다.**

축을 바꿔도 **상황**은 그대로여야 한다. 그러나 축이 정당하게 바꾸는
중간값도 있다 — 그건 인과 경로이지 오염이 아니다.

| 축 | 바뀌어도 되는 것 (인과 경로) | **바뀌면 안 되는 것** |
|---|---|---|
| `looseness` | `call_bias` 계수, `open_pct`, `defend_thresholds` tot | eq · eq_current · outs_true · board · opp_range · pot · tocall · made |
| `discipline` | 이탈 지속벳 배수 `1−0.085·disc` | 위 전부 + rel · nut_adv · range_adv |
| `bluff` | `cbet_freq` 의 `+0.020·b`, 블러프 분기 `p`, `line_bluff_prior` | 위 전부 |
| `aggression` | 밸류 `0.30+0.058·a`, `pot_control` `0.18+0.035·a`, `defend_tp` | 위 전부 |
| `potcontrol` | `pot_control` 라우팅 | 위 전부 |
| `thin_value_turn` | 밸류 배수 `0.55+0.09·sk` | 위 전부 |
| `cbet_flop` | `cbet_freq` 배수 `0.45+0.11·sk` | 위 전부 |

**공통 불변량** — `eq` · `eq_current` · `outs_true` · `board` ·
`opp_range` 서명 · `pot` · `tocall` · `made` · `n_opp` · `oop` ·
`initiative`.

`rel` 은 주의가 필요하다. `perceived_rel` 이 `draw_love` ·
`overpair_love` 를 읽으므로(`plan.py`), **그 두 축을 흔들 때만** 변해도
된다. 나머지 축에서 `rel` 이 변하면 오염이다.

`outs` 는 `calc_noise(profile,'outs',rng)` 를 타므로 `outs` **개념 축**을
흔들 때만 변해도 된다. `outs_true` 는 언제나 불변이어야 한다.

### 3-3. 위반 시 처리

```
불변량이 하나라도 깨지면 그 축의 결과를 해석하지 않는다.
먼저 왜 깨졌는지 코드에서 찾는다.
```

④에서 `potcontrol` 을 분기 안 `f` 로 재서 부호가 뒤집혔던 것,
`thin_value` 를 천장 포화 구간까지 넣어 ρ 가 눌렸던 것과 같은 종류의
오류가 여기서도 나올 수 있다.

---

## 4. 대상 축과 순서

`RESULT_AXIS.md` 의 관측 ρ 순이 아니라 **우선순위**다.

```
1순위   looseness  (+0.830)   discipline (−0.748)
        bluff      (+0.663)   aggression (+0.728 → 통제 후 +0.338)
2순위   potcontrol (+0.466)   thin_value_turn (+0.460)
3순위   cbet_flop  (+0.171 → 통제 후 +0.263)
```

`aggression` 은 통제 후 값이 절반 이하로 줄었으므로 **개입 효과가
관측 ρ 보다 작을 것으로 예상**한다. 반대로 `cbet_flop` 은 통제 후
올랐으므로 **관측 ρ 보다 클 것으로 예상**한다.

이 예상을 **측정 전에 적어둔다.** 맞든 틀리든 그대로 보고한다.

---

## 5. 측정 조건

```
entries=24 · hpl=12 · start_stack=30000    ④와 동일
시드          ④와 같은 5000–5054 를 쓴다 (같은 궤적 위에서 개입)
개입 값        1 과 9 (축 정의역 0~10 의 실용 양끝)
              원본 값도 함께 기록해 "원본 → 1" 과 "원본 → 9" 를 분리
```

**④의 궤적을 그대로 쓰는 이유** — 같은 상황 분포에서 재야 ④의 ρ 와
⑤의 플립률을 나란히 놓을 수 있다.

---

## 6. 보고 형식

축마다:

```
축   결정 수   난수정렬   불변량위반 | 계획변경%   행동변경%  |  f 중앙 변화
                                    (전환 행렬은 별표)
```

그리고 **위약 축**(`_rand_A`)을 같은 파이프라인에 태운다.
엔진이 안 읽는 축이므로 **계획 변경 0%, 행동 변경 0%, `f` 변화 0** 이
나와야 한다. 0 이 아니면 개입 코드가 프로필 사본을 잘못 만든 것이다.

④의 위약은 통계적 바닥이었지만, **⑤의 위약은 결정론적이다** —
정확히 0 이어야 하고 표본 변동을 허용하지 않는다.

---

## 7. 이 실험이 말하지 않을 것

```
- 하류 전파는 못 본다. 한 결정의 효과만 격리한다
- 축을 1↔9 로 흔드는 것은 필드에 실재하는 범위를 넘을 수 있다
  (예: semibluff 게이트 아래는 필드의 2.1% 뿐)
  → 실제 필드 분포에서의 효과크기와 구분해서 적는다
- entries=24 / hpl=12 / stack=30000 조건에서의 결과다
```
