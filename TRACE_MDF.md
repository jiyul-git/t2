# 배선 추적 — `gto.mdf` (오픈 사이즈가 디펜스 폭에 미치는 보정)

읽기 전용이다. `gto.py`·`preflop.py` 를 고치지 않았다.
반사실은 `gto.mdf` 를 메모리에서만 갈아 끼운다(`defend_pct` 가 전역으로 조회).

재현: `python3 tools/cf_mdf.py --table`, `python3 tools/cf_mdf.py --n 3000`

---

## 결론 먼저

**표준 오픈 구간(≤3bb, 실측의 73%)에서는 정상이다.** 이론 MDF 와 **소수점
셋째 자리까지 같다**(방어율 차이 +0.0%p).

**6bb 에서 포화한다.** `_interp` 가 표 바깥을 마지막 값으로 고정하므로
6bb 오픈과 50bb 오픈이 **같은 보정**을 받는다. 그 결과 큰 오픈에 대해
디펜스 폭이 이론보다 크게 넓다.

| 구간 | 실측 비중 | 방어율 (현재) | 이론 MDF | 차이 |
|---|---|---|---|---|
| ≤3bb | 73% | 38.3% | 38.3% | **+0.0%p** |
| 3~6bb | 4% | 30.3% | 29.5% | +0.8%p |
| 6~12bb | 7% | 25.3% | 19.4% | +6.0%p |
| **>12bb** | **16%** | **31.0%** | **9.4%** | **+21.6%p** |

---

## 1. 전수 추적 — 사용처는 한 곳뿐

```
gto.py:153  def mdf(open_bb):  return _interp(_MDF, max(1.5, open_bb))
gto.py:171  base *= mdf(open_bb) / mdf(3.0)          ← 유일한 호출부
```

`grep -rn "mdf" --include=*.py .` 로 확인. `tools/` 를 빼면 정의 1곳,
호출 1곳이다. 흘러가는 경로:

```
gto.mdf
  └ gto.defend_pct ── gto.threebet_pct (= defend_pct × TB_SHARE)
        └ preflop.defend_thresholds  (tp, tot)
              ├ preflop.defend_decision     프리플랍 폴드/콜/3벳
              ├ preflop.calloff_cap         프리플랍 올인 대면
              └ ranges.preflop_range        상대 레인지 모델
```

**포스트플랍에는 없다.**

## 2. 수식과 의미

프리플랍에서 오프너는 X bb 를 걸어 블라인드 1.5bb 를 딴다.
블러프 손익분기 폴드율 = `X/(1.5+X)` 이므로

$$\mathrm{MDF}=\frac{1.5}{1.5+X}$$

| 오픈 X | 표 값 | 이론 MDF | 표/이론 | **표 3x대비** | **이론 3x대비** | 차이 |
|---|---|---|---|---|---|---|
| 2.0 | 0.720 | 0.429 | 1.68× | 1.286 | 1.286 | **−0.0%** |
| 2.5 | 0.620 | 0.375 | 1.65× | 1.107 | 1.125 | −1.6% |
| 3.0 | 0.560 | 0.333 | 1.68× | 1.000 | 1.000 | 0.0% |
| 4.0 | 0.480 | 0.273 | 1.76× | 0.857 | 0.818 | +4.8% |
| 6.0 | 0.380 | 0.200 | 1.90× | 0.679 | 0.600 | +13.1% |

**절대값은 이론 MDF 가 아니다**(1.65~1.90배). 그러나 코드가 쓰는 것은
`mdf(x)/mdf(3.0)` **비율뿐**이라 절대값은 상쇄된다. 비율로 보면
2.0~3.0bb 는 이론과 1.6% 이내로 맞고, 커질수록 이론보다 **덜 줄인다.**

큰 오픈을 덜 줄이는 것 자체는 변론의 여지가 있다 — 큰 오픈은 레인지도
좁아서 순수 MDF 가 과하게 조이게 만든다. 다만 **6bb 에서 완전히 멈추는
것**은 그 논리로도 설명되지 않는다.

### 포화

```
open  5.0bb → 비율 0.768
open  6.0bb → 비율 0.679
open 12.0bb → 비율 0.679      ← 같다
open 25.0bb → 비율 0.679      ← 같다
open  100bb → 비율 0.679      ← 같다
```

`_interp` 는 표 밖을 끝값으로 고정한다. 6bb 를 넘는 모든 오픈이
**6bb 오픈과 동일 취급**이다.

## 3. 팟오즈·`tocall`·`bf` 와의 관계

`mdf(open_bb)` 는 **오픈 사이즈만**의 함수다. `tocall`·`bf`·프로필을 받지
않는다. 하는 일은 **문턱을 옮기는 것이 아니라 레인지 폭을 곱하는 것**이다.

`bf` 는 `defend_pct` 에 인자로 존재하지 않는다. `calloff_cap` 에서
`cap /= max(1.0, bf)` 로 따로 걸린다.

## 4. 행동까지의 경로

`defend_pct` → `base_tot` → (성향 진폭 `acc`, 방향 `looseness`) → `tot`
→ `defend_decision` 의 `logit` 혼합 → 폴드/콜/3벳.
`TRACE_DEFEND.md` 1·4절과 같은 경로다.

## 5. 성향은 `mdf` 에 들어가지 않는다

`defend_pct` docstring: *"디펜스 기준 폭. **성향은 들어가지 않는다.**"*
프로필 인자가 없다. 확인 완료.

즉 **오픈 사이즈에 대한 반응은 전원이 동일하다.** 개념 1 인 사람과 10 인
사람이 2bb→4bb 에서 똑같이 폭을 줄인다. `acc`(`pf_defend`)는 기준선에서
**벗어나는 정도**만 조절하지 기준선 자체는 못 건드린다.
(`TRACE_DEFEND.md` 8절에서 이미 지적한 빈틈)

## 6~7. 반사실 — 실제 행동 변화

`tools/cf_mdf.py`, BB 디펜스 3,000회. 오픈 크기는 아카이브 실측 분포
(≤3bb 73%, >6bb 24%)에 맞췄다.

| 변종 | 폴드 | 콜 | 3벳 | 쇼브 |
|---|---|---|---|---|
| **M0 현재** | 64.1% | 31.1% | 2.6% | 2.2% |
| M1 `mdf` 제거(비율 1.0) | 64.1% | 31.1% | 2.4% | 2.3% |
| M2 이론 MDF | 68.0% | 27.5% | 2.6% | 1.9% |

M0 대비 행동이 바뀐 상황: **M1 4.6%, M2 4.2%**.

구간별 방어율:

| 구간 | n | M0 | M1 제거 | M2 이론 | M0−M2 |
|---|---|---|---|---|---|
| ≤3bb | 2184 | 38.3% | 35.4% | 38.3% | **+0.0%p** |
| 3~6bb | 122 | 30.3% | 32.8% | 29.5% | +0.8%p |
| 6~12bb | 217 | 25.3% | 31.8% | 19.4% | +6.0%p |
| >12bb | 477 | 31.0% | 40.7% | 9.4% | **+21.6%p** |

M2 로 행동이 바뀐 109/125 건이 **>12bb 구간**에 몰려 있다.

### 경로별 (6bb 초과 4,000회)

| 경로 | n | M0 | M2 | 차이 |
|---|---|---|---|---|
| 올인 대면 (`calloff_cap`) | 1062 | 34.7% | 7.7% | **+27.0%p** |
| 일반 (`defend_decision`) | 2938 | 27.1% | 12.0% | **+15.1%p** |

포화가 두 경로 모두에 전파된다.

**단, 올인 대면에서는 이론 MDF 가 옳은 기준이 아니다.** 포스트플랍이
없으므로 순수 에쿼티 대 팟오즈 문제이고, `calloff_cap` 이 별도 축소
(`CALLOFF_TIGHTEN`, ICM)를 따로 건다. 거기서는 "M2 가 정답"이라고
말할 수 없다 — 포화가 그 경로까지 흘러간다는 것까지가 확인된 것이다.

### 아카이브 실측

BB 가 직면한 오픈 156회 중 **38회(24%)가 6bb 초과**다.

```
h55  111.8bb   h27 68.8bb   h173 47.0bb   h208 46.9bb   h157 39.4bb
h31   36.5bb   h123 31.2bb  h140 26.1bb   h204 25.0bb   h190 24.8bb
```

`h31`(36.5bb, 내 스택 86bb)·`h123`(31.2/37)·`h140`(26.1/31)·`h204`(25/33)·
`h190`(24.8/30)은 **올인 대면이 아니라 일반 경로**로 갔다. 이론 비율
0.118~0.171 자리에 0.679 가 쓰였다 — **4~6배 넓다.**

## 8. `need` 경로와의 중복

**없다.** `mdf` 는 프리플랍 레인지 폭이고 `calldown_need`/`decide_response`
는 포스트플랍 콜 문턱이다. 같은 양을 두 번 세는 곳이 없다.
`bf` 도 `defend_pct` 에는 없다.

## 9. 이력

```
git log -L '/^_MDF/,/^def mdf/:gto.py'          → 8fadcb3
git log -L '/^def defend_pct/,/^def threebet_pct/:gto.py' → 8fadcb3
```

**둘 다 루트 커밋이다.** 표의 다섯 점(2.0/2.5/3.0/4.0/6.0)이 어디서
왔는지, 6bb 에서 끊은 근거가 무엇인지 기록이 없다.
`TRACE_DEFEND.md` 6절의 혼합층과 같은 부류다.

---

## 판정

| 항목 | 판정 |
|---|---|
| ≤3bb 구간 (실측 73%) | **정상.** 이론 MDF 와 소수점 셋째 자리까지 일치 |
| 4~6bb 구간 | 의도적 완화로 볼 수 있음 (+4.8 ~ +13.1%) |
| **6bb 초과 포화** | **구조적 문제.** 표 밖을 끝값으로 고정. 실측 24% 의 상황 |
| 행동 영향 | **전체 4.2%**, 그러나 >12bb 구간에서는 **방어율 +21.6%p** |
| 성향 연결 | **없음** (설계상 의도. 다만 오픈 사이즈 반응이 전원 동일) |
| `need` 경로와 중복 | **없음** |
| 근거 기록 | **없음** (루트 커밋) |

**"정상이냐 구조적 문제냐"는 구간에 따라 다르다.**
표준 오픈에서는 정상이고, 6bb 를 넘으면 보정이 멈춘다.
빈도는 낮지만(24%) 그 구간에서 틀리는 폭이 크다.

## 한계

- 이론 MDF(`1.5/(1.5+X)`)가 **모든 구간에서 옳은 기준이라고 주장하지
  않는다.** 특히 올인 대면과, 큰 오픈일수록 레인지가 좁아지는 효과는
  순수 MDF 가 담지 못한다. M2 는 "포화가 없으면 어떻게 되는가"의
  기준선이지 정답이 아니다.
- 합성 상황이다. 오픈 크기 분포는 아카이브에 맞췄지만 오프너 포지션과
  스택 분포는 균등에 가깝다.
- `defend_decision` 의 혼합층 누수(`TRACE_DEFEND.md` 4절, 5.5%p)가
  세 변종 모두에 똑같이 걸려 있다. 여기서 재는 것은 그 위의 차이다.
