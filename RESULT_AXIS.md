# ④ 본 측정 결과 — 성격축과 행동 개인차의 연관성

**사전에 정의한 7개 행동 연관 가설 모두가 본 측정에서 재현 가능한
방향성을 보였다.**

"7개 축이 검증됐다" 가 아니다. 정확한 진술은 위 문장까지다 —
이유는 6절에 적는다.

---

## 1. 실험 설계 (사전 등록, `ANALYSIS_PLAN.md`)

```
드라이버       fieldsim (seat_pid·table_id 정상. tourney 는 좌석이 곧 사람이라 못 쓴다)
측정조건       entries=24 · hpl=12 · start_stack=30000
시드           5000–5054 (55개)
플레이어        1,320명 (시드당 24)
축 값          무틸트 설계값 (prof['concepts']/['temper'])
결과변수        플레이어별 산술평균, 최소 3건
looseness      z(open_pct)·z(defend_tot) 평균. 표준화는 전체에서 한 번
점추정          전체 플레이어 Spearman ρ
CI             시드 클러스터 부트스트랩 10,000회 백분위
p              자유 순열 10,000회 (축 값이 플레이어에 iid 배정 → 정확한 귀무)
다중비교        Holm-Bonferroni α=0.05, **정확히 7개** 주가설
통제           잠재3(study·aggro·exp) / +동일식 성향축 / +상황, 셋 다 보고
최소 검출 효과   |ρ|=0.20 — **합격선이 아니라** 설계 기준 (power 90%)
```

**표본은 결과를 보기 전에 고정했고 늘리지 않았다.**

---

## 2. 결과표

```
축                결과변수        n        ρ   95% CI              Holm p   기각
looseness        PRE_LOOSE    1307   +0.830  [+0.812, +0.848]   0.0007    O
aggression       defend_tp    1307   +0.728  [+0.702, +0.751]   0.0007    O
discipline       cbet_dev      631   -0.748  [-0.781, -0.712]   0.0007    O
bluff            bluff_br      291   +0.663  [+0.585, +0.729]   0.0007    O
potcontrol       ROUTING      1284   +0.466  [+0.414, +0.514]   0.0007    O
thin_value_turn  value_r85     460   +0.460  [+0.385, +0.530]   0.0007    O
cbet_flop        cbet_flop_f   324   +0.171  [+0.074, +0.263]   0.0023    O
```

7/7 Holm 기각, 7/7 기대 방향.

보조 진단 — `looseness` 두 지표 상관 **+0.429** (n=1307).
사전 정의대로 평균을 유지했다.

---

## 3. 홀/짝 재현성

```
축                 ρ 홀      ρ 짝     차이    r(결과)    n
looseness        +0.631   +0.630   0.001    .976   1307
aggression       +0.713   +0.716   0.003    .912   1305
discipline       -0.757   -0.751   0.006    .871    459
bluff            +0.622   +0.607   0.015    .661    187
potcontrol       +0.437   +0.439   0.002    .690   1250
thin_value_turn  +0.399   +0.454   0.055    .664    279
cbet_flop        +0.211   +0.220   0.009    .806    182
```

최대 차이가 `thin_value_turn` 의 0.055 다.

### 위약과의 대비 (8시드)

```
축                        ρ      ρ 홀     ρ 짝
_rand_A  cbet_dev     +0.170   +0.229   +0.055
_rand_B  bluff_br     -0.054   -0.223   +0.003
_rand_B  value_r85    +0.105   +0.070   +0.161
_rand_A  ROUTING      -0.023   +0.027   -0.101
_rand_A  PRE_LOOSE    -0.024   +0.000   -0.004
tilt_stack bluff_br   +0.177   +0.295   +0.057   (순수 위약 아님)
tilt_swing cbet_dev   -0.023   -0.054   -0.129   (순수 위약 아님)
```

**위약은 한쪽 반쪽에서만 크다.** `_rand_A × cbet_dev` 가 홀 +0.229 /
짝 +0.055 다. 실제 축은 양쪽이 같다. 이 대비가 결과의 핵심 근거다.

위약 다섯 기준 전부 통과 (Holm 유의 0 / CI 0 미포함 0 / |ρ|≥0.4 0 /
양 반쪽 |ρ|≥0.2 방향일치 0).

**한계:** 위약은 파이프라인 검증 목적으로 **8시드**에서 돌렸다.
55시드였다면 위약 CI 가 약 2.6배 좁아진다. 따라서 바닥값이 본 측정과
같은 정밀도로 측정된 것은 아니다. 다만 본 효과(|ρ| 0.17~0.83)가
8시드 위약 범위 밖에 있고, 홀/짝 재현성 대비는 정밀도와 무관한
질적 차이다.

---

## 4. 통제 결과

```
축                단변량    잠재3     +성향축   +상황     변화
looseness        +0.830   +0.813   +0.813   +0.810   −2%
discipline       -0.748   -0.796   -0.808   -0.811   +8% (강해짐)
thin_value_turn  +0.460   +0.454   +0.471   +0.473   +3%
potcontrol       +0.466   +0.365   +0.365   +0.365   −22%
bluff            +0.663   +0.463   +0.506   +0.512   −23%
aggression       +0.728   +0.338   +0.338   +0.360   −51%
cbet_flop        +0.171   +0.097   +0.252   +0.263   **+54%**
```

통제 집합:
`잠재3` = study·aggro·exp ·
`+성향축` = 같은 식에 들어가는 축 ·
`+상황` = 플레이어별 평균 n_opp·OOP 비율·rel.
**같은 통제를 7축 전부에 걸었다** — `cbet_flop` 에만 걸면 그 축에만
유리한 처리가 된다.

---

## 5. 사전 해석과 실제 결과

사전에 적어둔 해석(측정 전 `ANALYSIS_PLAN.md`)과 대조한다.

### `aggression` — 사전 해석과 일치, 다만 0 이 아니다

사전 기록: *"편상관이 0 에 가깝게 나오면 '이 축이 사실상 잠재요인
`aggro` 를 측정한다' 이지 '행동을 못 만든다' 가 아니다."*

```
+0.728  →  +0.338   (−51%)   고유분산 0.107
```

절반 넘게 줄었다 — 잠재 성분이 크다는 사전 예측이 맞다.
**그러나 0 이 아니라 +0.338 이 남았다.** 고유 성분도 행동을 만든다.
`decide_aggression` 의 밸류 분기(`0.30+0.058·aggression`)와
`pot_control` 분기(`0.18+0.035·aggression`)에 직접 계수로 들어가는
전달 경로가 실측으로 확인됐다.

### `cbet_flop` — 사전에 적은 두 갈래 중 **두 번째**

사전 기록:
```
r 높음 + ρ 낮음 + 상황 통제 후 변화 없음  → 축이 판단을 안 움직인다
r 높음 + ρ 낮음 + 상황 통제 후 ρ 상승     → 상황 배정이 가리고 있었다
```

```
+0.171  →  잠재3 +0.097  →  +성향축 +0.252  →  +상황 +0.263
```

**통제하자 ρ 가 오른다(+54%).** `r`(결과) 0.806 으로 개인차는 안정적인데
단변량 ρ 가 낮았던 조합이 이렇게 풀렸다.

원인은 사전에 기록한 레버 폭 차이다.

```
cbet_flop   0.45 + 0.11·sk     ±1.5sd 에서 0.81~1.31   1.6배
discipline  1 − 0.085·disc     ±1.5sd 에서 0.80~0.28   2.9배
```

같은 분기에서 `discipline` 의 레버가 1.8배 넓어 단변량에서 묻혀 있었다.
**"약한 축" 이 아니라 "가려져 있던 축" 이다.**

### 나머지 다섯

| 축 | 판정 |
|---|---|
| `looseness` | 매우 강하고(+0.830) 통제에 거의 영향 없다(−2%). 고유 성분이 지배한다 |
| `discipline` | 통제 후 **오히려 강해진다**(−0.748 → −0.811). 잠재요인이 반대 방향으로 약간 상쇄하고 있었다 |
| `thin_value_turn` | 통제에 안정적(+3%). 정의 수정(`rel<0.85`)이 없었으면 +0.130 으로 보였을 축이다 |
| `potcontrol` | 잠재 영향 −22%. 라우팅 비율로 재야 잡히는 축이다 — 분기 안 `f` 로 재면 부호가 뒤집힌다 |
| `bluff` | 잠재 영향 −23% 로 상당하지만 통제 후에도 +0.512 를 유지한다 |

---

## 6. 주장의 범위

**이 결과가 말하는 것**

```
사전에 정의한 7개 행동 연관 가설 모두가 본 측정에서 재현 가능한
방향성을 보였다. 개인차는 홀/짝 반분에서 재현되고 위약에서는 재현되지
않으며, 잠재요인·동일식 성향축·상황을 통제한 뒤에도 방향이 유지된다.
```

**이 결과가 말하지 않는 것**

- **인과효과의 증명이 아니다.** 축과 행동의 연관을 관측한 것이지,
  축을 개입시켜 행동이 바뀌는 것을 본 것이 아니다. 시뮬레이터라
  반사실 개입이 가능하므로 **다음 단계**로 넘길 수 있다.
- **`|ρ|=0.20` 을 합격선으로 쓰지 않았다.** 사전 설계의 최소 검출
  효과이고, 관측된 ρ 를 그대로 보고했다.
- **"7개 축 모두가 실제 행동을 원인으로 만든다" 가 아니다.**
  행동 개인차와 **안정적인 연관성**을 보였다는 것까지다.
- 측정은 `entries=24 / hpl=12 / start_stack=30000` 조건에서다.
  다른 스택 깊이·필드 구성에서 같은 크기가 나온다는 보장은 없다.
- 위약 바닥은 8시드에서 세웠다 (3절 한계 참조).

**다음 단계로 가능한 것**

축 값을 직접 흔드는 **반사실 개입**(같은 시드·같은 상황에서 축만 1↔9)으로
연관을 인과로 승격시킬 수 있다. 이번 결과는 그 실험의 대상 축과
기대 크기를 정해준다.

---

## 부록 — 이 측정에 도달하기까지 수정한 설계 결함

전부 **본 데이터를 보기 전에** 고쳤다.

```
①  id(profile) 로 플레이어 식별        관측 0건. axes 가 매번 새 dict 를 만든다
②  ρ 와 r 을 다른 집단에서 계산         판정 규칙이 성립하지 않는 표
③  potcontrol 을 분기 안 f 로 측정      선택 효과를 전달로 오독 (부호가 반대로)
④  thin_value 를 천장 포화 구간까지     ρ +0.130 → +0.460 (rel<0.85 로 제한)
⑤  need 를 raw 로 측정                 상황분포가 개인차를 압도 → need_ratio
⑥  부트스트랩 축 표준화에 고정 상수     검출력이 축 sd 순서대로 어긋남
⑦  DEFF 를 within-cluster 예측변수에    과보정
⑧  노이즈 있는 함수에 이분 탐색          43시드가 실제로는 79~80% 경계
⑨  tilt 축을 순수 위약으로 사용          dynamics → tilted_view 간접 경로 존재
⑩  split-half r 을 재현성 지표로 사용    축과 무관한 값. 홀/짝 ρ 로 교체
⑪  seed_power 와 인벤토리 정의 불일치    eligible 163 vs 62
```

⑨⑩ 은 **위약 검증 자체가 잡았다.** ⑥⑦⑧ 은 이론식과 시뮬레이션을
교차검증했기 때문에 드러났다. ①⑪ 은 온전성 카운터와 정의 대조가 잡았다.
