# ⑧ 설계 — 축별 경로 분화를 **관측**한다

**설계 문서다. 아직 아무것도 측정하지 않았다.**
기준점 `e1f3370` (⑦ 종료).

---

## 0. 질문의 전환

⑥ 이 후보 여섯을, ⑦ 이 경로 단계 넷을 각각 기각했다. 남은 것은
**flip 의 발생이 축마다 같지 않다는 현상 자체**다.

```
기존   축 → ??? → flip
이번   축 → 어느 분기 선택이 달라지는가 → R/S/T 의 차이 → flip
```

**이번 단계는 원인 가설을 세우는 것이 아니라 경로가 갈라지는 지점을
관측할 수 있게 만드는 것이다.**

### 0-1. `R`·`S`·`T` 를 원인으로 읽지 않는다

⑦ 에서 나온 관측은 이렇다.

```
aggression   R 1위 / S·T 4위
discipline   정반대
B            다섯 중 셋이 1
```

**이것을 "aggression 은 R 이 커서 flip 이 많다" 로 읽지 않는다.**
현재 아는 것은 다음까지다.

```
축이 다르면 → R/S/T 의 분포가 다르게 나타나고 → flip 패턴도 다르다
```

`R`·`S`·`T` 가 **원인인지, 중간 표현인지, 결과와 함께 움직이는 부산물인지
아직 모른다.** 이 불확실성을 이번 설계에서 유지한다.

---

## 1. 무엇을 "경로" 로 볼 것인가

`rows55b` 에서 경로를 가리킬 수 있는 것은 셋이다.

```
plan_{arm}_{tag}    계획 라벨
src_{arm}_{tag}     intent 의 사유 문자열 = decide_aggression 이 탄 분기
src_O               O 팔의 같은 것
```

정규화는 ⑦ 과 같게 한다 — `re.sub(r'\(\d+%\)', '(N%)', src)`.
확률이 문자열에 박혀 있어 정규화 없이는 다른 분기로 세어진다.

### 1-1. D 팔에서는 `src_lo` vs `src_hi` 가 거의 퇴화할 것으로 보인다

**이것은 코드 구조에서 나오는 추론이고, 측정으로 확인해야 한다.**

D 팔에서 `plan` 라벨은 안 바뀐다(⑦·⑥ 실측 전 축 0). `decide_aggression`
은 `plan` 으로 분기하므로(`plan.py:857·871·…`) 양쪽 tag 가 **같은 분기**를
타고 `p` 만 달라진다. 그러면 정규화한 `src` 는 다음 경우에만 갈린다.

```
한쪽만 size == 0 이라 '사이즈 0 → 체크' 로 바뀐 경우
```

⑥ 에서 센 그 309건·111건이다. **D 팔의 `src` 전이만 보면 이미 아는 것을
다시 세는 것이 된다.** 그래서 D 팔에서는 전이가 아니라 **점유**를 본다.

### 1-2. M 팔에서는 실제 분기 전이가 일어난다

M 팔은 계획 라벨을 바꾼다(⑥ 실측: `potcontrol` M 에서
`showdown → pot_control` 198/198). 라벨이 바뀌면 `decide_aggression` 의
분기가 바뀐다. **경로 분화를 문자 그대로 볼 수 있는 곳은 M 팔이다.**

M 팔은 ⑦ 에서 채점에 쓰지 않았다. **이번에도 flip 서열 채점에 쓰지
않는다** — 관측 대상으로만 쓴다.

---

## 2. 관측량 — 세 개

### P1. 분기 점유 (D 팔)

> 축마다 `Δp ≠ 0` 이 **어느 분기에서** 일어나는가.

```
모집단   ⑦ 과 동일 (attached_O, 양쪽 tag 정렬·불변량 통과, D 팔)
분기     normalize(src_O)
관측량   축별로 분기 → 건수, 그리고 축 안에서의 비율
```

⑦ 의 `B` 는 **가짓수**였다. P1 은 **분포**다. 같은 자료의 다른 요약이고,
`B` 를 다시 채점하지 않는다.

### P2. 분기 내 비교 (D 팔)

> 같은 분기 안에서도 축끼리 다른가, 아니면 분기가 같으면 비슷한가.

```
분기 b, 축 ax 마다  R(ax, b) · S(ax, b) · T(ax, b)   (⑦ 2-0-3 정의 그대로)
비교는 **같은 b 안에서만** 한다
```

**이것이 이번 설계의 핵심 관측이다.** 두 가지 중 어느 쪽인지 가른다.

```
축이 분기를 넘어 고유한 성질을 갖는다      → 같은 b 안에서도 축끼리 다르다
축의 차이가 "어느 분기에 사는가" 의 차이다  → 같은 b 안에서는 축끼리 비슷하다
```

### P3. 분기 전이 (M 팔, 관측 전용)

> 축을 흔들었을 때 어느 계획·분기에서 어느 쪽으로 옮겨가는가.

```
plan 전이   plan_M_lo → plan_M_hi        전이 행렬
src  전이   normalize(src_M_lo) → normalize(src_M_hi)
```

**행렬을 그대로 싣는다.** 요약 통계로 압축하지 않는다 — ⑥ 에서
`potcontrol` M 198건이 전부 한 전이였던 것처럼, 행렬 자체가 정보다.

---

## 3. 사전 이분법 — 예측이 아니라 판별 틀

P2 의 결과를 **측정 전에** 두 갈래로 나눠둔다. 어느 쪽이 나오든 그대로
기록한다.

```
분화형   같은 분기 안에서도 축끼리 R/S/T 가 다르다
         → 축의 차이는 분기 점유만으로 환원되지 않는다

점유형   같은 분기 안에서는 축끼리 비슷하고, 축 간 차이는 분기 점유에서 온다
         → 다음 질문은 "왜 축마다 다른 분기에 사는가" 가 된다
```

**판별 기준은 이번 설계에서 정하지 않는다.** ⑦ 에서 비 2.0·1.5 같은
문턱을 미리 박았는데, P2 는 분기별 표본 크기가 크게 다를 것이라 지금
문턱을 정하면 근거 없는 숫자를 또 만드는 것이 된다 (작업 원칙 2).

**절차:** P1 을 먼저 내서 분기별 표본 크기를 보고, **그 다음 별도 커밋으로
판별 기준을 고정하고, 그 다음 P2 를 채점한다.** ⑦ 에서 쓴
`d6fae23 → 2b768af → 측정` 과 같은 순서다.

---

## 3-1. P2 판별 기준 — **P1 을 보고 고정한다. P2 결과는 아직 안 봤다**

설계 6절의 3 단계다. P1(`efde4cd`)의 표본 구조를 근거로 정한다.

### 3-1-1. 분모가 축마다 같다

`src_O` 는 O 팔의 값이라 **축에 의존하지 않는다.** 실측으로 확인했다 —
분기별 전체 쌍 수 `N_b` 가 다섯 축에서 전부 같고 합계가 31,176 으로
⑦ 의 D 팔 모집단과 일치한다.

```
분기                                   N_b
포기 계획 + 이니셔티브 없음 → 체크          9,785
밸류 계획 실행(N%)                      9,380
DEVIATE:포기 계획이나 지속벳(N%)           4,212
팟컨트롤 → 대부분 체크                     3,655
블러프 계획 실행(N%)                      1,817
사이즈 0 → 체크                           559
함정 계획 → 체크                           447
…                                    (나머지는 전부 245 이하)
                                합계 31,176
```

### 3-1-2. `R` 을 P2 채점에서 뺀다

분기 안에서 `R(ax, b) = |{Δp≠0, src_O=b}| / N_b` 인데 **`N_b` 가 축마다
같으므로 `R` 비는 P1 의 건수 비와 같다.** 즉 `R` 은 P1 이 이미 낸 정보이고
P2 에서 새로 얻는 것이 없다.

**그리고 이미 그 값을 알고 있다.** `DEVIATE` 에서
`discipline` 4,212/4,212 = 100% 이고 `cbet_flop` 2,198/4,212 = 52.2% 라
`R` 비가 1.916 이다. **값을 아는 통계량에 문턱을 정하면 기준을 답에 맞추는
것이 된다.**

```
P2 채점은 S · T 로만 한다. R 은 참고로 표에만 싣는다
```

### 3-1-3. 채점 분기

```
주 채점   DEVIATE:포기 계획이나 지속벳(N%)
          **세 축 이상이 겹치는 유일한 분기다** (4축: aggression ·
          discipline · bluff · cbet_flop). 최소 건수도 2,198 로 가장 크다

보조 보고 밸류 계획 실행 (2축) · 블러프 계획 실행 (2축) · 사이즈 0 → 체크 (2축)
          표에 싣되 **채점하지 않는다.** 2축 비교는 max/min 이 곧 두 값의
          비라 "축끼리 비슷한가" 를 가르는 힘이 약하다
```

**분기를 고르는 규칙은 "겹치는 축 수가 가장 많은 것" 이다.** 건수 문턱을
새로 만들지 않았다 (작업 원칙 2).

### 3-1-4. 문턱 — ⑦ 의 값을 재사용한다

```
S 비 = max S(ax, b) / min S(ax, b)   문턱 2.0
T 비 = max T(ax, b) / min T(ax, b)   문턱 1.5
```

**둘 다 ⑦ 에서 결과를 보기 전에 고정한 값이다**(`d6fae23`). 새 임계값을
발명하지 않는다. `S` 에 2.0, `T` 에 1.5 를 쓴 것도 ⑦ 과 같은 배정이다.

`S`·`T` 의 정의는 ⑦ 2-0-3 그대로이고, 분기 `b` 로 한정할 뿐이다.

```
S(ax, b) = mean(|Δp|)  given  Δp≠0 and src_O = b
T(ax, b) = |{act_lo≠act_hi, src_O=b}| / |{Δp≠0, src_O=b}|
```

### 3-1-5. 판정 — 세 갈래

```
점유형   S 비 < 2.0  그리고  T 비 < 1.5
         같은 분기 안에서는 축끼리 비슷하다.
         축 간 차이는 분기 점유에서 온다

분화형   S 비 >= 2.0  그리고  T 비 >= 1.5
         같은 분기 안에서도 축끼리 다르다.
         축의 차이가 분기 점유만으로 환원되지 않는다

혼합     그 외
         **둘 중 하나로 판정하지 않는다.** 어느 통계량이 갈리고 어느 쪽이
         안 갈리는지 그대로 기록한다
```

혼합이 나올 수 있다는 것을 **측정 전에** 적어둔다. ⑦ 에서 1차 지표가
퇴화했을 때 지표를 바꾸지 않았던 것과 같은 취급을 한다.

### 3-1-6. 이 기준을 정할 때 본 것과 안 본 것

```
봤다     P1 의 분기별 Δp≠0 건수 (efde4cd)
         분기별 전체 쌍 수 N_b — 축마다 같다는 것과 그 값
안 봤다   S(ax, b) · T(ax, b) — P2 의 채점 대상이다. 아직 계산하지 않았다
```

---

## 4. 자료

```
필요      plan_{D,M,T}_{lo,hi} · src_{D,M,T}_{lo,hi} · src_O · p_ · act_ ·
          attached_O · shift_ · inv_ · street · pid · seed · hand · table
있는가    rows55b 에 전부 있다 (⑦ 에서 확인)
```

**새 시뮬레이션이 필요 없다.**

---

## 5. 하지 않을 것

```
R/S/T 를 원인이라고 쓰지 않는다 (0-1)
⑥·⑦ 의 기각된 가설을 변형해 되살리지 않는다
G1~G4 를 조합한 식을 만들지 않는다
B(가짓수)를 다시 채점하지 않는다 — P1 은 분포이고 다른 질문이다
M 팔을 flip 서열 채점에 쓰지 않는다 — 관측 전용이다
P2 의 판별 기준을 P1 을 보기 전에 숫자로 박지 않는다
Level 1·2 와 ⑥·⑦ 의 숫자를 수정하지 않는다
```

---

## 6. 순서

```
1  이 설계를 커밋한다                          ← 기준 커밋
2  P1 · P3 를 낸다 (분포와 전이 행렬. 채점 없음)
3  P1 의 표본 크기를 보고 P2 판별 기준을 고정해 **별도 커밋**
4  P2 를 채점한다
5  결과 문서
```

**3 을 2 보다 먼저 할 수 없다.** 분기별 표본 크기를 모르는 상태에서 문턱을
정하면 근거 없는 숫자가 된다. **그리고 3 은 P2 결과를 보기 전에 한다.**

---

## 7. 이 단계가 답하지 않는 것

```
축 서열의 원인 — 이번에도 답하지 않는다
어느 단계가 가장 큰가 — ⑦ 에서 닫혔고 다시 열지 않는다
```

이번 단계의 산출물은 **"축이 어디서 갈라지는가" 의 관측**이다. 그것이
flip 차이를 만드는지는 **그다음 단계의 질문**이다.
