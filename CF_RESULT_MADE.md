# 반사실 결과 — `465` 의 `made >= 1` 술어 교체

설계 `CF_DESIGN_MADE.md`. **예측 D1~D6 을 고치지 않았다.**
`plan.py` 무수정. EV·손익은 재지 않았다.

```
자료   시드 7000-7007, 1,000핸드, make_plan 호출 1,308건
단위   make_plan 반환 시점의 라벨 (update_plan 통과 전)
개입   plan.py:465 한 줄의 술어만 치환. 458 불변. 세 팔은 서로 독립
```

---

## 1. 사전등록 예측 대조 — 여섯 개 전부 충족

```
D1  465 미도달 1,267건 중 라벨 불일치         세 팔 모두 0건        충족
D2  pcz 진입 207 / 465 도달 41                네 팔 모두 동일        충족
D3  giveup 감소량 = showdown 증가량, 그 외 라벨 불변
      ARM-S  -41 / +41     ARM-R  -33 / +33     ARM-D  -11 / +11   충족
D4  ARM-S 의 465 giveup = 0건                                       충족
D5  ARM-R 의 465 giveup > 0  →  8건                                 충족
D6  ARM-D 의 465 giveup > 0  →  30건                                충족
```

`D3` 이 셋 다 정확히 대칭이고 다른 라벨이 하나도 안 움직였다.
**개입이 설계한 한 줄 밖으로 새지 않았다는 직접 증거다.**

---

## 2. 라벨 분포

```
팔       bluff_2street  giveup  pot_control  semibluff  showdown  trap  value_2street  value_3street
O                  63     543          188         32       147    24            133           178
ARM-S              63     502          188         32       188    24            133           178
ARM-R              63     510          188         32       180    24            133           178
ARM-D              63     532          188         32       158    24            133           178
```

`465` 가 만드는 giveup 은 41건, 전체 giveup 543건의 **7.6%** 다.
`make_plan` 호출 1,308건 대비로는 3.1% 다.

---

## 3. `D4` 가 정확히 0 이다

`465` 에 도달한 41건 **전부**가 `has_sd`(`made >= 1 or eq >= 0.42+0.05*mw`)를
만족했다. 설계 4절이 예측대로다.

```
pcz - (0.42 + 0.05*mw) = 0.08 + 0.01*mw + w*0.30*sg
```

이 표본에서 `w*0.30*sg` 가 이 차를 음수로 만든 경우가 **한 건도 없었다.**
`CLAUDE.md` 가 적어 둔 구조적 관찰 — "진입 조건이 `eq >= pcz` 라 최종
`has_sd` 가 항상 참. 이 자리에서 giveup 은 구조적으로 나올 수 없어야
한다" — 이 표본에서 예외 없이 성립했다.

**다른 표본에서도 0 이라고 말하지 않는다.** `sg` 가 충분히 음수면 깨진다.

---

## 4. 잔존 건의 성격 — 두 팔이 다른 집단을 남긴다

```
ARM-R 잔존  8건   rel 중앙 0.24   outs 중앙 10   eq 중앙 0.569
ARM-D 잔존 30건   rel 중앙 0.63   outs 중앙  0   eq 중앙 0.581
```

`465` 도달 41건을 두 술어로 가르면:

```
                    outs >= 8    outs < 8
rel >= 0.42                 4          29
rel <  0.42                 7           1
```

### 4-1. **남김없이 덮지는 않는다 — 1건이 남는다**

`rel >= 0.42` 도 `outs >= 8` 도 아닌 것이 **1건** 있다. 두 술어의 합집합은
40/41 이다. **"rel 과 outs 가 상보적이다" 로 단정하지 않는다.**

그 1건은 `made 0` · `rel < 0.42` · `outs < 8` 인데 `eq >= pcz` 다.
`CLAUDE.md` 가 적어 둔 것과 방향이 같다 — `draw_strength` 는 두 장이
필요한 개선(백도어)을 0 으로 센다. **다만 표본 1건이라 여기서 결론을
내리지 않는다.**

전부를 덮은 것은 `ARM-S` 뿐이고, 그건 `eq` 가 진입 기준 자체이기 때문이다.

---

## 5. 이 결과가 말하는 것과 말하지 않는 것

```
말한다     465 의 made >= 1 이 걸러내는 41건은, 이 코드베이스에 이미 있는
           다른 계속 가치 술어로 보면 전부(ARM-S) 또는 대부분(ARM-R 33 ·
           ARM-D 11) 계속할 만한 핸드로 분류된다
말한다     개입이 라벨 하나만 바꾼다 — 다른 분기·다른 라벨은 불변이다

말하지 않는다   어느 팔이 옳은가. 우열을 가르지 않는다
말하지 않는다   showdown 이 giveup 보다 나은 행동인가 — EV 를 재지 않았다
말하지 않는다   pcz 개입과 합치면 어떻게 되는가 — 따로 켰다
말하지 않는다   rel 과 outs 가 상보적인가 — 1건이 남는다 (4-1)
말하지 않는다   최종 행동 — 단위는 make_plan 반환 라벨이고 파이프라인 통과 후가 아니다
```

---

## 6. 재현

```
python3 tools/cf_made.py --seeds 7000-7019 --hands 1000
```

2×2 는 첫 집계를 본 뒤 도구에 붙였다 (채점 아님). 재실행에서 `D1~D3`
출력은 첫 실행과 **완전히 동일**했다.
