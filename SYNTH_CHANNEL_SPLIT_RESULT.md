# SYNTH_CHANNEL_SPLIT — 결과

사전등록: `SYNTH_CHANNEL_SPLIT_PREREG.md` (+ Amendment C1)
브랜치: `claude/synth-channel-split`  ·  기준 커밋 `4c5fd0d`
도구: `tools/synth_channel_split.py` · `tools/aggr_info_path.py`

production 무수정. `qx_ev_fixture.py`·`tier5_axis_validate.py` 무수정.
`stat_read` 구현 없음. 공격 채널 코드 무수정 — 관찰만 했다.

---

## 판정: `CHANNEL_CANCELLATION_CONFIRMED`

**그리고 상쇄의 공격 쪽은 production 성질이 아니라 fixture 배선 결함이다.**

---

## 1. 재현 관문

```
G1  원본 synth_control 직접 호출     0.0607374408 / 0.0603176072
    분리 도구 pooled                 0.0607374408 / 0.0603176072   -> 부동소수점 일치
G2  기록(QX_EV_VALIDATION 1-4)       calc 0.06074  pipe 0.06032  diff +0.00042
    재현                             calc 0.06074  pipe 0.06032  diff +0.00042   -> 일치
```

둘 다 통과. 계산은 같고 묶는 방식만 다르다.

`G2` 는 처음에 실패했다. 원인은 기록이 아니라 내 사전등록이었다 —
표본 수를 `qx_ev_validate.py` 의 argparse 기본값 120 으로 적었는데 기록을
만든 실행은 **150** 이었다(기록 문서 1-4 절 제목이 "150쌍"). `150` 에서
네 수치와 **신뢰구간 두 경계까지 전부** 일치한다. `Amendment C1` 참조.
정정 전에 120 기준 분리 수치를 봤다는 사실도 `C1-4` 에 남겼다.

결정론성도 따로 확인했다 — 같은 프로세스에서 cold / 반복 /
`profile_row` 40개 선행의 세 경우 모두 `0.0608406442 / 0.0603131024` 로
부동소수점까지 동일했다. 숨은 전역 상태나 호출 순서 의존은 없다.

## 2. 채널 분리

`d = CALC_ONLY − PIPE_ONLY`, 양수면 PIPE(잘 읽는 쪽)가 낫다.
프로필 150, 부트스트랩 4000회.

| 채널 | 상태 | CALC_ONLY | PIPE_ONLY | d | 95%CI | |
|---|---:|---:|---:|---:|---|---|
| **수비 D** | 24 | 0.03323 | 0.02796 | **+0.00527** | [+0.00116, +0.00947] | 유의, FLOOR 통과 |
| **공격 A** | 48 | 0.07449 | 0.07649 | **−0.00200** | [−0.00214, −0.00186] | 유의, 부호 반대 |
| 합침 | 72 | 0.06074 | 0.06032 | +0.00042 | [−0.00095, +0.00184] | 유의하지 않음 |

**상쇄는 산술적으로 정확하다.**

```
(24 × (+0.00527) + 48 × (−0.00200)) / 72 = +0.000423
```

기록된 pooled `+0.00042` 와 일치한다. **공격 상태가 수비의 두 배(48 vs 24)라
가중치가 두 배**여서, 공격 쪽 효과가 절반 크기인데도 수비 이득을 거의 정확히
지운다.

`QX_EV_VALIDATION` 의 "가장 약한 고리"였던 null 은 **효과가 없어서가 아니라
반대 부호 둘을 한 평균에 넣어서** 생긴 것이다.

## 3. 공격 채널 경로 — 계측 결과

코드 독해로 끝내지 않고 `PS.sk` / `PS.read_opponent` 를 monkeypatch 해서
공격 상태 48개 × 프로필 40 × arm 2 를 실제로 셌다.

### 3-1. 상대 정보가 들어가는 지점은 정확히 둘이다

| 줄 | 도달 조건 | 무엇을 하나 |
|---|---|---|
| `plan.py:918` | `plan ∈ {bluff_2street, semibluff, river_bluff}` 이고 `to_act_behind < 2` | `p *= clamp(1 + fold_equity/10 · w · 2.2 · street_gap, 0.40, 1.80)` |
| `plan.py:1240` (`cbet_freq`) | `plan ∈ {giveup, showdown}` + 이니셔티브 | `adj = f·(1 + 1.10·street_gap)`, `f = blend(f, adj, w)` |

`qx_ev_fixture.bot_action` 은 공격 채널에서 `giveup` 과 `semibluff(outs=9)` 를
평균하므로 **두 지점이 모두 발동한다.** 실측이 그것을 확인한다.

```
CALC_ONLY   plan.py:918   호출 1920   w>0 인 것    0 (0.0%)
            plan.py:1240  호출 1920   w>0 인 것    0 (0.0%)
PIPE_ONLY   plan.py:918   호출 1920   w>0 인 것 1920 (100.0%)
            plan.py:1240  호출 1920   w>0 인 것 1920 (100.0%)
```

`CALC_ONLY` 는 파이프라인 축이 전부 LO 라 `use = 0` → `w = 0` 으로
**한 번도 조정하지 않는다.** `PIPE_ONLY` 는 100% 조정한다. arm 대비는
의도대로 작동하고 있다.

두 지점 모두 방향이 같다 — `street_gap`(상대가 모집단보다 잘 접는 정도)이
양수면 **더 친다**. 이것은 옳은 배선이다.

### 3-2. 그런데 장부와 EV 오라클이 서로 반대다

```
r(b_true, 장부의 fold_to_bet)       −1.0000      ← State.book(): ftb = 1 − b_true
r(b_true, EV 엔진의 폴드 확률)       +1.0000      ← ev_actions(): 0.18 + 0.60·b_true
────────────────────────────────────────────
r(arm 이 믿는 ftb, EV 폴드 확률)     −0.8983
r(fold_gap(turn), EV 폴드 확률)     −0.8983
```

`b_true` 별 실측:

| `b_true` | 장부 `ftb` | EV 오라클의 폴드 확률 | `fold_gap` | arm 의 벳 확률 |
|---:|---:|---:|---:|---:|
| 0.15 | 0.850 | 0.270 | +0.1717 | 0.526 |
| 0.35 | 0.650 | 0.390 | +0.0712 | 0.507 |
| 0.55 | 0.450 | 0.510 | −0.0293 | 0.487 |
| 0.75 | 0.250 | 0.630 | −0.1298 | 0.467 |

**상대가 실제로 더 잘 접을수록(0.27 → 0.63) 공개 장부는 덜 접는다고
기록하고(0.85 → 0.25), 정확히 읽는 봇은 덜 친다(0.526 → 0.467).**

원인은 `qx_ev_fixture.py` 의 두 줄이 같은 파라미터에 **반대 방향으로**
걸려 있기 때문이다.

```python
# State.book()   — 봇에게 주는 공개 기록
ftb = max(0.05, min(0.95, 1.0 - self.b_true))      # 블러프 많이 = 안 접는다

# State.ev_actions()  — 정답을 만드는 오라클
folds = self.f_bluff if is_bluff else self.f_value  # 0.78 / 0.18
#   P(fold) = b_true·0.78 + (1−b_true)·0.18          # 블러프 많이 = 잘 접는다
```

두 모델링 각각은 그 자체로 말이 된다. 실제 포커에서 "벳할 때 블러프를 많이
섞는 사람"과 "벳을 마주쳤을 때 잘 접는 사람"은 **다른 축**이고 반대일 필요도
없다. 문제는 이 fixture 가 둘을 **같은 스칼라 `b_true` 에 반대 단조로 묶어
놨다**는 것이다.

그래서 공격 채널은 **구성상 반(反)정보적**이다. 정확히 읽을수록 손해를 보는
것이 당연하고, 그건 봇 판단의 성질이 아니라 실험 장치의 성질이다.

### 3-3. 음성 대조 — 수비 채널은 멀쩡하다

이 결함이 fixture 전체의 것이면 이번 결론이 성립하지 않는다. 그래서
수비 채널에 같은 검사를 걸었다.

```
r(b_true, arm 이 믿는 bluff)       +0.8766
r(b_true, line_bluff_prior)        +0.8361
r(b_true, 오라클의 콜 우위)         +0.9530
r(line_bluff_prior, 오라클 콜 우위) +0.8406      <- 양수 = 배선이 맞다
```

수비에서는 봇이 읽는 신호와 오라클이 쓰는 값이 **같은 방향**이다.
**결함은 공격 채널 한정이다.**

## 4. 이전 결론 두 개를 다시 읽어야 한다

**결과 문서는 수정하지 않는다**(CLAUDE.md). 정정은 여기 적는다.

### 4-1. `QX_EV_VALIDATION` 의 공격 채널 해석

그 문서는 `QX_E` 의 공격 채널 상관이 fit −0.2477 / holdout −0.2403 인 것을
"**공격 계열은 EV 와 반대로 붙어 있다**"로 읽고, `QX_E` 의 F1·F2·F6 을
EV proxy 로 쓰지 말라고 했다.

**운용 결론은 그대로 유효하다** — 그 fixture 의 공격 채널로는 아무것도
재면 안 된다. 그러나 **이유가 다르다.**

```
그때의 읽기   봇의 공격 판단이 EV 와 반대 방향이다
지금의 확인   fixture 의 공개 기록과 오라클이 반대 방향이다.
             봇은 기록을 정확히 읽었을 뿐이다
```

즉 그 −0.24 는 **production 의 공격 읽기가 나쁘다는 증거가 아니다.**
공격 채널에 대해 우리는 지금 **아무 유효한 측정도 갖고 있지 않다.**

### 4-2. `READ_USE_SEPARATION` 의 후속 제안

그 문서 5절·6절이 "`stat_read` 는 수비 채널 기준으로 과녁을 잡아야 한다 —
공격 계열은 EV 와 반대로 붙어 있으므로"라고 썼다.

**권고는 유지되지만 근거가 바뀐다.** 공격 채널을 빼야 하는 이유는
"거기서 읽기가 해롭다"가 아니라 **"거기에는 아직 믿을 수 있는 측정이 없다"**
이다. 이 구분은 중요하다 — 전자는 설계를 제약하지만 후자는 측정을 요구한다.

그 문서의 **수비 채널 결과 자체는 영향받지 않는다.** §3-3 이 수비 배선이
정합함을 보였고, `READ_USE_SEPARATION` 의 `dR` 은 전부 수비 상태에서 나왔다.

## 5. 다음

사전등록 4절은 부호의 원인에 대해 "가설은 적되 이번 판에서 검정하지 않는다"
였다. **검정이 필요 없었다** — 원인이 fixture 두 줄의 구조이고, 그 구조는
새 측정 없이 함수 두 개를 읽고 평가하는 것만으로 확정된다. 그래서
가설이 아니라 확정 사실로 적었다. 새 fixture 를 만들지 않았다.

남은 순서는 이렇게 바뀐다.

1. **공격 채널 fixture 를 고쳐야 한다.** `ftb`(벳을 마주쳤을 때 접는 빈도)를
   `b_true`(벳할 때 블러프 비율)와 **독립 파라미터로 분리**하는 것이 최소
   수정이다. 그래야 "상대가 잘 접는가"를 읽는 능력과 "상대가 블러프를 많이
   치는가"를 읽는 능력이 서로 다른 축이 된다 — 실제 포커에서도 그렇다.
   이것은 `qx_ev_fixture.py` 수정이므로 **`QX_EV_VALIDATION` 의 재현이
   깨진다.** 새 파일로 분기하고 기존 fixture 는 동결해야 한다.
2. 고친 fixture 에서 공격 채널을 다시 재고, `PIPE_ONLY` vs `CALC_ONLY` 를
   채널별로 다시 본다.
3. 그 뒤에야 `stat_read` 설계 여부를 판단한다. 지금 시점의 상태는
   **수비 근거는 확보, 공격 근거는 미측정**이다.

`stat_read` 는 이번 판에서도 만들지 않았다.

## 6. 한계

1. **공격 채널을 고치지 않았다.** 사전등록 5절이 수정을 금지했다.
   결함을 확정했을 뿐이다.
2. **`f_bluff = 0.78` / `f_value = 0.18` 자체의 타당성은 검사하지 않았다.**
   이번 결론은 "두 값이 `b_true` 에 대해 장부와 반대 단조를 만든다"에만
   걸려 있고, 값 자체가 현실적인지는 별개 문제다.
3. **수비 채널 대조는 상관으로만 봤다.** 배선 방향이 맞다는 것이지
   크기 보정(calibration)이 맞다는 뜻은 아니다. `READ_USE_SEPARATION` 이
   기울기 0.41 을 이미 기록했다.
4. **프로필 150 은 `QX_EV_VALIDATION` 과 같은 집합이다.** 새 표본이 아니므로
   이번 분리는 재현이지 독립 검증이 아니다. 독립 검증이 목적이 아니었다 —
   "그 null 이 무엇이었나"를 묻는 것이었다.

## 7. 최종 확인

```
production 무수정      plan.py / persona.py / reads.py / session.py  diff 0
fixture 무수정         qx_ev_fixture.py / tier5_axis_validate.py     diff 0
regression 지문        tools/regress.py check -> 전 시드 지문 일치
새 concept             없음. c5 / V7 tier 경계 / baseline 무변경
공격 채널              관찰만. 코드 무수정
```
