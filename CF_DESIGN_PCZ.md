# 반사실 설계 — `pcz` 에 `_pen` 을 적용하면 무엇이 바뀌는가

**측정 전에 고정한다.** 기준점 `37372d0`.
근거: `TRACE_EQREL.md` · `TRACE_EQREL_COUNT.md`

```
잰다      어떤 핸드가 어느 분기로 옮겨가는가 (라벨 전이)
안 잰다   EV · 손익 · 우열 판정 · baseline 지문
```

`plan.py` 는 **수정하지 않는다.** `tools/cf_szseen.py` 와 같은 방식으로
소스 텍스트에서 `make_plan` 을 떼어내 패치하고 `PL.__dict__` 에서 exec 한다.

---

## 1. 질문

`rel` 페널티(`plan.py:381-394`)는 `v3`·`v2` 에만 들어가고 `pcz` 에는
들어가지 않는다. **`pcz` 에도 같은 페널티를 넣으면 어떤 핸드가 어느
분기로 옮겨가는가?**

---

## 2. 개입 — **새 값을 발명하지 않는다**

"pcz 의 계수" 라는 값은 코드에 없다. `0.30` 은 `v3` 의 것, `0.22` 는
`v2` 의 것이다. 하나를 고르면 그것이 발명이다 (작업 원칙 2).

**그래서 기존 쌍을 그대로 두 팔로 쓰고, 우열을 가르지 않는다.**

```
O       현재 코드                                  pcz 무변경
ARM-A   _pen >= 0 → pcz += 0.22*_pen               v2 의 쌍
        _pen <  0 → pcz += 0.08*_pen
ARM-B   _pen >= 0 → pcz += 0.30*_pen               v3 의 쌍
        _pen <  0 → pcz += 0.10*_pen
```

삽입 지점은 `plan.py:390-394` 의 `if _pen >= 0:` 블록 안, `v2` 갱신
바로 뒤다. 다른 줄은 건드리지 않는다.

### 2-1. **이 개입은 `made >= 1` 을 건드리지 않는다**

실측에서 `465` 로 온 36건 중 25건의 병목은 `rel` 이 아니라 `made` 였다
(`TRACE_EQREL_COUNT.md` 2절). `pcz` 를 올리는 것은 그 핸드들을 **분기에서
빼내는 것**이지 내부 심사를 고치는 것이 아니다.

```
이 실험이 답하는 것    그 핸드들이 pcz 에 안 들어가면 어디로 가는가
답하지 않는 것         내부 심사(rel AND made)를 고치면 어떻게 되는가
```

**둘은 다른 개입이다. 결과를 섞어 읽지 않는다.**

---

## 3. 대상 집합 — 고정 `eq` 구간을 쓰지 않는다

`[0.50, 0.88)` 은 헤즈업·무리딩 유도값이라 대상 집합의 정의가 될 수 없다
(`TRACE_EQREL.md` 3-1). **실제 코드 조건으로만 가른다.**

```
진입      make_plan 이 남긴 why 문자열에 '중간강도' 가 있는가
탈락      '중간강도이나 상대레인지 열세' 가 있는가
단위      (hash, seat, street) 의 idx == 0
필터      그 기록의 street 접두사가 붙은 why 줄만 (작업 원칙 5)
```

---

## 4. 난수 — 어긋나는 것이 정상이다

`make_plan` 은 `rng = random.Random(seed)` 를 쓰고 분기마다 소비량이
다르다. `pcz` 가 바뀌어 분기가 갈리면 **그 뒤 난수 실현이 달라진다.**

**이것은 측정 결함이 아니라 개입의 일부다.** 축 반사실(⑤~⑩)에서 쓰던
alignment 기준을 여기 적용하지 않는다.

대신 harness 건전성은 이렇게 본다.

```
분기가 안 갈린 호출에서 O 와 ARM 의 plan 라벨이 전부 같은가
  → 같아야 한다. 다르면 패치가 의도 밖을 건드린 것이다
```

---

## 5. 사전등록 예측 — 코드에서 유도한 것이다

`pcz` 가 올라가 빠져나온 핸드는 아래 `elif` 로 내려간다.

```
468   semibluff       outs >= 8 · 뒤 액션자 <= 1 · sk · 확률
490   bluff_2street   eq < 0.42 가 필요하다
502   else            has_sd = made >= 1 or eq >= 0.42 + 0.05*mw
```

```
C1  진입 건수가 줄어든다.  O >= ARM-A >= ARM-B
C2  465 탈락 건수가 줄어든다
C3  빠져나온 핸드의 다수가 502 로 가고 has_sd 가 참이라
    pot_control 또는 showdown 이 된다  →  **giveup 총량이 줄어든다**
C4  outs >= 8 인 핸드 중 일부가 468 semibluff 를 새로 받는다 (0 이 아니다)
C5  490 bluff_2street 로 가는 것은 거의 없다 —
    빠져나온 핸드는 eq 가 개입 전 pcz 이상이라 eq < 0.42 를 못 넘는다
```

**틀리면 그대로 틀렸다고 적는다. 예측을 고치지 않는다.**

---

## 6. 하지 않을 것

```
EV · 손익을 재지 않는다 — 다음 단계다
두 팔의 우열을 가르지 않는다 — 계수를 고르는 것은 수정 단계의 결정이다
plan.py 를 수정하지 않는다. baseline 지문도 재지 않는다
made >= 1 게이트를 같이 건드리지 않는다 (2-1)
새 plan 라벨을 만들지 않는다
빠져나온 핸드가 '더 나은' 계획을 받았다고 말하지 않는다
```

---

## 7. 재현

```
python3 tools/cf_pcz.py --in band1000.jsonl        (없으면 시드로 새로 수집)
```
