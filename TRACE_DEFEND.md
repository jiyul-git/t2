# 배선 추적 — BB 방어

읽기 전용이다. `preflop.py` 를 수정하지 않았다.
`preflop.defend_decision`(preflop.py:518-643) 과 그 위의
`defend_thresholds`(458-516), `gto.defend_pct`(gto.py:165) 를 읽었다.

재현:
```
python3 tools/cf_defend.py --gto
python3 tools/cf_defend.py --n 3000
```

---

## 결론 먼저

세 가지가 각각 다른 답이다.

1. **`pf_defend` 는 장식이 아니다.** 방향이 아니라 **진폭**이다. 설계대로다.
2. **기준 폭(`gto.defend_pct`)은 정상이다.** BB vs CO 2.5bb = 46.3%.
3. **문제는 마지막 혼합 층이다.** 문턱이 43.8% 인데 실제 방어는 38.3% 로,
   **5.5%p 가 샌다.** 혼합이 빈도를 보존하지 않는다.

1차 보고서의 "`pf_defend` 가 안 닿는다" 는 관측은 맞았지만 **해석이 틀렸다.**

---

## 1. `pf_defend` 는 방향이 아니라 진폭이다

`defend_thresholds`(preflop.py:480-489):

```python
acc    = 0.10 + 0.80*min(1.0, PS.sk(prof,'pf_defend')/8.0)   # 0.10 ~ 0.90
loose  = PS.temper(prof,'looseness', 5.0)
aggr   = PS.temper(prof,'aggression', 5.0)
d_call = clip((loose - 5.0)/4.0, -1, 1)
d_tb   = clip(((0.45*loose + 0.55*aggr) - 5.0)/4.0, -1, 1)

tot = base_tot * (1.0 + (1.0-acc)*d_call*0.95)
tp  = base_tp  * (1.0 + (1.0-acc)*d_tb *1.10)
```

`pf_defend` 는 **`(1.0 - acc)` 한 자리에만** 들어간다. 부호가 아니라 크기다.

```
pf_defend 높음  →  acc 높음  →  (1-acc) 작음  →  기준선에서 덜 벗어난다
pf_defend 낮음  →  (1-acc) 큼  →  기질(looseness)대로 크게 벗어난다
```

방향은 **`looseness` 가 정한다** (`d_call`). `aggression` 은 3벳 구간
(`d_tb`)에만, 그것도 `looseness` 와 45:55 로 섞여 들어간다.

주석이 그렇게 적어놨다 — *"크기 <- 개념, 방향 <- 기질"*.

**그래서 "pf_defend 가 높은데 방어율이 안 오른다"는 버그가 아니다.**
설계상 오를 이유가 없다. 오르는 것은 `looseness` 다.

---

## 2. 반사실 — 실제로 무엇이 방어율을 움직이는가

`tools/cf_defend.py`, 상황 1,500개, 축을 1.0 ↔ 9.0 으로.
위약 축(`tilt_swing`/`tilt_stack`) 바닥값 **0.0%**.

| 축 | 뒤집힘 | 방어율(1) | 방어율(9) | 차이 | 가장 흔한 전환 |
|---|---|---|---|---|---|
| **`looseness`** | 22.1% | 28.4% | 49.9% | **+21.5%p** | `fold → call` ×322 |
| `pf_defend` | 9.7% | 37.7% | 41.5% | +3.8%p | `fold → call` ×93 |
| `aggression` | 9.3% | 41.7% | 39.3% | **−2.5%p** | `call → 3bet` ×58 |
| `gamble` | 0.3% | — | — | 0.0%p | `3bet → shove` ×5 |
| `pf_range` | 0.0% | | | 0.0%p | |
| `potodds` | 0.0% | | | 0.0%p | |
| `discipline` | 0.0% | | | 0.0%p | |
| `icm` | 0.0% | | | 0.0%p | |
| 위약 2축 | 0.0% | | | 0.0%p | |

**아카이브 실측과 거의 정확히 일치한다** (REVIEW_2.md 3절, 통제 101회):

| | 반사실 | 아카이브 |
|---|---|---|
| `looseness` <4 → >6 | 28.4% → 49.9% | 30% → 50% |
| `pf_defend` <4 → >6 | 37.7% → 41.5% | 32% → 36% |

`aggression` 이 방어율을 **낮추는** 것도 설계대로다 — 콜을 3벳으로
바꾸는 축이라 참여 폭 자체는 오히려 조금 좁아진다.

`pf_range`·`potodds`·`discipline`·`icm` 은 **BB 방어 결정에 아예 없다.**
`defend_decision` 과 `defend_thresholds` 어디서도 읽지 않는다.

---

## 3. 기준 폭은 정상이다

`gto.defend_pct('BB', ...)` — 성향이 들어가지 않는 기준선:

| 오프너 | 2.0bb | 2.5bb | 3.0bb |
|---|---|---|---|
| UTG | 39.6% | 34.1% | 30.8% |
| HJ | 47.8% | 41.1% | 37.2% |
| CO | 53.7% | **46.3%** | 41.8% |
| BTN | 64.8% | 55.8% | 50.4% |
| SB | 77.1% | 66.4% | 60.0% |

교과서적으로 맞는 숫자다. `base = 0.22 + 0.68·rfi(오프너)` 에
MDF 비율(`mdf(2.5)/mdf(3.0)`)과 자리 배수(`DEF_SEAT['BB'] = 1.00`)를 곱한다.

**기준선을 의심할 이유가 없다.**

---

## 4. 진짜 문제 — 혼합 층이 빈도를 보존하지 않는다

합성 4,000회 (표준 오픈 2~3bb, 스택 20~120bb):

```
성향 반영 문턱 tot     평균 43.8%
실제 방어율            38.3%
차이                   5.5%p   ← 여기가 샌다

레인지 안(r <= tot)  1,705회 → 폴드   263 (15%)
레인지 밖(r >  tot)  2,295회 → 참여    89 ( 4%)
```

**비대칭이다.** 안쪽에서 15% 가 새는데 바깥쪽에서는 4% 밖에 안 들어온다.

원인은 `defend_decision` 끝의 혼합 코드(preflop.py:600-624):

```python
w_cont = logit(r, tot, max(0.02, (tot-tp)*0.35))
w_call = max(0.0, w_cont - w_raise*0.6) * (1.5 - 0.055*a)
w_fold = max(0.0, 1.0 - w_cont)
...
if r > tot*1.35: w_raise = 0.0        # 바깥쪽 3벳 차단
if r > tot:      w_call *= 0.15       # 바깥쪽 콜 85% 차단
```

- `logit` 은 `r = tot` 에서 0.5 인 **매끄러운** 곡선이다. 그래서 레인지
  안쪽 핸드도 `1 - w_cont` 만큼 접는다. 혼합 전략으로서는 정상이다.
- 그런데 **바깥쪽만 하드컷이 있다** (`*= 0.15`, `w_raise = 0`).
  안쪽 누수를 되받아 줄 장치가 없다.
- 결과적으로 `tot` 은 "이 폭까지 방어한다"가 아니라 "이 폭보다 5%p 쯤
  좁게 방어한다"가 된다.

혼합 자체가 문제가 아니다. **혼합이 빈도를 보존하지 않는 것**이 문제다.

### 아카이브와 대조

표준 오픈·스택 20bb 이상 101회:

```
성향 반영 문턱 tot     중앙 44.3%
실제 방어율            35%
차이                   9%p

레인지 안 50회 → 폴드 16 (32%)
레인지 밖 51회 → 참여  1 ( 2%)
```

방향은 합성과 같다. 안쪽 폴드율이 32% 로 합성(15%)보다 크지만
**n=50 이라 신뢰구간이 넓다.** 실전에는 콜러·읽기·스택 분포가 더
섞여 있으므로 이 차이를 확정하지 말 것. 합성 쪽(5.5%p)이 통제된 값이다.

---

## 5. 그래서 1차 보고서를 다시 정리하면

| 1차 보고서 | 지금 |
|---|---|
| "BB 방어 28%" | 큰 오픈·짧은 스택 포함. 표준 조건으로 좁히면 **35%** |
| "`pf_defend` 가 안 닿는다" | 관측은 맞다. 다만 **설계상 닿을 이유가 없다** — 진폭 축이다 |
| "프리플랍 기술 개념이 방어에 안 닿는다" | `pf_range`·`potodds` 는 **정말로 없다** (0.0%) |
| "`looseness` 하나만 닿는다" | 맞다. 그게 방향 축이다 (+21.5%p) |
| (없었음) | **혼합 층이 문턱보다 5.5%p 좁게 방어한다** ← 새 발견 |

---

## 6. 아직 안 본 것

- **혼합 층을 고쳐야 하는가**는 아직 판단하지 않았다. 5.5%p 가
  의도된 보수성일 수도 있다. 도입 커밋의 근거를 찾아봐야 한다.
- `pf_range`·`potodds` 가 방어에 없는 것이 설계인지 누락인지.
  `potodds` 는 `plan.calldown_need`(포스트플랍)에서는 쓰인다.
- 앞서 "팟오즈를 15%p 이상 넘겼는데 폴드한 53건" — 그중 **레인지 안이었던
  것은 24%(16/66)** 다. 나머지 76% 는 봇의 자기 레인지 밖이라
  **모델상 일관된 폴드**다. 즉 그 53건의 상당수는 혼합 층이 아니라
  **`tot` 이 정한 레인지 폭 자체의 문제**로 봐야 한다.
- 포스트플랍 `decide_response` 는 아직 미추적.
