# 변수 사전

★ = 오늘(코드 재개 이후) 추가. 나머지는 이전부터 있던 것.
개념의 `base`/`spread` 는 **전부 잠정값** (`LOADING_PROVISIONAL = True`).

---

## 0. 잠재 요인 3종 — 사람을 만들 때만

`prof['latent']` 에 저장. 판단 시점에는 읽지 않는다.

| 변수 | 뜻 |
|---|---|
| `study` | 공부량. 이론·차트·솔버 |
| `aggro` | 공격 기질 |
| `exp` | 경험량. 친 판 수 |

이 셋에서 개념 30개와 기질 9개가 파생된다.

---

## 1. 기질 9종 — 성격. 방향을 정한다

```
값 = gauss(중심, 편차) + 잠재요인 보정
```

| 변수 | 산정식 | 해석 |
|---|---|---|
| `aggression` | `aggro + gauss(0, 0.8)` | 공격성. 거의 aggro 그대로 |
| `looseness` | `gauss(5.0+loose_bias, 2.2) + 0.25(aggro−4.6) − 0.30(study−4.5)` | 참여 폭. **공부가 좁힌다** |
| `gamble` | `gauss(5.0+0.6·loose_bias, 2.4) − 0.35(study−4.5) + 0.25(aggro−4.6)` | 도박성. **공부가 깎는다** |
| `discipline` | `gauss(5.0, 2.2) + 0.35(study−4.5) + 0.20(exp−4.5)` | 안 무너지는 정도 |
| `tilt_prone` | `gauss(4.8, 2.6) − 0.20(exp−4.5)` | 틸트 나는 빈도 |
| `tilt_recovery` | `gauss(5.0, 2.2) + 0.25(exp−4.5)` | 틸트에서 회복 |
| `adaptability` | `gauss(4.6, 2.3) + 0.30(exp−4.5)` | 읽은 걸 실제로 쓰는 의지 |
| `consistency` | `gauss(5.2, 2.1) + 0.30(study−4.5) + 0.25(exp−4.5)` | 사이즈·라인 일관성 |
| `attention` | `gauss(5.0, 2.3) + 0.25(exp−4.5)` | 관찰력. 빈도를 세는 능력 |
| `tilt_swing` ★ | `gauss(5.0, 2.5)` | 틸트 시 성향 **강화(높음) / 반전(낮음)** |
| `tilt_stack` ★ | `gauss(5.0, 2.4)` | 틸트 반복 시 **누적(높음) / 감쇠(낮음)** |

`loose_bias` 는 필드 등급에서 온다. 싼 대회일수록 +.

---

## 2. 개념 30종 — 능력. 크기를 정한다

```
값 = base + 0.90·ws·(study−5) + 0.85·wa·(aggro−5) + 0.85·we·(exp−5) + gauss(0, spread)
```

- `base` 낮을수록 **어렵다** (필드 평균이 낮다)
- `spread` 클수록 **사람마다 갈린다** (외웠나 아닌가로 나뉘는 개념)
- 둘은 다른 축이다. 어려운데 편차가 작을 수 있다 (거의 전원이 못하는 것)

| 개념 | base | spread | study/aggro/exp |
|---|---|---|---|
| `cbet_flop` | 5.4 | 1.05 | 0.35 / +0.45 / 0.30 |
| `bluffcatch_early` | 4.6 | 1.30 | 0.50 / +0.10 / 0.30 |
| `pf_range` ★ | 4.6 | 2.30 | 0.90 / +0.00 / 0.25 |
| `potcontrol` | 4.5 | 1.35 | 0.55 / -0.25 / 0.35 |
| `outs` | 4.5 | 1.20 | 0.75 / +0.00 / 0.30 |
| `semibluff` | 4.4 | 1.15 | 0.45 / +0.40 / 0.25 |
| `potodds` | 4.4 | 1.85 | 0.80 / -0.05 / 0.30 |
| `positional` ★ | 4.4 | 1.60 | 0.55 / +0.10 / 0.45 |
| `bluff` | 4.2 | 1.15 | 0.35 / +0.55 / 0.20 |
| `checkraise_flop` | 4.2 | 1.30 | 0.45 / +0.35 / 0.30 |
| `board_texture` | 4.2 | 1.25 | 0.70 / +0.05 / 0.35 |
| `barrel_turn` | 4.1 | 1.35 | 0.45 / +0.60 / 0.30 |
| `thin_value_turn` | 4.1 | 1.30 | 0.60 / +0.25 / 0.30 |
| `spr` | 4.0 | 2.00 | 0.85 / +0.05 / 0.25 |
| `range_read` | 4.0 | 1.90 | 0.85 / +0.10 / 0.35 |
| `sizing_tell` | 4.0 | 1.50 | 0.65 / +0.05 / 0.40 |
| `trap` | 3.8 | 1.50 | 0.25 / -0.10 / 0.40 |
| `stackoff` | 3.8 | 1.40 | 0.70 / +0.05 / 0.40 |
| `icm` | 3.8 | 2.10 | 0.80 / -0.15 / 0.30 |
| `blockbet` | 3.6 | 1.55 | 0.60 / +0.10 / 0.25 |
| `probe` | 3.6 | 1.55 | 0.55 / +0.35 / 0.25 |
| `equity_denial` | 3.5 | 1.55 | 0.70 / +0.30 / 0.30 |
| `blocker` | 3.5 | 2.20 | 0.90 / +0.10 / 0.20 |
| `delayed_cbet` | 3.4 | 1.60 | 0.60 / +0.25 / 0.35 |
| `reraise` | 3.4 | 1.70 | 0.55 / +0.50 / 0.35 |
| `bluffcatch_river` | 3.3 | 1.25 | 0.75 / +0.05 / 0.40 |
| `barrel_river` | 3.2 | 1.20 | 0.60 / +0.55 / 0.35 |
| `overbet` | 3.2 | 1.95 | 0.55 / +0.45 / 0.20 |
| `thin_value_river` | 3.0 | 1.10 | 0.80 / +0.20 / 0.40 |
| `checkraise_late` | 2.9 | 1.65 | 0.65 / +0.35 / 0.40 |
---

## 규약

| 종류 | 범위 | 기준값 | 예 |
|---|---|---|---|
| **능력** | 0 ~ 1 비율 | — | `pf_range` 정확도, `see_freq`, `positional` |
| **방향** | −1 ~ +1 | 0 | 루즈/타이트, `perceived_edge` |
| **상황 압박** | 1.0 기준 **배수** | 1.0 | `icm_press`, 예상 침식 |

**상황 압박을 0~1 로 두지 않는다.** 그러면 호출부마다 `max(0.15, ...)` 같은
하한을 따로 붙여야 하고, 평시에도 값이 남아 다른 항을 상쇄한다.
배수면 신호가 없을 때 1.0 이 되어 식에서 저절로 사라진다.

```python
icm_press(prof, bf) = 1.0                              # 평시
                    = 1 + 신호 × (0.15 + 0.85·icm/10)  # 버블
```

---

## 3. 파생 축 — 개념·기질을 조합해 만드는 값

계산 시점에 만들어지며 저장하지 않는다.

| 축 | 산정식 | 해석 |
|---|---|---|
| `overall_skill` | 계산 42% + 실행 38% + 규율 20% | 종합 실력 |
| `aware` | `(0.6·attention + 0.4·range_read)/10` | **자기 실력을 자각하는 능력** |
| `variance_seek` | 아래 참조 | 분산을 일부러 키우려는 정도 |
| `icm_signal(bf)` ★ | `(bf − 1)/3` | 버블팩터를 0~1 신호로 |
| `overpair_love` | `(10−potodds)`, `looseness` 등 | 오버페어 과대평가 |
| `bluff_fear` | `(10−bluffcatch_river)`, `(10−aggression)` 등 | 큰 벳에 과도하게 접음 |
| `draw_love` | `(10−outs)`, `gamble` 등 | 드로우 과대평가 |
| `hero_call` | `bluffcatch_river`, `aggression`, `tilt_prone` | 가볍게 콜 |
| `sticky` | `(10−discipline)`, `looseness` | 매몰비용. 못 놓음 |
| `tilt_decay` ★ | `1 − t·0.55·(0.25+0.75·계산비중)` | 틸트가 그 개념을 얼마나 깎나 |
| `tilt_direction` ★ | `((aggr−5)/4) × ((swing−5)/5) × 2` | 틸트 방향 −1(위축)~+1(난폭) |

### 틸트

**성향값을 밀어넣지 않는다.** 예전에는 `aggr+3t, bluff+4t, gamble+3t` 였다.
틸트는 개념을 잊는 것이 아니라 **안 쓰는 것**이므로 개념 가중치를 깎는다.
그러면 레인지가 흐트러지고 ICM 을 무시하고 포지션 구분이 사라지는 것이
따로 코딩하지 않아도 나온다.

**계산형이 먼저 무너지고 체화형은 남는다.** 별도 표 없이 `LOADING` 의
study/exp 적재량 비율에서 뽑는다. 틸트 0.8 에서:

```
blocker 56%  pf_range 60%  spr 61%   <- 먼저 무너짐
...
positional 84%  cbet_flop 85%  trap 89%   <- 남음
```

**진입점은 `persona.tilted_view`** — `play.Hand.axes()` 가 부른다.
개념은 `tilt_decay` 로 깎이고 기질은 `tilt_direction` 으로 흔들린다.
규율은 방향과 무관하게 떨어진다.

```
공격형+강화 틸트0.9   aggr 8.0->10.0  loose 6.1->8.1   (더 난폭)
공격형+반전 틸트0.9   aggr 8.0-> 5.3  loose 6.1->4.1   (갑자기 위축)
개념          pf_range 5.9->3.2(계산형)  cbet_flop 4.8->4.0(체화형)
```

**크기와 방향을 나눈다.** 방향을 `aggression` 하나로 정하면
'공격적인 사람은 난폭해진다'만 나오고 '얻어맞고 위축되는 사람'이 없다.
`tilt_swing` 이 강화/반전을 정해 네 조합이 다 나온다.

**손익은 절대 bb 가 아니라 스택 대비로 잰다.** 15bb 손실이 150bb 스택에서는
아무것도 아니고 25bb 스택에서는 치명적이다. 절대 기준(예전 8bb)을 두면
딥스택 초반에 사소한 팟마다 틸트가 쌓인다.

```
스택 대비 손실   15% 미만 안 쌓임 / 55% 에서 충격 최대
```

**결과가 푸는 기준도 상대값이다.** 대략 더블업(스택 대비 +0.8)이라야 확실히 풀린다.
틸트 0.60 에서 +0.3 이면 0.51, +0.5 면 0.33, +0.8 이면 0.05.

**반복 가중은 횟수가 아니라 '열기'.** 큰 손실마다 1 오르고 핸드마다 식는다
(약 12핸드에 한 사건이 잊힌다). 횟수만 세면 대회 초반의 한 번과 방금 전의 한 번이
같은 무게가 되는데, 연달아 맞는 것과 한참 만에 다시 맞는 것은 전혀 다르다.

연속 4회 −12bb (40bb 스택, prone 8):

| | 1회 | 2회 | 3회 | 4회 |
|---|---|---|---|---|
| 누적형 `tilt_stack 9` | 0.17 | 0.32 | 0.52 | **0.78** |
| 보통 5 | 0.17 | 0.28 | 0.40 | 0.51 |
| 감쇠형 1 | 0.17 | 0.24 | 0.29 | 0.32 |

같은 4회를 10핸드씩 띄우면 누적형도 매번 0.17 — 열기가 식어 누적되지 않는다.

**틸트 원인 (구현된 것)**

| 원인 | 조건 | 크기 |
|---|---|---|
| 큰 손실 | 스택 대비 15% 이상 | 55% 에서 최대 |
| 넣고 폴드 | 스택 대비 12% 이상 투자 후 폴드 | 같은 손실의 55% |
| 연속패 | **다툰 핸드** 4연패부터 | 회당 0.045×prone |
| └ 쇼다운 패 | 무게 1.0 | 9회에 0.57 |
| └ 넣고 폴드 | 무게 0.6 | 9회에 0.17 |
| 카드 안 옴 | 14핸드 이상 미참가 | 핸드당 0.012×prone |

**패배도 무게가 다르다.** 끝까지 가서 지는 것과 중간에 접는 것을 같은 1회로 세면
'잘 접었다'고 넘길 수 있는 핸드가 배드빗과 같은 무게를 갖는다.

연속패는 자발적 참가(VPIP)한 핸드만 센다. 블라인드만 낸 것을 포함하면
폴드만 하는 좌석이 66연패까지 가서 틸트가 0.94 에 붙는다(실제로 그랬다).

**보류 (정보 부족)**: 배드빗(에쿼티 우위였는데 짐), 내가 폴드한 뒤
상대가 블러프를 보여줌, 특정 상대에 대한 집착형 틸트.

### variance_seek 상세

```python
aware     = (0.6·attention + 0.4·range_read)/10
gap       = max(0, field_q·10 − my_skill)/10        # 필드가 나보다 센 정도
strategic = gap × aware × (0.25 + 0.075·gamble)
strategic ×= 1 − icm_signal(bf) × max(0.15, icm/10) × 0.90   ★
emotional = tilt × (1.2 − 0.09·discipline)
v         = 0.65·strategic + 0.55·emotional
if bb < 20: v ×= 0.45
```

**`gap` 이 0에서 잘려 있다.** 내가 필드보다 세면 0이다.
포스트플랍 엣지는 그 잘린 음수 쪽이다 — 클립만 풀면 같은 값에서 나온다.

```
edge = (my_skill − field_q·10)/10 × aware      # −1 ~ +1
gap  = max(0, −edge)
```

**미결**: `aware` 를 파생이 아니라 독립 개념(`self_assess`)으로 올릴지.
자각도 공부의 산물이고, "못하는데 모르는 사람"의 분포를 직접 정할 수 있게 된다.

---

## 4. 관찰 축 — read_opponent 산출

상대를 볼 때 만들어진다. 자세한 소비 현황은 `LEDGER.md`.

| 게이트 | 원천 | 읽는 것 |
|---|---|---|
| `see_freq` | `attention` | 빈도 |
| `see_line` | `range_read` | 스트리트 구분, 블러프 |
| `see_size` | `sizing_tell` | 사이즈의 의미 |
| `use` | `adaptability` | 알아도 바꿀 의지 |

`w = use × confidence × min(1, n/12)` 가 익스플로잇 전체의 상한.

**관찰은 기준 대비로 쌓는다.** 절대 빈도가 아니라 `gto.rfi` 대비 배수다.
포지션마다 따로 세지 않아도 되어 같은 표본으로 더 빨리 수렴한다.
`observe_preflop` 이 그 자리의 기준 오픈 폭을 같이 누적한다.

---

## 5. 레인지 층 ★ — gto.py

```
실제 = 기준 × (1 + (1−정확도) × 방향)
```

| 변수 | 뜻 |
|---|---|
| `RFI_BY_BEHIND` ★ | 기준 오픈 폭. **뒤에 남은 인원**으로 색인 |
| `RFI_SB` ★ | SB 별도 (0.35) |
| `_DEPTH_EARLY` / `_DEPTH_LATE` ★ | 깊이 배수. 40bb 정점, 얼리/레이트 보간 |
| `ANTE_MULT` ★ | 안테 없으면 0.90 |
| `avg_rfi` ★ | 포지션 평균. `positional` 낮은 사람이 눌리는 지점 |
| `adapt_mult` ★ | 3층 누적 판단. **미배선, 항상 1.0** |

`open_pct` 안에서:
```python
pos_acc   = 0.10 + 0.80·min(1, positional/8)      # 상한 0.90
flat      = (1 − pos_acc) × 0.60
base      = base×(1−flat) + avg_rfi×flat          # 곡선 평탄화
acc       = 0.10 + 0.80·min(1, pf_range/8)
direction = ((0.75·looseness + 0.25·aggression) − 5)/4    # −1 ~ +1
실제      = base × (1 + (1−acc) × direction × 0.95)
```

---

### 림프 — 동기가 둘이다

| | 이론적 림프 | 습관적 림프 |
|---|---|---|
| 깊이 | **20bb 아래에서만** | 무관 |
| 누가 | `pf_range` 높은 사람 | `pf_range` 낮은 사람 |
| 레인지 | 좁다 (22-88, A2s-A8s 계열) | 넓고 약한 핸드 위주 |
| 논리 | 내 림프 위로 쇼브해도 상대가 덜 번다. 쇼브당해 접어도 덜 잃는다 | 싸게 보고 트립스 이상이면 스택을 딴다 (임플라이드 오즈) |

예전 코드는 `feel >= 0.20`(28bb 이상)에서만 림프했다 — **이론과 정반대 방향**이다.
솔버 림프는 스택이 얕을 때 나오고, 깊으면 레이즈로 팟을 키우는 것이 맞다.

**쇼브 판정이 림프보다 먼저다.** 10bb 에서 림프를 먼저 물으면
쇼브해야 할 자리에서 38% 가 림프로 나갔다.

**미보정**: 실제 빈도가 아직 낮다(저바이인 0.05/핸드). 목표치가 정해지지 않아
구조만 고치고 수치는 두었다 — 분포 보정은 판단 층이 끝난 뒤에 한다.

---

### 프리플랍 블러프 — 판정이 아니라 레인지 폭

**`tb_polar` 는 '블러프했다'는 판정이 아니다.**
프리플랍 블러프는 쇼다운까지 가야 확인되는데 리레이즈에 바로 접는 경우가
많아 표본이 거의 없다. 그리고 4벳에 접었다고 블러프였던 것도 아니다 —
AK 로 3벳하고 4벳에 접는 것은 밸류 3벳이다.

그래서 개별 핸드가 아니라 **레인지 폭**에 대한 진술로 둔다.

```
tb_polar = (실제 3벳률 − 기준 밸류 3벳률) / 실제 3벳률
```

"3벳이 잦다 → 그 안에 로우 오프숏도 있겠다 → 그걸 가정하고 플랍을 친다."
소비처도 판정이 아니라 **레인지 추정과 참여 결정**이다.

| 3벳률 | rr 2 | rr 5 | rr 8 |
|---|---|---|---|
| 7% | 0.00 | 0.11 | 0.21 |
| 14% | 0.00 | 0.30 | 0.61 |
| 20% | 0.00 | 0.36 | 0.73 |

**신뢰도가 낮은 축이다.** 빈도만 세면 되는 `tb_gap`(see_freq)과 달리
레인지 구성을 읽어야 하므로 `see_line` 으로 막고 표본 요구도 훨씬 높다
(n/28 대 n/12). `range_read` 2 인 사람은 이 추정 자체를 못 한다.

**소비처 셋**
- `ranges.preflop_range` — 폴라라이즈된 3벳 레인지는 **상위 N% 슬라이스가 아니다.**
  밸류 덩어리(위)와 블러프 덩어리(아래)가 따로 있고 가운데가 비어 있다.
  슬라이스로 만들면 로우 오프숏이 안 들어가 플랍 계획이 그걸 가정하지 못한다.
  polar 0.70 에서 상위 64핸드 + 하위 100핸드, 최약 42퍼센타일까지 내려간다
- 3벳 대면 콜 — 폴드가 아니라 **참여**로 대응한다
- 4벳 — 폴라라이즈된 상대에겐 리레이즈가 먹힌다

---

### 디펜스 기준 — 상대 오픈 폭의 함수

**별도 표를 두지 않는다.** 공개 자료(9맥스, 3x 대면 BB)의 네 점이
`defend = 0.22 + 0.68 x rfi(오픈자)` 에 거의 정확히 맞는다.

| 오픈자 | 오픈 폭 | 자료 디펜스 | 식 |
|---|---|---|---|
| BTN | 51% | 56% | 57% |
| CO | 36% | 48% | 46% |
| HJ | 28% | 40% | 41% |
| UTG | 15% | 30% | 32% |

SB 오픈만 예외(60%) — 오픈은 좁은데 디펜스가 가장 넓다.
블라인드 대 블라인드라 우리가 액션을 닫고 포스트플랍 포지션을 갖는다.

여기에 MDF(오픈 사이즈)와 디펜딩 좌석 배수가 곱해진다.
`open_bb` 2.0x→72% / 2.5x→62% / 3x→56% / 4x→48%.

**`pf_defend` 개념 신설.** 오픈 차트를 아는 것과 디펜스를 아는 것은 별개다 —
'BB 과다 폴드'가 가장 흔하고 비싼 누수로 꼽힌다.

| BB vs BTN 3x | 3벳 | 참가 |
|---|---|---|
| 기준 | 10.2% | 56.7% |
| 레귤러 (acc9) | 9.3% | 51.5% |
| 과다폴드 (acc2, 타이트) | 6.6% | **39.8%** |
| 루즈 (acc2) | 9.5% | 62.9% |
| 공격 (acc2, aggr9) | **13.0%** | 55.9% |

---

### 올인 대면 — 별도 경로

포스트플랍이 없으므로 계산이 다르다.
- 임플라이드 오즈 소멸 — 셋마이닝·수티드커넥터의 가치가 크게 준다
- **ICM 이 가장 세게 걸린다.** 지면 그 자리에서 탈락이다
- 단계가 깊을수록 극단적으로 좁다

예전에는 올인 대면이 일반 레이즈와 같은 경로였다
(`defend_decision` 호출의 16.7%가 올인 대면인데 ICM 이 안 걸렸다).
`calloff_decision` 은 존재했지만 호출부가 없었고 구형 아키타입 라벨에
의존해 개념 벡터를 무시했다.

BB, 상대 20bb 쇼브 기준 콜 문턱:

| | 평시 | 15명 | 버블 | 위성 |
|---|---|---|---|---|
| 오픈 쇼브 | 33.5% | 17.7% | 12.6% | 8.4% |
| 3벳 쇼브 | 5.8% | 3.1% | 2.2% | 1.5% |
| 4벳 쇼브 | 1.5% | 0.8% | 0.6% | 0.5% |

스택 깊이별(오픈 쇼브, 평시): 12bb 43.6% / 20bb 33.5% / 45bb 23.6% / 70bb 17.7%.
짧은 쇼브일수록 넓게 받는다.

---

### 포스트플랍 — 관찰자와 행위자를 나눈다

`narrow_by_actions` 는 인자가 하나(관찰자 프로필)였고 거기서 `bluff`/`cbet` 을
꺼내 썼다. 그 둘은 **행위자 성향**이어야 한다.
결과적으로 **자기 블러프 성향으로 상대 레인지를 좁혔다** — 자기 투사다.

지금은 인자가 둘이다.
- `actor_read` — 레인지의 주인에 대한 읽기 (`read_opponent` 결과)
- `observer` — 축소를 수행하는 사람. 인식 한계는 `perceived_range` 가 건다

**벳 레인지 폭도 고정 상수였다.** 닛이 턴에 배럴하든 매니악이 하든
같은 상위 30%로 좁혀졌다. `barrel_gap` 축을 신설해 연결했다.

플랍+턴 배럴 후 남는 레인지:

| 행위자 | 남는 비율 |
|---|---|
| 닛 (`barrel_gap` −0.8) | 4% |
| 평균 | 36% |
| 매니악 (+0.8) | 97% |

### 포스트플랍 개념 3종 추가

| 개념 | 없을 때 문제 | 효과 |
|---|---|---|
| `range_merge` | `thin_value_*` 가 턴·리버 한정이라 플랍 단계의 밸류/팟컨트롤 갈림이 성향과 무관하게 고정 | 미들페어: mg1 팟컨트롤 57% → mg9 얇은밸류 77% |
| `multiway` | `0.62^(n-1)` 고정 상수. **누구나 같은 비율로** 줄였다 | 4명 씨벳: mw1 52% / mw9 17% |
| `fold_equity` | `semibluff` 는 아웃 계산이고 '이 상대가 접을까'는 별개 축 | 잘 접는 상대 대면 블러프: fe1 83% → fe5 95% |

`turn_probe` 는 만들지 않았다 — `probe`(플랍 체크백 후 턴 선제)와 같은 것이다.

### 레인지 우위 — 두 축

| | 재는 것 | 정하는 것 |
|---|---|---|
| `range_advantage` | 레인지 **전체**의 에쿼티 우위 | 얼마나 **자주** 칠까 |
| `nut_advantage` | **최상단** 구간 점유 | 얼마나 **크게** 칠까 |

씨벳 빈도: `range_adv` −0.6 → 34% / 0.0 → 70% / +0.6 → 95%.
`board_texture` 개념이 게이트다 — 보드가 누구에게 유리한지 못 읽으면 활용 못 한다.

턴 카드 효과(배럴 확률): 오버 K +0.37 → 95% / 브릭 +0.04 → 79% /
플러시드로 −0.23 → 64%.

---

## 6. 설계 중 — 아직 코드에 없음

| 이름 | 뜻 | 상태 |
|---|---|---|
| `depth_feel` | 스택 깊이 인식 0~1 연속 | 곡선 확정, 미구현 |
| `stack_decay` | 블라인드 침식을 **미리** 보는가 (0~4핸드 앞) | 개념 신설 예정. base 낮고 spread 큼 |
| `postflop_edge` | 필드 대비 자기 실력 (−1~+1) | `variance_seek` 의 `gap` 클립 해제 |
| `self_assess` | 자각 능력 | `aware` 를 독립 개념으로 승격할지 미정 |
| `open_size` | 오픈 사이즈 정확도 | 미신설 |
| 축적형 분산 | 강자의 도박 | `payout_flat`·`reentry` 가 통로 |
