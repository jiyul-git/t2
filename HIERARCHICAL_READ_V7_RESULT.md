# HIERARCHICAL_READ_V7 — 분포 calibration · 구조 검사 · 반사실

사전등록: `HIERARCHICAL_READ_V7_PREREG.md` (Amendment A1 포함).
기준 integration `2610ec9`. 브랜치 `claude/v7-tier`.

**production 무수정. LIVE 배선 없음. V6 도 승격하지 않는다.**
`tools/regress.py check` 전 시드 지문 일치 — `reads.py` 에 함수를 더한 것이
행동을 바꾸지 않았다.

---

## 1. 능력 축과 구간

```
E = app · (½·obs_mean + ½·obs_min)        app = use(adaptability)
Q = E · num                                num = NUMERIC 6개 평균 / 10
```

`app` 이 곱이라 의지가 0 이면 관찰이 아무리 좋아도 E=0 이고, `obs_min` 이 절반을
차지하므로 **attention 만 높은 사람은 위로 못 간다.**

경계 `c1=1/9 · c2=1/4 · c3=4/9` 는 기존 `_see` 앵커(개념 레벨 4·5·6)에서 유도했다.
적합한 것은 **`c5` 하나뿐**이다.

## 2. 모집단 분포 (400인 필드)

적합 `860001..860050` (399봇 × 50 = 19,950명, `field_q` 0.9138, hero 제외).
`c5 = Q 의 (1 − 3.5/400) 분위수 = 0.7087277777777778`.

검증 `870001..870020` — 적합에 쓰지 않은 시드:

| # | 구간 | 비율 | 400명당 |
|---|---|---:|---:|
| 1 | NO_EXPLOIT | 31.62% | 126.5 |
| 2 | IMPRESSION | 26.84% | 107.4 |
| 3 | STYLE_EXPLOIT | 23.73% | 94.9 |
| 4 | QUALITATIVE_REG | 16.84% | 67.4 |
| 5 | QUANTITATIVE_EXPERT | 0.96% | **3.86** |

빈 구간 없음. NO_EXPLOIT 층이 3분의 1 로 뚜렷하게 남는다.

**5단계는 기대값으로만 목표 안이다.** 필드별로는 1~7 로 흩어지고 3~4 에 정확히
든 필드는 20개 중 7개(35%)다. 같은 평균의 포아송에서 P(3≤X≤4)=40% 이므로
이 산포는 노이즈다. 사전등록대로 `c5` 를 다시 만지지 않았다.
**"400명 중 3~4명"은 비율로만 고정 가능하고 필드마다 정확히 3~4명은 구조적으로
불가능하다.**

`field.field_quality` 가 entries 에 의존한다. 100인 필드(`field_q` 0.7833)는
NO_EXPLOIT 39.3% / QUANT 0.35% 로 달라진다. **위 비율은 400인 필드 기준이다.**

## 3. 구조 전수 검사 — PASS

`tools/verify_hierarchical_read_v7.py`. 검증 시드 30개에서 구간별 프로필 60개씩,
합성 `opp_est` 8종의 전조합.

```
C1  1단계의 모든 채널이 0 이고 w 도 0                      위반 0
C2  1~3단계에서 정밀 채널 9개가 중립                        위반 0
C3  2단계는 open_gap · passive 두 채널만                    위반 0
C4  4단계 evidence 가 5점 격자 위에만 있다                  위반 0
C5  5단계만 연속 원값 (evidence == detail)                  위반 0
C6  out = (1−data_eff)·s·PRIOR + data_eff·EVIDENCE 와 일치  위반 0
C7  opp_est 를 바꾸면 출력이 바뀐다 (공허하지 않음)          통과
C8  구간이 올라갈수록 채널 크기 평균이 커진다               통과
```

구간 1~5 전부 실행됐다. **음성 대조**로 2단계에 정밀 채널 누출을 주입한 변종은
C2 에서 정확히 걸린다 — 검사가 공허하지 않다.

구간별 평균 `Σ|channel|` (합성 조합 기준)

```
1 → 0.0000    2 → 0.0997    3 → 0.4339    4 → 1.4924    5 → 2.4390
```

## 4. 반사실 — 주 실행 (사전등록, entries 100, `880001..880012`)

`engine_errors = 0`. 12시드 중 **4시드가 해시 동일**(880002·880006·880009·880012).

| seed | dHand | dEntry | 독립 | 연쇄 | 최초 divergence |
|---|---:|---:|---:|---:|---|
| 880001 | 2 | 3 | 2 | 1 | h14 e11 turn s4 bet 2600→2700 |
| 880002 | 0 | 0 | 0 | 0 | — |
| 880003 | 3 | 12 | 1 | 11 | h13 e18 river s1 call→fold |
| 880004 | 1 | 1 | 1 | 0 | h13 e13 river s2 bet 700→600 |
| 880005 | 3 | 11 | 4 | 7 | h23 e16 turn s2 fold→call |
| 880006 | 0 | 0 | 0 | 0 | — |
| 880007 | 1 | 4 | 4 | 0 | h15 e11 flop s5 bet→check |
| 880008 | 3 | 12 | 12 | 0 | h4 e14 turn s8 check→bet 8900 |
| 880009 | 0 | 0 | 0 | 0 | — |
| 880010 | 4 | 36 | 12 | 24 | h9 e5 preflop s3 fold→raise 600 |
| 880011 | 4 | 11 | 4 | 7 | h10 e17 river s9 fold→call |
| 880012 | 0 | 0 | 0 | 0 | — |
| **합** | **21** | **90** | **40** | **50** | 독립 원인 핸드 11개 |

**90 엔트리 중 40 이 독립 원인이고 50 이 연쇄다.** 사전 상태가 같은데 갈라진
핸드만 독립으로 셌다 (V6 에서 확립한 방식).

구간별 read 호출 (12시드 합 15,611회)

| # | 구간 | 호출 | 비중 | w>0 | mean s | mean w | mean Σ\|ch\| |
|---|---|---:|---:|---:|---:|---:|---:|
| 1 | NO_EXPLOIT | 7,437 | 47.64% | **0.0%** | 0.0000 | 0.0000 | **0.0000** |
| 2 | IMPRESSION | 3,116 | 19.96% | 96.5% | 0.1709 | 0.1277 | 0.0643 |
| 3 | STYLE_EXPLOIT | 2,233 | 14.30% | 97.1% | 0.3113 | 0.1946 | 0.2930 |
| 4 | QUALITATIVE_REG | 2,825 | 18.10% | 96.8% | 0.5862 | 0.3122 | 0.7390 |
| 5 | QUANTITATIVE_EXPERT | **0** | 0.00% | — | — | — | — |

**1단계는 호출의 47.6% 인데 `w>0` 이 정확히 0 건이다.** 상대별 조정이 구조적으로
없다는 것이 실행 중에도 확인된다. 호출 비중(47.6%)이 인원 비중(39.3%)보다 큰 것은
약한 관찰자가 더 많이 관찰 대상이 되어서가 아니라 **호출이 의사결정 단위**이기
때문이다 — 사람 수 비율과 같은 수가 아니다.

**5단계는 주 실행에서 호출이 0 이다.** 100인 필드 12개에 5단계가 1명뿐이고
(880009) 그 시드는 해시 동일이라 읽기 호출이 한 번도 안 잡혔다.
사전등록 Amendment A1 에서 예고한 커버리지 공백이 그대로 실현됐다.

## 5. 반사실 — 보조 실행 (entries 400, `890001..890012`)

**5단계 커버리지 전용.** 주 실행의 결론을 대체하지 않는다.
`engine_errors = 0`. 12시드 전부 갈라진다 (필드가 크니 읽기 기회도 많다).

```
diff 58핸드 / 383엔트리 — 독립 13핸드 / 42엔트리,  연쇄 341엔트리
```

| # | 구간 | 호출 | 비중 | w>0 | mean s | mean w | mean Σ\|ch\| |
|---|---|---:|---:|---:|---:|---:|---:|
| 1 | NO_EXPLOIT | 5,860 | 36.94% | **0.0%** | 0.0000 | 0.0000 | **0.0000** |
| 2 | IMPRESSION | 3,864 | 24.36% | 97.3% | 0.1684 | 0.1393 | 0.0695 |
| 3 | STYLE_EXPLOIT | 3,687 | 23.24% | 97.2% | 0.3149 | 0.2294 | 0.3072 |
| 4 | QUALITATIVE_REG | 2,321 | 14.63% | 96.9% | 0.5711 | 0.2904 | 0.7940 |
| 5 | QUANTITATIVE_EXPERT | **132** | 0.83% | 98.5% | 0.8207 | 0.4117 | **1.0515** |

5단계 호출 비중 0.83% 가 모집단 비율(0.96%)과 같은 크기다. 희귀층이 실행
중에도 희귀하게 남는다.

## 6. 증거가 prior 를 덮는가 — `n` 의존

`data = conf · min(1, n/12)` 가 evidence 의 가중이다. 관찰이 쌓이면 올라가야 한다.

**주 실행 (entries 100), 4단계**

| n 구간 | 호출 | mean data | mean Σ\|ch\| |
|---|---:|---:|---:|
| n<6 | 638 | 0.0200 | 0.3404 |
| 6–11 | 643 | 0.1721 | 0.6913 |
| 12–29 | 1,533 | 0.5346 | 0.9217 |
| n≥30 | 11 | 0.7300 | 1.1836 |

**보조 실행 (entries 400)**

| tier | n 구간 | 호출 | mean data | mean Σ\|ch\| |
|---|---|---:|---:|---:|
| 4 | n<6 | 591 | 0.0262 | 0.3931 |
| 4 | 6–11 | 491 | 0.1731 | 0.6524 |
| 4 | 12–29 | 1,220 | 0.5755 | 1.0181 |
| 4 | n≥30 | 19 | 1.0000 | 2.5395 |
| 5 | n<6 | 31 | 0.0220 | 0.6812 |
| 5 | 6–11 | 20 | 0.1635 | 0.7720 |
| 5 | 12–29 | 81 | 0.6273 | 1.2622 |
| 5 | n≥30 | 0 | — | — |

네 구간 전부 단조 증가다. **관찰이 쌓이면 detail 이 style prior 를 실제로 덮는다.**
`n<6` 에서 `data ≈ 0.02` 라 사실상 prior 뿐이고, `n≥30` 에서 `data = 1.00` 이면
prior 가 완전히 밀려난다.

## 7. 사전등록 8절 판정

**분포**

| 조건 | 결과 |
|---|---|
| 검증 표본에서 5단계가 필드당 3~4명 | **평균 3.86 — 충족.** 단 필드별 산포 1~7 (포아송 범위) |
| 1단계가 비어 있지 않다 | **충족** — 31.6% |
| 다섯 구간이 전부 비어 있지 않다 | **충족** |

**구조**

| 조건 | 결과 |
|---|---|
| tier 1 의 모든 채널이 0 | **충족** (전수 위반 0, 반사실에서도 `w>0` 0건) |
| tier 1~3 에서 정밀 채널 9개 중립 | **충족** (전수 위반 0) |
| tier 4 의 detail 기여가 5점 격자 위 | **충족** |
| tier 5 만 연속 원값 | **충족** |
| hidden target 정보 0 | **충족** — 입력이 observer prof 와 `opp_est` 뿐 |
| 같은 채널 두 번 더하지 않음 | **충족** — 혼합 가중이 `(1−d) + d = 1` |

**반사실**

| 조건 | 결과 |
|---|---|
| engine_errors = 0 | **충족** (주·보조 24 실행 전부) |
| production current regression exact match | **충족** |
| tier 가 올라갈수록 채널 크기 평균 증가 | **충족** — 0.000 / 0.064 / 0.293 / 0.739 / 1.052 |
| tier 4·5 에서 n↑ → detail 비중↑ | **충족** — 네 구간 전부 단조 |

**모든 구조 조건을 충족한다.** 5단계는 주 실행에서 호출 0 이라 **보조 실행으로만
측정됐다** — 이것은 사전등록에서 예고하고 사용자가 B 로 결정한 사항이다.

## 8. V6 대비 달라진 것

V6 의 실측 결함은 **약한 관찰자가 coarse 를 감쇠 없이 받는 것**이었다.
`see_freq ≈ 0.05` 인 관찰자가 `passive` 를 production 의 +0.018 대신 +0.262 로
받았다 (`HIERARCHICAL_READ_V6_RESULT.md` 3-2). V7 은 두 경로로 막았고 실측된다.

- 1단계(호출의 47.6%)는 `w` 까지 0 이라 조정이 아예 없다. V6 에는 이 층이 없었다.
- 2~3단계는 coarse 기여에 `s = E` 가 곱해져 평균 `Σ|ch|` 가 0.064 / 0.293 이다.
  4단계(0.739)·5단계(1.05)와 자릿수가 다르다.
- 정밀 채널은 1~3단계에서 **구조적으로** 0 이다. coarse 가 그 자리를 메우지 못한다.

**주의 — V6 와 직접 비교하지 마라.** 주 반사실의 시드 집합(`880001..880012`)이
V6 의 `850001..850012` 와 다르다. 위 서술은 "V7 이 그 결함 경로를 갖지 않는다"는
구조적 주장이고 같은 시드에서의 행동 비교가 아니다.

## 9. LIVE 승격 — 하지 않는다

사전등록 9절대로 `persona.read_opponent` 를 교체하지 않는다.
구조는 전부 충족했지만 아래가 아직 없다.

- **효과의 방향과 크기.** 주 실행에서 독립 원인 40 엔트리(12시드 × 30핸드)다.
  뒤집힘이 이득인지 손해인지 재는 축이 이번 측정에 없다.
- **5단계 행동 근거.** 주 실행 호출 0, 보조 132회뿐이다. 가장 희귀하게 설계한
  층이라 행동 표본을 모으려면 400인 필드를 훨씬 많이 돌려야 한다.
- **`num` 개념 묶음의 근거.** `NUMERIC` 6개는 `persona.CALC` 에서 의미로 고른
  것이고 실측으로 검증한 묶음이 아니다.

그림: `docs/HIERARCHICAL_READ_V7.png` (`tools/draw_v7_cf.py`)
구간 분포: `docs/HIERARCHICAL_READ_V7_TIERS.png` (`tools/draw_v7_tiers.py`)

## 10. 재현

```
python3 tools/v7_tier_population.py --extra 880001-880012:100 --out v7_tier_params.json
python3 tools/verify_hierarchical_read_v7.py
python3 tools/hierarchical_read_v7_cf.py --seed 880001 --entries 100
python3 tools/hierarchical_read_v7_cf.py --seed 890001 --entries 400
python3 tools/v7_cf_aggregate.py --main 'e100_*.out' --supp 'e400_*.out'
```
