# STYLE_CALIB_V2 결과 — calibration + holdout A/B

사전등록: `STYLE_CALIB_V2_PREREG.md` (Amendment A1 포함).
기준 integration: `035a298`. 측정 브랜치 `claude/style-calib-v2` (`cfa79a5` 기반).
production 무수정. `reads.style_shadow` 는 손대지 않았다. LIVE 배선 없음.

**한 줄 요약 — V2 는 게이트를 못 넘는다. 넘지 못한 정도가 아니라,
style posterior 가 상수 하나보다 못하다. 승격 근거 없음.**

---

## 1. calibration 방법

숨은 archetype 라벨을 쓰지 않는다. 입력은 raw 공개행동 집계뿐이다.

- **좌표**: `reads.style_shadow(est)` 가 낸 `L / A / X` 를 그대로 쓴다.
  `tools/style_params_v2.py` 는 그 함수를 호출만 하고 중심·스케일만 갈아끼운다.
  셀프테스트에서 V1 중심·스케일을 넣으면 `style_shadow` 와 확률이 1.0e-06
  (함수 자체 반올림) 안에서 일치하고, 중심을 옮기면 반드시 달라진다(음성대조).
- **표본**: calibration 시드 24개(820001..820024)의 각 (관찰자, 대상) pair 에서
  두 창. `W1` = 라운드 1..20 누적, `W2` = 21..40 delta, 각 `hands >= 6`.
  관찰자 노이즈·shrink 없는 `raw_est`. 총 **34,462 표본**.
- **군집**: 축별 z-scoring 후 k-means(k=6), k-means++ 재시작 20회(시드 777..796),
  inertia 최소해. 퇴화 재시작 0회, 채택 재시작 780, inertia 25,109.7853.
- **이름 배정**: 사전등록 3-4 의 순서 규칙. z 좌표 위치만 본다.
- **스케일**: 군집 내 잔차의 축별 표준편차(pooled within-cluster sd).
- **모집단**: 표본 평균. baseline A 는 이 상수다.

## 2. calibration 결과

시드 24개 전부 `engine_errors = 0`. 제외된 시드 없다.

| 스타일 | L | A | X | n |
|---|---|---|---|---|
| TIGHT_PASSIVE | 1.379 | 1.935 | 1.129 | 9,364 |
| NIT | 2.579 | 5.413 | 2.470 | 6,779 |
| MANIAC | 2.632 | 6.754 | 4.582 | 4,090 |
| TAG | 4.029 | 2.773 | 1.219 | 5,884 |
| LAG | 6.573 | 6.959 | 4.177 | 3,352 |
| LOOSE_PASSIVE | 6.822 | 3.734 | 1.908 | 4,993 |

정합성 점검

```
C1  L(NIT) < L(TAG) < L(LAG)                              OK
C2  L(TIGHT_PASSIVE) < L(LOOSE_PASSIVE)                   OK
C3  A(TIGHT_PASSIVE)<A(NIT), A(LOOSE_PASSIVE)<A(LAG)      OK
C4  X(MANIAC) = max                                       OK
C5  모든 군집이 표본의 1% 이상                              OK
C6  MANIAC 중심이 L, A 둘 다 모집단 평균 이상              **위반**
```

**C6 위반의 뜻.** MANIAC 이름을 받은 군집은 L = 2.632 로 모집단 평균 3.510
아래다. 타이트-공격 군집이지 매니악이 아니다. 사전등록대로 재배정하지 않았다.
따라서 아래 모든 표에서 **V2 의 MANIAC 빈도는 탐지력의 증거가 아니라 이름
배정의 결과다.**

**사전등록이 잡지 못한 문제 하나를 같이 적는다.** 규칙은 TAG 를 마지막 잔여로
배정한다. 그 결과 TAG 가 A = 2.773, NIT 이 A = 5.413 을 받았다. 타이트-공격이
닛보다 덜 공격적인 배정이다. C1~C3 는 L 순서와 A 두 쌍만 보기 때문에
TAG ↔ NIT 를 건드리지 않아 통과한다. **6칸 중 2칸(TAG, MANIAC)의 이름이
행동공간 위치와 맞지 않는다.** 규칙을 사후에 고치지 않고 그대로 두고 보고한다.

## 3. 동결 파라미터 (`style_params_v2.json`, 커밋 `8073d9f`)

```
CENTERS_V2   위 표
SCALES_V2    (0.9609, 1.2703, 0.6838)
POP_V2       (3.5101, 4.0835, 2.2275)
POP_SD       (2.2289, 2.2754, 1.4223)
```

평가 지표 정규화 상수는 사전등록대로 `(1.7, 1.7, 2.2)` 고정 — calibration
대상이 아니다. holdout 은 이 파일만 읽는다.

## 4. holdout A (830001..830012)

12시드 전부 `engine_errors = 0`. 예측쌍 7,550.

| | style | population | direct | style/pop | style/direct |
|---|---|---|---|---|---|
| **V2** | 1.25940 | 1.13409 | 1.32759 | **1.11049** | 0.94863 |
| V1 참고 | 1.21155 | 1.32299 | 1.32759 | 0.91577 | 0.91260 |

안정성 (컷 10 / 20 / 30 / 40)

```
V2  certainty  0.0899  0.2733  0.3744  0.4166     entropy  1.2344  0.9122  0.8426  0.8045
V1  certainty  0.0342  0.1294  0.1937  0.2207     entropy  1.5828  1.3858  1.3201  1.2910
V2  flip  10-20 0.4362  20-30 0.2904  30-40 0.2413
V1  flip  10-20 0.4656  20-30 0.3363  30-40 0.2699
```

round40 top1 — V2 `TAG 4502 · NIT 2845 · LOOSE_PASSIVE 2375 · TIGHT_PASSIVE 1411 · LAG 426 · MANIAC 181`
/ V1 `TAG 3959 · TIGHT_PASSIVE 3497 · NIT 2174 · LOOSE_PASSIVE 1498 · LAG 612 · MANIAC 0`

## 5. holdout B (840001..840012)

12시드 전부 `engine_errors = 0`. 예측쌍 7,440. **A 를 그대로 재현한다.**

| | style | population | direct | style/pop | style/direct |
|---|---|---|---|---|---|
| **V2** | 1.27363 | 1.14654 | 1.34662 | **1.11084** | 0.94580 |
| V1 참고 | 1.22869 | 1.32152 | 1.34662 | 0.92975 | 0.91243 |

```
V2  certainty  0.0915  0.2697  0.3757  0.4141     entropy  1.2276  0.9224  0.8303  0.7970
V1  certainty  0.0351  0.1274  0.1903  0.2138     entropy  1.5780  1.3914  1.3229  1.2997
V2  flip  10-20 0.4107  20-30 0.3178  30-40 0.2354
V1  flip  10-20 0.4633  20-30 0.3338  30-40 0.2635
```

round40 top1 — V2 `TAG 4458 · NIT 3002 · LOOSE_PASSIVE 2581 · TIGHT_PASSIVE 1254 · LAG 415 · MANIAC 208`
/ V1 `TAG 4140 · TIGHT_PASSIVE 3378 · NIT 2101 · LOOSE_PASSIVE 1659 · LAG 640 · MANIAC 0`

## 6. 사전등록 게이트 판정

```
G1   RMSE_style <= 0.90 x RMSE_population
G2   RMSE_style <= 1.10 x RMSE_direct
```

| | G1 | G2 | 둘 다 (pooled) | 시드별 둘 다 |
|---|---|---|---|---|
| holdout A · V2 | **FAIL** (1.11049) | PASS (0.94863) | **FAIL** | 0/12 |
| holdout B · V2 | **FAIL** (1.11084) | PASS (0.94580) | **FAIL** | 0/12 |
| holdout A · V1 | FAIL (0.91577) | PASS (0.91260) | FAIL | 2/12 |
| holdout B · V1 | FAIL (0.92975) | PASS (0.91243) | FAIL | 1/12 |

**승격 후보 아님.** 두 holdout 모두 G1 에서 떨어진다.

떨어진 방식이 중요하다. **V2 의 style RMSE 는 1보다 큰 비율이다 — 모집단
평균이라는 상수 하나보다 예측이 나쁘다.** V1 은 적어도 상수보다는 나았다
(0.916 / 0.930). calibration 이 두 방향으로 동시에 불리하게 작용했다.

1. style RMSE 자체가 나빠졌다. A 1.2116 → 1.2594, B 1.2287 → 1.2736.
2. baseline A 가 경험적 평균으로 바뀌면서 훨씬 강해졌다. 1.323 → 1.134.

(2) 는 사전등록 3-2 에 미리 적어 둔 대로다. `(5,5,1)` 이 임의 상수였던 것이지
V1 이 잘했던 것이 아니다. 참고로 V2 style 을 옛 `(5,5,1)` 상수와 비교하면
비율이 0.95193 로, 그래도 0.90 을 못 넘는다.

**정확도와 안정성이 반대로 움직였다.** V2 는 round40 certainty 가 0.41 대로
V1(0.21~0.22)의 두 배고 entropy 는 1.29 → 0.80, flip 도 전 구간에서 낮다.
군집 내 스케일이 `(0.96, 1.27, 0.68)` 로 V1 `(1.7, 1.7, 2.2)` 보다 좁아져
posterior 가 한 중심으로 몰린 결과다. 가중 중심이 모집단 평균에서 멀어지고,
정작 예측을 잘 하는 것은 그 평균이다. **확신이 오른 것이지 믿음이 좋아진 것이
아니다 — 과신이다.**

교차검증 하나. V1 팔은 공개된 자연상태 결과를 시드 노이즈 범위에서 재현한다
(`STYLE_NATURAL_V1_RESULT.md`: cert 0.0352/0.1297/0.1993/0.2228,
flip 0.4612/0.3174/0.2556, ratio_pop 0.92029 — 위 V1 열과 비교).
재구현이 shipped 파이프라인과 같게 돌고 있다는 뜻이다.

## 7. MANIAC 희소성 진단

사전등록 5절대로 식 / 중심 / 스케일 셋을 분리했다. 표본은 calibration 34,462.

**(c) 스케일 — 병목이 아니다.** V1 중심 고정, 스케일만 V1 → V2 로 바꾸면
MANIAC argmax 가 219 → 244. 34,462 중 +25 건, +0.07%p 다.

**(a) 식 — 클립되지 않는다.** `X = 1 + 9·Σ wᵢ·hi(xᵢ)` 에서 여섯 항의 p99 는
각각 0.984~1.000 이다. 전부 각자의 p99 면 X = 9.9487 로 상한 10 에 거의 닿는다.
식이 극단을 표현하지 못하는 것이 아니다.

| 항 | w | 중앙 | p90 | p99 | 0인 비율 |
|---|---|---|---|---|---|
| pf_3bet | 0.22 | 0.0000 | 0.3380 | 0.9971 | 82.3% |
| barrel | 0.20 | 0.0000 | 0.9837 | 0.9837 | 76.7% |
| aggr | 0.18 | 0.0000 | 0.8449 | 0.9904 | 71.0% |
| pf_4bet | 0.16 | 0.0000 | 0.0000 | 1.0000 | 98.3% |
| sz_big | 0.14 | 0.0000 | 0.9147 | 0.9997 | 78.7% |
| sz_sd | 0.10 | 0.0000 | 0.9985 | 0.9999 | 82.3% |

**진짜 병목은 동시성이다.** 여섯 항 전부 **중앙값이 정확히 0** 이다.
`hi = max(0, tanh(·))` 이 중심 아래 절반을 통째로 0 으로 깎기 때문이다.
그래서 개별 항은 최대치에 닿아도 한 사람의 20라운드 창에서 여섯이 같이
높은 일이 없다. 실측 X 는 중앙 1.684 / p90 4.375 / p99 6.160 / 최대 8.737,
`X >= 8` 이 **0.067%** 다. `pf_4bet` 은 98.3% 가 0 이라 사실상 기여하지 않고,
이 항만으로 흔한 플레이어의 상한이 1 + 9×0.84 = 8.56 으로 내려간다.

**(b) 중심 — 영역 자체가 비어 있다.** V1 MANIAC 중심 `(8.6, 9.0, 8.0)` 까지의
V1 스케일 거리는 최소 0.461, p1 1.689, 중앙 5.218. 구름 바깥에 동떨어진
위치는 아니다. 다만 그 중심이 최근접이 되는 표본이 **219/34,462 = 0.64%** 뿐이다.

holdout 에서도 같다. V1 중심 기준 raw 미래 창의 MANIAC 은 A 36/7,550(0.48%),
B 47/7,440(0.63%) 이고, **belief 의 MANIAC 은 두 holdout 모두 round40 에서
정확히 0** 이다. 공개된 V1 자연상태 결과(raw 24/7,464, belief 0)와 일치한다.
1% 미만 계층은 거의 균등한 prior 로의 shrink 를 이기지 못한다.

V2 에서 MANIAC 이 belief 181 / 208, raw 미래 717 / 651 로 뛰지만 **이것은
C6 위반의 산물이다.** 같은 표본을 타이트-공격 군집에 MANIAC 이라는 이름표로
다시 붙인 것이지 극단 공격을 잡아낸 것이 아니다.

**정리.** 스케일은 무관, 식은 충분, 남는 원인은 둘 — 여섯 행동의 동시 발현이
희소하다는 것과 그 희소 영역에 중심이 놓여 있다는 것이다. 임계값을 낮춰
MANIAC 을 만들어내지 않았고, 만들지 말아야 한다.

그림: `docs/STYLE_CALIB_V2.png` (`tools/draw_style_calib.py`)

## 8. LIVE 승격 근거 — 없다

- 사전등록 게이트를 두 holdout 모두 통과하지 못했다. 승격 후보 조건 불충족.
- V2 는 V1 보다도 나쁘다. calibration 을 한 쪽이 예측을 더 못한다.
- 이름 배정이 6칸 중 2칸에서 행동공간 위치와 어긋난다(C6, TAG↔NIT).
- `AUDIT_LEDGER.md` A-1 의 상태를 바꿀 근거가 없다. SHADOW 로 둔다.

## 9. 이번 측정이 실제로 확정한 것

게이트는 떨어졌지만 버릴 결과가 아니다. 세 가지가 확정됐다.

1. **`(5,5,1)` 대비 우위는 baseline artifact 였다.** V1 이 모집단을 이겼다는
   그림은 임의 상수와 비교했기 때문이다. 경험적 평균으로 바꾸면 V1 도
   0.916 / 0.930 으로 여유가 거의 없고, V2 는 아예 진다.
2. **6-스타일 압축은 현재 형태로 정보를 더하지 못한다.** direct(현재 L/A/X)
   대비로는 두 버전 다 G2 를 통과한다 — 압축해도 크게 잃지는 않는다.
   그런데 상수 대비로 지므로, 압축이 담고 있는 것이 모집단 평균 정도다.
3. **posterior 를 날카롭게 만드는 것은 개선이 아니다.** certainty 2배,
   entropy 0.80, flip 감소가 전부 예측 악화와 같이 왔다. 안정성 지표를
   품질 지표로 읽으면 안 된다.
4. **행동공간에 6개로 갈라질 구조가 뚜렷하지 않다.** `tools/style_separability.py`
   로 재면 k=6 설명분산이 0.7571 인데, **같은 평균·공분산을 가진 단일 정규분포
   귀무표본에 같은 절차를 돌려도 0.6883** 이 나온다. 실루엣은 0.3366 이다.
   그림 왼쪽 패널의 L-A 구름도 덩어리가 아니라 하나의 넓은 연속체로 보인다.
   **k-means 가 군집을 찾은 것이 아니라 연속체를 여섯 조각으로 자른 것에
   가깝다.** 이것이 style 중심이 모집단 평균을 못 이기는 이유와 맞는다 —
   자를 곳이 없으면 조각의 대표값들은 전부 평균 근처의 변형이다.
   다만 이것은 분리도 지표 두 개와 2D 투영의 관측이고 모형선택을
   정식으로 돌린 것은 아니다. 아래 선택지 B 가 그것을 하는 일이다.

## 10. 다음에 의논할 선택지 (지금 실행하지 않는다)

어느 것도 이번 결과를 사후에 통과시키려는 조정이 아니다. 새 버전으로
다시 사전등록해야 한다.

- **A. 이름 배정 규칙을 고친다.** 잔여 배정(TAG) 과 무조건 argmax(MANIAC) 을
  없애고, 6칸 전부에 위치 조건을 건다. 조건을 만족하는 군집이 없으면 그
  칸을 비우는 것을 허용한다. C6·TAG↔NIT 문제가 여기서 온다.
- **B. k 를 6 으로 고정하지 않는다.** 행동공간에 실제로 몇 개의 밀도 덩어리가
  있는지부터 재고(BIC/실루엣), 스타일 개수를 그 결과에 맞춘다. 다만 이름과
  의미를 늘리는 것은 CLAUDE.md 3항(라벨 증식 금지)과 부딪친다 — 먼저 의논.
- **C. 예측 대상을 바꾼다.** 지금 타깃은 20라운드 raw L/A/X 다. 상수가 이기는
  이유가 대상의 자기상관이 낮아서라면, 예측해야 할 것은 좌표가 아니라
  특정 의사결정(폴드/3벳/배럴) 빈도일 수 있다.
- **D. MANIAC 을 base style 에서 내린다.** 0.64% 짜리 계층을 6분의 1 칸으로
  두는 것 자체가 낭비다. 극단성은 modifier(`threebet_heavy`, `bluffy`,
  `big_sizer`, `size_volatile`)로 이미 따로 있다. **단 이것은 스타일 정의
  변경이라 STYLE_MODEL_V2 재사전등록 사안이다.**
- **E. 여기서 멈춘다.** style belief 를 기록 전용 SHADOW 로 확정하고,
  `eq_delta` 와 같은 처분을 한다. 이번 결과만 보면 이 선택이 가장 방어된다.

---

## 재현

```
python3 tools/style_dump_v2.py --seed <seed> --rounds 40 --entries 100 --out d<seed>.json
python3 tools/style_fit_v2.py  --dumps 'dumps_calib/*.json' \
        --out style_params_v2.json --diag style_calib_v2_diag.json
python3 tools/style_eval_v2.py --dumps 'dumps_a/*.json' --params style_params_v2.json \
        --label holdout_a --out style_calib_v2_holdout_a.json
python3 tools/style_separability.py --dumps 'dumps_calib/*.json' --params style_params_v2.json
python3 tools/draw_style_calib.py  --dumps 'dumps_calib/*.json' --params style_params_v2.json \
        --out docs/STYLE_CALIB_V2.png
```

`.github/workflows/style-calib-v2.yml` 이 시드 단위 matrix 로 같은 것을 돌린다
(job/step 양쪽 timeout, calibration 과 holdout 은 별도 dispatch).
**이번 측정은 이 세션에서 Actions 를 띄우지 못해 로컬 4-way 병렬로 돌렸다** —
세션 자격증명으로 push 해도 workflow run 이 등록되지 않는다(`list_workflows`
에 잡히지 않음). 시드당 88~114초, 시드별 `timeout 900`.
덤프는 저장소에 넣지 않았다(48시드 약 50MB). 위 명령으로 다시 만든다.
