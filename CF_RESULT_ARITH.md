# ⑩ 결과 — DEVIATE 내부 산술 위치

설계 `CF_DESIGN_ARITH.md` (사전등록 `457a7c3`, 범위 축소 `e8b092f`).
**사전등록 문구는 한 글자도 고치지 않았다.**

```
실행 측정   A5 · A6        tools/arith.py, 시드 5200-5204, 엔진 오류 0건
해석적 확정  A1~A4 · 4-1    실행하지 않았다
```

---

## 1. 해석적으로 확정한 것 (analytically established)

**실행 결과가 아니다. 코드에서 유도한 것이다.** 둘을 섞지 않는다.

```
A1  0.035 / 0.020 = 1.750
    PS.derive 가 항등이라(persona.py:245-246) 두 축의 입력 변화는 둘 다 8.0
A2  S3 이후가 f 에 대해 아핀이므로 클램프 밖에서 τ = 1 은 항등식이다
A3  가산항 계수는 상수, discipline 만 그 시점 크기에 비례하는 배율이다
A4  sc ∈ [1,9] → C(sc) = 0.45 + 0.11·sc ∈ [0.56, 1.44] ⊂ [0.35, 1.85]
    S2 클램프는 정의역 안에서 도달 불가능하다
```

### 1-1. 4-1 도 코드로 닫혔다

`profile['value']` 를 읽는 곳은 `plan.py:240`(trap_judgment, 계획층)과
`plan.py:942-943`(**밸류 계획 분기 안**) 뿐이다. `DEVIATE` 경로는
`plan.py:857-868` 에서 `return` 하므로 `942` 에 도달하지 않는다.

**`aggression` 스윕이 `derive` 의 `value` 를 뒤집어도 `DEVIATE` 분기의
하류를 오염시키지 못한다.** A1 의 알려진 위험 경로는 이 분기에서
성립하지 않는다.

---

## 2. A5 — 클램프 발동률 (충족)

flop 조건 (설계 4-2). DEVIATE 고정, D 팔, aligned 호출.

```
축              발동 / 호출        발동률
aggression       27 /  222        0.1216
bluff            19 /  222        0.0856
discipline       13 /  222        0.0586
cbet_flop        30 /  222        0.1351
```

**네 값이 전부 같지는 않다 → A5 충족.**

등록된 판정은 여기까지다. **크기 순서를 해석하지 않고, 유의성 기준을
만들지 않는다** (설계 4절 A5 ④, 8-3).

참고로 전 스트리트 기준은 각각 0.0579 / 0.0408 / 0.0279 / 0.0644 다
(466 호출). **채점 대상이 아니다** — `cbet_flop` 이 플랍 전용이라
분모가 맞지 않는다.

---

## 3. A6 — 클램프 미발동 교집합의 S 비 (충족)

두 축 모두 클램프 미발동인 호출만 (설계 8-4, 측정 전 고정).

```
                 S              n
aggression       0.104918824    438
bluff            0.059953614    438
비 = 1.750000000                문턱 1.750 ± 1e-3   →  충족
```

**계수 비와 소수점 아홉 자리까지 일치한다.**

### 3-1. 교집합이 아니면 어긋난다

```
축별 부분집합   aggression S = 0.105075833 (n 439)
                bluff      S = 0.060593038 (n 447)
                비 = 1.734123858
```

한 호출에서 한 축만 클램프에 걸리면 분모·분자의 호출 집합이 달라진다.
**교집합으로 읽기로 정한 것은 측정 전이다** (설계 8-4). 결과를 보고
고른 것이 아니다.

### 3-2. **⑨ 의 1.745 를 직접 설명한 것은 아니다**

A6 의 ③ 예측문은 "⑨ 의 1.745 와 계수 비 1.750 의 차이가 클램프 발동
건에서만 온다" 였고, ④ 판정 기준은 "부분집합 비가 1.750 ± 1e-3" 이었다.
**충족된 것은 ④ 다.**

`rows55c`(시드 5100-5154)에는 `cf` 가 기록돼 있지 않아 그 표본에서
클램프를 가려낼 수 없다. 이번 측정은 **다른 시드(5200-5204)** 에서
같은 구조가 성립함을 보인 것이다.

**두 표본을 잇는 다리는 재지 않았다.** ⑨ 의 1.745 가 클램프를 빼면
1.750 이 된다고 말하지 않는다.

### 3-3. A2 의 부산물로 읽지 않는다

비가 정확히 1.750 이 나온 것은 클램프 밖 경로가 아핀이라는 것과
모순되지 않는다. **그러나 이것을 A2 의 실행 검증이라고 부르지 않는다** —
A2 는 1절에서 해석적으로 확정했고 실행 채점 대상이 아니었다.

---

## 4. 고지 — 스모크 테스트가 먼저 있었다

도구를 만든 직후 `--seeds 5200 --cap 200` 으로 1회 동작 확인을 했고,
그 출력에 A6 값(1.750000000, n 87)이 이미 보였다. 등록 시드 구간
안이다.

**예측은 `457a7c3` 에 박혀 있어 값을 본 뒤 바뀐 것이 없다.** 다만
"첫 관측이 스모크 테스트에서 일어났다" 는 사실을 숨기지 않는다.

---

## 5. 판정

```
A1  해석적 확정      A2  해석적 확정      A3  해석적 확정
A4  해석적 확정      4-1 해석적 확정
A5  충족            A6  충족 (판정 기준 ④ 기준)
```

> `DEVIATE` 분기에서 축별 `Δp` 의 생성 지점은 코드가 정한 그대로다.
> 가산 두 축은 `S3` 에서 계수 비 그대로 갈리고, 그 비는 클램프가 걸리지
> 않은 호출에서 **정확히** 보존된다. 축별 차이를 만드는 비선형 경계는
> 클램프이고, 그 발동률은 축마다 다르다.

### 5-1. 이 판정이 말하지 않는 것

```
왜 계수가 그 값인가 — 근거가 기록되지 않은 숫자다. 이 단계의 질문이 아니다
축 서열의 원인 — ⑥·⑦ 에서 닫혔다. 사후 결합식을 만들지 않는다
축이 개념적으로 독립인가 — 여전히 답하지 않는다
DEVIATE 밖 — 넓히지 않는다
⑨ 의 1.745 의 출처 — 3-2 참조. 재지 않았다
```

---

## 6. 재현

```
python3 tools/arith.py --axes aggression,bluff,discipline,cbet_flop \
    --seeds 5200-5204 --jobs 4
```

`plan.py` 는 수정하지 않았고 재구현하지도 않았다. `tools/arith.py` 는
`tools/cf_axis_l2.py` 의 복사본에서 D 팔만 남기고 `cbet_freq` 반환값을
추가로 기록한 것이다.

---

## 7. 메타 조사 종료

⑥~⑩ 으로 확보한 것은 하나다.

> 반사실 축들이 하나의 동일한 실행축으로 환원된다는 설명은 지지되지
> 않았고, 표본 밖에서도 그 구조가 재현되며, 그 차이가 만들어지는
> 산술 위치와 비선형 경계가 코드에서 확인됐다.

**역추론 연구를 계속할 전제는 확보됐다.** 여기서 메타 조사를 닫는다.

다음은 `CLAUDE.md` 의 **현재 조사 / 아직 안 본 것** 이고,
`eq`(미래 포함) ↔ `rel`(현재만) 모순부터 본다.
