# eq ≥ pcz 진입 띠 — 건수

**건수만 센다. 손익도 수정 효과도 재지 않았다.** `plan.py` 무수정.
도구 `tools/band_count.py`, 근거 `TRACE_EQREL.md` (`c4ee28e`).

```
수집   tools/collect.py 의 run_one 을 시드 7000-7007 로 고정 호출
       1,000핸드 (collect.py 의 main 은 시드를 안 박아서 재현이 안 된다)
단위   (hash, seat, street) 의 idx == 0 기록
필터   그 기록의 street 접두사가 붙은 why 줄만 (작업 원칙 5)
판정   문턱을 재구성하지 않았다. make_plan 이 남긴 why 문자열로만 가른다
```

---

## 1. 세 숫자

```
전체 관측                                    1,689
  pcz 진입  (why '중간강도')                    183     10.83%
    465 else 로 탈락                            36     19.67%  (진입 대비)
      최종 plan == giveup                       31     86.11%  (탈락 대비)
```

`465` 는 `plan = 'showdown' if made >= 1 else 'giveup'` 이다.
**36건 전부 `made == 0` 이라 36/36 이 giveup 으로 나왔다.**
남은 5건은 같은 `update_plan` 호출 안에서 `refresh` 가 되살렸다
(`plan.py:1795` `elif old == 'giveup' and (rel >= 0.55 or made >= 2)`).

```
스트리트 분포   flop 30 · turn 4 · river 2
```

턴·리버에도 있는 이유는 `revise_plan`(`runner.py:147-150`)이 보드가
크게 바뀌면 `make_plan` 을 다시 부르기 때문이다. **플랍 전용이 아니다.**

---

## 2. 정정 — 다수 사례에서 구속 조건은 `rel` 이 아니라 `made` 다

`TRACE_EQREL.md` 3절은 이 경로를 "`442`·`458` 에서 `rel` 로 탈락" 이라고
적었다. **집계해 보니 다수 사례에서 맞지 않는다.**

`458` 의 조건은 `rel >= max(0.28, 0.52-0.080*_mg)` **and** `made >= 1` 이다.
`_mg` 는 기록되지 않아 문턱은 `[0.28, 0.52]` 구간이다. 36건을 `rel` 로 가르면

```
rel <  0.28            6     rel 로도 확실히 탈락
0.28 <= rel < 0.52     5     _mg 에 따라 갈린다
rel >= 0.52           25     **rel 문턱은 확실히 넘는다**
```

**25건은 `rel` 이 아니라 `made >= 1` 이 막았다.** `made` 중앙값이 0 이고
`rel` 중앙값이 0.58 이다 (최대 0.87).

### 2-1. 그래도 구조 진단은 바뀌지 않는다

`made` 도 `rel` 과 같은 성질의 값이다.

```
made = bot.made_strength(hero, board)     plan.py:310   지금 보드만
rel  = relative_strength(hero, board, …)  plan.py:29    지금 보드만
eq   = _eq_vs(...)                        plan.py:268   미래 전개 포함
```

그래서 진단은 이렇게 **좁혀진다.**

> 진입 심사는 미래를 포함한 `eq` 하나, `pcz` 내부 심사는 현재 보드만 보는
> 두 값(`rel` · `made`)의 논리곱이다. 실측에서 실제로 구속하는 쪽은
> `rel` 이 아니라 `made` 였다.

---

## 3. 드로우 규모 — 세미블러프 선점

```
전체 관측 중 outs >= 8                      115
  plan == semibluff                          27     23.48%
  pcz 진입 (plan.py:468 에 도달 불가)          21     18.26%
    465 로 탈락                               10
```

`semibluff` 분기(`plan.py:468`)는 `elif` 체인에서 `eq >= pcz` 아래에 있다.
**드로우 115건 중 21건(18.3%)이 `eq` 가 `pcz` 를 넘었다는 이유로 세미블러프
심사 자체를 받지 못했다.** 그중 10건이 `465` 로 떨어졌고 전부 giveup 이다.

```
rel   outs   eq      street
0.05    8   0.569    flop
0.07   23   0.559    flop
0.17   11   0.535    flop
0.24   26   0.448    turn
0.26   10   0.578    flop
0.39    8   0.644    flop
0.41    9   0.595    flop
0.45   12   0.573    flop
0.55    8   0.519    flop
0.61   11   0.729    flop
```

`h18`(eq .586 · outs 20 · rel .02)과 같은 부류다.

---

## 4. 참고 — `eq ∈ [0.50, 0.88)` 컷

```
pcz 진입 183 중 띠 안               176     96.17%
  465 탈락                           35
    giveup                           30
```

**채점용이 아니다.** `0.50`·`0.88` 은 헤즈업·무리딩 기준의 유도값이고
실제 `pcz` 는 `mw` 와 상대 읽기로 움직인다. 실제로 giveup 31건의 `eq`
최소가 `0.448` 이라 띠 아래에도 진입 사례가 있다 — `street_gap` 이 음수면
`pcz` 가 0.50 밑으로 내려간다 (`plan.py:356`).

---

## 5. 이 측정이 말하지 않는 것

```
이 31건의 행동이 실제로 손해인가 — EV 를 재지 않았다
pcz 에 rel 페널티를 넣는 것이 옳은가 — 수정 단계의 질문이다
made >= 1 게이트를 푸는 것이 옳은가 — 같다
반사실 영향 범위 — 아직 재지 않았다. 이 건수는 그 해석의 분모일 뿐이다
```

---

## 6. 재현

```
python3 tools/band_count.py --seeds 7000-7019 --hands 1000 --out band1000.jsonl
python3 tools/band_count.py --in band1000.jsonl
```

3절의 드로우 내역과 2절의 `rel` 3분할은 **첫 집계를 본 뒤 도구에 추가했다.**
채점이 아니라 규모 파악용이라 그대로 적는다.
