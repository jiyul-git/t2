# ④ 본 측정 시드 수 확정

`entries=24 / hpl=12 / start_stack=30000`, 기본 분석 기준 `≥3`.

---

## 설계 기준 (결과를 보기 전에 고정)

```
① 검정 단위   축당 하나의 주가설. 한 축이 여러 지표에 나타나도
              (looseness → open_pct·defend_tot) 두 번 세지 않는다
② PRIMARY     7축
③ 다중비교     Holm-Bonferroni, α=0.05
              CONDITIONAL·SECONDARY 는 이 family 에서 분리
④ 최소 효과   |ρ| = 0.20
⑤ 검정력      80% 기본 / 90% 민감도
⑥ 검증        이론식 + 클러스터 부트스트랩 + 실제 Holm 결합 시뮬레이션
```

**`|ρ| = 0.20` 은 "실제 효과가 0.20 일 것" 이라는 가정이 아니다.**
"이보다 작은 효과는 이번 연구에서 primary evidence 로 잡지 않는다" 는
설계 기준이다. 결과를 본 뒤 유리한 효과크기를 고르는 post-hoc 설계를
막기 위해 측정 전에 박았고, **끝까지 바꾸지 않는다.**

---

## 결정

```
power 80% (기본)     45 시드
power 90% (민감도)   55 시드
```

**채택: 55 시드.** 80% 최소는 45 지만 계산 비용 차이가 7분 대 9분이라
민감도 목표를 기본으로 올린다. 시드는 교란 0 으로 표본을 사는 유일한
손잡이이므로(같은 `entries` 면 `field_q` 가 결정론적으로 동일) 늘리는
데 통계적 대가가 없다.

90% 를 넘어서 더 올리지는 않는다 — 설계에 없는 기준을 즉흥으로 만들게 된다.

---

## 축별 종합

| 축 | eligible/192 | 시드당 | ICC | DEFF | 이론 s80 | 부트스트랩 s80 | Holm s45 | Holm s55 |
|---|---|---|---|---|---|---|---|---|
| `looseness` | 192 | 24.0 | .000 | 1.00 | 13 | 14 | 100.0% | 100.0% |
| `aggression` | 190 | 23.8 | .022 | 1.49 | 20 | 16 | 100.0% | 100.0% |
| `potcontrol` | 188 | 23.5 | .000 | 1.00 | 14 | 15 | 100.0% | 100.0% |
| `discipline` | 92 | 11.5 | .064 | 1.67 | 45 | 28 | 98.7% | 99.8% |
| `thin_value_turn` | 62 | 7.8 | .000 | 1.00 | 40 | 45 | 93.1% | 97.2% |
| `cbet_flop` | 49 | 6.1 | .022 | 1.11 | 56 | 56 | 85.1% | 92.5% |
| **`bluff`** | **45** | **5.6** | .000 | 1.00 | 55 | 60 | **81.6%** | **90.4%** |

**병목은 `bluff`** — 세 계산 전부에서 가장 불리하다. 시드당 eligible
5.6명이 원인이다.

---

## 55 시드에서의 규모

```
예상 player        55 × 24 = 1,320명
eligible player    looseness 1,320 / aggression 1,309 / potcontrol 1,293
                   discipline 633 / thin_value_turn 429 / cbet_flop 336
                   bluff 308
테이블핸드          55 × 378 = 20,790
계산 시간          4코어 약 9분
```

---

## 세 계산이 서로를 검증했다

```
이론식 (Fisher z, α/m)      상한·참고값
클러스터 부트스트랩          군집 구조 보존, 축별 독립 α/m
실제 Holm 결합 시뮬레이션     7축 동시, 문턱 α/(m-rank+1)
```

### 이론식 대 부트스트랩

부트스트랩이 일관되게 0~5시드 **높다** (군집 구조의 비용). 방향이
한쪽이라 읽을 수 있다. `discipline` 만 반대인데(45 → 28) 이론식에
`DEFF` 를 적용한 탓이다 — 아래 참조.

### 독립 α/m 대 실제 Holm

```
독립 α/m 병목    bluff 60 시드
실제 Holm        bluff 45 시드에서 81.6%
```

강한 축 셋(`looseness`·`aggression`·`potcontrol`)이 100% 로 먼저
기각되며 문턱을 최대 7배 풀어준다. **α/7 고정 가정은 보수적 근사였다.**

---

## 이 과정에서 잡은 도구 오류 셋

### (1) 부트스트랩 축 표준화에 고정 상수

```python
ys.append(a.rho*(xv-5.0)/2.2*sd_w + ...)   # 2.2 를 모든 축에 사용
```

축마다 평균·sd 가 달라 **실제 주입된 상관이 축마다 달라졌다.**
검출력이 축 sd 순서와 정확히 일치한 것이 단서였다.

```
looseness  sd 2.20 → 배율 1.000 → 78.9%   ← 내가 박은 상수와 같아 맞았다
aggression sd 2.35 → 1.068 → 97.0%
cbet_flop  sd 1.53 → 0.695 → 32.4%
```

**검출력이 양방향으로 어긋난 것**이 모델 오류의 표식이었다. 한 방향으로만
낮았다면 "군집 구조가 불리하다"로 읽고 시드를 늘렸을 것이다.

### (2) `DEFF` 를 within-cluster 예측변수에 적용

`DEFF = 1+(m−1)·ICC` 는 군집 **평균**에 대한 공식이다. 여기서는
예측변수(축 값)가 군집 **안에서** 변하므로 과보정이다.
`discipline`(DEFF 1.67)이 45시드 이론값인데 부트스트랩은 28이었다.
표에는 참고로 남기되 결정에서 뺐다.

### (3) 노이즈 있는 함수에 이분 탐색

이분 탐색이 "전 축 80% 인 최소 시드 43" 을 냈는데 같은 43을 다시 재니
`bluff` 78.0% 였다. 400회면 검정력 0.8 근처 표준오차가 **2.0%p** 라
경계 근처 비교가 동전던지기가 된다.

반복 3,000회(SE 0.73%p)로 고정 후보에서 다시 재니 단조 곡선이 나왔고
`S=40` 에서 78.2% 였다 — **43은 실제로 79~80% 경계였다.**

같은 값을 두 번 계산하지 않았으면 43을 확정했을 것이다.

### (4) 지표 정의 불일치

`seed_power` 가 `thin_value_turn` 에 `rel<0.85` 를 안 걸어 인벤토리와
어긋났다.

```
필터 없음   163명   ρ +0.130   필요 18시드
rel<0.85     62명   ρ +0.391   필요 45시드
```

`|ρ|=0.20` 고정 설계라 실측 ρ 는 시드 계산에 안 들어가지만 **eligible 이
163 → 62 로 줄어 시드 요구가 오른다.** 정의를 안 맞췄으면 과소평가했다.
`PRIMARY` 표에 인벤토리와 일치해야 한다는 주석을 박았다.
