# ⑨ 설계 — 독립 재현 (표본 밖)

**측정 전에 고정한 문서다.** 기준점 `5184144` (⑧ 종료 + 사후 관측 봉인).
이 문서를 커밋한 다음에 측정한다. **측정 후에 이 문서를 고치지 않는다.**

---

## 0. 질문

⑧ 은 `DEVIATE` 분기 하나에서 환원 가설 하나를 기각했다. 다른 분기에서도
같은 현상이 관측되는지는 **열려 있다** (`CF_RESULT_DIVERGE.md` 5-1).

> `DEVIATE` 에서 관측된 "같은 분기 내부에서도 축별 차이가 난다" 는 현상이
> **다른 축 조합의 분기에서도 관측 가능한 형태로 재현되는가?**

"다른 분기에서도 분화형인가" 가 아니다. 2축 분기는 `max/min` 이 곧 두 값의
비라 판별력이 약하다고 ⑧ 설계 3-1-3 에 이미 적었다.

---

## 1. 대상 분기 — P1 이 정한다

축이 둘 이상 겹치는 분기는 넷뿐이다 (`efde4cd` P1-b). 나머지는 단일 축이라
축 간 비교가 성립하지 않는다.

```
DEVIATE:포기 계획이나 지속벳(N%)   4축   aggression · discipline · bluff · cbet_flop
밸류 계획 실행(N%)                2축   aggression · thin_value_turn
블러프 계획 실행(N%)               2축   aggression · bluff
사이즈 0 → 체크                   2축   aggression · bluff
```

축 조합은 **자료가 정한 것이고 고를 여지가 없다.**

새 자료에서 다섯 번째 다축 분기가 나타나면 **채점하지 않고 기록만 한다.**
대상 분기는 위 넷으로 이 커밋에서 닫는다.

---

## 2. 통계량과 문턱 — 새로 만들지 않는다

```
S(ax, b) = mean(|Δp|)  given  Δp≠0 and src_O = b        ⑦ 2-0-3 정의
T(ax, b) = |{act_lo≠act_hi, src_O=b}| / |{Δp≠0, src_O=b}|
비        = 그 분기에 있는 축들 위에서의 max / min
문턱      S 2.0 · T 1.5                                  ⑦ d6fae23 재사용
판정      점유형 / 분화형 / 혼합                          ⑧ 3-1-5 재사용
```

`R` 은 채점에서 제외한다 — `N_b` 가 축마다 같아 P1 의 건수 비와 같으므로
새 정보가 없다 (⑧ 설계 3-1-2).

분모가 0 이거나 min 이 0 이면 비는 정의되지 않는다. **`inf` 를 문턱과
비교하지 않는다** — `사이즈 0 → 체크` 의 `T` 가 ⑧ 에서 이미 그랬다
(양쪽 `T = 0`, `CF_RESULT_DIVERGE.md` 6절). 정의되지 않으면 그대로
"정의되지 않음" 으로 적는다.

---

## 3. 왜 새 시드가 필요한가

⑧ 은 세 분기의 값을 **보조 보고로 이미 공표했다** (`CF_RESULT_DIVERGE.md` 6절).

```
밸류 계획 실행      S 비 1.765   T 비 1.794
블러프 계획 실행     S 비 1.799   T 비 2.060
사이즈 0 → 체크     S 비 1.690   T 비 정의되지 않음
```

**같은 자료(`rows55b`, 시드 5000-5054)에서 이 통계량을 다시 내는 것은
검증이 아니다.** 값을 보고 나서 그 값을 예측할 수는 없다.

그래서 **표본 밖 재현**으로 간다.

```
기존   시드 5000-5054   rows55b   ⑧ 이 본 자료
새것   시드 5100-5154   rows55c   아직 아무도 안 본 자료
```

문턱 `2.0`·`1.5` 는 ⑦(`d6fae23`) 에서 이미 고정됐고 새 자료의 값은 아직
아무도 보지 않았으므로 **사전 등록이 성립한다.**

`DEVIATE`(4축)도 함께 재현 대상이다 — ⑧ 의 `S` 비 5.237 이 표본 밖에서
다시 문턱을 넘는지가 진짜 재현 검증이다.

**이것이 ⑨ 에서 유일하게 새 시뮬레이션이 필요한 지점이다.**
비용: 55시드 실행 1회 + 원자료 약 7.4MB.

---

## 4. 사전 등록 예측

**측정 전에 적는다. 결과가 다르면 이 예측을 고치지 않고 틀렸다고 적는다.**

```
E1  DEVIATE 의 S 비가 문턱 2.0 을 다시 넘는다
E2  DEVIATE 의 T 비가 문턱 1.5 를 다시 넘는다
E3  세 2축 분기의 S 비는 셋 다 2.0 을 넘지 못한다  (⑧ 에서 1.69~1.80 이었다)
E4  분기별 축 조합이 1절 표와 같다
```

E1·E2 가 맞으면 `DEVIATE` 의 분화형 판정이 표본 밖에서 재현된 것이다.
E1 이나 E2 가 틀리면 **⑧ 의 판정은 한 표본에 한정된 관측으로 격하된다.**
그 경우 ⑧ 문서를 고치지 않고 이 문서에 격하 사실을 적는다.

E3 는 **2축 분기가 분화하지 않는다는 주장이 아니다** — 2축에서 비가 작게
나오는 것이 판별력 부족 때문인지 실제로 안 나뉘어서인지는 이 설계가
구분하지 못한다 (⑧ 3-1-3). E3 는 "⑧ 에서 본 대략적 크기가 표본 밖에서도
같은 구간인가" 만 본다.

---

## 5. 절차

```
1  이 설계를 커밋한다                              ← 기준 커밋
2  시드 5100-5154 를 cf_axis_l2.py --rows 로 돌린다
   기존 코드 그대로. 새 계측 없음. 인자는 rows55b 와 시드만 다르다
3  건전성만 확인한다 — 엔진 오류 0 · attached_O ↔ call_seq==0 불일치 0
   21행 집계표가 rows55b 와 **같을 필요는 없다** (다른 시드다)
4  tools/diverge.py 로 네 분기의 S·T 를 내고 2절 규칙으로 판정한다
5  결과 문서 CF_RESULT_REPLICATE.md
```

**4 의 판정 규칙을 2 이후에 바꾸지 않는다.**

실행 명령:

```
python3 tools/cf_axis_l2.py \
  --axes potcontrol,aggression,bluff,thin_value_turn,discipline,looseness,cbet_flop \
  --seeds 5100-5154 --jobs 4 --rows rows55c.jsonl.gz
python3 tools/diverge.py rows55c.jsonl.gz
```

---

## 6. 하지 않을 것

```
DEVIATE 내부 산술 위치를 이번에 분석하지 않는다 — ⑩ 이다
  CF_RESULT_DIVERGE.md 8-1 에 사후 관측으로 봉인했고 예측으로 쓰지 않는다
2축 분기 결과를 '분화형이다/아니다' 로 단정하지 않는다 — 판별력이 약하다
새 문턱을 만들지 않는다 — ⑦ 의 2.0 · 1.5 를 그대로 쓴다
⑧ 의 숫자를 수정하지 않는다
두 자료를 합쳐서 하나의 표본으로 만들지 않는다 — 재현은 따로 낸 값의 대조다
H3 를 되살리지 않는다
```

---

## 7. 이 단계가 답하지 않는 것

```
왜 축마다 다른가 — ⑩ 의 질문이다
축 서열의 원인 — ⑥·⑦ 에서 닫혔다
축이 개념적으로 독립인가 — 이번에도 답하지 않는다
2축 분기가 실제로 분화하지 않는지 — 판별력이 부족하다
```
