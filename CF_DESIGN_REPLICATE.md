# ⑨ 설계 초안 — 독립 재현

**초안이다. 아직 아무것도 측정하지 않았다.**
기준점 `9ce3896` (⑧ 종료).

> **설계에 막히는 지점이 하나 있다. 3절을 먼저 본다.**

---

## 0. 질문

⑧ 은 `DEVIATE` 분기 하나에서 환원 가설 하나를 기각했다. 다른 분기에서도
같은 현상이 관측되는지는 **열려 있다** (`CF_RESULT_DIVERGE.md` 5-1).

> `DEVIATE` 에서 관측된 "같은 분기 내부에서도 축별 차이가 난다" 는 현상이
> **다른 축 조합의 분기에서도 관측 가능한 형태로 재현되는가?**

"다른 분기에서도 분화형인가" 가 아니다. 2축 분기는 `max/min` 이 곧 두 값의
비라 판별력이 약하다고 ⑧ 설계 3-1-3 에 이미 적었다.

---

## 1. 대상 분기 — P1 이 정한다

축이 둘 이상 겹치는 분기는 넷뿐이다 (`efde4cd` P1-b). 나머지는 단일 축이라
축 간 비교가 성립하지 않는다.

```
DEVIATE:포기 계획이나 지속벳(N%)   4축   aggression · discipline · bluff · cbet_flop
밸류 계획 실행(N%)                2축   aggression · thin_value_turn
블러프 계획 실행(N%)               2축   aggression · bluff
사이즈 0 → 체크                   2축   aggression · bluff
```

축 조합은 **자료가 정한 것이고 고를 여지가 없다.**

---

## 2. 통계량과 문턱 — 새로 만들지 않는다

```
S(ax, b) = mean(|Δp|)  given  Δp≠0 and src_O = b        ⑦ 2-0-3 정의
T(ax, b) = |{act_lo≠act_hi, src_O=b}| / |{Δp≠0, src_O=b}|
비        = 그 분기에 있는 축들 위에서의 max / min
문턱      S 2.0 · T 1.5                                  ⑦ d6fae23 재사용
판정      점유형 / 분화형 / 혼합                          ⑧ 3-1-5 재사용
```

---

## 3. **막히는 지점 — 세 분기의 값을 이미 알고 있다**

⑧ 이 세 분기를 **보조 보고로 이미 공표했다** (`CF_RESULT_DIVERGE.md` 6절).

```
밸류 계획 실행      S 비 1.765   T 비 1.794
블러프 계획 실행     S 비 1.799   T 비 2.060
사이즈 0 → 체크     S 비 1.690   T 비 inf   (양쪽 T = 0)
```

**같은 자료에서 이 통계량을 다시 내는 것은 검증이 아니다.** 사전 등록이
성립하지 않는다 — 값을 보고 나서 그 값을 예측할 수는 없다.

⑦·⑧ 에서 지킨 "결과를 보기 전에 기준을 고정한다" 를 이번에는 **같은
자료로는 지킬 수 없다.**

### 3-1. 갈래 A — 새 시드로 표본 밖 재현

```
rows55b   시드 5000-5054
새 실행    시드 5100-5154 (55개, 같은 entries/hpl/stack)
```

같은 문턱(`2.0`·`1.5`)을 **완전히 새 자료**에 적용한다. 문턱은 ⑦ 에서
이미 고정됐고 새 자료의 값은 아직 아무도 안 봤으므로 **사전 등록이
성립한다.**

`DEVIATE`(4축)도 함께 재현 대상이 된다 — ⑧ 의 `S` 비 5.237 이 표본 밖에서
다시 문턱을 넘는지가 진짜 재현 검증이다.

**비용:** 55시드 실행 1회(70분 안팎) + 원자료 약 7.4MB.
**⑨ 에서 유일하게 새 시뮬레이션이 필요한 지점이다.**

### 3-2. 갈래 B — 재현이라고 부르지 않는다

같은 자료의 세 분기 값을 **관측 기록**으로만 남기고 ⑨ 를 열지 않는다.
⑧ 6절에 이미 실려 있으므로 추가 작업이 없다. 대신 **"다른 분기에서도
재현되는가" 는 미검증으로 남는다.**

### 3-3. 판단이 필요하다

**A 는 새 시뮬레이션을 요구하고, B 는 질문을 닫지 못한다.**
⑨ 를 진짜 검증으로 만들려면 A 여야 한다. 이 선택은 설계 결정이다.

---

## 4. A 를 고를 경우의 절차

```
1  이 설계를 확정하고 커밋한다                    ← 기준 커밋
2  시드 5100-5154 를 cf_axis_l2.py --rows 로 돌린다
   기존 코드 그대로. 새 계측 없음
3  재현 검증 — 새 실행의 21행 집계표가 기존과 **같을 필요는 없다**
   (다른 시드다). 대신 엔진 오류 0 · attached_O↔call_seq 불일치 0 만 본다
4  네 분기에 S·T 를 내고 ⑧ 과 같은 규칙으로 판정한다
5  결과 문서
```

**4 의 판정 규칙을 2 이후에 바꾸지 않는다.**

---

## 5. 하지 않을 것

```
DEVIATE 내부 산술 위치를 이번에 분석하지 않는다 — ⑩ 이다
  CF_RESULT_DIVERGE.md 8-1 에 사후 관측으로 봉인했고 예측으로 쓰지 않는다
2축 분기 결과를 '분화형이다/아니다' 로 단정하지 않는다 — 판별력이 약하다
새 문턱을 만들지 않는다 — ⑦ 의 2.0 · 1.5 를 그대로 쓴다
⑧ 의 숫자를 수정하지 않는다
H3 를 되살리지 않는다
```

---

## 6. 이 단계가 답하지 않는 것

```
왜 축마다 다른가 — ⑩ 의 질문이다
축 서열의 원인 — ⑥·⑦ 에서 닫혔다
축이 개념적으로 독립인가 — 이번에도 답하지 않는다
```
