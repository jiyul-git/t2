# `need` 경로 — 두 의심점의 실제 행동 영향

읽기 전용이다. `plan.py` 를 고치지 않았다.
`tools/cf_needpath.py` 가 실행 중인 소스에서 문제 블록만 뺀 변종 함수를
메모리에 만들어(`inspect.getsource` → 문자열 제거 → `exec`, plan 모듈 전역
사용) 호출부 `act_with_plan` 의 전역 조회에 잠시 끼워 넣는다. 소스에서
파생하므로 재구현 표류가 없고, 측정이 끝나면 원래 함수로 되돌린다.

상황·프로필·seed 는 네 조건이 완전히 같다. 상대 추정치와 배팅라인 리딩을
모두 켠 조건이다(덮어쓰기는 `opp_est` 가 있어야 발동한다).

재현: `python3 tools/cf_needpath.py --n 500 --street flop|turn|river`

---

## 결론 먼저

**B(편향 이중 적용)는 무시해도 된다.** 행동 변화 0.4~1.2%, `station` 편향이
큰 집단에서는 세 스트리트 모두 **0.0%** 다.

**A(`need` 덮어쓰기)는 실제 플레이를 14~17% 바꾼다.** 그런데 조사 중에
**방향이 뒤집혔다.**

> 덮어쓰기가 버그가 아니다. **그 앞의 팟오즈 식이 틀렸고, 덮어쓰기가
> 그것을 우연히 고치고 있다.** 대가로 성향 보정 전체를 버린다.

---

## 1. 팟오즈 식이 틀렸다

`calldown_need` 첫 줄 (plan.py:609):

```python
need_true = (tocall*bf)/max(1.0, float(pot))        # pot 은 pot_live
```

호출부 주석(plan.py:1264-1266)이 `pot` 을 `pot_live`(상대 벳 포함)로 정의하고,
*"여기서 tocall 을 또 더하면 분모에 콜 비용이 두 번 들어간다"* 고 적어놨다.

**그 주석이 틀렸다.** 콜하면 내 칩도 팟에 들어간다. 교과서 팟오즈는

$$\text{필요승률}=\frac{\text{tocall}}{\text{벳전팟}+\text{상대벳}+\text{내콜}}
=\frac{\text{tocall}}{\text{pot\_live}+\text{tocall}}$$

이다. 분모에 `tocall` 이 **두 번** 들어가는 것이 맞다 — 한 번은 상대 벳으로,
한 번은 내 콜로.

덮어쓰기 식(plan.py:670-671)은

```python
_p0  = pot - tocall                       # 벳 전 팟
need = (sz*_p0)/(_p0 + 2*sz*_p0)          # = sz/(1+2sz),  sz = tocall/_p0
```

이고, `sz = tocall/_p0` 를 대입하면 `tocall/(_p0 + 2·tocall)` — **교과서식과
완전히 같다.**

| 벳/팟 | `need_true` | 덮어쓰기식 | 교과서 | 과대율 |
|---|---|---|---|---|
| 0.33 | 0.248 | 0.199 | 0.199 | 1.25× |
| 0.50 | 0.333 | 0.250 | 0.250 | 1.33× |
| 0.66 | 0.398 | 0.284 | 0.284 | 1.40× |
| 1.00 | 0.500 | 0.333 | 0.333 | 1.50× |
| 2.00 | 0.667 | 0.400 | 0.400 | 1.67× |

**`need_true` 는 전 구간에서 과대평가다.** 배수는 정확히

$$\frac{\text{need\_true}}{\text{교과서}}=\frac{\text{pot\_live}+\text{tocall}}{\text{pot\_live}}
=1+\frac{\text{sz}}{1+\text{sz}}=\frac{1+2\,\text{sz}}{1+\text{sz}}$$

다 (위 표의 '과대율' 열). 앞서 이것을 `(1+sz)` 배라고 적었는데 **틀린
표기였다** — 표의 숫자는 맞고 대수식만 틀렸다.

호출 맥락도 확인했다. `session.py:449` 의
`pot_live = pot_now + sum(r2.contrib.values())` 는 상대 벳을 포함하고
**내 콜(`tc`)은 포함하지 않는다.** 그래서 분모에 `tocall` 이 빠진 것이 맞다.

덮어쓰기식은 `_p0` 가 무엇이든 대수적으로 교과서식과 같다:
`sz = tocall/_p0` 를 대입하면
`(sz·_p0)/(_p0 + 2·sz·_p0) = tocall/(_p0 + 2·tocall) = tocall/(pot_live + tocall)`.

### CLAUDE.md '아직 안 본 것' 2번이 이것이다

> **h69 응답 산수** — `eq .338 ≥ 필요승률 .311` 인데 폴드했다.
> giveup 계획은 `decide_response:719` 조기 return 으로 순수 팟오즈 산수
> 경로를 타는데, **그 산수조차 맞지 않았다**. 표본 1건

`.311` 이 옳은 값이라면 코드가 쓰는 값은 `.311 × (1+sz)` 다.
`sz ≈ 0.66` 이면 `.435` 이고, `eq .338 < .435` → 폴드. **정확히 맞는다.**
표본 1건이던 것의 메커니즘이 나왔다.

---

## 2. 그런데 덮어쓰기는 성향 체인을 버린다

```python
need *= PS.call_bias(profile, street, _sz_seen, made_now, draw)   # 곱하고
if abs(_sz_seen - _sz_true) > 1e-9:
    need = (_sz_seen*_p0)/max(1.0, _p0 + 2*_sz_seen*_p0)          # 다음 줄에서 버린다
```

**대입**이므로 발동하면 그 앞이 전부 사라진다 — `potodds` 계산 오차,
`to_act_behind` 가산, 리딩 `trust`(`range_read`·`bluffcatch_*`·`sizing_tell`·
`aggr`), 상·하한 클램프, 그리고 바로 윗줄의 `call_bias`.

발동률 (표본 3,000): `opp_est` 없으면 **0.0%**, 있으면 **84.1%**.
그때 사이즈 오차 중앙값은 **1%** 다.

**즉 엔진은 두 상태 중 하나다.**

| | 팟오즈 | 성향 |
|---|---|---|
| `opp_est` 없음 (16%) | **틀림** (1+sz 배 과대) | 살아 있음 |
| `opp_est` 있음 (84%) | 맞음 | **버려짐** |

둘 다 동시에 맞는 경로가 없다.

---

## 3. 측정 — 각 변종이 행동을 얼마나 바꾸나 (각 스트리트 n=500)

| 조건 | 플랍 폴드/콜/레이즈 | 턴 | 리버 |
|---|---|---|---|
| **현재** | 51.8 / 36.6 / 11.6 | 52.8 / 30.2 / 17.0 | 58.2 / 29.4 / 12.4 |
| A 덮어쓰기 없음 | 62.8 / 26.4 / 10.8 | 63.8 / 20.2 / 16.0 | 69.8 / 18.8 / 11.4 |
| B 편향 한 번 | 51.4 / 37.0 / 11.6 | 52.4 / 30.6 / 17.0 | 58.0 / 29.6 / 12.4 |
| **C 팟오즈 교정 + 성향 보존** | 50.2 / 37.4 / 12.4 | 50.4 / 32.0 / 17.6 | 58.0 / 29.2 / 12.8 |

### A — 덮어쓰기만 빼면

| | 플랍 | 턴 | 리버 |
|---|---|---|---|
| 행동이 바뀐 상황 | **16.6%** | **14.4%** | **14.6%** |
| 폴드율 변화 | +11.0%p | +11.0%p | +11.6%p |
| 전환의 대부분 | `call → fold` 75~85% | | |
| `need` 차이 중앙 | 0.102 | 0.102 | 0.135 |

**이 변종은 나쁜 쪽이다.** 틀린 `need_true` 를 그대로 두게 되어 봇이
11%p 더 접는다. A 는 "고치면 이렇게 된다"가 아니라 "덮어쓰기가 지금
무엇을 하고 있는가"의 크기다.

### C — 팟오즈 식을 고치고 덮어쓰기를 빼면 (성향 보존)

| | 플랍 | 턴 | 리버 |
|---|---|---|---|
| 행동이 바뀐 상황 | **16.4%** | **12.2%** | **9.4%** |
| 폴드율 변화 | −1.6%p | −2.4%p | −0.2%p |
| 전환 방향 | `fold→call` 38 / `call→fold` 32 | 28 / 16 | 23 / 22 |

**집계는 거의 안 움직이는데 개별 결정의 9~16% 가 바뀐다.**
그리고 전환 방향이 양쪽으로 갈린다 — 한쪽으로 쏠리는 A 와 다르다.

이것이 "산수는 맞고 성향은 살아 있는" 상태의 모양이다.
계획별로는 `giveup`·`pot_control`·`value_2street` 에 몰린다.

### B — 편향을 한 번만 적용하면

| | 플랍 | 턴 | 리버 |
|---|---|---|---|
| 행동이 바뀐 상황 | 1.2% | 0.4% | 0.6% |
| `station` 편향 0.3 초과 집단 | **0.0%** | **0.0%** | **0.0%** |

**코드는 지저분하지만 행동은 안 바뀐다.** 우선순위에서 내린다.

---

## 4. 판정

| 항목 | 판정 |
|---|---|
| `need_true` 팟오즈 식 | **분모에 내 콜이 빠졌다.** `(1+2sz)/(1+sz)` 배 과대. CLAUDE.md h69 의 원인 |
| 덮어쓰기 블록 | 산수는 맞다. 다만 **대입이라 성향 체인을 버린다** (84% 발동) |
| 둘을 같이 고치면(C) | 행동의 9~16% 가 바뀐다. 집계는 거의 그대로 |
| `station`/`bluff_fear` 이중 적용 | **무시해도 된다** (0.0~1.2%) |

---

## 5. 한계

- **어느 쪽이 "옳은" 플레이인지는 판정하지 않았다.** 교과서 팟오즈가
  맞다는 것까지다. `need` 는 순수 팟오즈가 아니라 읽기·편향을 얹은 값이라,
  최종 문턱이 어디여야 하는지는 별개 문제다.
- 상황이 합성이다. 실제 대국의 사이즈·스택 분포와 정확히 같지 않다.
- **C 는 실제 코드가 아니다.** 반사실 안에서만 존재한다. 실제로 고치려면
  `plan.py` 를 건드려야 하고 baseline 지문이 깨지므로 별도 브랜치다.
- **덮어쓰기를 지우는 것이 전략적으로 더 나은지는 미판정이다.** 주석은
  "오독한 사이즈로 팟오즈를 다시 계산한다"는 설계를 명시한다. 확정된 것은
  (a) `need_true` 산수 경로가 재검증 대상이고, (b) 재대입이 앞단 체인을
  끊는 구조이며, (c) C 의 85% 뒤집힘은 개선의 증거가 아니라 판단 근거가
  달라진 결과라는 것까지다.
- `bf`(버블팩터)가 `need_true` 에 곱해져 있다. 분모를 고치면 ICM 압박의
  실효 크기도 같이 바뀐다 — 그 영향은 따로 재야 한다.

---

## 6. C 뒤집힘 분해 — 두 효과는 성격이 다르다

`python3 tools/cf_needpath.py --n 500 [--street river]`
(bf = 1.0 고정. ICM 은 들어가지 않았다.)

덮어쓰기가 **발동했는지**로 가르면 C 의 차이가 무엇인지 갈린다.

- 발동했으면 → C 는 **버려졌던 체인을 되살린 것**
- 발동 안 했으면 → C 는 **틀린 산수를 고친 것**

| | 플랍 (82건) | 리버 (47건) |
|---|---|---|
| 발동함 → **체인 복원** | 70건 (85%) | 41건 (87%) |
| 발동 안 함 → **산수 교정** | 12건 (15%) | 6건 (13%) |

### 산수 교정 — 단방향이다

| | 폴드→콜 | 콜→폴드 | `need` 평균 변화 |
|---|---|---|---|
| 플랍 | **12** | **0** | **−0.152** |
| 리버 | **6** | **0** | **−0.156** |

**전부 `폴드 → 콜` 이다.** `need` 가 평균 0.15 내려간다.
틀린 팟오즈 때문에 과잉 폴드하던 것이 그대로 교정된다.
이건 판정에 논란의 여지가 없다.

### 체인 복원 — 양방향이다

| | 폴드→콜 | 콜→폴드 | `need` 평균 변화 |
|---|---|---|---|
| 플랍 | 26 | 32 | +0.030 |
| 리버 | 17 | 22 | +0.046 |

양쪽으로 갈리고 집계는 거의 안 움직인다(+0.03~0.05).
**개별 판단이 다시 근거를 갖는 것이지, 봇이 전체적으로 타이트/루즈해지는 것이 아니다.**

### 복원되는 것은 기질 편향이 아니다

`call_bias` 가 뒤집힘 방향을 **전혀 예측하지 못한다.**

| | `call_bias < 1` 비율 |
|---|---|
| 폴드 → 콜 로 뒤집힌 건 | 26% (플랍) / 26% (리버) |
| 콜 → 폴드 로 뒤집힌 건 | 31% (플랍) / 32% (리버) |

그리고 `call_bias` 평균이 **1.00** 이다 — 사실상 중립.
이유는 이미 측정돼 있다: `PS.bias` 세 축의 **중앙값이 0.00** 이고
`max(0.0, bias(...))` 로 음수를 자르므로 필드의 절반은 편향이 정확히 0 이다
(`TRACE_PLAN.md` 5절).

**따라서 체인 복원의 실체는 `station`·`bluff_fear` 같은 기질 편향이 아니라
리딩 `trust`(`range_read`·`bluffcatch_*`·`sizing_tell`)와 `potodds` 계산
오차다.** `tools/cf_response.py --read` 에서 `sizing_tell` 21.2% /
`range_read` 13.8% 로 나온 것과 같은 것을 가리킨다.

### 판정

| | 판정 |
|---|---|
| 산수 교정 (15%) | **명백한 개선.** 단방향, 과잉 폴드 해소 |
| 체인 복원 (85%) | **양방향.** 판단 근거가 돌아오는 것은 맞지만, **그것이 더 나은 플레이인지는 이 측정으로 판정할 수 없다.** 승률·EV 로 재야 한다 |

"양쪽으로 흔든 것"인지 "복원"인지는 **둘 다**다. 15% 는 복원이 확실하고,
85% 는 근거의 복원이되 결과의 개선인지는 미판정이다.
