# 결합 반사실 설계 — `pcz` × `made` 2팔 × 3팔 교차 EV

**측정 전에 고정한다.** `plan.py` 무수정.
선행: `CF_RESULT_PCZ.md` · `CF_RESULT_MADE.md` · `CF_RESULT_EV.md`

---

## 1. 팔 — 새 임계값을 하나도 쓰지 않는다

`pcz` 는 팔이 둘, `made` 는 셋이다. **하나씩 고르면 그것이 우열 가정이므로
전부 교차한다.**

```
pcz 개입   plan.py:390-394 블록에 한 줄 추가 (CF_DESIGN_PCZ 2절 그대로)
  P-A   _pen >= 0 → pcz += 0.22*_pen   else → pcz += 0.08*_pen
  P-B   _pen >= 0 → pcz += 0.30*_pen   else → pcz += 0.10*_pen

made 개입  plan.py:465 술어 치환 (CF_DESIGN_MADE 1절 그대로)
  M-S   made >= 1 or eq >= 0.42 + 0.05*mw
  M-R   made >= 1 or rel >= 0.42
  M-D   made >= 1 or outs >= 8
```

```
팔 구성   O(1) · P(2) · M(3) · PM(6)  = 핸드당 12회 재생
```

### 1-1. 숫자 감사 — 전부 기존 코드에서 옮겨 온 것

```
0.22 · 0.08   plan.py:391 · 394     v2 의 쌍
0.30 · 0.10   plan.py:390 · 393     v3 의 쌍
0.42 · 0.05   plan.py:503           has_sd 식
0.42          plan.py:1579          river_fix 의 쇼다운 가치 술어
8             plan.py:468           semibluff 게이트
```

**새로 만든 숫자는 하나도 없다.** 하나라도 들어가면 이 절에서 걸러낸다.

---

## 2. 모집단 — **P 와 M 의 작용 범위가 다르다**

`made` 개입은 `465` **한 자리**에서만 작동한다. `pcz` 개입은 그 핸드의
**모든 pcz 진입**에서 작동한다. 같은 핸드에 올려도 개입 대상이 다르다.

```
주 분석 모집단 (PM 공통)
  기준 실행에서 pcz 진입이 **정확히 1회**이고, 그 진입이 465 에 도달한 핸드
  → 네 팔이 같은 결정 하나에 붙는다
```

```
보조 (직접 비교하지 않는다)
  P-only    pcz 진입은 있으나 465 에 도달하지 않은 핸드
  M-only    주 모집단에서 P 팔이 라벨을 바꾸지 못한 핸드
```

**서로 다른 모집단의 ΔEV 를 직접 비교하지 않는다.**
**승격이 일어난 핸드만 골라 모집단으로 자르지 않는다** — 결과에 유리한
경로를 고르는 것이 된다. `refresh` 승격은 **효과 경로로 기록**하되
모집단 조건이 아니다.

---

## 3. 지표

```
ΔEV_X = EV(X) - EV(O)          X ∈ {P-A, P-B, M-S, M-R, M-D, PM-*}
상호작용  Interaction(p, m) = ΔEV_PM(p,m) - ΔEV_P(p) - ΔEV_M(m)
```

**부호도 우열도 사전등록하지 않는다.** 상호작용은 기록 대상이다.

결과변수는 `CF_DESIGN_EV.md` 5절 그대로 — 대상 좌석의 핸드 순칩 변화.
분기 전 기여는 모든 팔에서 같으므로 차분에서 소거된다.

---

## 4. 페어링과 난수

```
재생    t.run 을 떼고 deepcopy 한 **같은 스냅샷**에서 12개 팔을 각각 실행
카드    핸드 시작에 배분되므로 모든 팔에서 동일하다
```

행동이 갈리면 그 이후 결정 시드가 달라진다 (`extra = len(r2.log)`).
**이것을 공통 event tape 이라고 부르지 않는다.** 고정되는 것은 카드와
분기점 이전의 모든 것이다 (`CF_DESIGN_EV` 3-1).

---

## 5. 페어마다 남길 trace — 이번에 새로 넣는다

`ΔEV = 0` 을 "효과 없음" 으로 뭉개지 않기 위해서다.

```
분기 시점      O 라벨 · CF 라벨
후속 스트리트마다
   O 라벨 · CF 라벨
   refresh 진입 여부 · 출발 라벨 · 도착 라벨
최종           행동 열 · 벳 금액 · 핸드 순칩 · ΔEV
```

특히 `CF_RESULT_EV` 3-1 에서 확인된 갈림을 직접 기록한다.

```
plan.py:1795  giveup 에서   문턱 0.55  갈림 0.72  →  {showdown, value_2street}
plan.py:1803  showdown 에서 문턱 0.70  갈림 0.88  →  {value_2street, value_3street}
```

이 기록으로 세 경우를 **분리해서** 보고한다.

```
라벨만 다르고 downstream action 은 같았다
refresh 승격 경로가 달랐으나 최종 EV 는 같았다
betting path 까지 달라졌다
```

---

## 6. 사전등록 — 관측형이다. 방향을 예측하지 않는다

```
V1  action 이 동일한 pair 중 ΔEV ≠ 0 인 것이 있는지 **관측한다**
V2  action 변화가 발생한 pair 가 존재하는지 **관측한다**
V3  refresh 출발 라벨 차이에 따른 승격 경로 차이를 **기록한다**
V4  ΔEV 의 부호와 팔 간 우열을 **사전등록하지 않는다**
```

`CF_RESULT_EV` 의 `V3`(특정 방향 전환이 반드시 나온다)처럼 방향을
박았다가 틀린 전례가 있다. 이번에는 방향을 예측하지 않는다.
**그래도 틀리는 것이 나오면 그대로 틀렸다고 적는다.**

---

## 7. 표본 — 먼저 고정하고, 나오는 대로 보고한다

1,000핸드에서 이전 모집단이 28쌍이었다. 단순 비례로 10,000핸드면
280쌍 안팎이지만 **예측치일 뿐이고 보장된 크기가 아니다.**
이번 주 모집단은 조건이 더 좁아(pcz 진입 정확히 1회) **더 작을 수 있다.**

```
핸드 수를 먼저 고정한다
실제 모집단 수를 그대로 보고한다 — 28쌍이면 28쌍, 300쌍이면 300쌍
**모집단을 채우려고 조건을 사후 변경하지 않는다**
검정력을 주장하지 않는다
```

### 7-1. 비용 — **재서 정했다. 1패스가 더 싸다**

초안은 2패스(1패스로 자격 핸드 식별 → 2패스에서 그 핸드만 스냅샷)를
적었다. **실측하니 틀렸다.**

```
스냅샷 없음        120핸드  20.7s   핸드당 0.172s
핸드마다 스냅샷     120핸드  21.5s   핸드당 0.179s
```

`t.run` 을 뗀 `deepcopy` 는 **핸드당 0.007s(4%)** 다. 2패스는 기준 패스를
두 번 돌아 0.344s/핸드가 되므로 1패스(0.179s)보다 거의 2배 비싸다.

```
1패스로 간다 — 핸드마다 스냅샷을 뜨고, 자격 핸드에서만 11개 반사실을 재생
10,000핸드 ≈ 30분 + 자격 핸드 × 11회 재생
```

이것은 **결과가 아니라 비용에 대한 측정**이라 사전등록과 무관하다.

---

## 8. 하지 않을 것

```
팔 간 우열을 가르지 않는다
승격이 일어난 핸드만 모집단으로 자르지 않는다
서로 다른 모집단의 ΔEV 를 직접 비교하지 않는다
새 임계값·새 plan 라벨을 만들지 않는다
plan.py 를 수정하지 않는다
ΔEV = 0 을 '효과 없음' 으로 뭉개지 않는다 (5절 세 분리)
```
