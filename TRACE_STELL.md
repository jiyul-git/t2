# `sizing_tell` 해부 — 사이즈를 읽는 축이 아니라 덮어쓰기의 on/off 스위치다

**production code 는 수정하지 않았다.** 측정만 했다.

- baseline `6574360` (1-A 적용, 지문 `44da00bf…`)
- 도구 `tools/cf_stell.py`, 변종은 `tools/cf_szseen.py` 의 ① / ③ 을 그대로 사용
- 스트리트 3 × n=400, `bf=1.0`
- 그림 `sizing_tell.png`

---

## 0. 먼저 배선부터 — `size_read` 는 이 엔진에서 한 번도 동작한 적이 없다

`sizing_tell` 의 이름값을 하는 함수는 `persona.size_read` 다. docstring 전체가
"정의역(2팟) 밖을 못 읽는 사람은 오독한다"는 이야기다. 그런데 첫 줄이

```python
s = max(0.0, float(size_frac or 0.0))
if s <= 2.0:
    return s          # 정의역 안: 그대로 읽는다   ← persona.py:755
```

2.0팟 이하에서는 **항등함수다.** 측정 표본 800회 중 800회(100%) 항등이었다.

실제 핸드에서 2팟이 나오기는 하는가 — 아카이브 11개 파일에서 포스트플랍
저항에 직면한 **648건**을 재구성했다:

```
sz = tocall / (벳 전 팟)
  중앙 0.421   90분위 0.573   99분위 0.739   최대 0.915
  sz > 1.0 : 0건        sz > 2.0 : 0건
```

엔진이 2팟을 만들 수 있는 유일한 경로는 `overbet_frac` 의 `min(2.2, …)` 인데,
개념·양극화·넛우위 게이트에 확률 상한 0.55 까지 걸려 있어 648건 중 1팟을
넘은 것조차 없다. **`size_read` 는 사실상 죽은 코드다.**

따라서 `sizing_tell` 이 `_sz_seen` 에 닿는 경로는 `size_read` 가 아니라

```
sizing_tell ─► _see(stl) = see_size ─► size_gap / size_big / size_info
                                          │
                                          ▼
                                   opp_size_norm ─► _sz_norm ─► (size_read: 항등) ─► _sz_seen
```

이다. `persona.py:951` 의 주석 "사이즈 축은 sizing_tell 에만 걸린다" 가 실제 배선이다.

## 1. 그래서 무슨 일이 벌어지나 — 발동률이 0% ↔ 89% 로 갈린다

`see_size = max(0, min(1, (stl-2)/6))` 이므로 `sizing_tell ≤ 2` 면 `size_gap`,
`size_big` 이 **정확히 0** 이 되고, `opp_size_norm` 은 입력을 그대로 돌려준다.
그러면 `_sz_seen == _sz_true` 라서 **덮어쓰기가 아예 걸리지 않는다.**

| 스트리트 | `sizing_tell=1` 발동률 | `sizing_tell=9` 발동률 |
|---|---|---|
| flop | **0.0%** | 89.0% |
| turn | **0.0%** | 89.0% |
| river | 88.5% | 89.0% |

**이게 `sizing_tell` 14.5% 의 정체다.** 사이즈를 더 잘 읽어서 판단이 바뀌는 게
아니라, **응답 체인을 통째로 지울지 말지를 켜고 끄는 스위치**로 동작한다.

- `sizing_tell` 이 낮은 사람 → 덮어쓰기 안 걸림 → `calc_noise`·리딩·`call_bias`
  가 전부 살아서 `need` 를 만든다
- `sizing_tell` 이 높은 사람 → 덮어쓰기 걸림 → 그 전부가 지워지고 날 팟오즈로 판정

즉 **개념이 높은 사람일수록 개성이 사라진다.** 방향이 뒤집혀 있다.

## 2. 리버가 다른 이유 — `size_river` 만 게이트가 없다

리버는 `sizing_tell=1` 에서도 88.5% 가 발동한다. `opp_size_norm` 이 리버에서만
`size_river` 를 절반 섞는데, `read_opponent` 의 그 항만 `see_size` 곱셈이 빠져 있다:

```python
'size_gap':   ... * see_size,
'size_info':  ... * (see_size ** 2),
'size_big':   ... * see_size,
'size_river': g('sz_river', g('sz_mean', 0.62)),      # ← 게이트 없음
```

그래서 리버에서는 사이즈를 전혀 못 읽는 사람도 `_sz_norm` 이 어긋나고,
**모두가 체인을 잃는다.** 같은 파일의 다른 주석들이 경계하던 바로 그 우회로다
(`bluff_gap` 을 날것으로 읽어 `see_line` 을 우회했다는 기록, `passive`/`station`
이 무게이트로 남아 있었다는 기록).

이번 라운드에서는 **수정하지 않는다.** 기록만 남긴다.

## 3. 질문별 답

### sizing_tell↑ 이면 콜인가 폴드인가 — **방향이 없다**

플랍, `sizing_tell` 1 → 9:

| | 폴드 1→9 | 콜 1→9 | 전환 |
|---|---|---|---|
| ① 덮어쓰기 | 50.8% → 49.5% | 37.2% → 39.2% | fold→call 27, call→fold 20, 그 외 11 |
| ③ 체인보존 | 50.8% → 49.5% | 37.2% → 38.5% | fold→call 6, call→fold 1 |

`fold→call 27` 과 `call→fold 20` 이 거의 맞먹는다. 순 효과는 콜 +2.0%p 뿐인데
58건이 양쪽으로 움직였다. **방향성 있는 읽기가 아니라 양방향 교란이다.**

### need 를 얼마나 움직이는가 — **크게, 그러나 무방향으로**

| 변종 | 스트리트 | 부호있는 중앙 | \|Δneed\| 중앙 | \|Δ\| 90분위 |
|---|---|---|---|---|
| ① 덮어쓰기 | flop | −0.0077 | **0.0807** | 0.1909 |
| ① 덮어쓰기 | turn | −0.0124 | **0.0834** | 0.1980 |
| ① 덮어쓰기 | river | −0.0006 | 0.0019 | 0.0067 |
| ③ 체인보존 | flop | −0.0009 | 0.0053 | 0.0193 |
| ③ 체인보존 | turn | −0.0010 | 0.0055 | 0.0201 |
| ③ 체인보존 | river | −0.0007 | 0.0039 | 0.0151 |

`|Δ|` 중앙 0.081 에 부호 중앙은 −0.008. **크기는 큰데 방향이 없다** — 잡음이다.

그리고 이 0.081 은 사이즈 인식 차이에서 오는 게 **아니다.** 같은 표본에서
`sz_seen(9) − sz_seen(1)` 중앙은 −0.0019 뿐이고, 그 차이는 리버에서도 똑같다
(−0.0017). 그런데 리버 `|Δneed|` 는 0.0019 다. 차이를 만드는 것은 인식값이
아니라 **덮어쓰기가 켜지느냐 꺼지느냐** 하나다 (1절 표).

### 벳 크기별 방향

| 구간 | n | ① 콜 1→9 | ③ 콜 1→9 |
|---|---|---|---|
| 작은 벳(≤0.55) | 125 | 40.0% → 40.8% | 40.0% → 39.2% |
| 표준(0.66~0.75) | 128 | 42.2% → 43.0% | 42.2% → 43.8% |
| 큰 벳(≥1.0) | 147 | 30.6% → 34.7% | 30.6% → 33.3% |

큰 벳에서만 +4.1%p 로 눈에 띄는데, 두 변종이 같은 방향·비슷한 크기(+4.1 / +2.7)라
덮어쓰기 고유 효과라고 보기 어렵다. 나머지 두 구간은 ±1%p 로 노이즈 범위다.

### 인지한 사이즈를 크게 읽는가 작게 읽는가

`sizing_tell=9` 일 때 실제 대비: 작게 읽음 211건 / 크게 읽음 145건 / 그대로 44건.
큰 벳(≥1.0)에서 특히 작게 읽는 쪽으로 기운다(88 vs 37). `opp_size_norm` 의
`adj = size_frac/(1+w*0.40*max(0, size_gap))` 가 `size_gap ≥ 0` 일 때만 나눗셈을
하기 때문이다 — 구조적으로 축소 편향이 있다.

### 충돌하면 누가 이기는가 — **2×2 요인**

플랍, 셀 값은 콜 비율, n=400:

| ① 덮어쓰기 | range_read=1 | range_read=9 | | ③ 체인보존 | range_read=1 | range_read=9 |
|---|---|---|---|---|---|---|
| st=1 | 41.8% | 35.8% | | st=1 | 41.8% | 35.8% |
| st=9 | 44.5% | 35.0% | | st=9 | 42.5% | 36.0% |

```
①  st 뒤집힘  11.5%(rr=1) / 15.5%(rr=9)      rr 뒤집힘 11.5%(st=1) / 14.5%(st=9)
③  st 뒤집힘   1.2%(rr=1) /  0.8%(rr=9)      rr 뒤집힘 11.5%(st=1) / 13.0%(st=9)

①  st 뒤집힘  14.5%(po=1) / 12.8%(po=9)      po 뒤집힘  9.2%(st=1) /  2.2%(st=9)
③  st 뒤집힘   0.5%(po=1) /  1.5%(po=9)      po 뒤집힘  9.2%(st=1) /  8.2%(st=9)
```

- `range_read` 는 `sizing_tell` 에 안 눌린다. 두 변종 모두 11~15% 를 유지한다.
  둘 다 `read_opponent` 를 타서 같은 방향(레인지를 세게 읽으면 폴드)이라 충돌이 적다
- **`potodds` 는 눌린다.** ① 에서 `sizing_tell=1` 일 때 9.2% 이던 것이
  `sizing_tell=9` 에서 **2.2%** 로 떨어진다. ③ 에서는 9.2% → 8.2% 로 유지된다.
  1절 그대로다 — `sizing_tell` 이 켜지면 덮어쓰기가 걸리고, `calc_noise(potodds)`
  가 그 순간 지워진다. **"팟오즈 계산 오차"라는 개념이 "사이즈를 잘 읽는 사람"
  에게서만 사라진다.**

---

## 결론

1. `size_read` 는 2팟 이하에서 항등이고, 실전 648건 중 1팟을 넘은 벳이 0건이다.
   **`sizing_tell` 의 이름값을 하는 경로는 작동한 적이 없다.**
2. 실제로 작동하는 경로는 `see_size → size_gap → opp_size_norm` 이고, 그 효과는
   "사이즈를 다르게 본다"(중앙 0.0019)가 아니라 **"덮어쓰기를 켠다"**(0% ↔ 89%)다.
3. 그 결과 `sizing_tell` 이 높을수록 `potodds` 개념이 죽는다(9.2% → 2.2%).
   **개념이 높은 사람일수록 개성이 지워진다** — 설계 의도의 반대다.
4. 리버는 `size_river` 가 `see_size` 게이트를 빠져나가서 `sizing_tell` 과 무관하게
   모두가 체인을 잃는다.

## 이번 라운드에서 하지 않는 것

- `size_read` 의 `2.0` 문턱, `see_size` 의 `(v-2)/6`, `trust` 블록의 `0.10` 계수 —
  전부 임계값이다. 재발명 금지(작업원칙 2).
- `size_river` 게이트 누락도 고치지 않는다. `_sz_seen` 구조 결정과 같이 봐야 한다.
- ③ 을 production 에 넣지 않는다. `sizing_tell` 의 적정 영향력은 별도 설계 문제다.

## 다음 판단에 필요한 재료는 이제 다 나왔다

- 2-A 의 8~18% 행동 변화는 전부 체인 삭제분이다 (`TRACE_SZSEEN.md`)
- 그 삭제를 켜는 스위치가 `sizing_tell` 이다 (이 문서)
- `sizing_tell` 이 그 스위치 말고 하는 일은 `need` 를 0.002 움직이는 것뿐이다
