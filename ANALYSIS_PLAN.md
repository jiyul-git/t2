# ④ 본 측정 분석 설계 (사전 등록)

`entries=24 / hpl=12 / start_stack=30000 / seed=55 / 기준 ≥3`.
**측정 전에 확정한다.** 결과를 보고 고치지 않는다.

---

## 0. 성공 기준을 먼저 못 박는다

**`|ρ| = 0.20` 은 합격선이 아니다.**

```
사전 설계   "55시드면 ρ=0.20 인 효과를 90% 수준으로 검출할 수 있다"
본 측정     "실제로 각 축에서 얼마의 ρ 가 관측됐고, 방향과 재현성은 어땠는가"
```

실측이 0.31 이면 0.31 을, 0.12 면 0.12 를 그대로 보고한다.
"ρ ≥ 0.20 이면 성공" 같은 합격/불합격 판정을 내리지 않는다.

축마다 **다섯 값을 항상 함께** 싣는다.

```
ρ (점추정)  ·  95% CI  ·  Holm 보정 p  ·  split-half r  ·  n
```

`r` 없이 `ρ` 만 보고하지 않는다 — 둘의 조합이 해석을 정한다.

---

## 1. PRIMARY 7축의 검정 단위

### 1-1. 축 값

**설계값(무틸트)** 을 쓴다. `fieldsim.Field.players[pid]['prof']` 의
`concepts`/`temper` 원본이다.

`play.Hand.axes` 가 돌려주는 `PS.tilted_view` 값은 **쓰지 않는다.**
성향은 설계이고 틸트는 상태다. 틸트가 섞이면 "이 사람의 성향"이 아니라
"이 사람이 그 순간 보인 상태"를 재게 된다.

### 1-2. 행동 확률 `f` 의 집계

플레이어별 **산술평균**. 그 축의 분모 안 관측만 쓰고 최소 3건.

```
기준        ≥3 (기본)        민감도로 ≥2 / ≥4 를 함께 낸다
집계        평균 (기본)      민감도로 중앙값을 함께 낸다
```

평균을 기본으로 두는 이유는 split-half `r` 을 평균으로 추정했기 때문이다.
집계 방식을 바꾸면 `r` 도 다시 재야 한다.

### 1-3. 축당 하나의 검정 — 지표가 둘인 축의 처리

`looseness` 는 `open_pct` 와 `defend_tot` 둘에 나타난다. **두 개의 독립
가설로 세지 않는다.**

```
H₁ : looseness 가 증가할수록 프리플랍 참가·디펜스가 증가한다
     조작적 측정 둘 → 플레이어별로 각각 z 표준화한 뒤 평균해 단일 결과변수
```

두 지표의 상관을 함께 보고한다. 낮으면 평균이 부적절하다는 신호다.

나머지 여섯 축은 지표가 하나씩이다.

```
축                 결과변수
looseness          z(open_pct) 와 z(defend_tot) 의 평균
aggression         defend_tp
discipline         이탈 지속벳 f (cbet_dev 분기)
bluff              블러프 분기 f
thin_value_turn    밸류 분기 f, 플랍·턴, rel<0.85
potcontrol         pot_control 라우팅 비율
cbet_flop          이탈 지속벳 f, 플랍
```

### 1-4. 군집(시드)을 반영한 추론

**점추정** — 전체 플레이어를 모아 Spearman ρ.

**신뢰구간·p 값** — **클러스터 부트스트랩**. 시드를 복원추출해
10,000회 재계산하고 백분위 CI 를 낸다. 검정력 계산이 이 구조를
가정했으므로 추론도 같은 구조로 한다.

실측 ICC 가 0.000~0.064 로 작아 군집 효과는 미미할 것이다. 그래도
**작다고 가정하지 않고 실제로 부트스트랩한다.**

Fisher z + 군집보정 SE 는 정규성을 가정하므로 쓰지 않는다.

### 1-5. Holm

**정확히 7개** p 값에 적용한다. α = 0.05.

```
정렬 후 i번째 문턱 = α/(7−i+1)
```

CONDITIONAL 8축과 SECONDARY 묶음은 **이 family 에서 분리**하고
exploratory 로 명시한다. 보정을 섞지 않는다.

---

## 2. 잠재 3요인 통제

### 2-1. 방법

순위 잔차 편상관.

```
rank(X)  를 rank(study), rank(aggro), rank(exp) 에 회귀 → 잔차 X⊥
rank(Y)  를 같은 셋에 회귀              → 잔차 Y⊥
편상관 = Pearson(X⊥, Y⊥)
```

잠재요인은 `prof['latent']` 에 그대로 있다. 실제 관측 연구와 달리
**정확히 통제할 수 있다.**

### 2-2. 세 값을 나란히 싣는다

```
단변량 ρ        전체 관측 연관성
잠재3 통제 편상관  잠재요인으로 설명되지 않는 추가 연관성
축 고유 분산      생성모델에서 이 축이 갖는 독립 정보의 몫 (1 − R²)
```

세 번째가 두 번째의 상한을 정한다.

### 2-3. `aggression` 의 해석을 미리 박는다

고유 분산이 **0.107** 이다. 잠재를 통제하면 남는 것이 거의 없다.

편상관이 0 에 가깝게 나오면 결론은 **"`aggression` 이라는 이름의 축이
사실상 잠재요인 `aggro` 를 측정한다"** 이다.
**"`aggression` 이 행동을 못 만든다" 가 아니다** — `decide_aggression` 의
밸류 분기(`0.30 + 0.058·aggression`)와 `pot_control` 분기
(`0.18 + 0.035·aggression`)에 직접 계수로 들어가므로 전달 경로는 굵다.

이 문장을 결과를 보기 전에 적어둔다. 나중에 유리한 쪽으로 해석을
고르지 않기 위해서다.

---

## 3. `cbet_flop` 특별 처리

현재 `r` = .744 인데 ρ = +0.131 이다. **같은 분기에 다른 축이 동시에
들어간다.**

```
cbet_freq(plan.py:1169)
    base × (0.45 + 0.11·cbet_flop)
        + 0.035·aggression + 0.020·bluff
    × (0.80 − 0.30·multiway)^(n_opp−1)
    × [1 + (텍스처배수 − 1)·board_texture]
    × range_adv 보정
decide_aggression(plan.py:866)
    × max(0.05, 1 − 0.085·discipline)
```

### 3-1. 통제 집합 둘

**(A) 성향 축 통제** — 같은 식에 들어가는 축 전부.

```
잠재3 + discipline + aggression + bluff + multiway + board_texture
```

**(B) 상황 통제** — 플레이어별 상황 평균.

```
평균 n_opp · OOP 비율 · 평균 rel · 평균 range_adv
```

(B)가 핵심이다. `r` 이 높다는 것은 "개인 간 차이가 반복된다"는 뜻이지
**그 차이가 성향에서 왔다는 뜻이 아니다.** 어떤 플레이어가 계속 다인원
팟·젖은 보드를 배정받으면 `f` 가 일관되게 낮다.

```
r 높음 + ρ 낮음 + (B) 통제 후에도 변화 없음   → 축이 판단을 안 움직인다
r 높음 + ρ 낮음 + (B) 통제 후 ρ 상승          → 상황 배정이 가리고 있었다
```

### 3-2. 레버 폭을 함께 보고한다

```
cbet_flop   0.45 + 0.11·sk    평균 5.54 sd 1.53 → ±1.5sd 에서 0.81~1.31  (1.6배)
discipline  1 − 0.085·disc    평균 5.43 sd 2.08 → ±1.5sd 에서 0.80~0.28  (2.9배)
```

`discipline` 의 레버가 1.8배 넓다. ρ 비교만으로 "`cbet_flop` 이 약하다"고
하면 레버 폭 차이를 성향 차이로 오독한다.

### 3-3. (B) 상황 통제는 PRIMARY 전 축에 적용한다

`cbet_flop` 만 하면 그 축에만 유리한 처리를 한 셈이 된다.
**같은 통제를 7축 전부에 걸고 통제 전후를 모두 보고한다.**

---

## 4. 위약 바닥

`tilt_swing` · `tilt_stack` 을 같은 파이프라인에 태운다.
`plan.py` 가 읽지 않는 축이므로 ρ 가 0 근처여야 한다.

**0 이 안 나오면 도구가 틀린 것이고, 결과를 해석하지 않는다.**

순열 귀무(축 값을 플레이어에 무작위 재배정)도 함께 돌려 ρ 의 귀무분포를
만든다. 위약 축과 순열 귀무가 둘 다 0 근처여야 한다.

---

## 5. 보고 형식

축마다 한 줄:

```
축  n  ρ  [95% CI]  Holm p  split-r  |  편상관(잠재3)  편상관(+성향축)  편상관(+상황)
```

그리고 축마다 한 문장으로 **방향·크기·재현성**을 적는다.
합격/불합격으로 적지 않는다.

CONDITIONAL 8축은 같은 표를 별도 family 로 내고 **exploratory** 를
제목에 박는다.

---

## 6. 사전 등록 고정 목록

아래는 결과를 본 뒤 바꾸지 않는다.

```
측정조건      entries=24 / hpl=12 / start_stack=30000 / seed=55
PRIMARY       7축, 축당 하나의 주가설
결과변수      1-3 표 그대로
최소 기회     ≥3 (민감도 ≥2/≥4)
집계          평균 (민감도 중앙값)
축 값         무틸트 설계값
추론          클러스터 부트스트랩 10,000회
다중비교       Holm α=0.05, 정확히 7개
통제          잠재3 / +동일식 성향축 / +상황, 셋 다 보고
최소 의미 효과 |ρ|=0.20 — 합격선이 아니라 설계 기준
위약          tilt_swing · tilt_stack + 순열 귀무
```
