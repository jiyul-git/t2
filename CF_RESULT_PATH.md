# ⑦ 결과 — 축 서열은 경로의 어느 단계에서 생기는가

설계는 `CF_DESIGN_PATH.md`. **가설과 반증 조건은 `d6fae23`, 측정 정의는
`2b768af` 로 측정 전에 고정했고 이후 수정하지 않았다.**
`rows55b` 만 썼다. 새 시뮬레이션 없음.

> **네 가설 전부 기각됐다.**

---

## 1. 판별 통계량 (D 팔, 비교 축 집합 A)

```
축                          R          S        T     B(N)     flip
aggression           51.880%    0.18160    0.159        5     9.2%
discipline           13.510%    0.32004    0.320        1     4.3%
bluff                19.544%    0.18362    0.161        3     3.5%
thin_value_turn       9.389%    0.28880    0.284        1     2.7%
cbet_flop             7.050%    0.14582    0.139        1     1.0%

G3 공통 표본수 N = 2,198  (A 중 최소 Δp≠0 건수 = cbet_flop)
모집단 = 31,176 (전 축 동일)
```

A 밖 두 축은 예상대로다.

```
potcontrol  D   Δp≠0 = 0       looseness  D   Δp≠0 = 0
```

---

## 2. 순서 판정 — 완전 일치만 "일치"

```
flip   aggression > discipline > bluff > thin_value_turn > cbet_flop
R      aggression > bluff > discipline > thin_value_turn > cbet_flop   어긋남   ρ=+0.900
S      discipline > thin_value_turn > bluff > aggression > cbet_flop   어긋남   ρ=+0.300
T      discipline > thin_value_turn > bluff > aggression > cbet_flop   어긋남   ρ=+0.300
B      동점 1×3 → 엄격 순서와 완전 일치 불가                            어긋남
       값: aggression 5  discipline 1  bluff 3  thin_value_turn 1  cbet_flop 1
```

ρ 는 n=5 라 **참고 수치이고 판정에 쓰지 않았다** (설계 2-0-4).

`B` 는 동점이 셋이라 엄격 순서를 만들 수 없다. **동점 때문만이 아니라
내용으로도 어긋난다** — flip 은 `discipline > bluff` 를 요구하는데
`B` 는 `discipline 1 < bluff 3` 이다.

```
비 (A 위 max/min)   R 7.359    S 2.195    T 2.299
```

---

## 3. 채점

| 가설 | 판정 | 근거 |
|---|---|---|
| G1 도달 지배 | **기각** | `S` 비 2.195 ≥ 2.0 이고 `R` 순서도 어긋남 — 두 조건 모두 실패 |
| G2 크기 지배 | **기각** | `R` 비 7.359 ≥ 2.0 이고 `S` 순서도 어긋남 — 두 조건 모두 실패 |
| G3 분기 다양성 | **기각** | `B` 순서 어긋남 |
| G4 하류 전달 | **기각** | `T` 비 2.299 ≥ 1.5 는 통과하고 `R` 순서 불일치도 통과했으나, **`T` 순서가 어긋남** |

**네 가설 전부 기각이다.**

---

## 4. 숫자에서 직접 확인되는 것

해석이 아니라 표에 그대로 있는 것만 적는다.

**`R` 이 flip 에 가장 가깝지만 완전 일치는 아니다.** `discipline` 과
`bluff` 가 뒤집힌다. ⑥ 에서 이미 보인 그 자리이고, 이번에 잠근 기준으로도
같다.

**`S` 와 `T` 의 순서가 서로 완전히 같다.** 다섯 축 전부 같은 순위다.
두 통계량이 독립적인 정보를 주지 않는다.

**`aggression` 은 `R` 1위인데 `S`·`T` 는 4위다.** 도달은 압도적으로 많고
(51.9%) 한 번 도달했을 때의 폭과 전달률은 A 중 하위다.

**`discipline` 은 `S`·`T` 1위인데 `R` 은 3위다.** `aggression` 과 정확히
반대 모양이다.

**`cbet_flop` 은 네 통계량 전부 최하위다.** `R`·`S`·`T`·`B` 모두.

**`B` 는 다섯 축 중 셋이 1 이다.** `cbet_flop`·`thin_value_turn`·
`discipline` 이 공통 표본 2,198개 안에서 단 한 종류의 분기에서만
`Δp≠0` 을 낸다. `aggression` 이 5, `bluff` 가 3 이다.

---

## 5. 해석하지 않는 것

```
네 통계량을 가중합해 flip 서열을 재현하는 식을 만들지 않는다
  ⑥ 4절과 ⑦ 설계 4절의 금지가 그대로 유효하다. G1~G4 는 어느 인자가
  변하는가를 가르는 가설이었고, 전부 기각됐다는 것이 결과다

R 의 ρ=+0.900 을 "거의 맞았다" 로 읽지 않는다
  판정 기준은 완전 일치이고 측정 전에 그렇게 잠갔다. n=5 에서 ρ 는
  참고 수치다

S 와 T 의 순서가 같다는 것에서 인과를 읽지 않는다
  두 통계량이 같은 순위를 냈다는 관측이다

기각된 가설을 변형해 다시 세우지 않는다
  설계 6절에 "남는 가설을 급히 만들지 않는다" 를 측정 전에 적었다
```

---

## 6. ⑦ 의 결론

> **S1~S8 중 단일 단계의 단독 효과로는 관측된 축 서열을 설명할 수 없다.**

⑥ 이 후보 다섯을 각각 기각했고, ⑦ 이 경로 단계 네 개를 각각 기각했다.
**도달률·조건부 크기·분기 다양성·하류 전달률 중 어느 하나도 서열과 일치
하지 않는다.**

### 6-1. 이 결론이 **아닌 것**

```
아니다   "S1~S8 의 구조적 경로 전체가 축 서열과 무관하다"
맞다     "단일 후보 / 단일 단계의 단독 효과로는 설명할 수 없다"
```

기각된 것은 **각 가설이 단독으로 서열을 재현한다는 주장**이다. 경로가
서열과 무관하다는 것은 측정한 적도 없고 주장하지도 않는다.

`R` 이 한 쌍(`discipline` ↔ `bluff`)만 어긋난 채 가장 가깝다는 것은
사실이지만, **사전에 고정한 기준으로는 기각이다.**

### 6-2. G4 기각의 의미

`G4` 가 기각됐다는 것은 `p` 까지 차이가 만들어지는 과정뿐 아니라 **그
차이가 최종 `act` 로 전달되는 비율(`T`)도 서열을 그대로 설명하지 못한다**는
뜻이다. 상류와 하류 양쪽에서 각각 기각됐다.

### 6-3. 현재 논리 상태

```
⑥ 후보 원인       H1 · H2 · H3 · H4 · H5-A · H5-B    각각 단독 설명 실패
⑦ 경로 단계       G1 · G2 · G3 · G4                  각각 단독 설명 실패
                         ↓
"단일 후보 / 단일 단계의 단독 효과로는 관측된 flip 서열을 설명할 수 없다"
```

**여기서 G1~G4 를 조합해 새 식을 만들지 않는다.** 결과를 보고 "사실은
G1+G4 였다" 로 넘어가면 ⑥ 에서 금지한 사후 결합식과 같은 문제가 된다.
다음 가설은 이전 결과를 조합하지 않고 **새로 설계한다.**

---

## 7. 미검증으로 남기는 것

```
M·T 팔에서의 같은 판별 — 참고 출력만 했고 채점하지 않았다 (설계 2-0-1)
B 의 동점 처리 — 설계가 동점을 명시하지 않았다. 이번에는 "엄격 순서와
  완전 일치 불가 → 어긋남" 으로 처리했고, 내용으로도 어긋나 결론은 같다
⑥ 에서 남긴 것들 (H2 를 서열과 대조하기, trace 40 상한 등)
```

## 8. 재현

```
python3 tools/path_test.py rows55b.jsonl.gz
```

`rows55b.jsonl.gz` 는 `CF_RESULT_NORM.md` 8절의 명령으로 다시 만든다.
