# ⑦ 설계 — 축 서열은 경로의 어느 단계에서 생기는가

**설계 문서다. 아직 아무것도 측정하지 않았다.**
기준점은 `67d486f` (⑥ 종료).

---

## 0. 질문이 바뀌었다

⑥ 에서 후보 다섯을 각각 쟀고 **단독으로 서열을 설명하는 것이 하나도
없었다** (`CF_RESULT_ORDER.md`). 그래서 질문을 바꾼다.

> 축이 입력에서 최종 행동까지 가는 **경로의 어느 단계**에서 축 사이의
> 차이가 생기는가?

경로는 이렇다.

```
S1 축 입력        persona 값
S2 파생 변환      derive / bias / street_concept
S3 분기 도달      그 축을 읽는 분기에 상황이 도달하는가
S4 계수 적용      p 식에서의 기울기
S5 게이트·클램프
S6 roll < p 판정
S7 size 단계
S8 최종 act
```

⑥ 은 **단계별로** 잰 것이 아니라 **후보별로** 쟀다. H5-A 는 S1, H3 는 S4,
H4 는 S5, H1 은 S3 의 정적 상한, H5-B 는 S4~S5 를 합친 출력(`p`)이다.
**S3 를 실측으로 잰 적이 없다.**

---

## 1. 이미 손에 있는 구조적 단서

**새 측정이 아니라 ⑥ 의 결과다.** 가설의 출발점으로만 쓴다.

```
aggression D     Δp≠0 51.9%   방향 16,174↑ / 0↓        완전 단조
discipline D     Δp≠0 13.5%   방향      0↑ / 4,212↓    완전 단조 (반대 방향)
bluff      D     Δp≠0 19.5%   방향  6,093↑ / 0↓
potcontrol D     Δp≠0  0.0%   실행층 p 에 아예 도달하지 않는다
looseness  D     Δp≠0  0.0%   같음
thin_value_turn M · cbet_flop M   0.0%   그 팔에 소비처가 없다
```

여기서 나오는 **가설**(결론이 아니다):

> 축의 영향력은 계수 크기나 입력 분산만으로 정해지지 않고, 그 축이 실제
> 의사결정 경로에서 **어떤 변환·소비 구조를 통과하느냐**에 의존할 수 있다.

이 문장은 그대로는 검증할 수 없다. 아래에서 판별 가능한 형태로 쪼갠다.

---

## 2. 측정 정의 — 측정 전에 잠근다

가설을 적기 전에 용어를 고정한다. **아래 정의는 결과를 보고 바꾸지 않는다.**

### 2-0-1. 모집단과 팔

```
모집단   rows55b 에서 attached_O == True 이고, 그 팔의 양쪽 tag 가
         정렬(shift 없음)·불변량(inv) 을 통과한 쌍
팔       D. 설명하려는 대상이 L1 POST flip 이고 그 대응이 L2 D 이기 때문이다
         M·T 는 참고로만 같이 출력하고 가설 채점에 쓰지 않는다
쌍       같은 decision unit 의 (lo, hi) 두 반사실. ⑥ 과 같은 단위다
```

### 2-0-2. 비교 축 집합

```
Δp≠0 건수가 0 인 축은 조건부 크기가 정의되지 않는다.
D 팔에서 potcontrol · looseness 가 그렇다 (⑥ 실측 0.0%).

**비교 축 집합 A = {aggression, discipline, bluff, thin_value_turn, cbet_flop}**
potcontrol · looseness 는 표에 함께 싣되 비·순서 판정에서 제외한다.
```

대조할 관측 서열(⑥, 고정):

```
flip 순서   aggression 9.2% > discipline 4.3% > bluff 3.5%
            > thin_value_turn 2.7% > cbet_flop 1.0%
```

### 2-0-3. 기본량

```
Δp        = p_D_hi − p_D_lo                        쌍마다 하나
도달        pair 가 Δp ≠ 0 인가.  **부동소수 동등 비교를 그대로 쓴다**
            (p 는 같은 식에서 나온 값이라 개입이 없으면 비트까지 같다)
R(ax)     = |{Δp ≠ 0}| / |모집단|                  도달률
S(ax)     = mean(|Δp|)  given  Δp ≠ 0              조건부 크기
            **평균을 쓴다.** ⑥ 에서 median 이 0 편중으로 퇴화했으므로
            조건부 분포에도 median 을 쓰면 같은 위험이 있다
act≠      = act_D_lo ≠ act_D_hi                    ⑥ 의 L2실제flip 과 같은 정의
T(ax)     = |{act≠}| / |{Δp ≠ 0}|                  전달률
```

**`act≠` 는 `act_L2` vs `act_L1` 이 아니라 같은 팔의 `lo` vs `hi` 다.**
설명하려는 L1 POST flip 자체가 `act_lo != act_hi` 로 정의돼 있어
(`cf_axis.py:354`) 같은 축으로 맞춘 것이다.

### 2-0-4. 비(ratio)와 순서 판정

```
비        비교 축 집합 A 위에서의 **max / min**.
          예: R비 = max(R(ax) for ax in A) / min(R(ax) for ax in A)
          인접 순위 간 비가 아니다

순서 일치  A 의 다섯 축을 그 통계량으로 정렬한 순위 벡터가
          flip 순위 벡터와 **완전히 같을 때**만 "일치" 다
순서 어긋남 완전 일치가 아니면 전부 "어긋남" 이다. 한 쌍만 뒤집혀도 그렇다
```

Spearman ρ 는 **참고 수치로만 출력한다.** n=5 라 판정에 쓰지 않는다.

---

## 2-1. 경쟁 가설 — 판별 예측을 측정 전에 적는다

`mean(|Δp|)` 는 항등적으로 **도달률 × 조건부 크기** 로 분해된다.

```
mean(|Δp|) = P(Δp ≠ 0) × E[|Δp| given Δp ≠ 0]
```

**이것은 항등식이지 설명 모형이 아니다.** G1·G2 는 두 인자 중 **어느 쪽이
축 사이에서 변하는가**를 가르는 가설이다.

### G1 — 도달이 지배한다 (S3)

> 축 사이의 차이는 주로 "그 축을 읽는 분기에 얼마나 자주 도달하는가"에서
> 생기고, 한 번 도달했을 때의 크기는 축 사이에 비슷하다.

**판별 예측 (측정 전 고정)**

```
S 비 = max S(ax) / min S(ax)  <  2.0        (ax ∈ A)
그리고
R 순서가 flip 순서와 **완전 일치**
```

**반증 조건** — 조건부 크기 비가 2.0 이상이거나, 도달률 순서가 flip 순서와
어긋나면 G1 은 기각된다.

**이미 알고 있는 것** — `Δp≠0` 순서는 `aggression` 51.9% > `bluff` 19.5% >
`discipline` 13.5% 인데 flip 순서는 `aggression` > `discipline` > `bluff` 다.
**가운데 둘이 이미 뒤집혀 있다.** G1 은 이 상태에서 출발한다.

### G2 — 크기가 지배한다 (S4·S5)

> 도달률은 축 사이에 비슷하고, 한 번 도달했을 때 `p` 가 움직이는 폭이
> 축을 가른다.

**판별 예측**

```
R 비 = max R(ax) / min R(ax)  <  2.0        (ax ∈ A)
그리고
S 순서가 flip 순서와 **완전 일치**
```

**반증 조건** — 도달률 비가 2.0 이상이면 기각. `aggression` 51.9% vs
`cbet_flop` 7.1% 로 이미 7.3배다. **G2 는 사실상 이 자리에서 기각될
가능성이 높지만, 예측을 적어두고 그대로 확인한다.**

### G3 — 분기 다양성 (S3 의 구조)

> 서열은 도달 **횟수**가 아니라 도달하는 **분기의 가짓수**를 따른다.
> 여러 종류의 상황에서 읽히는 축이 더 넓게 행동을 바꾼다.

**판별 예측**

```
B(ax) = 동일 표본수 N 에서의 nunique(branch)
B 순서가 flip 순서와 **완전 일치**
```

**반증 조건** — `B` 순서가 flip 순서와 어긋나면 기각.

#### 측정 프로토콜 — 결정론. 난수를 쓰지 않는다

```
1  모집단      각 축의 D 팔에서 Δp ≠ 0 인 쌍
2  branch     정규화한 src_O 문자열.
              re.sub(r'\(\d+%\)', '(N%)', src_O)
              **정규화가 필수다** — '밸류 계획 실행(75%)' 처럼 확률이
              문자열에 박혀 있어 정규화 없이는 가짓수가 부풀려진다
3  N          N = min over ax ∈ A of |{Δp ≠ 0}(ax)|
              (⑥ 실측상 cbet_flop 이 최소일 것으로 보이나 측정 시점에 정한다)
4  표본 선택   무작위 추출을 하지 않는다. 각 축에서 키
              (seed, hand, table, pid, street) 오름차순 정렬 후 **앞 N 개**
              를 쓴다. 정렬 키는 유일하다 (⑥ 에서 261,968행 전부 유일 확인)
5  통계량      B(ax) = 그 N 개에서의 nunique(branch)
6  재현성      난수가 없으므로 같은 원자료에서 항상 같은 값이 나온다.
              반복 추출·평균을 내지 않는다
```

**표본을 줄이지 않은 전수 `nunique` 도 함께 출력한다.** 다만 **채점은
`B(ax)` 로만 한다** — 전수 값은 표본 크기에 오염돼 있다.

### G4 — 하류 전달률 (S6~S8)

> `p` 까지의 차이는 축 사이에 비슷하고, `roll` 판정과 `size` 단계가
> 축마다 다르게 흡수해서 최종 서열이 만들어진다.

**판별 예측**

```
T(ax) = |{act_D_lo ≠ act_D_hi}| / |{Δp ≠ 0}|        (2-0-3 정의)

T 순서가 flip 순서와 **완전 일치**
그리고
R 순서가 flip 순서와 **완전 일치가 아니다**
```

두 조건을 **모두** 만족해야 G1 이 아니라 G4 다. 뒤 조건이 없으면 G1 과
구별되지 않는다.

**반증 조건** — `T 비 = max T / min T < 1.5` 이면 기각. 전달률이 축 사이에
거의 같다는 뜻이고, 그러면 S6~S8 은 서열을 만들지 않는다.

**`act≠` 의 정의는 2-0-3 에 있다** — `act_L2` vs `act_L1` 이 아니라 같은
팔의 `lo` vs `hi` 다. 설명 대상인 L1 POST flip 이 그 정의이기 때문이다.

**이미 알고 있는 것** — ⑥ 의 2×2 에서 D 팔은 `cx=·act≠` 가 전 축 0 이었다.
`size` 단계가 D 팔에서 flip 을 **만들지는** 않는다. 지우기만 한다.

---

## 3. 측정 대상과 자료 — 새 시뮬레이션이 필요한가

필드 가용성만 확인했다. **통계량은 계산하지 않았다.**

```
가설   필요한 것                                   기존 자료
G1     p_{D,M,T}_{lo,hi} · attached_O · shift · inv   rows55b  전부 있음
G2     같음                                          rows55b  전부 있음
G3     src_{D,M,T}_{lo,hi} · src_O · plan_O           rows55b  전부 있음
G4     act_·size_·p_ 전부                             rows55b  전부 있음
층화   street · plan_O · pid · seed · hand · table     rows55b  전부 있음
```

**네 가설 전부 `rows55b` 만으로 검증 가능하다. 새 시뮬레이션이 필요 없다.**

`collected.jsonl` 은 `spr`·`oop`·`init`·`n_opp`·`behind`·`danger` 까지 있어
상황 층화에 쓸 수 있지만 **다른 실행이다.** `rows55b` 와 섞지 않는다.

---

## 4. 이 단계에서 하지 않을 것

```
⑥ 의 H1~H5 를 변형해 다시 검증하지 않는다 — 닫혔다
0.37~0.42 를 L1↔L2 대응 근거로 쓰지 않는다 (CF_RESULT_NORM.md 0절)
서열에 맞는 사후 결합식을 만들지 않는다 (CF_RESULT_ORDER.md 4절)
  G1~G4 는 **어느 인자가 변하는가**를 가르는 가설이지, 인자들을 가중합해
  서열을 재현하는 모형이 아니다. 계수를 적합하지 않는다
새 설명변수를 끌어오지 않는다 — S1~S8 밖의 것을 도입하지 않는다
Level 1·2 와 ⑥ 의 숫자를 수정하지 않는다
```

**`E[|Δp| given Δp≠0]` 는 ⑥ 에서 의도적으로 계산하지 않은 통계량이다**
(`CF_RESULT_ORDER.md` 2절 H5-B). ⑦ 에서 처음 쓰는 것이고, **그 사용을
측정 전인 이 문서에 적어둔다.**

---

## 5. 순서

```
1  d6fae23    가설·반증 조건을 적었다 (방향·임계값 확정)
2  이 커밋     측정 정의를 잠갔다 — 2-0-1 ~ 2-0-4, G3 프로토콜, G4 act≠ 정의
              **가설 방향과 반증 임계값(2.0 / 2.0 / 1.5)은 바꾸지 않았다**
3  네 가설의 판별 통계량을 한 번에 낸다     rows55b 만 사용
4  각 가설을 예측 대비로 채점한다          예측은 2-1 그대로, 수정 금지
5  결과 문서를 쓴다
```

**3 은 2 를 커밋한 뒤에 돌린다.** H5-B 에서 지킨 것과 같은 순서다.

(`6416d98` → `e253f93` 이 같은 패턴이었다.)

---

## 6. 예상되는 결과와 그 처리

네 가설이 전부 기각될 수 있다. 1절에 적었듯 `Δp≠0` 순서가 이미 flip 순서와
가운데 둘에서 어긋나 있고, `size` 단계는 D 팔에서 한 방향으로만 작동한다.

**전부 기각되면 그대로 기록한다.** 남는 가설을 급히 만들지 않는다.
그 경우의 결론은 "S1~S8 의 단일 단계로는 서열이 생기지 않는다" 이고,
그것도 결과다.
