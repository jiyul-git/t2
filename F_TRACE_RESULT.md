# ④ 판단층 f 계측 결과 — `hpl` 결정

`tools/f_trace.py`, `entries=24`, `start_stack=30000`, 시드 8개(5000-5007),
조건마다 플레이어 192명.

---

## 1. 결정: `hpl = 12`

```
축                   ρ(전원) 12→18     ρ(≥4) 12→18      split r 12→18   n(≥4) 12→18
cbet_flop           +0.111 → +0.012   +0.131 → +0.037   0.744 → 0.787    29 → 44
barrel_turn         +0.283 → +0.418   +0.086 → +0.320   1.000 → 0.922     6 → 18
barrel_river        -0.049 → +0.016      -   → -0.410     -   → 0.900     3 →  5
bluff               +0.655 → +0.611   +0.801 → +0.597   0.629 → 0.607    33 → 44
thin_value_turn     +0.317 → +0.422   +0.476 → +0.529   0.640 → 0.735    43 → 65
thin_value_turn<65  +0.465 → +0.452   +0.400 → -0.108   0.800 → 0.857     4 →  7
thin_value_river    +0.173 → +0.364   +0.400 → +0.354   0.200 → 0.364     4 → 12
aggression          +0.250 → +0.300   +0.328 → +0.393   0.548 → 0.647   138 → 149
discipline          -0.643 → -0.687   -0.681 → -0.665   0.854 → 0.843    67 → 90
potcontrol(라우팅)   +0.454 → +0.431        —            0.713 → 0.657   182 → 184
```

**`hpl=18` 은 신뢰도를 사지 않는다.** `r` 이 0.744↔0.787, 0.629↔0.607,
0.854↔0.843, 0.713↔0.657 로 오르내리기만 한다. 사는 것은 `n(≥4)` 1.4배뿐이고,
그것은 시드가 교란 0 으로 산다.

계산 비용도 같다 — eligible 1명당 테이블핸드가 `hpl=12` 105, `hpl=18` 98 이다.
7% 를 위해 유효 스택 +13.6%(59.7 → 67.8bb)를 낼 이유가 없다.
`hpl=12` 는 `standard` 포맷 값이기도 하다.

```
entries     = 24     확정
start_stack = 30000  확정
hpl         = 12     확정
seed        = 미정   검정 설계(다중비교 포함) 후 역산
```

---

## 2. 보고 하한 — `n < 20` 인 ρ 는 싣지 않는다

```
thin_value_turn<65   n=4 에서 +0.400  →  n=7 에서 -0.108
barrel_river         n=5 에서 -0.410
barrel_turn          n=6 에서 +0.086  →  n=18 에서 +0.320
```

부호가 뒤집힌다. **신호가 아니다.** 표에 하한을 걸고 `-` 로 찍는다.

믿을 수 있는 칸만 추리면 (`hpl=12`):

```
축                 ρ(≥4)    r      n     판정
discipline        -0.681  0.854   67    강함, 기대 방향 ↓
bluff             +0.801  0.629   33    강함, 기대 방향 ↑
thin_value_turn   +0.476  0.640   43    중간, 기대 방향 ↑
potcontrol(라우팅) +0.454  0.713  182    중간, 기대 방향 ↑
aggression        +0.328  0.548  138    약중, 기대 방향 ↑
cbet_flop         +0.131  0.744   29    ρ≈0 인데 r 높음
```

---

## 3. `cbet_flop` — 구조 후보이지만 아직 올리지 않는다

두 조건 모두 ρ ≈ 0 (+0.131 / +0.037) 인데 신뢰도는 0.744 / 0.787 로 높다.
정의서 판정표대로면 "축이 판단을 안 움직인다" 후보다.

**아직 결론이 아니다.** 같은 분기에서 `discipline` 이 ρ −0.681 로 지배하고
레버 폭이 다르다.

```
cbet_flop   0.45 + 0.11·sk     평균 5.54 sd 1.53 → ±1.5sd 에서 0.81~1.31  (1.6배)
discipline  1 − 0.085·disc     평균 5.43 sd 2.08 → ±1.5sd 에서 0.80~0.28  (2.9배)
```

`discipline` 의 레버가 1.8배 넓다. n=29~44 에서 `cbet_flop` 효과가 묻혔을
가능성이 충분하다. **잠재3 통제 편상관 + `discipline`·`aggression`·`bluff`
통제**를 해야 갈린다 — 본 측정의 3단 분해가 할 일이다.

---

## 4. 도구에서 잡은 결함 셋

### (1) `id(profile)` 로는 플레이어를 못 잡는다 — 관측 0건

`play.Hand.axes` 가 `PS.tilted_view(base, t)` 로 **매 호출마다 새 dict** 를
만든다(`play.py:93`). 온전성 카운터가 없었으면 "분기가 안 걸린다"로 읽었을
자리다. `profile['id']` 를 쓴다 — `fieldsim` 이 채우고(`fieldsim.py:70`)
`tilted_view` 가 정체성으로 보존한다(`persona.py:616`).

### (2) ρ 와 `r` 을 다른 집단에서 냈다

ρ 는 기회 ≥1 전원(대부분 1건), `r` 은 기회 ≥4 소수. 1건짜리의 "평균 f" 는
상황 노이즈라 ρ 를 누른다. **판정 규칙("ρ 작다 + r 크다")을 정의서에 써놓고
그 규칙이 성립하지 않는 표를 냈다.** 집단을 맞추면 값이 크게 달라진다.

```
             ρ(전원) → ρ(≥4)
aggression   +0.314  → +0.554
discipline   -0.688  → -0.771
bluff        +0.678  → +0.571
```

### (3) 두 축의 분모가 틀렸다

**`potcontrol`** — 분기 안 f 로 재면 안 된다. `pot_control` 분기 확률식은
`rel<0.30 → 0.04`, 아니면 `0.18 + 0.035·aggr` 로 **축이 아예 없다**
(CLAUDE.md: 확률식 `_pc_p` 에 축이 안 들어간다). 분기 안 f 의 ρ −0.446 은
전달이 아니라 **라우팅 선택 효과**였다 — 축이 높으면 약한 핸드까지
`pot_control` 로 가서 0.04 가 늘고 평균 f 가 내려간다.
라우팅 비율로 재니 ρ **+0.454**, `r` 0.713, n=182.

**`thin_value`** — f 가 천장에서 포화한다. `plan.py:945` 의
`if rel >= 0.65` 부터 천장 곡선이 f 를 0.97 로 밀어올려 배수 효과를 지운다.
`rel<0.85`(`plan.py:940` 배수 적용 구간)에서 ρ +0.317, `rel<0.65` 로
좁히면 +0.465. 둘 다 코드에 있는 값이라 새 임계값 발명이 아니다.

---

## 5. 새 이슈 — `blockbet` 기회 0건

8시드 × 2조건, `attach_intent` 관측 11,127건에서 `block` 계획이
**한 번도 나오지 않았다.**

`river_bluff` 0건 때와 같은 모양이다. 그때는 기대 빈도가 0.45건이라
정상이었고, 그 항목에서 세 번 틀렸다. **`block` 의 기대 빈도는 아직
구하지 않았다.** 파이프라인을 계측하기 전에는 버그로도 정상으로도
읽지 않는다.

---

## 6. `block` 계획은 드문 것이 아니라 **도달 불가능**하다

`tools/block_trace.py`, `entries=24 hpl=12`, 시드 8개, `make_plan` 3,631건.

```
S0 포스트플랍 make_plan                   3631
S1 block_p>0 조건 (oop·무이니·0.25<=rel<=0.80)  535  (14.7%)
S2 eq>=pcz 분기 진입                       587  (16.2%)
S3 S1 ∩ S2   ← block opportunity          169  ( 4.7%)
S4 블락벳 메시지 (게이트+굴림 통과)          18  (10.7% of S3)
S5 make_plan 반환 plan==block                0  ( 0.0% of S4)
S6 attach_intent 시점 plan==block            0
```

### 코드가 확정한다 — 통계가 아니다

```
447   if sk('blockbet') >= 1 and rng.random() < block_p:
448       plan = 'block'; why.append('OOP 중간강도 → 블락벳으로 가격 통제')
      ...
456   if sk('potcontrol') >= 1 and rng.random() < _pc_p:
457       plan = 'pot_control'
458   elif rel >= max(0.28, 0.52 - 0.080*_mg) and made >= 1:
459       plan = 'value_2street'
461   else:
465       plan = 'showdown' if made >= 1 else 'giveup'
```

**456/458/461 은 완전한 `if/elif/else` 이고 세 갈래 전부 `plan` 을 대입한다.**
448 을 통과한 `block` 이 살아남는 경로가 **하나도 없다.**
`plan = 'block'` 은 코드 전체에서 448 한 곳뿐이다.

실측이 일치한다. 블락벳 메시지가 남은 18건의 목적지:

```
value_2street 12 · giveup 3 · pot_control 2 · showdown 1    합 18, block 0
```

**네 목적지가 전부 나왔다** — 빠짐없이 덮어쓴다는 직접 증거다.

### 내 진단 정정 — "생존 가능 자리 26%" 는 틀렸다

`rel < max(0.28, 0.52−0.080·_mg)` 이거나 `made == 0` 이면 살아남는다고
계산했는데, **그 조건은 `else` 로 가는 조건**이고 `else` 도
`showdown`/`giveup` 을 대입한다. `value_2street` 를 피하는 것과 `block` 이
살아남는 것을 같은 것으로 봤다. **생존 가능 자리는 0% 다.**

### `river_bluff` 와 성격이 다르다

```
river_bluff   경로가 열려 있고 기대 빈도가 0.45건이었다   → 통계 문제
block         경로가 닫혀 있다                            → 구조 문제
```

`block` 은 기대 빈도 계산 자체가 의미가 없다.

### 연쇄적으로 죽은 코드

```
plan.py:448   plan = 'block'            유일한 생성 지점 — 도달 불가
plan.py:922   if plan == 'block':       decide_aggression 의 block 분기
plan.py:1109  SIZING['block']           flop 0.25 / turn 0.28 / river 0.30
plan.py:1624  _allowed 의 'block': 'blockbet'
persona       blockbet 축               소비처가 위 둘뿐이다
```

`blockbet` 게이트는 병목이 아니다 — 평균 3.73 sd 1.97 이고 `make_plan`
게이트(`PS.sk >= 3.33`)를 **59.4%** 가 통과한다.

**`blockbet` 축은 약한 것이 아니라 관측 불가능하다.** ④에서 제외하되
이유는 "표본 부족"이 아니라 "유일한 소비처가 도달 불가" 다.

### 지금 고치지 않는다

이번 연구 목적은 축의 관측 가능성 평가다. 수정하면 지문이 깨지고
지금까지의 분석 대조가 끊긴다. 기록만 하고 수정 여부는 별도로 결정한다.

---

## 7. 최소 기회 기준 민감도

같은 8시드 데이터. `n` / 반쪽당 관측 중앙값(작은 쪽) / `r`.

```
축                  ≥2              ≥3              ≥4              ≥5              ≥6
                 n   관측   r     n   관측   r     n   관측   r     n   관측   r     n   관측   r
cbet_flop        81  1.0 .647    49  2.0 .735    29  2.0 .744    17  2.0 .672     7  4.0 .393
barrel_turn      46  1.0 .691    19  1.0 .590     6  2.0 1.000    3   -    -      1   -    -
barrel_river     24  1.0 .394     5  2.0 .600     3   -    -      0   -    -      0   -    -
bluff            70  1.0 .555    45  2.0 .655    33  2.0 .629    19  3.0 .730    13  3.0 .577
thin_value_turn 111  1.0 .490    62  2.0 .594    43  2.0 .640    25  3.0 .828    15  4.0 .839
aggression      174  3.0 .520   163  3.0 .544   138  4.0 .548   128  4.0 .543   108  4.5 .577
discipline      114  2.0 .782    92  2.0 .814    67  3.0 .854    53  3.0 .898    39  4.0 .888
blockbet          0   -    -      0   -    -      0   -    -      0   -    -      0   -    -
```

### 기준 완화 비용은 작다 — 대조군이 답을 준다

```
cbet_flop   ≥2 .647  →  ≥3 .735  →  ≥4 .744  →  ≥5 .672  →  ≥6 .393
```

`≥3` 에서 이미 `≥4` 와 같은 수준이고, `≥5`·`≥6` 은 `n` 이 17·7 로 줄어
오히려 떨어진다 — **소표본 노이즈**다. 같은 모양이 `discipline`
(.782→.814→.854), `bluff`(.555→.655→.629), `aggression`(.520→.544→.548)
에서도 보인다.

**`≥3` 을 이번 측정의 기본 분석 기준으로 채택한다. 최종 규칙으로 박지
않는다** — `≥2`/`≥4` 를 민감도로 함께 싣는다.

```
≥2   표본은 많지만 반쪽당 중앙 관측이 1건인 축이 많다
≥3   n 이 줄지만 r 이 대체로 유지된다        ← primary
≥4   일부 축에서 n 이 급격히 감소
≥5/6 n 감소가 커져 r 자체가 소표본화
```

### `barrel_turn` — `≥4 n=6 r=1.000` 은 착시였다

`≥2` 에서 `n=46 r=.691` 로 `cbet_flop` 의 `≥2`(.647)보다 높다.
다만 `≥3` 에서 `n=19` 라 경계선이다. **살렸다고 확정하지 않는다** —
`f` 층이 존재한다는 것과 player-level 분석에 충분하다는 것은 다르다.
시드 역산에 "`≥3` 에서 `n ≥ 20` 안정 확보"를 조건으로 넣는다.

### `barrel_river` — 이번 라운드 제외

`≥2` 에서도 `n=24 r=.394` 다. 같은 `≥2` 에서 `cbet_flop` .647,
`barrel_turn` .691 인 것의 절반 수준이다. `≥3` 은 `n=5` 로 해석 불가.

250시드를 들여 `n` 을 늘리는 것보다, **현재 구조에서 왜 리버 기회가
이렇게 희박한지를 별도 분석 대상으로 남기는** 편이 낫다.

### `r` 이 말하는 것과 말하지 않는 것

```
split-half r   개인별 평균 f 의 순위가 시간적으로 반복되는 정도
```

모두가 같은 f 를 내면 순위가 무작위가 되어 `r` 이 0 에 가까워진다.
그래서 `r` 이 높다는 것은 이미 **개인 간 차이가 존재하고 반복 재현된다**는
뜻이다.

**그러나 그 차이가 성향에서 왔다는 뜻은 아니다.** 어떤 플레이어가 계속
어려운 보드·다인원 팟을 배정받으면 f 가 일관되게 높거나 낮게 나온다.
`r` 은 상황 배정과 성향을 구별하지 못한다.

```
r      측정 안정성        개인 간 차이가 반복되는가
ρ      방향성             그 차이가 축과 정렬되는가
편상관  잔여 축 효과        상황·잠재요인 통제 후에도 남는가
```

`cbet_flop` 의 `r` .744 / ρ +0.131 이 정확히 이 조합이다. **"축이 약하다"로
결론내면 안 된다** — 잠재요인과 상황변수(n_opp·텍스처·rel·range_adv)를
통제한 뒤 판단한다.
