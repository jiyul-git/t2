# ATTACK_FIXTURE_FIX — 결과

사전등록: `ATTACK_FIXTURE_FIX_PREREG.md` (+ Amendment D1, D2)
브랜치: `claude/attack-fixture-fix` · 기준 커밋 `b9e3387`(동결)
도구: `tools/attack_fixture.py` · `tools/attack_read_measure.py` · `tools/attack_read_aux.py`

`tools/qx_ev_fixture.py` **무수정**. 기존 QX / READ_USE / SYNTH 산출물 무수정.
production 무수정. `stat_read` 없음. 새 축 없음.

---

## 판정

```
주 판정 (V3)   ATTACK_READ_NULL      d_A = −0.00166 / −0.00159   ← 통계량이 교락돼 있다
보조 판정 (W3) AUX_READ_NULL         ΔR_A = +0.00150 / +0.00131  ← 깨끗하지만 FLOOR 미만
```

**둘 다 `NULL` 이지만, 표본이 충분한 절반에서는 양쪽 기준을 다 넘는다.**

```
깨끗한 대조, n_obs = 40   ΔR_A  SET_A +0.00292 [+0.00255, +0.00332]
                                SET_B +0.00255 [+0.00216, +0.00295]
깨끗한 대조, n_obs = 6    ΔR_A  SET_A +0.00009   SET_B +0.00007
```

`n = 40` 에서 **FLOOR 0.002 를 넘고 두 집합에서 재현**된다. `n = 6` 에서는 0 이다.
합산이 FLOOR 아래인 것은 fixture 가 두 표본 수준을 **1:1 로 섞기** 때문이다.

라벨과 수치를 분리해서 적는다 — `READ_USE_SEPARATION` 에서 `R4` 라벨이
데이터를 잘못 요약한 일을 되풀이하지 않기 위해 사전등록 4절에 명시한 대로다.

---

## 1. 무엇을 고쳤나

```python
# 옛 qx_ev_fixture (동결)
book()        ftb = 1 − b_true                  r(b_true, ·) = −1.000
ev_actions()  P(fold) = 0.18 + 0.60·b_true      r(b_true, ·) = +1.000   ← 반대 단조

# 새 attack_fixture
ftb_true 를 b_true 와 **독립 격자**로 분리
book()        fold_to_bet / f2b_* = n·ftb_true
ev_actions()  f_bluff = ftb_true + (1−b)·D ,  f_value = ftb_true − b·D
              주변 폴드확률 ≡ ftb_true          (항등, 잔차 1.11e−16)
```

`D = 0.28` 상수. 격자에서 `f_bluff` 최대 0.918, `f_value` 최소 0.110 이라
클램프가 안 걸린다. 약한 손이 더 접는 구조는 유지된다.

**수비 상태는 옛 `State` 를 그대로 썼다.** 수비 경로(`calldown_need`)는 `ftb` 를
읽지 않으므로 결함의 영향이 없고, 그래야 비트까지 대조된다 — 게이트 5.

## 2. 게이트 7/7 (본 측정과 분리 실행)

```
[G1] r(ftb_true, b_true)              +2.83e−19          독립
[G3] r(ftb_true, 오라클 P(fold))       +1.0000   잔차 1.11e−16
[G2] r(ftb_true, 믿는 ftb)            +0.8961
     r(b_true,  믿는 ftb)             −0.0000            ← 옛 결함은 −1.000
[G4] read_opponent w>0   PIPE 100.0% (918·1240 둘 다)  CALC 0.0%
[G4b] r(ftb_true, 벳확률)  PIPE +0.1150   CALC +0.0000
[G5] 수비 재현  0.03323 / 0.02796 / +0.00527   (기준과 소수점 다섯째까지 일치)
[G6] 최적=bet 0.500   ftb 뒤집힘 32/36
```

`G5` 가 결정적이다 — 옛 수비 결과를 **정확히** 재현하므로, 공격 쪽 변화가
fixture 수정 때문이지 내가 모르는 다른 변경 때문이 아니다.

## 3. 주 측정 (사전등록 통계량)

`d = CALC_ONLY − PIPE_ONLY`, 프로필 150, 부트스트랩 4000.

| 채널 | 상태 | SET_A `d` | 95%CI | SET_B `d` | 95%CI |
|---|---:|---:|---|---:|---|
| 공격 | 288 | −0.00166 | [−0.00184, −0.00148] | −0.00159 | [−0.00178, −0.00140] |
| 수비 | 24 | +0.00527 | [+0.00117, +0.00922] | +0.00203 | [−0.00189, +0.00582] |
| 합산(가중) | 312 | −0.00113 | [−0.00143, −0.00083] | −0.00131 | [−0.00165, −0.00098] |

`n_obs` 분리에서 **부호가 뒤집힌다.**

```
공격 n_obs=6    SET_A −0.00396   SET_B −0.00384
공격 n_obs=40   SET_A +0.00063   SET_B +0.00066
```

**합산 값은 상태 수 가중에 크게 의존한다.** 옛 24:48 과 새 24:288 은 다르므로
합산끼리 비교하면 안 된다. 채널별 값이 1차 근거다.

옛 공격 결과 `−0.00200` 은 결함 fixture 의 역사적 값으로 동결했다.
**나란히 적되 비교하지 않는다** — 다른 fixture, 다른 상태다.

## 4. 주 통계량이 교락돼 있다 (Amendment D1)

`n_obs` 부호 반전을 추적하다 찾았다. 내 첫 설명(“`n=6` 양자화로 `ftb=0.56`
수준의 부호가 뒤집혀서”)은 **틀렸다** — 손해가 그 수준에 몰려 있지 않고
네 수준에 고르게 퍼져 있었다(−0.0033/−0.0036/−0.0041/−0.0048).

실제 원인은 arm 구성이다.

```python
PIPE_CONCEPTS   = ('range_read', 'sizing_tell')
PIPE_TEMPER     = ('attention', 'adaptability', 'consistency')
APPLY_ONLY_CALC = PS.CALC 에서 PIPE_CONCEPTS 를 뺀 나머지 전부   # ~20개
HI, LO = 9.0, 2.0
```

두 arm 은 읽기 파이프라인만 다른 것이 아니라 `fold_equity`·`board_texture`·
`multiway`·`potodds`·`spr`·`blocker` 등 **APPLY 개념 다발이 통째로 반대**다.
그리고 `decide_aggression` 과 `cbet_freq` 는 그 개념들을 **상대 추정치 없이
직접** 읽는다.

```
CALC  n_obs=6  p_bet 0.5322     n_obs=40  p_bet 0.5322      차이 +0.00000
PIPE  n_obs=6  p_bet 0.5159     n_obs=40  p_bet 0.5116
PIPE − CALC    n=6  −0.01628          n=40  −0.02059
```

`n=6` 에서 `w ≈ 0.110` 이라 읽기 조정이 거의 없는데도 벳 확률이 이미
**−0.016** 벌어져 있다. 읽기가 만든 차이가 아니다.

`make_arms` 는 `TIER5_AXIS_DESIGN` 에서 **배터리 음성 대조**로 만들어진 것이다.
그것을 "읽기 정확도의 EV 효과" 측정에 쓴 것은 내 통계량 선택 실수다.

**이 교락은 `QX_EV_VALIDATION` 의 합성 대조와 `SYNTH_CHANNEL_SPLIT` 의 채널
분리에도 똑같이 있다.** 다만 `SYNTH` 의 채널 상쇄 산술은 그대로 유효하다 —
같은 수치를 다시 묶은 것이므로. 바뀌는 것은 해석이다:
`PIPE_ONLY` 는 "잘 읽는 사람"이 아니라 **"읽기 축은 높고 계산 축은 낮은 사람"**
이었다.

**`READ_USE_SEPARATION` 은 영향받지 않는다.** 거기서는 같은 프로필의 **믿음만**
블렌드하고 나머지를 비트까지 고정했다. 그 설계가 처음부터 옳았고, `QX_EV` 가
스스로 "가장 약한 고리"라고 적었던 합성 대조가 약했던 **진짜 이유**가 이것이다.

### 순수 읽기 효과의 arm 내부 추정

`CALC` 는 `n_obs` 에 불변이므로 `PIPE` 의 `n` 간 차이는 **개념 교락 없이**
읽기 효과만 잰다.

```
PIPE(n=6) − PIPE(n=40)   SET_A  0.12769 − 0.12310 = +0.00459
                         SET_B  0.12780 − 0.12329 = +0.00451
```

6핸드 읽기에서 40핸드 읽기로 가면 공격 regret 이 팟의 **0.45% 개선**된다.

## 5. 깨끗한 대조 (Amendment D1 보조 분석)

합성 arm 을 버리고 `READ_USE_SEPARATION` 의 R 축을 그대로 썼다 —
**실제 필드 프로필**의 믿음만 모집단 사전분포로 블렌드(`a = 0.20` vs `1.00`),
`n`·`confidence` 제외로 `w` 는 비트까지 동일, `u` 는 production 값 그대로.
개념 다발 교락이 원리적으로 없다.

```
조작 점검 (W0', Amendment D2)
  slope(ftb_true → 믿는 ftb)  LOW +0.0846  HIGH +0.4228   비율 5.00
  sd(믿는 ftb)                LOW 0.0131   HIGH 0.0653    비율 5.00
  이론 비율 A_HI/A_LO = 5.0 과 일치
```

| | SET_A | 95%CI | SET_B | 95%CI |
|---|---:|---|---:|---|
| `ΔR_A` 합산 | +0.00150 | [+0.00131, +0.00171] | +0.00131 | [+0.00111, +0.00152] |
| `n_obs = 6` | +0.00009 | [+0.00007, +0.00010] | +0.00007 | [+0.00006, +0.00009] |
| **`n_obs = 40`** | **+0.00292** | [+0.00255, +0.00332] | **+0.00255** | [+0.00216, +0.00295] |

**부호가 전부 양수이고 전부 유의하다.** 합산이 FLOOR 아래인 것은 `n=6` 이
절반을 차지하고 거기서 효과가 0 이기 때문이다.

`n=6` 에서 0 인 것은 구조적이다 — `data = min(1, conf)·min(1, n/12)` 이라
`w ≈ 0.110` 이고, `_shrink` 가 추정치를 `PRIOR` 0.52 쪽으로 강하게 당긴다.
**읽을 것이 없어서가 아니라 읽은 것을 쓰지 못하게 게이트가 막는다.**
그리고 그 게이트는 옳게 작동한 것이다 — 6핸드짜리 표본을 세게 쓰면 안 된다.

### 조작 점검이 한 번 무효였다 (Amendment D2)

D1 의 `W0` 은 `r(ftb_true, 믿는 ftb)` 를 봤는데, R 축 개입이 **양의 아핀
변환**이라 피어슨 상관은 **불변**이다. 실측에서 `LOW +0.8688 = HIGH +0.8688`
로 완전히 같았다. 통과할 수 없는 검사였다. `READ_USE_SEPARATION` 에서 같은
이유로 상관 대신 기울기·sd 를 쓴 것을 여기서 되풀이하지 못했다.
`ΔR_A` 수치를 본 뒤에 고쳤다는 사실을 `D2-0` 에 공개했다.

## 6. 질문에 대한 답

> 공격 정보를 정상적으로 배선했을 때도 읽기 정확도가 실제 EV 를 개선하는가?

**개선한다. 다만 표본이 충분할 때만이고, 크기는 수비보다 작다.**

```
공격  n=40   +0.00292 / +0.00255     FLOOR 통과, 두 집합 재현
공격  n=6    +0.00009 / +0.00007     사실상 0
공격  합산    +0.00150 / +0.00131     양수·유의하나 FLOOR 미만
수비  production u 에서 (READ_USE 사다리 평균)  약 +0.0033 / +0.0038
```

공격은 수비의 **40~45%** 규모다. 그리고 공격에는 수비에 없는 조건이 하나
붙는다 — **표본 문턱**. `data` 게이트가 `n < 12` 에서 `w` 를 선형으로 깎기
때문에, 공격 읽기는 관측이 쌓이기 전까지 사실상 꺼져 있다.

## 7. 사전등록된 A/B 대응

```
A  stat_read 설계 진입 가능     W1 이어야 함
B  채널별 분리 또는 개념 재검토   W2 또는 W3
```

보조 판정이 `W3` 이므로 **규칙대로는 B** 다. 규칙을 사후에 바꾸지 않는다.

다만 `B` 안에서 방향은 좁혀졌다. 이번 데이터가 지지하는 것은
"공격에서는 읽기가 소용없다"가 **아니라**

> **읽기 정확도의 EV 효과는 채널마다 크기가 다르고(수비 > 공격 약 2.3배),
> 공격에는 표본 문턱이 추가로 걸린다**

이다. 즉 `stat_read` 를 단일 스칼라로 넣으면 **두 채널에 같은 크기로 작용해서
공격 쪽을 과대 반영**하게 된다. 채널별 분리가 먼저다.

**`stat_read` 는 이번에도 만들지 않았다.**

## 8. 한계

1. **`n_obs` 가 6 과 40 뿐이다.** 문턱이 어디인지는 못 잰다. `data` 식이
   `n/12` 로 꺾이므로 8~16 구간이 정보가 많을 텐데 격자에 없다.
2. **수비 대조는 `READ_USE` 의 사다리 평균과 비교했다.** 거기엔 CI 가 없다.
   "수비 > 공격 2.3배"는 점추정 비교다.
3. **공격 셀의 가격이 전부 1.10~1.80 팟이다.** 폴드 에쿼티가 결정적인 구간이
   거기라 그렇게 뽑혔다. 작은 벳에서도 같은지는 안 봤다.
4. **`D = 0.28` 은 내가 정한 값이다.** "약한 손이 얼마나 더 접는가"의 크기가
   공격 EV 격차를 정한다. 근거는 옛 fixture 의 0.78/0.18 (격차 0.60)을
   클램프가 안 걸리는 선까지 줄인 것뿐이다.
5. **주 통계량의 교락을 측정 후에 발견했다.** 보조 분석으로 보완했지만,
   사전등록 단계에서 `make_arms` 의 구성을 확인했어야 했다.

## 9. 최종 확인

```
production 무수정   plan.py / persona.py / reads.py / session.py    diff 0
fixture 동결        tools/qx_ev_fixture.py / tier5_axis_validate.py  diff 0
기존 산출물         QX / READ_USE / SYNTH 문서·도구 전부             diff 0
regression 지문     tools/regress.py check -> 전 시드 지문 일치
새 concept          없음. c5 / V7 tier 경계 / baseline 무변경
```
