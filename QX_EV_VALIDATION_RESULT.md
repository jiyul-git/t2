# QX_EV_VALIDATION — 결과

사전등록: `QX_EV_VALIDATION_PREREG.md` (Amendment A1 포함, 문턱·규칙·시드 무변경).
기준 `claude/tier5-axis-design` `00399b4`. 브랜치 `claude/qx-ev-validation`.

**production 무수정 · LIVE 배선 없음 · `stat_read` 추가 없음 · `c5` 무변경 ·
tier 경계 무변경 · baseline 갱신 없음.**

**판정: `QX_PARTIAL`** — 근거와 한계는 2·3·4절.

---

## 1. 사실

### 1-1 fixture

decision-state 144개 (READ 72 / GEN 72, D 채널 / A 채널 각 절반).
상태마다 상대의 **진짜 정책** `b_true` 는 fixture 가 정하고 봇에게는 같은
파라미터에서 표본 `n` 으로 만든 **공개 `opp_est`** 만 준다.

- 히어로 쇼다운 승률은 `bot.equity_vs_combos` 로 실제 카드 평가에서 나온다.
- 연속구간에 리버 베팅 · fold equity · 스택 이동이 들어간다.
- 한 상태의 모든 후보 행동이 **같은 난수 표본**을 공유한다 (난수를 인자로 넘겨
  행동별 소비 횟수 차이로 스트림이 어긋나지 않게 했다).
- 상태당 연속구간 표본 1,200.

```
상태별 EV 격차 (팟 대비)        mean 0.4686
프로필간 READ regret sd          0.01179 (팟 대비)  mean 0.06643  범위 0.0466~0.1374
READ 조합 중 b_true 로 최적행동이 뒤집히는 것   9/9
```

퇴화 조건(판정 0번: regret sd < 팟의 1%, 또는 최적행동 불변)에 걸리지 않는다.

### 1-2 Amendment A1 수정의 효과

| 진단 | 수정 전 | 수정 후 |
|---|---:|---:|
| regret ~ 행동빈도 | −0.858 | **−0.302** |
| regret ~ aggression | −0.245 | −0.043 |
| regret ~ looseness | −0.473 | −0.143 |
| READ 최적행동 | fold 13/call 35 · check 14/bet 34 | fold 6/call 18 · **check 24/bet 24** |

수정 과정에서 시드 버그를 잡았다 — `ev_actions` 가 `sid` 로 시드를 잡는데
뒤집힘점 탐색은 `sid = −1` 프로브를 써서 탐색과 실제 상태가 **서로 다른 표본**을
봤다. 시드를 상태 내용에서 유도하도록 고쳤다 (paired design 요건이기도 하다).

### 1-3 QX ↔ regret (부호: `r(QX, −regret)`, 양수면 QX 높을수록 regret 낮음)

**fit `940001..940010`, 400명**

| QX | READ_all | READ_D | READ_A | GEN_all | partial(overall) | partial(+행동빈도) |
|---|---:|---:|---:|---:|---:|---:|
| `QX_E` | +0.2476 | **+0.3012** | **−0.2477** | +0.0864 | +0.2252 | +0.2283 |
| `QX_S` | +0.2952 | +0.2941 | +0.0166 | +0.2023 | +0.2162 | +0.1885 |

**holdout `950001..950010`, 400명 — 적합에 쓰지 않았다**

| QX | READ_all | READ_D | READ_A | GEN_all | partial(overall) | partial(+행동빈도) |
|---|---:|---:|---:|---:|---:|---:|
| `QX_E` | **+0.2244** | **+0.2770** | **−0.2403** | +0.0877 | +0.1883 | +0.1826 |
| `QX_S` | +0.1496 | +0.1530 | −0.0053 | +0.1242 | +0.0601 | +0.0410 |

표본 크기별 (READ)

| QX | fit n=6 | fit n=40 | holdout n=6 | holdout n=40 |
|---|---:|---:|---:|---:|
| `QX_E` | +0.1921 | +0.2316 | +0.1318 | +0.2383 |
| `QX_S` | +0.2366 | +0.2713 | +0.1454 | +0.1234 |

사분위 분할 (READ 평균 regret, 팟 대비)

```
fit      QX_E 하위25% 0.0747  상위25% 0.0606   (−0.0141)
         QX_S 하위25% 0.0725  상위25% 0.0630   (−0.0094)
holdout  QX_E 하위25% 0.0731  상위25% 0.0602   (−0.0128)
         QX_S 하위25% 0.0692  상위25% 0.0636   (−0.0056)
```

### 1-4 합성 프로필 대조 (READ, 150쌍)

```
CALC_ONLY  regret 0.06074
PIPE_ONLY  regret 0.06032
차이 (CALC − PIPE)  +0.00042   95%CI [−0.00095, +0.00184]   유의하지 않다
```

**두 극단 프로필이 갈리지 않는다.**

## 2. 사전등록 판정

```
0  INCONCLUSIVE 검사   regret sd 0.0118 (팟의 1.18%) ≥ 1%, 최적행동 9/9 뒤집힘  → 통과
1  QX_INVALID          fit READ r 둘 다 양수 (+0.2476, +0.2952)                → 아님
                       CALC_ONLY(0.06074) ≤ PIPE_ONLY(0.06032) 아님            → 아님
2  QX_VALID            PIPE < CALC 의 95%CI 가 0 을 포함 → **불충족**
                       QX_S 는 holdout 0.1496 < 0.15, partial 0.0601 < 0.10 → 별도로도 불충족
3  QX_PARTIAL          QX_E·QX_S 둘 다 부호가 맞고 holdout |r| ≥ 0.10 재현     → **충족**
```

**판정: `QX_PARTIAL`**

## 3. 해석

**3-1 `QX_E` 는 수비 채널에서 EV 와 실제로 연결된다.**
`READ_D` 가 fit +0.3012 / holdout +0.2770 이고, 전반 실력(`overall CALC`)을
통제해도 +0.1883, 잔여 행동빈도 편향까지 통제해도 +0.1826 으로 남는다.
사분위로 보면 상위 25% 가 하위 25% 보다 팟의 **1.3%p** 적은 regret 를 낸다.
`calldown_need` 경로에 한해 **QX 는 빈 과녁이 아니다.**

**3-2 그런데 공격 채널에서는 부호가 반대다.** `READ_A` 가 fit −0.2477,
holdout −0.2403 으로 **재현된다.** `QX_E` 가 높을수록 공격 상태에서 regret 가
**커진다.** `TIER5_AXIS_DESIGN_RESULT` 진단에서 A 채널의 `b_true` 반응 변동폭이
0.067(D 는 0.672)이었던 것과 맞는다 — `decide_aggression` 이 상대 블러프 빈도에
거의 반응하지 않으므로, A 계열에서 `QX_E` 가 높다는 것은 EV 와 무관한 무언가를
재고 있다는 뜻이다. **`QX_E` 의 F1·F2·F6(공격 계열)은 EV proxy 로 쓰면 안 된다.**

**3-3 `QX_S` 는 재현이 약하다.** fit +0.2952 → holdout +0.1496, partial 은
+0.2162 → +0.0601 로 떨어진다. 추정 단계 자체의 인구 편차가 작다는 앞선 관측
(`QX_S` 자연 sd 0.0140)과 일치한다 — 변별할 것이 거의 없다.

**3-4 가장 약한 고리는 합성 대조다.** 극단 프로필 `CALC_ONLY` 와 `PIPE_ONLY`
의 regret 차이가 0.0004 (CI 0 포함)로 **없다.** 연속 QX 로는 연결이 보이는데
프로필 극단으로는 안 갈린다. 두 프로필이 파이프라인 5축을 통째로 2.0/9.0 으로
밀어버려 `w = use·data` 까지 같이 바뀌는 것이 원인일 수 있다 —
`PIPE_ONLY` 는 읽기도 좋지만 **조정 폭도 크다.** 이 fixture 에서 큰 조정이
항상 이득은 아니다. 원인을 특정하지 못했다.

**3-5 `GEN` 에서도 `QX_S` 가 +0.20/+0.12 로 붙는다.** 상대를 안 읽어도 풀리는
상태인데 상관이 남는다는 것은 `QX_S` 가 부분적으로 일반 능력을 재고 있다는
신호다. `QX_E` 의 GEN 상관은 +0.086/+0.088 로 훨씬 작다 — 이 점에서는
`QX_E` 가 더 깨끗한 분리를 한다.

**3-6 행동빈도 편향의 부호가 뒤집혔다.** 수정 전 `regret ~ 행동빈도` 가
−0.858 이었는데 rebalance 후 fit 에서 **+0.4047** 이다. 편향이 사라진 게 아니라
반대 방향으로 남았다 — 이제는 덜 치고 덜 콜하는 쪽이 유리하다. 그래서
행동빈도를 통제한 컬럼을 같이 실었고, `QX_E` 의 연결은 그 통제에서도
+0.1826 으로 **살아남는다.** 이것이 3-1 결론의 주된 방어선이다.

## 4. 아직 모르는 것

- **왜 합성 극단 대조가 null 인지 모른다** (3-4). 읽기 능력과 조정 폭이
  `PIPE_ONLY` 에서 같이 올라가는 것을 분리하지 않았다.
- **`READ_A` 음의 상관의 원인을 특정하지 못했다.** `decide_aggression` 이
  b_true 에 둔감한 것은 확인했지만, 왜 `QX_E` 가 높을수록 더 나빠지는지는
  추적하지 않았다.
- **fixture 는 한 가지 연속구간 모형이다.** 상대의 리버 정책(`barrel_true`,
  `f_bluff`, `f_value`, `sz_river`)을 내가 고정했다. 다른 상대 정책에서 같은
  결론이 나오는지 안 봤다.
- **raise / 3bet 채널이 없다** (사전등록 2절에 미리 적은 범위 한계).
- **자연 게임으로 확장하지 않았다.** first-divergence 원칙은 준비만 해 두었다.
- 토너먼트 맥락(ICM·스택 깊이 변화·버블)이 빠져 있다. chip EV 이지 $EV 가 아니다.

## 5. 다음 단계 (사전등록 8절)

`QX_PARTIAL` 이므로 사전등록상 **"충분한 QX_PARTIAL 이면 `stat_read` 설계 시작"**
조건에 걸린다. 다만 그대로 넘어가기 전에 3-2·3-4 두 가지가 걸린다.

- `stat_read` 를 설계한다면 **수비 채널(`calldown_need`) 기준으로** 과녁을 잡아야
  한다. `QX_E` 의 공격 계열은 EV 와 반대로 붙어 있다.
- 합성 극단 대조가 null 인 것을 먼저 풀지 않으면, 새 축이 "읽기"를 올리는지
  "조정 폭"을 올리는지 구분할 수 없다.

구현으로 갈 경우의 전제는 그대로다 — **pid 유도 독립 RNG.**
`LOADING` 순회에 키를 추가하는 방식은 지문 `d8e9271e…` · regression baseline
3세대 · reachability reference · V7 `c5` calibration 을 전부 무효로 만든다.

## 6. 재현

```
python3 tools/qx_ev_fixture.py
python3 tools/qx_ev_validate.py --n 400 --synth-n 150
```
